# Bitácora — 2026-09-19 03:39 UTC (ciclo 266º, MODO CICLO)

## Qué comprobé

- `kimiko_drafts`: revisado antes que nada pese al disparo `schedule`. Las 4
  filas históricas siguen `status = 'hecho'`, sin ninguna `en_curso` colgada,
  sin orden nueva de Telegram este ciclo.
- `npm ci` + `npm run build`: exit 0, 36/36 páginas generadas sin errores.
- `npm run lint`: sin warnings ni errores.
- `npm audit`: sin cambio desde el ciclo 223º — 9 vulnerabilidades (1
  moderate, 7 high, 1 critical), todas en `postcss`/cadena de `next-intl`.
  Sin fix no-breaking disponible; `--force` seguiría rompiendo subiendo
  `next` a 16.3.5 y `next-intl` a 4.14.5.
- 8/8 rutas del checklist en producción devuelven 200 (siguiendo
  redirecciones): `/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
  `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
- `/admin` sin sesión → `308` a `/admin/` → `200` en
  `/login/?redirect=%2Fadmin%2F`. `middleware.ts` confirmado en la raíz del
  repo (no dentro de `app/`).
- `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`.
  `sitemap.xml` con 37 `<loc>`. `robots.txt` correcto, referencia el
  sitemap.
- Vercel: últimos 5 despliegues (`kimiko` y `quantum-holistic-2`) en estado
  `READY`.
- Diccionario (`plants`, 52 filas): 4 filas `publicada=true AND
  ficha_verificada=true` (`albahaca`, `arnica`, `equinacea`, `hinojo`), sin
  cambio. Confirmadas en disco las 4 imágenes
  (`public/images/plants/{slug}-cientifica.jpg`). Las 9 plantas peligrosas
  (aconito, datura, datura-metel, amanita-muscaria, cannabis,
  cornezuelo-centeno, beleno-negro, tejo, hierba-mora) confirmadas
  `publicada=false`.
- Rehecho desde cero el cruce de hash SHA-256 sobre la columna JSON
  `ficha_cientifica` de las 52 filas: mismos **7 grupos / 21 filas**
  duplicadas de siempre (6 pares de fichas reales que comparten contenido +
  las 9 peligrosas compartiendo el placeholder). Sin cambios. Pasada
  quincuagésima segunda de este check permanente (abierto en el 211º/212º).
- `lavanda`: sigue con `image_cientifica_url` apuntando a
  `lavanda-cientifica.jpg`, fichero inexistente en disco. Confirmado de
  nuevo que no existe. `publicada=false`, `ficha_verificada=false` — sin
  blast radius, la regla de hierro se cumple (no se activó sin imagen).
  Hallazgo abierto desde 2026-09-03, cumple **16 días** sin resolverse.
- `updated_at` máximo en `plants` sigue en 2026-09-01 → sin cambios que
  auditar visualmente este ciclo.
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Repasados los 22 publicados: sin títulos duplicados, todos con
  `excerpt` <155 caracteres e `image_url` presente.
- `citas`: última fila sigue siendo la cita de Cicerón (insertada en el
  262º), hueco de **~18h45min** al comprobar — por debajo del umbral de
  ~24h, sin necesidad de insertar. 43 filas, sin cambio.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2 filas).

## Qué arreglé

Nada — quincuagésimo segundo ciclo consecutivo (212º-266º) sin hallazgos
técnicos nuevos en el checklist mecánico. Sin hallazgos de negocio nuevos.
Sin commits de código ni escrituras en Supabase este ciclo (solo esta
bitácora y la memoria).

## Qué encontré sospechoso

- `lavanda` sin imagen en disco, ya documentado desde el 2026-09-03, ahora
  con 16 días abierto. Sigue sin blast radius (no publicada), pero conviene
  que Kristian decida si sube la imagen o retira la ficha.
- Los 6 pares de fichas científicas duplicadas entre sí (`tulsi`/`loto`,
  `valeriana`/`cinamomo`, `muerdago`/`ginseng`, `sauco`/`ashwagandha-fruto`,
  `manzanilla`/`nigela`, `equinacea`/`echinacea`) siguen sin explicación
  documentada de por qué comparten contenido exacto. No hay evidencia de
  cruce erróneo de datos (podría ser contenido plantilla reutilizado a
  propósito), pero no lo puedo confirmar sin más contexto.

## Tareas manuales de Kristian hoy

1. Decidir qué hacer con `lavanda`: subir `lavanda-cientifica.jpg` a
   `public/images/plants/` (con la imagen real y verificada) o retirar la
   ficha, ya lleva 16 días con la URL apuntando a un fichero que no existe.
2. Revisar si los 6 pares de fichas con `ficha_cientifica` idéntica
   (ver arriba) son contenido plantilla intencional o un error de carga —
   Kimiko no puede distinguir esto sin instrucción explícita.
3. `npm audit`: 9 vulnerabilidades siguen sin fix no-breaking desde hace
   semanas (raíz en `postcss`, arrastrada por `next-intl`). Si se quiere
   resolver, requiere subir `next` a 16.3.5 y `next-intl` a 4.14.5 (breaking,
   fuera del mandato de auto-ejecución de Kimiko).
