# Bitácora — 2026-08-28 16:27 UTC — Ciclo cloud (175º)

## Salud del sitio
- `npm ci && npm run build`: OK, sin errores.
- 8 rutas del checklist en 200 (tras redirect 308 por `trailingSlash: true`, esperado).
- `/admin/` sin sesión → 307 a `/login/?redirect=%2Fadmin%2F`. `middleware.ts` confirmado en la raíz.
- `canonical`/`og:url` → `https://quantum-holistic.com/`. `sitemap.xml` 200, `robots.txt` correcto.
- `npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio respecto a ciclos anteriores.

## Hallazgo grave: `ficha_verificada` pasó de 0/52 a 42/52 sin verificación visual, y `publicada` no gateaba nada en código

Los últimos ~29 días todos los ciclos reportaban `ficha_verificada` en 0/52 (bloqueado desde
2026-07-30). Al empezar este ciclo apareció en **42/52 `true`** — un cambio hecho fuera de este
ciclo (no hay commit de código asociado; el flag vive solo en Supabase). Antes de confiar en el
dato apliqué la regla de hierro:

1. **Existencia de archivo**: las 42 fichas verificadas SÍ tienen su `image_cientifica_url`
   presente en `public/images/plants/`. Ninguna imagen rota.
2. **Correspondencia especie↔imagen** (auditoría a fondo, máximo 6/ciclo): revisé
   `ashwagandha`, `valeriana`, `manzanilla`, `salvia`, `echinacea` y `equinacea`. Resultado:
   **5 de 6 tenían la imagen de otra especie**:
   - `manzanilla` (*Matricaria chamomilla*) mostraba granadas (*Punica granatum*) colgando de un árbol.
   - `ashwagandha` (*Withania somnifera*) mostraba una raíz tipo hortaliza con hojas de col, sin
     relación con el aspecto real de la planta (arbusto bajo, bayas anaranjadas en cáliz papiráceo).
   - `valeriana` (*Valeriana officinalis*, herbácea) mostraba un arbusto/árbol leñoso en bosque.
   - `salvia` (*Salvia officinalis*) mostraba una planta tipo jengibre/cúrcuma con brote rosa-blanco.
   - `echinacea` (*Echinacea purpurea*) mostraba una espiga de flores moradas tipo menta/orégano,
     sin la forma de margarita/cono característica.
   - `equinacea` (duplicado del mismo `Echinacea purpurea`, id distinto) sí tenía la ilustración
     botánica correcta — se dejó intacta.

   **Acción tomada**: bajé las 5 fichas mal emparejadas (`publicada = false`,
   `ficha_verificada = false`) vía REST de Supabase. Verificado en producción tras el deploy que
   ya no aparecen en el listado ni por URL directa.

   No pude auditar las otras 36 verificadas a fondo (límite de 6/ciclo) — quedan sin descartar
   cruces adicionales. Recomiendo a Papu una revisión manual de las 36 restantes antes de confiar
   plenamente en el gate, dado que 5 de 6 auditadas fallaron.

3. **Bug de código descubierto durante la auditoría — corregido y desplegado**: ni
   `app/diccionario/page.tsx` ni `app/diccionario/[slug]/page.tsx` leían el campo `publicada`.
   El listado traía las 52 filas sin filtrar y la ficha de detalle solo ocultaba
   `ficha_cientifica`/`ficha_mistica` según `ficha_verificada` — es decir, **las 9 plantas
   peligrosas (cannabis, datura, datura-metel, amanita-muscaria, cornezuelo-centeno,
   beleno-negro, tejo, hierba-mora, aconito) aparecían listadas en `/diccionario` y tenían página
   propia indexable** (`<title>Cannabis — Diccionario Botánico</title>`, meta description con el
   nombre científico), aunque sin datos clínicos ni místicos — ese contenido sí estaba bien
   bloqueado. Esto contradice el límite inamovible "las 9 peligrosas siguen despublicadas, con
   placeholder, sin excepción": estaban despublicadas de contenido pero no de listado/URL.

   Corregido en `86b6bce`: nueva constante compartida `lib/plantas-peligrosas.ts`, ambas rutas
   ahora excluyen `PLANTAS_PELIGROSAS` y respetan `publicada = false` devolviendo `notFound()`.
   Build verificado, probado en local (servidor real con las env vars de Supabase) y reverificado
   en producción tras el deploy (`dpl_47JpmU8EyKzMFuLgqgwXchn8vAm1`, READY):
   - `/diccionario/cannabis/` y las otras 8 peligrosas → 404.
   - `/diccionario/manzanilla/` (recién bajada) → 404.
   - `/diccionario/tomillo/`, `/diccionario/equinacea/` (siguen publicadas) → 200.
   - Listado ahora muestra 38 tarjetas (43 no peligrosas − 5 bajadas), "37 de 38 verificadas".
   - Las 8 rutas del checklist general siguen en 200 tras el deploy.

## Gate `ficha_verificada`/`publicada` — estado tras el ciclo
- 52 filas en `plants`. 9 peligrosas: `publicada=false`, sin imagen (campo null), ahora también
  invisibles en listado/URL por el fix de código.
- De las 43 no peligrosas: 37 publicadas y verificadas (auditoría de imagen pendiente para 31 de
  ellas, no cubiertas este ciclo), 1 (`lavanda`) publicada pero no verificada — su imagen sigue
  con 404 en vivo (`image_cientifica_url` apunta a un archivo que no existe en disco ni en
  producción, arrastrado de ciclos anteriores), 5 recién bajadas por cruce de contenido
  (`manzanilla`, `ashwagandha`, `valeriana`, `salvia`, `echinacea`).
- Duplicado `equinacea`/`echinacea` (ids 21/52, mismo `Echinacea purpurea`): con este ciclo queda
  resuelto de facto — `echinacea` bajada por imagen incorrecta, `equinacea` (imagen correcta)
  sigue siendo la única visible. Sigue pendiente decidir si se borra la fila duplicada o se deja
  despublicada permanentemente.

## Checkout Gumroad
- Sigue roto, ~30 días 22h desde 2026-07-28T18:25:20.881Z, 175º ciclo consecutivo. CTA real en
  `/producto/ritual-descanso` apunta a `kristian320.gumroad.com/l/ritual-descanso` (404 en vivo,
  reconfirmado). `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado) sigue siendo el revert
  viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel sin cambio desde esa fecha. No se toca la env var
  sin OK explícito de Kristian.

## Contenido / blog
- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. No se generó contenido nuevo
  este ciclo — el hallazgo de plantas consumió el foco del ciclo y el backlog de drafts sigue
  pendiente de la decisión de deduplicación de Kristian (documentada en ciclos previos).

## Negocio
- `leads`: 0 filas, sin actividad. Funnel `/regalo/primera-noche` → `/producto/ritual-descanso`
  verificado por código y por rutas, sin cambios. "El Ritual del Descanso" sigue sin tocar
  checkout/precio, según el límite inamovible.

## Cita diaria
- Última (John Locke, 10:09:53 UTC de hoy) con ~6h18min de antigüedad al cerrar el ciclo, por
  debajo del umbral de 24h — sin inserción nueva. 27 citas en la tabla, sin cambio.

## Commits de este ciclo
- `86b6bce` — fix(diccionario): `publicada` gatea listado y ficha, peligrosas nunca listadas.
- Cambios de datos en Supabase: 5 plantas bajadas (`publicada=false`, `ficha_verificada=false`):
  `manzanilla`, `ashwagandha`, `valeriana`, `salvia`, `echinacea`.

## Tareas manuales de Kristian hoy
1. **Revisar y sustituir las imágenes de `manzanilla`, `ashwagandha`, `valeriana`, `salvia` y
   `echinacea`** (o decidir borrar la fila duplicada `echinacea`) antes de volver a marcarlas
   `publicada`/`ficha_verificada` — todas mostraban la especie equivocada.
2. **Auditar a mano las 31 fichas verificadas que este ciclo no pudo revisar** (límite de 6/ciclo)
   — dado que 5 de 6 auditadas fallaron, hay riesgo real de más cruces sin detectar en el resto.
3. **Gumroad**: seguir bloqueado, ~31 días. Decidir entre arreglar `kristian320.gumroad.com` o
   revertir `NEXT_PUBLIC_GUMROAD_URL` al link viable (`kristiantronco.gumroad.com/l/ugsqtg`).
