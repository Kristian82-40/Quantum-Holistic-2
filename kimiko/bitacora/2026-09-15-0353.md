# Bitácora — 2026-09-15 03:53 UTC (ciclo 249º, MODO CICLO)

## Comprobado

- `kimiko_drafts`: comprobado primero, antes del resto del checklist. Sin filas
  `pendiente`, sin `en_curso` colgado. Sin orden de Telegram este ciclo pese al
  disparo `schedule`.
- `npm ci` + `npm run build`: 36/36 páginas, limpio. `npx next lint`: sin avisos.
- `npm audit`: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate, 7
  high, 1 critical), sigue sin fix no-breaking disponible.
- 8/8 rutas del checklist en 200 con `-L` (`/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`). `/admin` sin sesión → 308 → `/admin/` → 307 → `/login/?redirect=...`,
  correcto. `middleware.ts` confirmado en la raíz del repo, no dentro de `app/`.
- `canonical` y `og:url` en `https://quantum-holistic.com/` correctos.
- `sitemap.xml`: 37 `<loc>`, 3 reintentos consistentes. `robots.txt` correcto,
  apunta al sitemap con el dominio bueno. Vercel: últimos 5 despliegues `READY`.
- **Corrección al check permanente del 211º/212º: la cifra vuelve a ser 7
  grupos / 21 filas (no 6, como había quedado "corregido" en el 247º).**
  Rehice el cruce de hash de `ficha_cientifica` desde cero sobre las 52
  filas, sin mirar el número anotado en el 248º. Resultado: 6 pares reales
  entre fichas (`ashwagandha-fruto`/`sauco`, `cinamomo`/`valeriana`,
  `echinacea`/`equinacea`, `ginseng`/`muerdago`, `loto`/`tulsi`,
  `manzanilla`/`nigela`, 12 filas) **más un séptimo grupo: las 9 peligrosas
  comparten un único hash de `ficha_cientifica`** (el placeholder), 9 filas.
  6+1=7 grupos, 12+9=21 filas — coincide con el baseline original del
  212º-246º (34 ciclos). El 247º se equivocó al "corregir" 7→6: al
  re-derivar la lista se fijó solo en los pares de ficha real y descartó el
  grupo de las 9 peligrosas, que siempre fue parte del cruce (ya estaba en
  la nota del 213º, solo que listado aparte). Ver la entrada completa del
  249º en `KIMIKO_MEMORIA.md` para el desglose y la lección: no sobrescribir
  una cifra de un check permanente sin re-ejecutar el cruce mecánico. Sin
  blast radius en vivo: los 21 slots siguen `publicada=false` salvo
  `equinacea` (correcta, verificada). `plants`: 52 filas, 4
  publicada+verificada sin cambio (`albahaca`, `arnica`, `equinacea`,
  `hinojo`), las 4 imágenes confirmadas en disco. Las 9 peligrosas
  confirmadas `publicada=false`. `updated_at` máximo sin cambio
  (2026-09-01) → sin auditoría visual completa este ciclo.
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio.
  Draft más reciente sigue en 2026-07-10, ya descartado en 206º-209º.
- `citas`: última fila sigue siendo Proverbios 18:14 (247º), hueco de
  **~9h58min** — muy por debajo del umbral de ~24h, sin necesidad de
  insertar. 40 filas, sin cambio.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Cambios este ciclo

- Corrección de la cifra del check de duplicados (6→7 grupos, revirtiendo el
  error del 247º) en `KIMIKO_MEMORIA.md`. Sin commits de código en el repo
  de producción, sin escrituras en Supabase.

## Tareas manuales de Kristian hoy

1. Ninguna tarea manual urgente esta vez. La cola de vulnerabilidades de
   `npm audit` (9, sin fix no-breaking) sigue ahí desde el 223º si en algún
   momento quiere revisarla con margen para un upgrade mayor.
