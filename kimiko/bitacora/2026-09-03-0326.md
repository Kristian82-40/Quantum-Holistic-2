# Bitácora — 2026-09-03 03:26 UTC (197º ciclo, MODO CICLO)

## Qué comprobé
- `npm ci && npm run build`: limpio, 36/36 páginas.
- `npm audit`: 9 vulnerabilidades, sin cambio desde el 185º ciclo (todas requieren
  `--force`/breaking change: postcss vía next-intl, Next.js major).
- 8/8 rutas del checklist en 200 (`/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
  `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`).
- `/admin` sin sesión → cadena completa 308 → `/login/?redirect=%2Fadmin%2F` (200),
  `middleware.ts` en la raíz (confirmado que no existe copia en `app/`).
- `canonical`/`og:url` apuntan a `https://quantum-holistic.com`, `og:image` presente
  (`og-default.jpg`, fix del 196º ciclo sigue vivo en producción). `sitemap.ts`/
  `robots.ts` presentes, sitemap con 85 `<loc>`, robots correcto. Vercel: últimos 5
  despliegues `READY`.
- `plants`: 52 filas, sin `UPDATE` nuevo desde el incidente del 189º ciclo (mismo
  `updated_at` exacto `2026-09-01T09:21:58Z` en las filas más recientes), así que no
  repetí la auditoría visual completa de las 40 imágenes. 23 publicada+verificada, 9
  peligrosas confirmadas una a una `publicada=false, ficha_verificada=false`
  (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno,
  beleno-negro, tejo, hierba-mora).
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), mismo conteo exacto que
  el ciclo anterior, sin `UPDATE` nuevo.
- `leads` en 0. `citas`: última inserción `2026-09-02T08:35:24Z`, misma fila que en el
  196º ciclo (~19h al comprobar), sin inserción nueva. `kimiko_drafts`: cola vacía, sin
  filas colgadas en `en_curso`, sin orden de Telegram este ciclo.

## Qué arreglé
Nada — checklist limpio de principio a fin. El fix de `og:image` del 196º ciclo sigue
verificado en vivo (home sirve `og-default.jpg`, sin regresión).

## Qué encontré sospechoso (sin tocar, documentado)
Nada nuevo. Los dos pendientes opcionales de ciclos anteriores siguen igual de
opcionales (rutas locales rotas en `image_url` de `blog_posts`, y `verify-plants.html`
sin documentar en ningún README) — no son bloqueantes y no he tocado nada porque no
hay señal nueva que los haga urgentes.

## Tareas manuales de Kristian hoy
1. Ninguna acción urgente — séptimo ciclo seguido sin hallazgos desde el incidente del
   189º.
2. Opcional, sin prisa: subir ficheros reales para los `image_url` locales de
   `blog_posts` que hoy son rutas rotas (o limpiar la columna), si se quiere imagen
   específica por post en vez del fallback genérico de `og-default.jpg`.
3. Sin más pendientes nuevos.
