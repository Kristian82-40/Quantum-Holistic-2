# Bitácora — Ciclo 265º (MODO CICLO, 2026-09-18 22:24 UTC)

## Kimiko_drafts
- Comprobado primero, antes del resto del checklist, pese al disparo `schedule`.
- Las 4 filas históricas siguen en `status = 'hecho'`. Sin `en_curso` colgado,
  sin orden de Telegram nueva este ciclo.

## Salud del sitio
- `npm ci` limpio. `npm run build`: exit 0, 36/36 páginas generadas, sin
  errores de lint ni de tipos.
- `npm audit`: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate,
  7 high, 1 critical), todas de `postcss`/`next`/`next-intl`, sin fix
  disponible sin romper (`npm audit fix --force` sube `next` a 16.3.5 y
  `next-intl` a 4.14.5, cambios breaking).
- 8/8 rutas del checklist en 200 siguiendo redirecciones (`/`, `/diccionario`,
  `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`); todas devuelven 308 sin trailing slash y 200
  al seguirlo, comportamiento esperado desde el 264º.
- `/admin` → 308 → `/admin/` → 307 → `/login/?redirect=%2Fadmin%2F` sin
  sesión. `middleware.ts` confirmado en la raíz del repo.
- `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`.
  `sitemap.xml` con 37 `<loc>`. `robots.txt` con `Allow: /` y referencia al
  sitemap correcto.
- Vercel: últimos 5 despliegues en `READY`.

## Integridad del diccionario
- `plants`: 52 filas, sin cambio. 4 publicada+verificada (`albahaca`,
  `arnica`, `equinacea`, `hinojo`), las 4 imágenes confirmadas en
  `public/images/plants/`.
- Las 9 peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) confirmadas
  `publicada = false`.
- `lavanda` sigue `publicada = false`, `ficha_verificada = false`, con
  `image_cientifica_url` rellena en la fila (`lavanda-cientifica.jpg`) pero
  sin fichero en disco — hallazgo abierto desde 2026-09-03, sin blast radius
  porque está despublicada.
- **Check permanente del 211º/212º, quincuagésima primera pasada:** hash de
  la columna `ficha_cientifica` (JSON) recalculado desde cero sobre las 52
  filas. Mismos **7 grupos / 21 filas** de siempre (6 pares de fichas reales
  con contenido cruzado + las 9 peligrosas compartiendo el placeholder). Sin
  cambios.
- `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual
  completa este ciclo (tope de 6 plantas auditadas a fondo por ciclo, sin
  candidatas nuevas de alto tráfico pendientes).

## SEO / blog
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Repasados los 22 publicados: sin títulos duplicados, todos con
  `excerpt` <155 caracteres e `image_url` presente.

## Citas
- Última fila sigue siendo Cicerón, *De Senectute* (insertada en el 262º).
  Hueco de **~13h30min** al comprobar — por debajo del umbral de ~24h, sin
  necesidad de insertar. 43 filas, sin cambio.

## Negocio
- `leads` en 0, `purchases` en 0, `products` sin cambio (2). Sin actividad
  nueva de negocio que revisar.

## Resumen
Quincuagésimo primer ciclo consecutivo (212º-265º) sin hallazgos técnicos
nuevos en el checklist mecánico de código/build/rutas, y sin hallazgos de
negocio nuevos. Sin commits de código ni escrituras en Supabase este ciclo.

## Tareas manuales de Kristian hoy
1. Subir el fichero de imagen científica de `lavanda`
   (`public/images/plants/lavanda-cientifica.jpg`) para poder verificar y
   publicar esa ficha — abierto desde 2026-09-03.
2. Decidir si se aborda `npm audit fix --force` (sube `next` a 16.3.5 y
   `next-intl` a 4.14.5) en una ventana de mantenimiento, dado que son
   cambios breaking que Kimiko no aplica por su cuenta.
3. Ninguna otra tarea manual urgente: sitio, diccionario y blog estables.
