# Bitácora — 2026-09-08 03:33 UTC (ciclo 220º, MODO CICLO)

## Salud del sitio
- `npm ci` limpio (415 paquetes). `npm run build`: 36/36 páginas generadas sin
  error, lint (paso integrado en `next build`) sin avisos.
- `npm audit`: 9 vulnerabilidades (1 moderate, 8 high), sin cambio desde el 185º.
- 8/8 rutas del checklist en 200 (con `-L`, siguiendo el redirect 308 de
  `trailingSlash: true` en `next.config.js`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión → cadena completa `308 /admin/` → `/login/?redirect=%2Fadmin%2F`
  (200). `middleware.ts` confirmado en la raíz (no existe `app/middleware.ts`).
- `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`.
  `sitemap.xml`: 37 `<loc>`, sin cambio. `robots.txt` correcto, referencia el
  sitemap.
- Vercel: últimos 5 despliegues (`quantum-holistic-2` y `kimiko`) en `READY`.

## Diccionario
- Novena pasada del check permanente del 211º/212º (cruce de hash de
  `ficha_cientifica`, `json.dumps(sort_keys=True)`, sobre las 52 filas):
  exactamente los mismos 7 grupos que el 212º-219º, sin grupos nuevos
  (`loto`/`tulsi`, `cinamomo`/`valeriana`, `ginseng`/`muerdago`,
  `ashwagandha-fruto`/`sauco`, `manzanilla`/`nigela`, `echinacea`/`equinacea`,
  y el grupo compartido de las 9 peligrosas).
- `plants`: 52 filas, 4 publicada+verificada sin cambio (`albahaca`, `arnica`,
  `equinacea`, `hinojo`), las 4 imágenes confirmadas en `public/images/plants/`.
  Las 9 peligrosas confirmadas `publicada=false`. `echinacea`/`equinacea` sigue
  sin duplicado real en vivo (`/diccionario/echinacea/` → 404,
  `/diccionario/equinacea/` → 200). Sin `UPDATE` de Kristian sobre `plants`
  desde el 212º.

## SEO
- `blog_posts`: 22 publicados, sin títulos duplicados, todos ≤60 car., `excerpt`
  ≤155 car., todos con `image_url`. Sin cambio en la muestra desde ciclos
  anteriores.

## Negocio
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio. El
  draft actualizado más reciente sigue siendo de 2026-07-10, anterior al cruce
  en bloque del 209º: backlog sigue sin candidatos nuevos que revisar.
- `citas`: última inserción sigue siendo la misma fila
  (`2026-09-05T03:28:37 UTC`), hueco ahora en **72h04min** (3 días y 4min) —
  sexto ciclo consecutivo (215º-220º) sobre la misma sequía. Aplicando el
  check permanente corregido en el 219º: se reporta solo el hueco absoluto,
  sin narrar el delta entre cierres como tendencia de negocio (ese delta mide
  la cadencia de ejecución de Kimiko, no la sequía).
- `leads` en 0, `purchases` en 0, `products` sin cambio (2). `kimiko_drafts`: 4
  filas totales, todas `hecho`, cola vacía (0 `pendiente`/`en_curso`), sin
  orden de Telegram este ciclo.

Sin commits de código ni escrituras en Supabase este ciclo — noveno ciclo
consecutivo (212º-220º) sin hallazgos técnicos nuevos en el checklist
mecánico; el único hallazgo de este ciclo sigue siendo de negocio (`citas`),
ya documentado y sin acción posible desde código.

## Tareas manuales de Kristian hoy
1. **`citas` lleva sin una reserva nueva desde 2026-09-05T03:28:37 UTC — ya
   72h04min (3 días).** Sigue siendo señal de negocio (funnel de reservas o
   estacionalidad), no un bug de código: revisar el canal de reservas y, si
   aplica, la disponibilidad publicada de citas.
2. Sin más tareas manuales nuevas este ciclo — el resto del checklist mecánico
   (build, rutas, SEO, diccionario) sigue limpio por noveno ciclo consecutivo.
