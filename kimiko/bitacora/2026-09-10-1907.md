# Bitácora — 2026-09-10 19:07 UTC (ciclo 230º, MODO CICLO)

## kimiko_drafts (comprobado primero)
- Cola de `pendiente` vacía, sin fila `en_curso` colgada. Sin orden de Telegram
  este ciclo pese al disparo `schedule`.

## Salud del sitio
- `npm ci` limpio (415 paquetes). `npm run build`: 36/36 páginas generadas sin
  error. `npx next lint`: sin avisos.
- `npm audit`: 1 moderate, 7 high, 1 critical (total 9) — sin cambio desde el
  223º. Sigue sin fix no-breaking en la rama `next@14.2.x` (única corrección
  de fondo es la subida mayor a `next@16.3.4`, pendiente de decisión de
  Kristian por su blast radius).
- 8/8 rutas del checklist en 200 (con `-L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` → `/login/?redirect=%2Fadmin%2F` (307→200). `middleware.ts`
  confirmado en la raíz del repo (no en `app/`).
- `canonical`, `og:url`, `og:image` en `/` apuntan a `quantum-holistic.com`.
  `sitemap.xml`: 37 `<loc>` (sin cambio). `robots.txt` correcto, con
  referencia al sitemap.
- Vercel: últimos 5 despliegues `READY`.

## Diccionario (`plants`)
- 52 filas. Publicada+verificada sin cambio: `albahaca`, `arnica`,
  `equinacea`, `hinojo` — las 4 imágenes (`*-cientifica.jpg`) confirmadas en
  disco bajo `public/images/plants/`.
- Las 9 peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) confirmadas
  `publicada = false`.
- **18ª pasada del check permanente (211º/212º)**: cruce de hash de
  `ficha_cientifica` sobre las 52 filas → exactamente los mismos 7 grupos
  que en los ciclos 212º-229º, sin grupos nuevos (incluye el grupo de las 9
  peligrosas, ya despublicadas, y 6 pares más, entre ellos
  `equinacea`/`echinacea`).
- `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual
  completa este ciclo (no hay fichas nuevas para auditar).
- `echinacea`/`equinacea`: confirmado en vivo que no hay duplicado real
  (`/diccionario/echinacea/` → 404, `/diccionario/equinacea/` → 200).

## Blog (`blog_posts`)
- 109 filas: 79 draft / 22 published / 8 rejected, sin cambio.
- 22 publicados sin duplicados de título, todos con título ≤60 caracteres y
  `excerpt` ≤155 caracteres, todos con `image_url`. Draft más reciente sigue
  en 2026-07-10, ya descartado en los ciclos 206º-209º por canibalización de
  keywords.

## Citas (`citas`)
- Última fila sigue siendo Proverbios 12:25 (insertada en el 229º,
  2026-09-10T16:02:08Z). Hueco al comprobar: **~3h05min** — muy por debajo
  del umbral de ~24h. Sin necesidad de insertar. 36 filas en `citas`, sin
  cambio.

## Negocio
- `leads` en 0, `purchases` en 0, `products` sin cambio (2 filas).

## Resumen
- Decimoctavo ciclo consecutivo (212º-230º) sin hallazgos técnicos nuevos en
  el checklist mecánico, y sin hallazgos de negocio nuevos.
- Sin escritura en Supabase este ciclo (hueco de `citas` por debajo del
  umbral). Sin commits de código de negocio.

## Tareas manuales de Kristian hoy
1. Decidir si autoriza la subida mayor de Next.js (14.2.35 → 16.3.4, salta
   la v15, rompe `next-intl` 3→4) para cerrar la vulnerabilidad crítica de
   `npm audit` — puedo hacerlo en rama/PR separada si da el OK.
2. Cuenta de pago (Gumroad/Lemon Squeezy) para activar el checkout de
   "Ritual del Descanso" — sigue siendo el único bloqueador del primer
   ingreso real (no toco precios/pasarelas por iniciativa propia).
3. Revisar el backlog de 79 drafts de blog acumulados (la mayoría restos
   del agente nocturno antiguo, abril-mayo 2026) y decidir si se descartan
   en bloque o se rescatan candidatos puntuales.
