# Bitácora — 2026-08-29 22:33 UTC (180º ciclo, MODO CICLO)

## Qué comprobé
- `npm ci && npm run build`: pasa limpio, sin fixes necesarios. `npm audit`: 17 vulns (1 low, 4
  moderate, 12 high), sin cambio respecto a ciclos anteriores.
- 8/8 rutas del checklist en 200 (`/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
  `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`). `/admin` sin sesión →
  redirect a `/login/?redirect=%2Fadmin%2F`. `middleware.ts` confirmado en la raíz.
- `canonical`/`og:url` de la home correctos. Canonical en subpáginas (muestra: `/diccionario`,
  `/blog`, `/terminos`, `/terapeutas/papu`, ficha de planta, post de blog) correcto — el fix del
  179º ciclo sigue vivo en producción.
- `sitemap.xml` y `robots.txt` responden 200, dominio correcto en ambos.
- `plants`: 52 filas, sin cambio. 5 publicadas / 4 verificadas (`albahaca`, `arnica`,
  `equinacea`, `hinojo` + `lavanda` publicada sin verificar por imagen ausente, caso ya
  documentado). Sin backlog nuevo que auditar — ninguna fila con `ficha_verificada=true` fuera
  de las 4 ya auditadas por ciclos anteriores. Las 9 peligrosas reconfirmadas
  `publicada=false` sin excepción. Duplicado `equinacea`/`echinacea` (ids 52/21) sin cambio.
- `blog_posts`: 90 draft / 19 published, sin títulos duplicados entre published. Post con
  lenguaje de "curación" (Aceite de Oliva Virgen Extra) sigue publicado, pendiente de decisión
  editorial de Papu (identificado ciclo 140, sin mandato para despublicar por iniciativa propia).
- `leads` en 0 filas. Tabla `citas`: última inserción 13:44 UTC (Marco Aurelio), ~8h43min de
  antigüedad al arrancar el ciclo — por debajo del umbral de 24h, sin inserción nueva.
- Imágenes: 71 archivos en `public/images/plants/`, sin cambio.

## Qué arreglé
- **Bug de SEO pendiente del ciclo anterior (179º):** `openGraph.url` (y `openGraph.title`/
  `description`) seguía sin definirse en 9 páginas estáticas (`/blog`, `/diccionario`,
  `/terminos`, `/privacidad`, `/cookies`, `/terapeutas`, `/terapeutas/papu`,
  `/producto/ritual-descanso`, `/regalo/primera-noche`), así que heredaban el `og:url` de la
  home aunque su `alternates.canonical` ya fuera propio desde el ciclo anterior. Añadido bloque
  `openGraph` propio en las 9. Commit `17df359`, desplegado en Vercel
  (`dpl_8RLvyQzL29VAd8jVuQBXBK1bebrR`, `READY` confirmado vía API) y verificado en vivo: las 9
  páginas ahora sirven su propio `og:url`.

## Hallazgo sospechoso (fuera de mis límites, documentado para Kristian)
- **Gumroad: variable de entorno puesta, pero el enlace sigue roto.** Por primera vez en ~32
  días, `NEXT_PUBLIC_GUMROAD_URL` aparece configurada en producción en Vercel (antes ausente de
  las 14 vars totales). El botón de `/producto/ritual-descanso` ya enlaza a
  `https://kristian320.gumroad.com/l/ritual-descanso`, pero esa URL devuelve **404** en Gumroad
  (verificado con `curl`). No es algo que pueda arreglar yo — no gestiono cuentas de Gumroad ni
  toco pasarelas de pago (límite del Paso 5). Queda como tarea manual.

## Tareas manuales de Kristian hoy
1. **Gumroad sigue sin funcionar aunque ya tiene la env var puesta.** La URL
   `https://kristian320.gumroad.com/l/ritual-descanso` da 404 — revisar en el panel de Gumroad
   si el producto está publicado/despublicado o si el slug es distinto.
2. **Post "El Aceite de Oliva Virgen Extra: Un Tesoro de Curación de la Naturaleza"** sigue
   publicado con lenguaje de "curación" en el título (identificado hace 40 ciclos, ciclo 140,
   2026-08-21). Decidir si se reescribe el título/cuerpo o se despublica — Kimiko no lo toca sin
   tu OK por ser contenido ya publicado.
3. Sin tercer punto: el resto del checklist (salud del sitio, gate de peligrosas, SEO,
   negocio) está sano o ya documentado en ciclos previos sin cambio de estado.
