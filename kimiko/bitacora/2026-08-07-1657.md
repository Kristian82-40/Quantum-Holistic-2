# Bitácora Kimiko — Ciclo cloud 2026-08-07 16:57 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz. `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio frente al ciclo anterior.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200 (siguiendo redirect de trailing slash, esperado en Next.js): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
- `/admin` → 308 → `/admin/` → 307 → `/login/?redirect=%2Fadmin%2F` (200). `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` con dominio correcto (`quantum-holistic.com`).
- Supabase `plants`: 52 filas íntegras. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila.
- `next-env.d.ts` regenerado por el build se revirtió sin commitear (cambio no funcional, mismo patrón de siempre).

## Monetización
- **Checkout Gumroad sigue roto — 57º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~9 días 22h30min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) reconfirmada de tipo `sensitive` en producción, `updatedAt` sin cambio desde 2026-07-28T18:25:20.881Z. No se toca la env var sin OK explícito de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado por código (`app/api/leads/route.ts`, `app/regalo/primera-noche/RegaloForm.tsx`, ambos sin cambios desde el último commit) y rutas 200, sin cambios. Test E2E real con escritura más reciente sigue siendo el del ciclo 2026-08-06 10:45 UTC (~1 día 6h12min de antigüedad) — por debajo del umbral informal de 48h, no se repite este ciclo. `leads`: 0 filas (sin actividad real de usuarios todavía) — sin volumen suficiente para proponer variantes de A/B de CTA.
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Propuesta de esquema sin cambio (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu, bloqueada además por las 34 fichas contaminadas (ver Higiene de datos).

## Contenido
- Sin blog nuevo — backlog sin cambio: 90 `draft` / 19 `published` (109 total). Checklist por keyword de título: 7 títulos distintos / 8 filas con violación (chakras ×4, reiki, cristales, biodescodificación, nutrición cuántica), sin cambio frente al ciclo anterior. Las 8 filas confirmadas todas en estado `draft` — ninguna violación llegó nunca a `published`.
- Tabla `citas`: última inserción (Galeno, 2026-08-07 00:55:55 UTC) tiene ~16h de antigüedad — sin inserción nueva este ciclo (no supera el umbral de 24h); corresponde al ciclo que lo supere (~2026-08-08 00:55 UTC).
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "faltan dos horas para los 10 días": el checkout lleva casi diez días roto y el número exacto (9 días, 22 horas, 30 minutos) importa más que la ronda "diez días" — porque la disciplina no es reportar cifras redondas, es reportar la cifra real, la tenga uno ganas de redondear o no.
  - **LinkedIn** — ángulo "verificar dos veces no es desconfiar del sistema, es respetar el dato": este ciclo reprodujo desde cero el cruce de las 34 fichas contaminadas contra el dataset original y el resultado no cambió — pero el proceso de reproducirlo con el método exacto, y no uno parecido, es lo que hace que el número "sin cambio" sea confiable en vez de solo repetido.

## Higiene de datos (reproducido desde cero con el método documentado, sin cambio de estado real)
- 34 fichas contaminadas (25 seguras + 9 peligrosas) reconfirmadas reproduciendo el cruce exacto: identidad byte a byte de `ficha_mistica` entre `plants` y `app/fichas-50-valid.json` cruzando por `id`, con `nombre_latino` distinto entre ambas filas — los 9 pares peligrosos coinciden fila por fila con el listado documentado en el ciclo 2026-07-30 10:26 UTC. Sin cambio real. Pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC (~8 días 14h20min).
- El fix ya aplicado (commit `46f1022`, 2026-07-30 10:26 UTC) que oculta la sección mística completa para las 9 plantas peligrosas en `app/diccionario/[slug]/page.tsx` sigue activo y confirmado — el hallazgo es sobre el estado del *dato* en Supabase (sigue contaminado), no sobre lo que se renderiza en producción para las 9 peligrosas (ya bloqueado por código).
- Duplicado de contenido `equinacea` (`id`:52, sin `id` en el dataset original de 50) / `echinacea` (`id`:21, presente en el dataset original, no contaminada por el método correcto) sin cambio. `lavanda`: `image_cientifica_url` apunta a `/images/plants/lavanda-cientifica.jpg`, que devuelve 404 en producción (reconfirmado en vivo) — sin cambio real de estado, pendiente de decisión de Papu.
- Gate `ficha_verificada`: 0/52 plantas verificadas, sin cambio.
- `/api/webhooks/btcpay` heredado (pasarela cripto descartada por directiva) reconfirmado en el build, sin tocar.

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. Sin escrituras nuevas en Supabase este ciclo (citas por debajo del umbral de 24h, funnel probado hace ~1 día 6h12min). `next-env.d.ts` regenerado por el build se revirtió sin commitear (cambio no funcional). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — `NEXT_PUBLIC_GUMROAD_URL` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 9 días 22h30min. El link `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que sirven `ficha_mistica` de una especie distinta a la real — pendiente desde el 2026-07-30, bloquea poder activar cualquier feature que use ese campo (incl. "Tu Planta Aliada").
3. **Higiene del diccionario**: decidir slug canónico entre `equinacea`/`echinacea` (contenido duplicado) y aprobar el reemplazo de la imagen rota de `lavanda` (404 en producción, ruta actual `/images/plants/lavanda-cientifica.jpg`).
