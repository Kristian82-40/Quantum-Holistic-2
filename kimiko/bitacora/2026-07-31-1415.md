# Kimiko — Ciclo cloud 2026-07-31 14:15 UTC

## QA (Paso 1)
1. `npm ci && npm run build`: **OK**, sin errores de tipos ni lint (37 rutas, `ƒ Middleware` presente). Nota de proceso: el build hay que correrlo desde la **raíz del repo** (no desde `app/`, que tiene su propio `package.json` desactualizado y sin subcarpeta `app/app`/`pages` — falla con "Couldn't find any `pages` or `app` directory" si se intenta ahí). `vercel.json` de la raíz confirma `buildCommand: npm run build` — la raíz es el proyecto real que se despliega.
2. Producción (`https://quantum-holistic.com`): canonical y `og:url` == `https://quantum-holistic.com/`. OK.
3. Rutas 200 (siguiendo redirect de trailing slash): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`. OK.
4. `/admin` sin sesión → `308` a `/admin/` → `307` a `/login/?redirect=%2Fadmin%2F` (200 final). `middleware.ts` confirmado en la raíz del repo, no en `app/`. OK.
5. Supabase `plants`: **52/52** filas íntegras (`content-range: 0-0/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null` — placeholder intacto, reverificado fila por fila.
6. `leads`: 0 filas. `purchases`: 0 filas. Sin conversiones ni segmentación por dosha que analizar todavía.
7. `sitemap.xml` (200) y `robots.txt` (200): dominio correcto (`Sitemap: https://quantum-holistic.com/sitemap.xml`).

**QA 7/7 OK**, sin fixes de código necesarios este ciclo. `git status` mostró solo `next-env.d.ts` regenerado por el build (comentario de URL de docs de Next, no funcional); revertido sin commitear.

## Hallazgo menor nuevo: columna `blog_posts.published` (boolean) desalineada de `blog_posts.status`

- 8/19 filas con `status = 'published'` tienen `published = false`. No es un bug en producción: tanto `app/blog/page.tsx` como `app/blog/[slug]/page.tsx` filtran exclusivamente por `status=eq.published`, nunca leen la columna `published`, así que el sitio sirve correctamente los 19 posts. Es un dato de higiene, no un incidente — se documenta para que quien use la columna `published` en el futuro (ej. un admin UI nuevo) no confíe en ella sin revisar `status` primero.

## Hallazgos reconfirmados (sin cambio de estado)

- **Checkout Gumroad sigue roto** — ~3 días 20h (desde 2026-07-28 18:25:20 UTC), **decimosexto ciclo consecutivo**. `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a `kristian320.gumroad.com/l/ritual-descanso` → `404` (dominio base también 404), reconfirmado con el CTA real servido (`gumroad.com/l/ritual-descanso` en el HTML). URL previa funcional `kristiantronco.gumroad.com/l/ugsqtg` → `200`, revert sigue siendo viable. Sin tocar la env var sin OK de Papu.
- **`lavanda` sigue con `image_cientifica_url` apuntando a un archivo inexistente** (`/images/plants/lavanda-cientifica.jpg` → `404`). Sin escritura en `plants` este ciclo.
- **34/52 fichas (25 seguras + 9 peligrosas) siguen con `ficha_mistica`/`ficha_cientifica` contaminada** con contenido idéntico al de otra especie, sin gate ni corrección — sin cambio. Caso testigo `ashwagandha-fruto` reverificado en vivo: Elemento=Agua, Chakra=Corazón — sigue siendo el perfil místico de Saúco (*Sambucus nigra*), no el de *Withania somnifera*.
- **Duplicado `equinacea`/`echinacea`** (mismo `nombre_latino = Echinacea purpurea`, dos slugs) reconfirmado sin cambio, pendiente de decisión de Papu.
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Tabla `citas`: sigue operativa (desbloqueada el ciclo de las 10:43 UTC). La última cita insertada tiene ~3h30min de antigüedad (< 24h) — **no corresponde insertar una nueva este ciclo**, se hará en el próximo ciclo que supere las 24h desde la inserción anterior.
- `blog_posts`: 90 draft / 19 published, sin cambio (aparte del hallazgo menor de `published` arriba).
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio.
- `public/images/plants/`: 71 archivos, sin cambio.

## Monetización (Paso 2)

- Funnel `/regalo/primera-noche` → lead (`RegaloForm.tsx` → `POST /api/leads`) → `/producto/ritual-descanso`: verificado en código y rutas 200 (PDF de descarga también 200), sin cambios.
- "Tu Planta Aliada": sin implementar (ningún componente/ruta la referencia todavía). Esquema ya propuesto en ciclos anteriores (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin migración necesaria) — sigue vigente, bloqueada por la misma decisión de producto pendiente sobre las 25 fichas seguras contaminadas.

## Optimización continua (Paso 5)

1. Funnel A/B: `leads`/`purchases` en 0 filas, sin datos suficientes para proponer variantes de CTA.
2. "Tu Planta Aliada": test negativo de las 9 peligrosas sigue pasando (imagen y contenido místico ocultos en el render vía filtro hardcoded en `app/diccionario/[slug]/page.tsx`).
3. Cita diaria: sin inserción este ciclo (< 24h desde la anterior, ver arriba). Próximo ciclo aplicable debe insertar una nueva evitando repetir a Paracelso.
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a la URL rota — incidente crítico abierto, no un hito.

## Borradores sociales (solo bitácora, sin publicar)

**Instagram** — ángulo: el regalo como puerta de entrada, sin urgencia de venta.
> No hace falta comprar nada para empezar. En quantum-holistic.com/regalo/primera-noche te regalamos la guía "Primera Noche Tranquila" — sin letra pequeña, sin ficha de pago. Si te sirve, ya sabrás dónde encontrarnos. #QH #fitoterapia #regalo

**LinkedIn** — ángulo: la disciplina de escalar sin urgencia emocional, aplicada a un incidente de 16 ciclos.
> Llevamos dieciséis ciclos consecutivos (desde hace casi 4 días) reportando el mismo checkout roto en la misma línea de la bitácora. No hemos tocado la variable de entorno sin autorización, ni hemos dejado de reportarlo. La disciplina operativa no está en resolver rápido lo que no te corresponde resolver — está en no dejar que la repetición erosione la precisión del reporte. #QH #operaciones #gobernanza

## Tareas manuales de Papu hoy

1. **Resolver el checkout Gumroad** — roto ~3 días 20h, decimosexto ciclo escalándolo. Revert de `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional, verificado 200 este ciclo) o arreglar/republicar el producto en la cuenta `kristian320`. Impacto directo en ventas — cada visitante que pulsa "Comprar por 19€" llega a un 404.
2. **Decidir el alcance del problema de `ficha_mistica`/`ficha_cientifica` sin gate** en las 25 fichas seguras contaminadas: (a) extender el gate `ficha_verificada` a las 52 fichas, o (b) corregir/despublicar fila por fila. Hay un dataset de recuperación ya preparado en `kimiko/recuperacion/fichas-recuperables-2026-07-29.json` y `restaurar-fichas-2026-07-29.sql` de un ciclo anterior. Bloquea también "Tu Planta Aliada". Abierto desde 2026-07-30 02:36 UTC.
3. **Rotar `SUPABASE_SERVICE_ROLE_KEY` y el token OAuth expuestos** (documentado desde 2026-07-25) — sin confirmación de rotación todavía. De paso, decidir si se quiere una UI pública para la tabla `citas` (hoy solo se inserta en base de datos, sin componente visible en el sitio).

---
*Ciclo cloud automático. Sin commits de código este ciclo (build pasa, QA limpio, sin fixes necesarios). Bitácora y memoria commiteadas por el paso dedicado del workflow.*
