# Bitácora — 2026-09-19 15:37 UTC (ciclo 268º, MODO CICLO)

## Qué comprobé

- `kimiko_drafts`: revisado antes que nada pese al disparo `schedule`. Las 4
  filas históricas siguen `status = 'hecho'`, sin ninguna `en_curso` colgada,
  sin orden nueva de Telegram este ciclo.
- `npm ci` + `npm run build`: exit 0, build limpio (misma superficie de
  páginas que ciclos previos, sin errores).
- `npm run lint`: sin warnings ni errores.
- `npm audit`: sin cambio desde el ciclo 223º — 9 vulnerabilidades (1
  moderate, 7 high, 1 critical), todas en `postcss`/cadena de `next-intl`.
  Sin fix no-breaking disponible; `--force` seguiría rompiendo subiendo
  `next` a 16.3.5 y `next-intl` a 4.14.5.
- 8/8 rutas del checklist en producción resuelven a 200 siguiendo
  redirecciones (`curl -L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` sin sesión → `308` a `/admin/` → `200` en
  `/login/?redirect=%2Fadmin%2F`. `middleware.ts` confirmado en la raíz del
  repo (no dentro de `app/`).
- `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`.
  `sitemap.xml` con 37 `<loc>`. `robots.txt` correcto, referencia el
  sitemap.
- Vercel: últimos 5 despliegues (`kimiko` y `quantum-holistic-2`) en estado
  `READY`.
- Diccionario (`plants`, 52 filas): 4 filas `publicada=true AND
  ficha_verificada=true` (`albahaca`, `arnica`, `equinacea`, `hinojo`), sin
  cambio. Las 9 plantas peligrosas confirmadas `publicada=false`.
- Rehecho desde cero el cruce de hash SHA-256 sobre la columna JSON
  `ficha_cientifica` de las 52 filas (pasada quincuagésima cuarta del check
  abierto en el 211º/212º): mismos **7 grupos / 21 filas** duplicadas de
  siempre. Sin cambios.
- `lavanda`: confirmado de nuevo que `lavanda-cientifica.jpg` no existe en
  `public/images/plants/`. `publicada=false`, `ficha_verificada=false` — sin
  blast radius. Hallazgo abierto desde 2026-09-03, cumple **17 días** sin
  resolverse.
- `updated_at` máximo en `plants` sigue en 2026-09-01 → sin cambios que
  auditar visualmente este ciclo.
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Repasados los 22 publicados: sin títulos duplicados, todos con
  `excerpt` <155 caracteres e `image_url` presente.
- **`citas`: el hueco cruzó el umbral de ~24h.** Última fila previa (cita de
  Cicerón, insertada en el 262º) tenía ~30h43min de antigüedad al comprobar.
  Verifiqué y luego inserté una cita nueva: Ralph Waldo Emerson, ensayo
  *Power* (*The Conduct of Life*, 1860) — "The first wealth is health.
  Sickness is poor-spirited, and cannot serve any one: it must husband its
  resources to live." Texto contrastado contra Project Gutenberg #39827
  (dominio público), autor no repetido entre las 43 filas previas, temática
  de salud, sin lenguaje de curación/pseudociencia. `citas` pasa de 43 a 44
  filas.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2 filas).

## Qué arreglé

- **`KIMIKO_MEMORIA.md` había crecido a 11344 líneas / 912KB, por encima del
  límite de lectura de la herramienta Read (256KB)** — no se podía leer
  completo al arrancar el ciclo. Archivé las entradas de los ciclos 1º-198º
  (2026-07-05 a 2026-09-03, ~767KB) en
  `kimiko/memoria-archivo/archivo-01-ciclos-1-a-198.md`, verifiqué con
  `diff` que no se perdió ni duplicó contenido en el corte, y resumí en la
  sección "Checks críticos" del fichero principal los checks permanentes
  que solo constaban en el cuerpo archivado (cruce de hash de
  `ficha_cientifica` del 211º/212º con la nota técnica de `JSON.stringify`,
  y el hallazgo abierto de `lavanda`). El fichero principal queda en ~170KB,
  con margen para varias semanas más antes de repetir el movimiento (dejé
  la instrucción anotada para cuando vuelva a acercarse al límite).
- Inserción en `citas` (ver arriba) — primera escritura en Supabase desde el
  257º/262º.

## Qué encontré sospechoso

- `lavanda` sin imagen en disco, documentado desde el 2026-09-03, ahora con
  17 días abierto. Sigue sin blast radius (no publicada).
- Los 6 pares de fichas científicas duplicadas entre sí siguen sin
  explicación documentada de por qué comparten contenido exacto (mismo
  hallazgo de ciclos anteriores, sin cambio).

## Tareas manuales de Kristian hoy

1. Decidir qué hacer con `lavanda`: subir `lavanda-cientifica.jpg` a
   `public/images/plants/` (imagen real y verificada) o retirar la ficha —
   ya lleva 17 días con la URL apuntando a un fichero que no existe.
2. Revisar si los 6 pares de fichas con `ficha_cientifica` idéntica son
   contenido plantilla intencional o un error de carga — Kimiko no puede
   distinguir esto sin instrucción explícita.
3. `npm audit`: 9 vulnerabilidades siguen sin fix no-breaking desde hace
   semanas (raíz en `postcss`, arrastrada por `next-intl`). Si se quiere
   resolver, requiere subir `next` a 16.3.5 y `next-intl` a 4.14.5 (breaking,
   fuera del mandato de auto-ejecución de Kimiko).
