# Bitácora — 2026-09-05 08:12 UTC (207º ciclo, MODO CICLO)

## Salud del sitio
- `npm ci`: limpio, 415 paquetes.
- `npm run build`: 36/36 páginas generadas sin errores ni avisos de tipos/lint.
- `npx next lint`: sin avisos.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin cambio
  desde el 185º. Fix solo disponible con `--force` (breaking: next@16, postcss
  vía next) — no lo aplico sin validación de Kristian.
- 8/8 rutas del checklist en 200 (`curl -L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión: cadena completa verificada `308` (`/admin` → `/admin/`)
  → `307` (`/admin/` → `/login/?redirect=%2Fadmin%2F`) → `200`.
- `middleware.ts` confirmado en la raíz del repo (no existe duplicado en `app/`).
- Canonical (`https://quantum-holistic.com/`), `og:url` y `og:image` correctos
  en home. `sitemap.xml`: 85 `<loc>`. `robots.txt` con `Allow: /` y referencia
  al sitemap.
- Vercel: últimos 5 despliegues en estado `READY`.

## Integridad del diccionario (`plants`)
- 52 filas totales, 23 con `publicada=true AND ficha_verificada=true`.
- Las 9 plantas peligrosas confirmadas una a una: todas `publicada=false`.
- `updated_at` máximo en `plants` sigue siendo `2026-09-01T09:21:58.968322+00:00`
  (mismo valor exacto que el 189º), sin cambio → no repetí la auditoría visual
  completa este ciclo.
- Duplicado `equinacea`/`echinacea` sigue sin resolver — fuera de mi alcance
  (implicaría borrar/fusionar una fila).

## SEO (`blog_posts`)
- 22 publicados (`published=true`): sin títulos duplicados, ninguno >60
  caracteres, todos con `excerpt` presente y ≤155 caracteres. Único slug de
  diccionario enlazado sigue siendo `hinojo` (`publicada=true,
  ficha_verificada=true`). Ninguna planta peligrosa enlazada en contenido
  publicado.

### Backlog de borradores — un candidato casi listo, pero no publicado
Retomé el backlog investigado a fondo por el 206º (79 `draft`, 32 fragmentos
rotos, 18 títulos duplicados entre los 47 restantes). Filtré por contenido
real (`content` >500 car., con `excerpt` y `tags`) y descarté duplicados por
título: quedan 25 títulos únicos con cuerpo completo. Leí el más prometedor —
*"Nutrición Km0 y Herbología: el Poder de las Plantas Medicinales de
Proximidad"* (slug `nutricion-km0-herbologia-plantas-medicinales-de-proximidad-1783698312`) —
porque es el único cuyo título no contiene ya un claim de curación explícito
ni toca ayuno/adaptógenos/rasayanas/detox hepático. Al leer el cuerpo completo
encontré dos motivos para no publicarlo tal cual:
1. El excerpt y el cierre usan literalmente "comer y **curarse** con lo que
   crece cerca" / "Comer y **curarse** desde la proximidad" — lenguaje de
   claim de curación, aunque en tono coloquial de "alimento como medicina".
2. Describe el diente de león como "apoyo hepático y digestivo" sin incluir
   ninguna contraindicación específica (p. ej. cálculos biliares/vía biliar
   obstruida, la contraindicación clásica de esta planta) — el aviso médico
   final es genérico (embarazo/lactancia/medicación), no cubre ese caso
   concreto que el propio texto menciona.
No es un rechazo del backlog entero, es un "casi": si se sustituye "curarse"
por una formulación no curativa (p. ej. "nutrirse") y se añade la
contraindicación del diente de león, este texto en concreto pasaría el
filtro del Paso 2.4 tal como yo lo aplico. Los otros 24 títulos únicos
restantes no los revisé línea a línea completa este ciclo — la mayoría toca
ya en el título categorías que exigen contraindicaciones obligatorias
(adaptógenos, ayuno, detox) o usa claim de curación directo, así que no
cambian la conclusión del 206º. "Duda = borrador": no publiqué nada.

## Negocio
- `leads`: 0 filas.
- `citas` (frases célebres): última inserción 2026-09-05T03:28:37 UTC (206º
  ciclo) — ~4h44min antes del cierre de este, dentro del umbral normal de 24h,
  sin acción necesaria.
- `kimiko_drafts`: cola vacía (`status in (pendiente, en_curso)` → 0 filas),
  sin orden de Telegram nueva este ciclo (evento `schedule`, MODO CICLO).
- Producto "El Ritual del Descanso": sin cambios de funnel ni de checkout por
  iniciativa propia, según límite del Paso 5.

## Resumen
Salud/SEO/diccionario sin hallazgos técnicos nuevos (undécimo ciclo
consecutivo, 197º–207º). Sin escrituras en Supabase ni commits de código este
ciclo. Único avance real: identifiqué un candidato concreto y casi publicable
del backlog de blog, con los dos cambios exactos que le faltan para pasar el
filtro — más útil para el próximo ciclo (o para Kristian) que repetir que "el
backlog está sucio".

## Tareas manuales de Kristian hoy
1. Si quieres que publique *"Nutrición Km0 y Herbología..."*
   (`nutricion-km0-herbologia-plantas-medicinales-de-proximidad-1783698312`),
   dímelo o edítalo tú: solo necesita quitar el verbo "curarse" del
   excerpt/cierre y añadir la contraindicación de cálculos biliares al hablar
   del diente de león. Tal como está hoy, sigue en `draft`.
2. Duplicado `equinacea`/`echinacea` en `plants` sigue sin resolver — decidir
   cuál fila fusionar o borrar.
3. Las 9 vulnerabilidades de `npm audit` llevan desde el 185º sin cambio; el
   único fix disponible implica subir a Next 16 (breaking change) — decidir si
   se aborda o se sigue aceptando el riesgo.
