# Bitácora — 2026-09-06 08:26 UTC (212º ciclo, MODO CICLO)

## Salud del sitio
- `npm ci`: limpio, 415 paquetes.
- `npm run build`: 36/36 páginas generadas sin errores.
- `npx next lint`: sin avisos.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin cambio
  desde el 185º.
- 8/8 rutas del checklist en 200 (`curl -L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión: cadena completa verificada `308` (`/admin` → `/admin/`)
  → `307` (`/admin/` → `/login/?redirect=%2Fadmin%2F`) → `200`.
- `middleware.ts` confirmado en la raíz del repo.
- Canonical (`https://quantum-holistic.com/`), `og:url` y `og:image` correctos
  en home. `sitemap.xml`: 37 `<loc>` (sin cambio desde el fix del 211º).
  `robots.txt` con `Allow: /` y referencia al sitemap.
- Vercel: últimos 5 despliegues en estado `READY`.

## Integridad del diccionario (`plants`)
- 52 filas totales, **4 con `publicada=true AND ficha_verificada=true`**
  (`albahaca`, `arnica`, `equinacea`, `hinojo`) — sin cambio desde el fix del
  211º. Las 9 plantas peligrosas confirmadas una a una: todas `publicada=false`.
- Aplicado el check permanente fijado en el 211º: cruce mecánico de hash de
  `ficha_cientifica` sobre las 52 filas de golpe (no solo las publicadas).
  Resultado:
  - Los 3 pares de copia exacta que el 211º ya había bajado
    (`tulsi`/`loto`, `valeriana`/`cinamomo`, `manzanilla`/`nigela`) siguen
    correctamente `publicada=false`.
  - **Dos pares nuevos de copia exacta no detectados en el 211º:**
    `muerdago`/`ginseng` y `sauco`/`ashwagandha-fruto`. Ambos pares ya están
    `publicada=false` en las 4 filas, así que no hay ningún riesgo en
    producción ahora mismo — pero si Kristian republica una sola fila de
    cualquiera de los dos pares sin pasar por este cruce, se repetiría
    exactamente el bug del 211º. Lo anoto como ampliación de aquella cicatriz,
    no como incidente nuevo.
  - El clúster de las 9 peligrosas comparte un único hash de
    `ficha_cientifica` — es el placeholder compartido esperado, sin riesgo
    (todas `publicada=false`).
  - `equinacea`/`echinacea` sigue siendo el duplicado conocido y sin resolver
    de fondo (la fila viva, `equinacea`, no comparte hash con ninguna otra;
    `echinacea` está despublicada). No crea duplicado en vivo.
- Verificado que las imágenes de las 4 fichas vivas existen en
  `public/images/plants/` (`albahaca-cientifica.jpg`, `arnica-cientifica.jpg`,
  `equinacea-cientifica.jpg`, `hinojo-cientifica.jpg`).
- Verificado en producción (`curl -sL /diccionario`) que la página lista
  exactamente esas 4 fichas y ninguna otra.

## SEO (`blog_posts`)
- 22 publicados: sin títulos duplicados, ninguno >60 caracteres, todos con
  `excerpt` ≤155 caracteres.
- Backlog no publicado: 87 filas (79 draft + 8 rejected), sin cambio desde el
  209º/210º/211º — sigue sin candidatos nuevos que auditar. El hallazgo del
  209º (≈80% de los títulos únicos duplica tema con un post ya publicado)
  sigue vigente.

## Negocio
- `leads`: 0 filas.
- `citas`: última inserción 2026-09-05T03:28:37 UTC (206º ciclo) — ~29h antes
  del cierre de este ciclo. Dentro del rango de huecos habituales de esta
  tabla (24-40h, según el histórico completo revisado en el 211º). Sin acción.
- `purchases`: 0 filas. `products`: 2 filas, sin cambio.
- `kimiko_drafts`: cola vacía (0 en `pendiente`/`en_curso`), sin orden de
  Telegram nueva este ciclo (evento `schedule`, MODO CICLO).
- Producto "El Ritual del Descanso": sin cambios de funnel ni de checkout por
  iniciativa propia, según límite del Paso 5.

## Resumen
Sin hallazgos técnicos nuevos que requieran acción en producción o en
Supabase este ciclo. El cruce mecánico de `ficha_cientifica` (check
permanente del 211º) encontró dos pares de duplicado exacto adicionales
(`muerdago`/`ginseng`, `sauco`/`ashwagandha-fruto`) que ya estaban
despublicados por otras razones — sin blast radius en vivo, pero documentado
para que una republicación futura de esas filas no repita el bug del 211º.
Sin commits de código ni escrituras en Supabase este ciclo (solo bitácora y
memoria).

## Tareas manuales de Kristian hoy
1. El backlog de 79 borradores de blog sigue mayoritariamente repitiendo
   temas ya publicados (≈80% de los títulos únicos, cuantificado en el
   209º). Vale la pena decidir si se borra en bloque o se usa como material
   de referencia para actualizar los posts ya publicados.
2. Duplicado `equinacea`/`echinacea` en `plants` sigue sin resolver — decidir
   cuál fila fusionar o borrar. Lo mismo aplica ahora a los pares
   `muerdago`/`ginseng` y `sauco`/`ashwagandha-fruto`: ninguna de las 4 filas
   está publicada, así que no urge, pero conviene revisar cuál contenido es
   el correcto de cada par antes de intentar republicar alguna.
3. Las 9 vulnerabilidades de `npm audit` llevan desde el 185º sin cambio; el
   único fix disponible implica subir a Next 16 (breaking change) — decidir
   si se aborda o se sigue aceptando el riesgo.
