# 2026-08-20 12:54 UTC — Ciclo cloud: QA limpio, cita diaria insertada, Gumroad sigue roto (133º ciclo)

## QA (8/8 OK)

1. **Build**: `npm ci && npm run build` pasa sin errores ni warnings de tipos, corrido desde la
   raíz (exit code 0).
2. **Producción**: `curl -s https://quantum-holistic.com` → canonical y `og:url` ==
   `https://quantum-holistic.com/`. OK.
3. **Rutas** (siguiendo redirect por `trailingSlash: true`, esperado): `/`, `/diccionario`,
   `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` →
   200 todas.
4. **`/admin` sin sesión** → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` (200 final).
   `middleware.ts` confirmado en la raíz del repo (no existe `app/middleware.ts`).
5. **Supabase — tabla `plants`**: 52 filas (`content-range: 0-51/52`). Las 9 plantas peligrosas
   (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro,
   tejo, hierba-mora) reverificadas fila por fila: `image_cientifica_url` e `image_mistica_url`
   en `null` en las 9, sin excepción. Placeholder intacto.
6. `npm audit`: 17 vulnerabilidades (1 low, 4 moderate, 12 high, 0 critical), sin cambio frente al
   ciclo anterior.
7. `sitemap.xml` (200) y `robots.txt` (200) presentes, dominio correcto
   (`https://quantum-holistic.com`).
8. Git: única diferencia tras el build fue `next-env.d.ts` (regenerado, no funcional) — revertida
   sin commitear, como en ciclos anteriores.

## Monetización — Checkout Gumroad sigue roto

**~22 días 18h, 133º ciclo consecutivo** desde 2026-07-28T18:25:20.881Z. CTA real reconfirmado en
el HTML servido de `/producto/ritual-descanso/`: apunta a
`https://kristian320.gumroad.com/l/ritual-descanso` (404 en vivo, reconfirmado). El revert viable
sigue siendo `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado). Entrada
`NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, target `production`, tipo
`sensitive`) reconfirmada sin cambio vía API de solo lectura (`updatedAt` = 1785263120881 =
2026-07-28T18:25:20.881Z, idéntico). Sin tocar ninguna env var sin OK de Papu — es un cambio en un
secreto de producción, fuera de mi alcance autónomo.

- Funnel `/regalo/primera-noche` → lead → producto verificado por código (`RegaloForm.tsx`,
  `RitualCheckout.tsx` presentes), rutas 200, sin cambios. `leads`: 0 filas en reposo, sin datos
  nuevos para segmentación A/B por dosha.
- "Tu Planta Aliada": sin implementación en código (confirmado por grep de `planta.?aliada` en
  `app/`, `components/`, `lib/` — sin resultados), sigue pendiente de OK de Papu sobre el esquema
  propuesto y del gate `ficha_verificada`.

## Contenido — sin cambios

- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los 19 drafts en violación del
  checklist identificados en ciclos previos (8 en temas prohibidos: biodescodificación, nutrición
  cuántica, cristales, reiki, chakras; 11 en categoría de contraindicaciones sin cubrir: ayuno,
  adaptógenos, detox hepático, rasayanas) reconfirmados sin cambio, ninguno publicado ni
  reescrito. Los ≥71 drafts restantes siguen sin auditoría frase a frase — sin publicar nada sin
  esa verificación íntegra.
- Sin borradores sociales nuevos este ciclo. Los 2 pendientes de `2026-08-16-0125.md` (IG: "107
  revisiones del mismo semáforo en rojo"; LinkedIn: "tres decisiones de Papu, tres bloqueos
  distintos") siguen sin publicar y no se duplican, para no generar ruido en la bitácora.

## Gate `ficha_verificada` y datos sucios del diccionario — sin cambio

- 0/52 plantas verificadas (pendiente desde 2026-07-30 ~02:36 UTC, ~21 días 10h).
- Duplicado `equinacea` (id 52) / `echinacea` (id 21) sin resolver, mismo `nombre_latino`
  (`Echinacea purpurea`).
- `lavanda` (id 11) sigue sirviendo `image_cientifica_url` que devuelve 404 en vivo
  (`/images/plants/lavanda-cientifica.jpg`, reconfirmado).

## Cita diaria — insertada

Última cita anterior (Thomas Sydenham, `created_at` 2026-08-19T12:52:50.686751Z) alcanzó
~24h01min de antigüedad al arrancar el ciclo, superando el umbral de 24h. Se insertó una nueva
cita de autor no repetido (19 autores previos, todos distintos, verificado antes de insertar):

> "La salud no lo es todo, pero sin ella, todo lo demás es nada." — Arthur Schopenhauer

Autor de dominio público (filósofo, m. 1860), sentencia ampliamente atribuida y verificable, sin
claims de curación ni referencias a biodescodificación/cristales/reiki/chakras — pasa el filtro
anti-pseudociencia. Ahora 20 citas en la tabla, todas de autores distintos.

## Resumen de escrituras este ciclo

Una escritura en Supabase: nueva fila en `citas` (Arthur Schopenhauer, id
`70cb62f5-abc8-4adc-818d-7f30911bcdb2`). Sin commits de código (build pasa sin fixes necesarios;
`next-env.d.ts` revertido sin commitear). Bitácora y memoria las commitea el paso dedicado del
workflow.

## Tareas manuales de Papu hoy

1. **Arreglar el checkout Gumroad** (~22d18h roto, 133 ciclos consecutivos): decidir entre
   publicar el producto en `kristian320.gumroad.com/l/ritual-descanso` (URL actualmente
   configurada, 404) o revertir `NEXT_PUBLIC_GUMROAD_URL` en Vercel (`production`) a
   `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado). Bloquea todo ingreso — máxima
   prioridad, sin cambio desde 2026-07-28.
2. **Decidir el gate `ficha_verificada`** (~21 días pendiente): 0/52 plantas verificadas
   manualmente; bloquea el diccionario y "Tu Planta Aliada".
3. **Resolver el duplicado `equinacea`/`echinacea`** (ids 52/21) y la imagen 404 de `lavanda` —
   limpieza de datos de baja complejidad pero acumulada desde hace semanas.
