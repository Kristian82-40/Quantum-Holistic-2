# Bitácora Kimiko — Ciclo Cloud 2026-07-23 16:50 UTC

## QA — 8/8 checks OK
1. `npm ci && npm run build` → **pasa**. Aviso: `next@14.2.5` tiene vulnerabilidad de seguridad conocida (aviso oficial 2025-12-11, "npm audit" reporta 16 vulnerabilidades: 1 low, 4 moderate, 10 high, 1 critical en el árbol completo de deps). No se actualizó en este ciclo — un bump de versión mayor de Next no es un fix trivial, requiere validación de build/rutas completa. Recomendado para sesión dedicada.
2. Producción: canonical y og:url == `https://quantum-holistic.com/` ✔
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → todas 200 ✔
4. `/admin` sin sesión → 308 (trailing slash) → 307 a `/login/?redirect=%2Fadmin%2F` → 200. Redirect correcto, middleware activo. ✔
5. Supabase tabla `plants` (nombre real de la tabla, no `plantas`): **52 plantas íntegras** confirmado con `Prefer: count=exact`. Desglose por categoría: Maestras 18, Mágicas 18, Sagradas 16. ✔
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora): `image_cientifica_url = null` en las 9. Verificado además que no existe archivo huérfano `public/images/plants/<slug>-cientifica.jpg` para ninguna de las 9 (no hay riesgo de activación accidental). Frontend usa placeholder cuando el campo es null. ✔
7. `leads`: 0 filas, `purchases`: 0 filas — sin actividad nueva desde el ciclo anterior.
8. `sitemap.ts` y `robots.ts` presentes en `app/`, dominio `quantum-holistic.com` correcto en ambos. ✔

## Monetización
- `/producto/ritual-descanso`: `NEXT_PUBLIC_GUMROAD_URL` **sigue sin existir** en Vercel (proyecto `prj_DASuxCUuV72w8CLpZejVij8XcXvL`, revisado vía API). `RitualCheckout.tsx` cae correctamente al fallback de captura de email ("Avísame cuando esté listo"). Sin cambios de código necesarios.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso`: **verificado extremo a extremo en producción** — POST real a `/api/leads/` (nota: usar slash final, `trailingSlash:true` en next.config hace 308 en la variante sin slash y German curl no reenvía el método salvo que se apunte directo con slash), confirmado el insert en Supabase, y borrado inmediato del registro de prueba. Sin regresión.
- **Hallazgo (no accionado):** el build incluye una ruta `/api/webhooks/btcpay`, código de una pasarela de pago (BTCPay/cripto) explícitamente descartada por directiva. Es código heredado, no se tocó (no es mío decidir borrar rutas de pago sin instrucción explícita). Queda documentado para que Papu decida si se elimina.
- **Feature "Tu Planta Aliada" — propuesta de esquema (sin migrar):** no hace falta ninguna migración nueva. La tabla `plants` ya tiene el campo `ficha_mistica.afinidad_ayurvedica` (texto libre tipo "Pitta y Kapha") poblado en 50/52 plantas (faltan `manzanilla` y `equinacea` — dato faltante, no bug). Verificado que las 43 plantas seguras (52 − 9 peligrosas) tienen `image_cientifica_url` válido y el archivo existe en disco — 0 faltantes. Propuesta: un endpoint/función que filtre `plants` por dosha vía `ficha_mistica.afinidad_ayurvedica ILIKE '%<Dosha>%'` con el filtro hardcoded de exclusión de las 9 peligrosas aplicado siempre en el WHERE, no solo en el frontend. No implementado este ciclo — queda como propuesta para OK de Papu.

## Optimización continua
- **Funnel A/B**: `leads` sigue en 0 filas — no hay datos suficientes para proponer variantes de CTA con ningún fundamento. Sin propuesta este ciclo.
- **Cita diaria (tabla `citas`)**: **bloqueado por infraestructura**, no por decisión. La tabla `citas` no existe (`PGRST205`), y este entorno cloud solo tiene `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (API REST de datos vía PostgREST), sin token de Management API ni connection string de Postgres — no hay canal para ejecutar DDL (`CREATE TABLE`). No se intentó ningún workaround. **Necesito que Papu añada un secret con permiso de ejecutar SQL** (Management API token o connection string) al workflow `kimiko-cloud.yml`, o cree la tabla manualmente con este esquema: `id uuid default gen_random_uuid() primary key, texto text not null, autor text not null, created_at timestamptz default now()`.
- **Monitoreo pago**: sin cambios, `NEXT_PUBLIC_GUMROAD_URL` sigue sin valor en Vercel (ver Monetización).

## Contenido
- Backlog `blog_posts`: **90 en `status='draft'`** (subió de 88 registrado el 2026-07-23 tarde/noche — no se generó nada nuevo en medio, el crecimiento es de antes de esa fecha, solo no se había contado bien o cambió el filtro de conteo). 19 `published`.
- Desglose por categoría de los 90 drafts: sabiduría 33+4(sabiduria)=37, nutrición 11, "Bienestar Holístico" 11, herbología 9+3=12, ayurveda 7, bienestar 6, detox 6.
- **Hallazgo concreto y accionable**: al menos **8 drafts violan el checklist anti-pseudociencia explícitamente por el título** — 4× chakras, 1× reiki, 1× cristales/gemoterapia, 1× biodescodificación, 1× "nutrición cuántica". Estos nunca deben publicarse tal cual (ni reescritos ligeramente) — el checklist los excluye por completo, no es un tema de matices.
- **Decisión (sin generar contenido nuevo este ciclo):** seguir sumando drafts sin que nadie revise el backlog no ayuda. Se prioriza este hallazgo sobre generar un draft #91.
- **2 borradores sociales** (no publicados, solo aquí):

  **IG/LinkedIn #1** (foco regalo → funnel):
  > Tu primera noche de descanso real empieza con una guía gratuita. 🌙 Sin suscripciones, sin letra pequeña — descárgala y prueba el ritual esta misma noche. Link en bio → `/regalo/primera-noche`
  > #DescansoReal #QuantumHolistic

  **IG/LinkedIn #2** (foco diccionario/autoridad de contenido):
  > 52 plantas, fichas con evidencia científica y tradición ancestral lado a lado — sin promesas mágicas, sin curas milagrosas. Así construimos el diccionario de Quantum Holistic. Explóralo → `/diccionario`
  > #Herbología #EvidenciaYTradición

## Tareas manuales de Papu hoy
1. **Crear cuenta Gumroad y pasar la URL del producto** (`NEXT_PUBLIC_GUMROAD_URL` en Vercel) — único bloqueador para activar el primer cobro de €19.
2. **Dar a Kimiko Cloud un canal para ejecutar SQL** (Management API token o connection string como secret en `kimiko-cloud.yml`) — sin esto no puedo crear la tabla `citas` ni futuras migraciones reversibles que se propongan (ej. "Tu Planta Aliada" si se decide migrar en vez de usar el campo ya existente).
3. **Decidir qué hacer con el backlog de 90 drafts de blog** (37 "sabiduría", 8 de ellos con títulos que violan el checklist anti-pseudociencia sin ambigüedad — revisar/archivar/borrar). Mientras no haya decisión, este ciclo seguirá sin generar contenido nuevo.

## Notas técnicas
- La tabla de plantas en Supabase se llama `plants`, no `plantas` (el prompt del ciclo usa `plantas`; documentado aquí por si el prompt se actualiza en el futuro — no se tocó `kimiko/PROMPT.md` este ciclo, es una nota informativa).
- Sin cambios de código este ciclo — QA pasó sin fixes necesarios, no hay commit de código (solo esta bitácora, que commitea el workflow).
