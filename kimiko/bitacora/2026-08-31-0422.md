# Bitácora Kimiko — 2026-08-31 04:22 UTC (185º ciclo cloud)

## Qué comprobé

- `npm ci && npm run build`: limpio, sin errores. `npm run lint`: sin warnings/errores.
- 8/8 rutas del checklist en 200 tras seguir redirects (`/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`). `/admin` → `/admin/` (308, `trailingSlash`) → `/login/?redirect=%2Fadmin%2F`
  (307, middleware) → 200. `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` en 200, dominio correcto en `robots.txt`.
- Canonical/`og:url` de la home y de una muestra de páginas internas (`/blog/`,
  `/diccionario/`, `/terapeutas/papu/`): todas apuntan a sí mismas, sin regresión sobre
  los fixes de los ciclos 179º/180º.
- `plants`: 52 filas, sin cambio. **5 publicadas / 4 verificadas** (`albahaca`, `arnica`,
  `equinacea`, `hinojo`); `lavanda` publicada pero sin verificar (caso ya documentado).
  Las 9 peligrosas: las 9 con `publicada=false, ficha_verificada=false`, sin excepción.
  Duplicado `equinacea`/`echinacea` (ids 52/21) reconfirmado sin cambio.
- `kimiko_drafts`: cola vacía, 0 `pendiente`, 0 `en_curso`. Nada que ejecutar en modo orden.
- `blog_posts`: 19 `published` / 90 `draft`, sin cambio de conteo. Sin títulos duplicados
  entre los 19 publicados. Post con lenguaje de "curación" (Aceite de Oliva) sigue
  publicado pendiente de Papu (ciclo 140, 45 ciclos sin cambio).
- `leads`: 0 filas, sin cambio.
- Cita diaria: última inserción (Proverbios 17:22) a las 19:09:07 UTC del 08-30,
  ~9h de antigüedad al comprobar (04:18 UTC) — por debajo del umbral de 24h, sin
  inserción nueva este ciclo.
- Gumroad: `https://kristian320.gumroad.com/l/ritual-descanso` sigue en 404. Sin cambio
  de env var desde el 180º ciclo.

## Qué arreglé

**`npm audit` nunca se había desglosado por vulnerabilidad individual en 184 ciclos —
siempre se citó como bloque ("17 vulns sin cambio").** Al desglosar con `npm audit --json`,
10 de las 17 tenían `fixAvailable` sin salto de versión mayor (`esbuild`, `js-yaml`,
`minimatch`, `nanoid`, `resend`, `svix`, `uuid`, `ws`, `@typescript-eslint/parser`,
`@typescript-eslint/typescript-estree`, `brace-expansion`). Corrí `npm audit fix` (sin
`--force`): **17 → 9 vulnerabilidades**, `package.json` sin cambios (todo dentro del
rango semver ya declarado, solo se movió `package-lock.json`). Verificado con `npm ci`
limpio desde cero + `npm run build` + `npm run lint` tras el cambio: sin errores, sin
regresión. Commit `ec913e7`, desplegado en Vercel (`dpl_GLrzV3Fkvh6mEbq2Lku8bqRx2aRA`
tras el nuevo build, `READY` confirmado vía API) y reverificado en vivo: 8/8 rutas en
200, cadena de `/admin` intacta, canonical de la home correcto.

**Check permanente para el Paso 2.1: no basta con comparar el conteo bruto de
`npm audit` ciclo a ciclo — desglosar con `npm audit --json` y mirar `fixAvailable`
por paquete al menos periódicamente. El conteo bruto puede quedarse estancado durante
decenas de ciclos escondiendo una fracción con fix seguro disponible (sin `--force`,
sin salto de versión mayor), como pasó aquí: 41 ciclos citando "17 vulns" sin que
nadie comprobara si `npm audit fix` sin más las resolvía.**

Las 9 vulnerabilidades restantes (`next`, `postcss`, `glob`, `eslint-config-next`,
`next-intl`) requieren `--force` con cambio de versión mayor (Next.js 16 actual →
mayor; `next-intl` 4.9.1 → 4.14.1 con posible breaking change) — no se tocaron, fuera
del criterio de "reversible y aditivo" de mis herramientas para este tipo de cambio.
Documentado como tarea manual.

## Tareas manuales de Kristian hoy

1. **Gumroad sigue en 404** tras 33 días+ roto y con la env var ya puesta desde el
   180º ciclo (`NEXT_PUBLIC_GUMROAD_URL`) — hay que publicar/revisar el producto
   `ritual-descanso` directamente en el panel de Gumroad, no es algo que se arregle
   desde el repo.
2. **Post de Aceite de Oliva** (`aceite-de-oliva-virgen-extra-medicina-556960`) sigue
   publicado con lenguaje de "curación" sin revisión de Papu — 45 ciclos sin cambio.
3. **Las 9 vulnerabilidades restantes de `npm audit`** (`next` 16.3.3, `postcss`,
   `next-intl` 4.14.1, `eslint-config-next`, `glob`) necesitan `npm audit fix --force`
   con cambio de versión mayor — decisión de producto/QA, no algo que Kimiko deba
   forzar por su cuenta sin revisión de comportamiento tras el upgrade.
