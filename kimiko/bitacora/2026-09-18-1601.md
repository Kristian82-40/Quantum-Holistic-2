# Bitácora — 2026-09-18 16:01 UTC (ciclo 263º, MODO CICLO)

## kimiko_drafts
Comprobado primero, antes del resto del checklist, pese al disparo `schedule`:
las 4 filas históricas siguen `status = 'hecho'`, sin ninguna `en_curso`
colgada. Sin orden de Telegram nueva este ciclo.

## Salud del sitio
- `npm ci` limpio.
- `npm run build`: exit code 0, sin errores, todas las páginas compiladas
  (estático + dinámico + middleware).
- `npm audit`: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate,
  7 high, 1 critical), todas exigen `--force` con breaking changes
  (`next@16.3.5`, `next-intl@4.14.5`). Sigue sin fix no-breaking disponible.
- Rutas del checklist: 8/8 resuelven en 200 siguiendo redirects (`/` directo
  en 200; el resto da 308 a la versión con barra final, comportamiento
  esperado del i18n routing, y termina en 200).
- `/admin` sin sesión → `/admin/` (308) → `/login/?redirect=%2Fadmin%2F`
  (307) → 200. Protección de middleware funcionando.
- `middleware.ts` confirmado en la raíz del repo, no dentro de `app/`.
- Producción: `canonical` y `og:url` en `https://quantum-holistic.com/`,
  correctos.
- `sitemap.xml`: 37 `<loc>`. `robots.txt` presente y con `Sitemap:` correcto.
- Vercel: últimos 5 despliegues (`quantum-holistic-2` y `kimiko`) en `READY`.

## Diccionario (check permanente del 211º/212º)
Rehecho el cruce de hash de la columna JSON `ficha_cientifica` desde cero
sobre las 52 filas de `plants` (no los ficheros de imagen). Mismos
**7 grupos / 21 filas** de siempre:
- `tulsi` / `loto`
- `valeriana` / `cinamomo`
- `muerdago` / `ginseng`
- `sauco` / `ashwagandha-fruto`
- `manzanilla` / `nigela`
- `equinacea` / `echinacea`
- las 9 peligrosas compartiendo el mismo placeholder

Sin cambios respecto al 249º.

`plants`: 52 filas. 4 publicada+verificada sin cambio (`albahaca`, `arnica`,
`equinacea`, `hinojo`), las 4 imágenes (`*-cientifica.jpg`) confirmadas en
`public/images/plants/`. Las 9 peligrosas confirmadas `publicada = false`.
`lavanda` sigue con `image_cientifica_url` rellena en la fila pero el
fichero sigue ausente en disco (hallazgo abierto desde 2026-09-03,
despublicada y sin `ficha_verificada`, sin blast radius).

## SEO / Blog
`blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin cambio.
Repasados los 22 publicados: sin títulos duplicados, todos los `excerpt`
por debajo de 155 caracteres, `image_url` presente en los 22.

## Citas
Última fila sigue siendo la de Cicerón (*De Senectute*, insertada en el
262º a las 08:54:55 UTC). Hueco al comprobar: **~7h06min** — muy por debajo
del umbral de ~24h. `citas`: 43 filas, sin cambio, sin necesidad de
insertar este ciclo.

## Negocio
`leads` en 0, `purchases` en 0, `products` sin cambio (2 filas).
Estrategia "El Ritual del Descanso" sigue sin tocar — no aprobada.

## Resumen
Cuadragésimo noveno ciclo consecutivo (212º-263º) sin hallazgos técnicos
nuevos en el checklist mecánico de código/build/rutas, y sin hallazgos de
negocio nuevos. Sin commits de código ni escrituras en Supabase este
ciclo (el único cambio de estado, `citas`, no cruzó el umbral).

## Tareas manuales de Kristian hoy
1. Decidir qué hacer con `lavanda`: falta el fichero de imagen en disco
   desde hace 15 días. O se sube la imagen y se verifica la ficha, o se
   retira la fila de la cola de trabajo pendiente para dejar de arrastrarla.
2. Las 9 vulnerabilidades de `npm audit` (incluida 1 crítica) siguen sin
   fix que no rompa `next`/`next-intl`. Si quieres que las actualice,
   dime y planifico el upgrade de major version aparte, con su propio
   ciclo de pruebas — no lo voy a hacer por iniciativa propia por el
   riesgo de romper el build.
3. Ninguna acción urgente más: sitio, diccionario, SEO y negocio estables.
