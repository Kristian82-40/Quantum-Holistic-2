# Bitácora Kimiko — Ciclo Cloud 2026-07-23 21:09 UTC

## QA — 8/8 checks OK
1. `npm ci && npm run build` → **pasa**. Mismo aviso persistente: `next@14.2.5` con vulnerabilidad de seguridad conocida (aviso 2025-12-11), `npm audit` reporta 16 vulnerabilidades (1 low, 4 moderate, 10 high, 1 critical). No se actualiza este ciclo — requiere sesión dedicada de validación, no es un bump trivial.
2. Producción: canonical y og:url == `https://quantum-holistic.com/` ✔
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → todas 200 ✔
4. `/admin` sin sesión → 307 a `/login/?redirect=%2Fadmin%2F`. Middleware activo en raíz (`middleware.ts`, no en `app/`). ✔
5. Supabase tabla `plants`: **52 plantas íntegras** (`content-range: 0-0/52`). ✔
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora): `image_cientifica_url` y `image_mistica_url` en `null` para las 9. Sin archivo huérfano en `public/images/plants/`. ✔
7. `leads`: 0 filas, `purchases`: 0 filas — sin actividad nueva.
8. `sitemap.ts` y `robots.ts` presentes, dominio `quantum-holistic.com` correcto en ambos. ✔

Sin diferencias frente a los dos ciclos anteriores del mismo día (16:52 y 17:27 UTC) — ningún commit de código entre medias.

### Hallazgo nuevo (menor, sin acción): plantas duplicadas
`plants` tiene dos filas para la misma especie: `equinacea` (nombre_es "Equinácea") y `echinacea` (nombre_es "Echinacea"), ambas con `nombre_latino = "Echinacea purpurea"`. Ambas son seguras (no están en la lista de las 9 peligrosas), así que no es un riesgo de seguridad — es un problema de calidad de contenido/duplicación de datos. No se toca sin decisión de Papu sobre cuál slug es el canónico (afecta URLs ya indexadas en `/diccionario/[slug]`, cambiarlo sin cuidado rompería enlaces).

### Confirmación: `image_mistica_url` no es un bug
Las 43 plantas seguras tienen `image_mistica_url = null` — comprobado contra el código (`app/diccionario/page.tsx`, `app/diccionario/[slug]/page.tsx`): el frontend solo lee `image_cientifica_url`, el campo "mística" no está implementado en ningún componente todavía. No es una regresión ni un dato faltante que arreglar, es un campo sin feature construida encima.

## Monetización
- `/producto/ritual-descanso`: `NEXT_PUBLIC_GUMROAD_URL` **sigue sin existir** en Vercel (verificado vía API contra `prj_DASuxCUuV72w8CLpZejVij8XcXvL`). `RitualCheckout.tsx` cae al fallback de captura de email. Sin cambios de código.
- Funnel `/regalo/primera-noche` → `/producto/ritual-descanso`: páginas live, sin regresión. No se repitió el test end-to-end de POST/DELETE en `leads` este ciclo (ya verificado extremo a extremo en el ciclo de 16:52 UTC, sin cambios de código desde entonces).
- **Hallazgo heredado, sin acción:** `/api/webhooks/btcpay` (pasarela cripto descartada por directiva) sigue en el build. No se toca sin instrucción explícita de Papu.
- **"Tu Planta Aliada":** sin cambios respecto a la propuesta ya en bitácora (ciclo 16:52 UTC) — usar `ficha_mistica.afinidad_ayurvedica` (poblado en 50/52 plantas; faltan `manzanilla` y `equinacea`) con filtro hardcoded de exclusión de las 9 peligrosas en el `WHERE`/lógica de selección. Sigue pendiente de OK de Papu, no implementada.

## Optimización continua
- **Funnel A/B:** `leads` sigue en 0 filas — sin datos para proponer variantes de CTA.
- **Cita diaria (`citas`):** sigue bloqueada. Confirmado de nuevo: `PGRST205`, la tabla no existe y este entorno cloud no tiene canal DDL (solo `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` vía REST, sin Management API token ni connection string).
- **Monitoreo pago:** sin cambios, `NEXT_PUBLIC_GUMROAD_URL` sigue sin valor en Vercel.

## Contenido
- Backlog `blog_posts`: **90 en `status='draft'`, 19 `published`** — sin cambio desde los ciclos anteriores del día (sabiduría 33+4, nutrición 11, "Bienestar Holístico" 11, herbología 9+3, ayurveda 7, bienestar 6, detox 6).
- Confirmados los mismos **8 drafts con violación explícita del checklist anti-pseudociencia** por título (chakras ×3, reiki, cristales/gemoterapia, biodescodificación, "nutrición cuántica"): `chakras-y-alimentacion-colores-que-sanan-498063`, `reiki-y-bioenergetica-fundamentos-de-la-sanacion-energetica-1780093279`, `los-7-chakras-principales-y-como-equilibrarlos-1780097836`, `cristales-y-gemoterapia-fundamentos-y-practica-1780097196`, `biodescodificacion-y-el-lenguaje-del-cuerpo-1780096414`, `chakras-y-alimentacion-colores-que-sanan`, `chakras-y-colores-que-sanan-557229`, `nutricion-cuantica-y-coherencia-celular-1780097677`. Quedan listados aquí explícitamente para que Papu pueda actuar sobre ellos directamente (archivar/borrar) sin tener que volver a buscarlos.
- **Decisión (repetida, sin generar contenido nuevo):** mientras el backlog de 90 drafts siga sin revisión de Papu, seguir generando drafts nuevos no ayuda.
- Sin borradores sociales nuevos este ciclo — los 2 del ciclo de las 16:52 UTC siguen vigentes y sin publicar, no se duplican aquí.

## Tareas manuales de Papu hoy
1. **Crear cuenta Gumroad y pasar la URL del producto** (`NEXT_PUBLIC_GUMROAD_URL` en Vercel) — único bloqueador para el primer cobro de €19.
2. **Decidir sobre el backlog de 90 drafts de blog** — en particular los 8 listados arriba con violación inequívoca del checklist anti-pseudociencia (candidatos directos a archivar/borrar).
3. **Dar a Kimiko Cloud un canal para ejecutar SQL** (Management API token o connection string como secret en `kimiko-cloud.yml`) — necesario para crear la tabla `citas` y para decidir el slug canónico del duplicado `equinacea`/`echinacea` sin riesgo de romper enlaces indexados.

## Notas técnicas
- Sin cambios de código este ciclo — QA pasó sin fixes necesarios. Este ciclo no genera commit de código propio; bitácora y memoria las commitea el paso dedicado del workflow.
- Tercer ciclo cloud del día (anteriores: 16:52 y 17:27 UTC) — todos los indicadores (QA, leads, purchases, backlog) siguen congelados exactamente igual, consistente con ninguna actividad de usuarios reales ni cambio manual de Papu en la ventana entre ciclos.
