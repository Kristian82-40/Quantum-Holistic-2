# Bitácora Kimiko — Ciclo Cloud 2026-07-27 07:39 UTC (Vigesimocuarto ciclo)

## QA — 8/8 checks OK

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.5, 36 rutas generadas, `ƒ Middleware` presente).
2. **Producción:** `curl -s https://quantum-holistic.com` → canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (con `-L`): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → todas `200` ✅.
4. **`/admin` sin sesión** → redirect a `/login/?redirect=%2Fadmin%2F` (`200` final) ✅. `middleware.ts` confirmado en raíz, no en `app/` ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`) ✅.
6. **9 plantas peligrosas con placeholder:** `image_cientifica_url`/`image_mistica_url` = `null` para las 9 (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) ✅. Nunca activadas.
7. **`leads`/`purchases`:** 0 filas en ambas tablas al cierre. Último test E2E real de escritura fue hace ~4h13min (ciclo 03:23 UTC), por debajo del umbral de 24h → no se repite el test de escritura real este ciclo (evita ensuciar datos sin necesidad).
8. **`sitemap.ts` y `robots.ts`:** presentes en `app/`, dominio `https://quantum-holistic.com` correcto en ambos ✅.

## Monetización

- `NEXT_PUBLIC_GUMROAD_URL` sigue ausente en Vercel (12 env vars totales, confirmado vía project ID directo `prj_DASuxCUuV72w8CLpZejVij8XcXvL`). `RitualCheckout.tsx` mantiene el fallback correcto: sin Gumroad, formulario de captura de email a `/api/leads` con `source: 'ritual_descanso_waitlist'`. Vigesimocuarto ciclo consecutivo sin el secret.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso`: sin cambios estructurales, páginas verificadas 200 en el QA de este ciclo.
- **"Tu Planta Aliada":** sin implementar, sin cambios en la propuesta (cruce `profiles.dosha` × `plants.ficha_mistica->>afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin necesitar migración). Cobertura reconfirmada: 41/43 plantas seguras con `afinidad_ayurvedica` (faltan `manzanilla` y `equinacea`, sin cambios). Pendiente OK de Papu para migrar/activar.
- Canal DDL para tabla `citas` (paso 5.3) sigue bloqueado: sin `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` en el entorno cloud (`/rest/v1/citas` → `404`, tabla no existe). 25º ciclo consecutivo bloqueado, sin herramienta MCP de Supabase disponible en este entorno para saltar el bloqueo.

## Contenido

- Backlog `blog_posts`: 90 drafts / 19 published, sin cambio. Sin contenido nuevo generado este ciclo (backlog grande sin revisar por Papu; generar más no ayuda, decisión ya tomada en ciclos previos).
- **8 drafts con violación de checklist**, mismo total y mismo desglose que el ciclo anterior: 4 chakras + 1 reiki + 1 cristales + 1 biodescodificación + 1 nutrición cuántica. Ninguno publicado.
- Duplicados reconfirmados sin cambios: `equinacea`/`echinacea` (real, pendiente decisión de Papu) y `ashwagandha`/`ashwagandha-fruto` (descartado, partes distintas de la planta).
- **Funnel A/B (`leads`):** sin datos, tabla en 0 filas reales. No hay volumen para proponer variantes de CTA todavía.
- 2 borradores sociales nuevos (sin publicar, solo aquí):
  - **Instagram:** "Hay noches en que no hace falta un cambio de vida entero, solo un ritual de tres pasos: una infusión suave, una rutina de desconexión de 10 minutos y un ajuste ligero de la cena. Te dejamos la guía completa, gratis, en el enlace de la bio — 'Tu Primera Noche Tranquila'. Sin promesas milagrosas: es un punto de partida, no una cura."
  - **LinkedIn:** "Seguimos sin pasarela de pago activa para 'El Ritual del Descanso' — así que en vez de simular una compra que no existe, el CTA de la página muestra justo eso: una lista de espera real, con un email real, mientras terminamos de activar el cobro. Preferimos una página honesta sobre dónde estamos a una que finge estar más avanzada de lo que está."

## Imágenes huérfanas y vulnerabilidades

- `public/images/plants/`: 73 archivos totales, **30 huérfanos** (no referenciados por ninguna URL en `plants`) — 1 ya conocido (`plant-00-beleno-negro-cientifica.jpg`, candidato peligroso sin resolver) + 29 nombres de plantas comunes no catalogadas. Sin cambios.
- `npm audit --json`: 1 critical / 10 high / 4 moderate / 1 low — mismo desglose que ciclos anteriores. **Detalle nuevo identificado este ciclo:** el critical es la propia dependencia `next` (`14.2.5`), afectada por varios CVEs conocidos incluyendo "Next.js Cache Poisoning" (`GHSA-gp8f-8m3g-qvj9`, CVSS 7.5) — `npm audit` reporta `fixAvailable` a `next@14.2.35`, **sin salto de versión mayor** (`isSemVerMajor: false`). No lo actualizo de forma autónoma en este ciclo: es una dependencia de framework que toca todo el build/middleware, y ciclos anteriores ya documentaron un incidente real de middleware mal ubicado — un cambio así merece revisión humana antes de un push directo a `main` sin PR.

## Tareas manuales de Papu hoy

1. **Actualizar `next` de `14.2.5` a `14.2.35`** (sin salto de versión mayor) para cerrar la vulnerabilidad crítica de caché ("Next.js Cache Poisoning", CVSS 7.5) que `npm audit` viene reportando desde hace varios ciclos sin que se identificara la causa específica hasta hoy.
2. **Rotar el token OAuth expuesto en ciclos anteriores** (2026-07-25, 06:22 y 09:52 UTC) — sigue sin confirmación de rotación, ya son 10 ciclos escalándolo.
3. **Activar Gumroad** (`NEXT_PUBLIC_GUMROAD_URL` en Vercel) para desbloquear el CTA transaccional de `/producto/ritual-descanso` — bloqueo de monetización desde hace 24 ciclos consecutivos.

*(Nota: las 30 imágenes huérfanas en `public/images/plants/` siguen pendientes de decisión de Papu, pero se retiran del top-3 este ciclo para dar espacio al hallazgo nuevo de seguridad — sin cambio de estado, ver sección anterior.)*

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios); bitácora y memoria las commitea el paso dedicado del workflow.
