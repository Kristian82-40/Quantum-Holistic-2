# Kimiko — Ciclo cloud 2026-07-31 21:10 UTC

## QA (Paso 1)
1. `npm ci && npm run build` (desde la raíz del repo): **OK**, sin errores de tipos ni lint (37 rutas, `ƒ Middleware` presente, 79.5 kB).
2. Producción (`https://quantum-holistic.com`): canonical y `og:url` == `https://quantum-holistic.com/`. OK.
3. Rutas 200 (siguiendo redirect de trailing slash): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`. OK.
4. `/admin` sin sesión → `308` a `/admin/` → `307` a `/login/?redirect=%2Fadmin%2F` (200 final). `middleware.ts` confirmado en la raíz del repo, no en `app/`. OK.
5. Supabase `plants`: **52/52** filas íntegras (`content-range: 0-51/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null` — placeholder intacto, reverificado fila por fila.
6. `leads`/`purchases`: ver hallazgo del ciclo abajo (test E2E real repetido, tabla de vuelta a 0 filas).
7. `sitemap.xml` (200) y `robots.txt` (200): dominio correcto (`Sitemap: https://quantum-holistic.com/sitemap.xml`). `app/sitemap.ts` y `app/robots.ts` presentes en la raíz.

**QA 7/7 OK**, sin fixes de código necesarios este ciclo. `git status` mostró solo `next-env.d.ts` regenerado por el build (comentario no funcional); revertido sin commitear.

## Hallazgo del ciclo: el test E2E real de `leads` llevaba desde 2026-07-28 06:31 UTC sin repetirse

Al rastrear la cadena completa de la memoria (no solo la bitácora inmediatamente anterior, que solo repetía "por debajo del umbral de 24h" sin recomputar la fecha), el último `POST`/`DELETE` real documentado fue el del **trigésimo ciclo (2026-07-28 06:31 UTC)** — todos los ciclos posteriores, incluida la sesión interactiva del 29-jul y once ciclos cloud más, solo leyeron `Content-Range` (`*/0`) sin repetir la escritura, pese a que el umbral de 24h se cruzó hace más de dos días y medio. Es un fallo de proceso, no un incidente de producto: cada bitácora heredó la frase "sin test E2E repetido, por debajo del umbral" de la anterior sin verificar la fecha real contra la memoria completa.

Repetido este ciclo sin incidencias:
- `POST https://quantum-holistic.com/api/leads/` (con `-L`, trailing slash) con email de prueba `kimiko-qa-20260731210931@example-qa.invalid`, `source=kimiko_qa_e2e` → `{"ok":true}`.
- Verificación directa en Supabase por `email` → fila presente (`id`, `created_at` confirmados).
- `DELETE` de limpieza por `id` → `204`.
- Reverificación de `content-range: */0` en `leads` — tabla de vuelta a 0 filas, sin rastro del registro de prueba.

**Regla derivada:** al citar "último test real: HH:MM UTC" en una bitácora, no basta con copiar la frase de la bitácora anterior — hay que confirmar la fecha contra el propio hallazgo documentado (`grep` de "Test E2E real de leads repetido" en `KIMIKO_MEMORIA.md`), porque una frase repetida sin recomputar puede arrastrar una fecha obsoleta indefinidamente.

## Hallazgos reconfirmados (sin cambio de estado)

- **Checkout Gumroad sigue roto** — ~3 días 2h45min (desde 2026-07-28 18:25:20 UTC), **decimoctavo ciclo consecutivo**. El CTA real servido en `/producto/ritual-descanso/` sigue apuntando a `https://kristian320.gumroad.com/l/ritual-descanso` → `404` (dominio base `kristian320.gumroad.com` también `404`). URL previa funcional `kristiantronco.gumroad.com/l/ugsqtg` → `200`, revert sigue siendo viable. Sin tocar `NEXT_PUBLIC_GUMROAD_URL` sin OK de Papu. `NEXT_PUBLIC_GUMROAD_URL` sigue sin existir como env var en Vercel (mismo trío de secrets: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `VERCEL_TOKEN`, sin novedad).
- **`lavanda` sigue con `image_cientifica_url` apuntando a un archivo inexistente** (`/images/plants/lavanda-cientifica.jpg` → `404`). Sin escritura en `plants` este ciclo.
- **34/52 fichas (25 seguras + 9 peligrosas) siguen con `ficha_mistica`/`ficha_cientifica` contaminada.** Caso testigo `ashwagandha-fruto` reverificado en vivo (label + valor extraído del HTML, no grep literal): Elemento=Agua, Chakra=Corazón — sigue siendo el perfil místico de Saúco (*Sambucus nigra*), no el de *Withania somnifera*.
- **Duplicado `equinacea`/`echinacea`** (mismo `nombre_latino = Echinacea purpurea`, dos slugs) reconfirmado sin cambio, pendiente de decisión de Papu.
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Tabla `citas`: última cita (Paracelso, insertada 2026-07-31 10:43:33 UTC) tiene ~10h27min de antigüedad (< 24h) — **no corresponde insertar una nueva este ciclo**. Próxima inserción aplicable a partir de ~2026-08-01 10:43 UTC.
- `blog_posts`: 90 draft / 19 published, sin cambio. Reconfirmados por título al menos 7 drafts con violación explícita del checklist anti-pseudociencia (chakras ×3, reiki, cristales/gemoterapia, biodescodificación) — backlog crónico, dieciocho+ ciclos sin revisión de Papu.
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low (16 total), sin cambio. Incluye vulnerabilidad conocida en `ws` (uninitialized memory disclosure + DoS por fragmentos, fix vía `npm audit fix` sin breaking changes) — no aplicado de forma autónoma este ciclo, se documenta para que Papu decida si vale la pena el bump aislado.
- `public/images/plants/`: 71 archivos, sin cambio.
- `/api/webhooks/btcpay` (pasarela cripto descartada por directiva) sigue presente en el build — deuda técnica heredada, sin tocar.

## Monetización (Paso 2)

- Funnel `/regalo/primera-noche` → lead (`RegaloForm.tsx` → `POST /api/leads`) → `/producto/ritual-descanso`: verificado en código y en producción, incluyendo el test E2E real de este ciclo (ver hallazgo arriba). PDF de descarga (`/downloads/primera-noche-tranquila.pdf`) sin cambios.
- "Tu Planta Aliada": sin implementar. Esquema propuesto en ciclos anteriores (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin migración necesaria) sigue vigente, bloqueada por la decisión pendiente sobre las 25 fichas seguras contaminadas.

## Optimización continua (Paso 5)

1. Funnel A/B: `leads`/`purchases` en 0 filas reales (el registro de prueba de este ciclo se limpió), sin datos suficientes para proponer variantes de CTA.
2. "Tu Planta Aliada": test negativo de las 9 peligrosas sigue pasando (imagen y contenido místico ocultos en el render vía filtro hardcoded en `app/diccionario/[slug]/page.tsx`).
3. Cita diaria: sin inserción este ciclo (< 24h desde la anterior, ver arriba).
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a la URL rota — incidente crítico abierto, no un hito.

## Borradores sociales (solo bitácora, sin publicar)

**Instagram** — ángulo: la guía de regalo sigue funcionando aunque el checkout no.
> Mientras seguimos resolviendo el checkout, la guía "Primera Noche Tranquila" sigue disponible gratis en quantum-holistic.com/regalo/primera-noche. No hace falta esperar a que todo esté listo para empezar algo pequeño. #QH #fitoterapia #regalo

**LinkedIn** — ángulo: por qué verificar de nuevo algo que "ya se sabía" no es perder el tiempo.
> Esta semana encontramos que un chequeo automático llevaba días citando una fecha de verificación desactualizada sin volver a comprobarla. La corrección no fue dramática: solo repetir la prueba real en vez de confiar en la nota anterior. La mayoría de la fiabilidad operativa se construye así, no con grandes rescates. #QH #operaciones #QA

## Tareas manuales de Papu hoy

1. **Resolver el checkout Gumroad** — roto ~3 días 2h45min, decimoctavo ciclo escalándolo. Revert de `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional, verificado 200 este ciclo) o arreglar/republicar el producto en la cuenta `kristian320`. Impacto directo en ventas — cada visitante que pulsa "Comprar por 19€" llega a un 404.
2. **Decidir el alcance del problema de `ficha_mistica`/`ficha_cientifica` sin gate** en las 25 fichas seguras contaminadas: (a) extender el gate `ficha_verificada` a las 52 fichas, o (b) corregir/despublicar fila por fila. Dataset de recuperación ya preparado en `kimiko/recuperacion/fichas-recuperables-2026-07-29.json` y `restaurar-fichas-2026-07-29.sql`. Bloquea también "Tu Planta Aliada". Abierto desde 2026-07-30 02:36 UTC.
3. **Rotar `SUPABASE_SERVICE_ROLE_KEY` y el token OAuth expuestos** (documentado desde 2026-07-25), sin confirmación de rotación todavía. De paso, decidir el duplicado `equinacea`/`echinacea` y si aplicar `npm audit fix` para la vulnerabilidad de `ws` (sin breaking changes, según el propio `npm audit`).

---
*Ciclo cloud automático. Sin commits de código este ciclo (build pasa, QA limpio, sin fixes necesarios). 1 escritura de datos de prueba (test E2E de leads) con limpieza inmediata confirmada. Bitácora y memoria commiteadas por el paso dedicado del workflow.*
