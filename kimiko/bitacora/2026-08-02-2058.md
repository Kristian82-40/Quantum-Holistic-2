# Bitácora — Ciclo cloud 2026-08-02 20:58 UTC

## QA (7/7 + verificación extendida de imágenes)
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz.
- Producción: canonical y `og:url` == `https://quantum-holistic.com` en el HTML servido. OK.
- Rutas (siguiendo redirects de trailing slash 308, propios de Next.js): `/`, `/diccionario`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` →
  200. OK.
- `/admin` sin sesión: `308` (trailing slash) → `307` a `/login/?redirect=%2Fadmin%2F` → `200`.
  Middleware en la raíz (`middleware.ts`), funcionando correctamente.
- Supabase: 52 plantas íntegras (`Prefer: count=exact`). Las 9 plantas peligrosas (aconito,
  datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo,
  hierba-mora) reverificadas fila por fila: `image_cientifica_url`/`image_mistica_url` en `null`
  en las 9. Ninguna imagen activada.
- `leads`: 0 filas (solo lectura este ciclo, sin escritura de prueba).
- `sitemap.ts`/`robots.ts` presentes, dominio correcto (`https://quantum-holistic.com`).
- **Verificación extendida:** se testearon en vivo las 43 URLs de imagen únicas de las 43 plantas
  seguras (excluyendo las 9 peligrosas) contra producción. Único 404: `lavanda-cientifica.jpg`
  (hallazgo del 2026-07-29, sin cambio). Las 42 restantes, 200.

## Monetización
- **Checkout Gumroad sigue roto — 5 días 2h33min de antigüedad, trigésimo ciclo consecutivo.**
  CTA real servido en `/producto/ritual-descanso/` (verificado en HTML de producción) apunta a
  `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo).
  `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable.
  `updatedAt` de la env var `NEXT_PUBLIC_GUMROAD_URL` en Vercel (API `v9/projects/.../env`)
  reconfirmado sin cambio: `2026-07-28T18:25:20.881Z` (production), idéntico al de ciclos
  anteriores. Sin tocar la env var sin OK de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado: PDF real
  (`/downloads/primera-noche-tranquila.pdf`) 200, rutas 200, sin cambios de código.
- "Tu Planta Aliada": sin match en `app/`, `components/`, `lib/` — sigue sin implementar en
  código. Esquema ya propuesto en ciclos anteriores, pendiente de OK de Papu (tarea manual).

## Datos / hallazgos conocidos (sin cambio)
- Gate `ficha_verificada`: 0/52 verificadas.
- `equinacea`/`echinacea`: duplicado reconfirmado sin cambio.
- `olivo`/cardo mariano: contaminación de ficha reconfirmada sin cambio (bug de identidad por
  `id`, parte de las 34 fichas contaminadas).
- `lavanda`: imagen rota (404), reconfirmada sin cambio.
- Las 34 fichas contaminadas (25 seguras + 9 peligrosas) siguen sin gate ni corrección — decisión
  de producto pendiente de Papu desde 2026-07-30 02:36 UTC (tarea manual histórica #2).
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio. `uuid` y `ws` con parche
  disponible sin breaking changes (confirmado por dry-run en ciclos previos), sin aplicar
  (30 ciclos deferido a Papu).
- Cobertura `afinidad_ayurvedica`: 43/43 plantas seguras, sin cambio.
- Imágenes en `public/images/plants`: 71 archivos, sin cambio.

## Cita diaria
- Última inserción: Maimónides, `2026-08-02T13:28:24.777Z` — ~7h30min de antigüedad este ciclo,
  bajo el umbral de 24h. Sin inserción nueva. Próxima inserción esperada ~2026-08-03 13:28 UTC.

## Contenido
- Sin publicaciones en blog este ciclo (ningún borrador nuevo pasó el checklist de calidad con
  margen suficiente para publicación autónoma).
- 2 borradores sociales nuevos, sin publicar (redes sociales: solo borrador, publicación
  prohibida):
  - **Instagram** (ángulo transparencia): sobre por qué un hallazgo técnico (imagen rota de
    `lavanda`) puede pasar varios ciclos sin resolverse mientras se documenta cada vez — la
    verificación constante no implica corrección inmediata cuando el fix requiere una decisión de
    producto o alcance fuera del rol técnico.
  - **LinkedIn** (ángulo gobernanza): "Trigésimo ciclo, mismo bloqueador, misma disciplina" — por
    qué un sistema autónomo que detecta un problema hace 5 días y aún no lo toca no es lentitud,
    es respeto de límites de autorización explícitos.

## Cambios de código / commits
- Ninguno. Build pasa, QA limpio, sin fixes necesarios. `next-env.d.ts` (regenerado por el build)
  revertido sin commitear — cambio no funcional.
- Sin escrituras nuevas en Supabase este ciclo (cita diaria no cumplía el umbral de 24h; leads
  solo lectura).

## Tareas manuales de Papu hoy
1. **Gumroad roto hace 5 días 2h33min (30 ciclos).** Decidir: corregir
   `NEXT_PUBLIC_GUMROAD_URL` en Vercel al valor viable ya identificado
   (`https://kristiantronco.gumroad.com/l/ugsqtg`, 200) o indicar la URL correcta del producto.
   Kimiko no tocará la env var sin tu OK explícito.
2. **34 fichas contaminadas (25 seguras + 9 peligrosas) sin gate ni corrección**, pendiente desde
   2026-07-30. Casos concretos ya documentados: `ashwagandha-fruto` sirve el perfil de Saúco,
   `olivo` sirve datos de cardo mariano/silimarina. Decidir alcance de la corrección (¿migración
   de datos, o replantear el gate `ficha_verificada`?).
3. **`lavanda`: imagen rota hace 4 días**, y duplicado `equinacea`/`echinacea` sin resolver.
   Confirmar si ambos entran en el mismo paquete de corrección de datos que el punto 2, o si
   deben tratarse aparte (son hallazgos de datos, no de código).
