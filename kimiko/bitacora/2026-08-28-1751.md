# Bitácora — 2026-08-28 17:51 UTC — MODO ORDEN

## Contexto del disparo

`repository_dispatch` tipo `kimiko-buzon` con `client_payload.draft_id =
68cc01cd-b8e5-473e-8cc1-3ebc9a8d1c2f`. Al consultar esa fila en `kimiko_drafts`
(Supabase `vctetjugbvyllwjpxcxh`) ya estaba `status = 'hecho'`, con `copy` y
`tg_message_id = 10` rellenos, `updated_at` a las 17:49:20 UTC — posterior al
último commit del repo (17:44 UTC, ciclo anterior). Conclusión: la orden fue
recogida y resuelta por otra ejecución (probablemente en paralelo o inmediatamente
anterior a esta) que respondió a Kristian por Telegram pero no llegó a dejar
bitácora ni memoria en el repo antes de terminar. Revisé la cola completa:
0 filas `pendiente`, 0 `en_curso`. No había ningún trabajo real pendiente para
este ciclo.

## Verificación de lo ya respondido (antes de darlo por bueno)

La orden era: *"dime qué planta está publicada sin verificar y por qué le falta
la ficha"*. El `copy` ya enviado por Telegram decía que era **lavanda**
(`publicada=true`, `ficha_verificada=false`) por faltar el fichero
`lavanda-cientifica.jpg`. Antes de cerrar el ciclo comprobé que siguiera siendo
cierto, en vez de asumirlo:

- `ls public/images/plants/ | grep -i lavanda` → sin resultados, el fichero no existe.
- `SELECT slug,publicada,ficha_verificada,image_cientifica_url FROM plants WHERE slug='lavanda'`
  vía REST → `publicada=true`, `ficha_verificada=false`,
  `image_cientifica_url=/images/plants/lavanda-cientifica.jpg`.
- `SELECT slug FROM plants WHERE publicada=true AND ficha_verificada=false` →
  únicamente `lavanda`. Confirma que la respuesta fue completa, no se dejó
  ninguna otra planta en ese estado sin mencionar.

Todo correcto. No hice cambios de código ni de datos porque no hacía falta:
la respuesta ya dada era exacta y la regla de hierro (no verificar sin imagen
real) ya se estaba respetando — nadie puso `ficha_verificada=true` a lavanda
para forzarla.

## Cierre

Sin escrituras nuevas en Supabase, sin mensajes nuevos por Telegram (ya se
mandó en el ciclo que resolvió la orden), sin cambios de código.

### Tareas manuales de Kristian hoy

1. Ninguna urgente. Si quiere que lavanda aparezca completa en
   `/diccionario/lavanda`, hace falta subir una imagen científica real a
   `public/images/plants/lavanda-cientifica.jpg` — mientras no exista, el gate
   de código sigue mostrando "Ficha científica en revisión" correctamente.
