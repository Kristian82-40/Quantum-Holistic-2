# Bitácora Kimiko Cloud — 2026-07-26 02:54 UTC (Decimoséptimo ciclo)

## QA — 8/8 checks OK

1. **Build**: `npm ci && npm run build` pasa limpio, sin errores. `npm audit`: 16 vulnerabilidades (1 critical / 10 high / 4 moderate / 1 low), mismo desglose de ciclos anteriores, dominado por `next@14.2.5` — no se hace bump este ciclo, sigue sin ser un cambio trivial.
2. **Producción**: canonical `https://quantum-holistic.com/` y `og:url` correctos.
3. **Rutas 200**: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` — todas responden 308 (trailing slash) → 200 siguiendo el redirect, sin cambios de comportamiento.
4. **`/admin` sin sesión**: cadena `308 → /admin/` → `307 → /login/?redirect=%2Fadmin%2F` → `200`. Middleware activo (confirmado también por `ƒ Middleware`, 80.2 kB, en el output del build).
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`).
6. **9 plantas peligrosas** (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora): `image_cientifica_url = null` en las 9, sin archivo huérfano `{slug}-cientifica.*` en `public/images/plants/` para ninguna. Placeholder intacto.
7. **`leads`/`purchases`**: funnel E2E real repetido (POST a `https://quantum-holistic.com/api/leads` con `-L` para seguir el 308 → `{"ok":true}` → verificado en Supabase → `DELETE` de limpieza → confirmado 0 filas). Justificado por umbral de 24h: último test real fue 2026-07-25 02:50 UTC, ~24h04min antes, por encima del umbral. Sin incidencias — funnel sano.
   - **Matiz de esta corrida**: un `curl -X POST` sin `-L` contra la ruta desnuda devuelve `308` y **no llega al handler** (el body nunca se procesa) — hace falta `-L` explícito para que curl reenvíe el POST tras el redirect de trailing slash. No es una regresión real: el `fetch('/api/leads')` del cliente en `RegaloForm.tsx`/`RitualCheckout.tsx` es una petición same-origin desde dentro de la app y sigue redirects por defecto, así que el flujo real de usuario no se ve afectado. Vale la pena recordar este matiz para no interpretar un `308` sin `-L` como funnel roto en un chequeo futuro apresurado.
8. **`sitemap.ts`/`robots.ts`**: presentes, dominio correcto, `Sitemap: https://quantum-holistic.com/sitemap.xml` en robots.txt, `sitemap.xml` → 200.

## Monetización

- **Gumroad**: `NEXT_PUBLIC_GUMROAD_URL` sigue sin existir en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) — 12 env vars totales, ninguna Gumroad. **Decimoséptimo ciclo consecutivo** sin este secret. `RitualCheckout.tsx` mantiene el fallback correcto (captura de email vía `/api/leads` cuando la var no está) — código sin cambios, releído línea por línea este ciclo.
- **Funnel `/regalo/primera-noche` → `/producto/ritual-descanso`**: verificado extremo a extremo con test real este ciclo (ver punto 7 de QA). Sano.
- **"Tu Planta Aliada"**: sigue sin implementar. Propuesta de esquema sin cambios (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin migración necesaria). Cobertura del campo: 41/43 plantas seguras (faltan `manzanilla` y `equinacea`, sin cambio). Pendiente de OK explícito de Papu.

## Datos — chequeos recurrentes (sin hallazgos nuevos)

- **Duplicados por `nombre_latino`**: `equinacea`/`echinacea` (*Echinacea purpurea*, contenido real duplicado, pendiente decisión de Papu) y `ashwagandha`/`ashwagandha-fruto` (*Withania somnifera*, descartado — fichas distintas a propósito). Reconfirmado sin cambios.
- **43 plantas seguras**: las 43 con imagen válida en `public/images/plants/` (73 archivos totales en el directorio), 0 faltantes.
- **Backlog `blog_posts`**: **90 drafts / 19 published**, sin cambio — decimoséptimo ciclo consecutivo con el mismo pendiente crónico. Ningún post *published* viola el checklist anti-pseudociencia (verificado por `ilike` contra chakra/reiki/cristal/biodescodificación, 0 resultados).
- **Tabla `citas`**: sigue sin existir (`404 PGRST205`) — bloqueada por falta de canal DDL en el entorno cloud, mismo bloqueador desde 2026-07-23 16:50 UTC (ya 17 ciclos consecutivos). Sin `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` disponibles.

## Contenido

- **Blog**: sin borrador nuevo — backlog de 90 drafts sin revisar por Papu sigue siendo la razón para no generar más.
- **Borradores sociales** (NO publicados, solo bitácora):

  **Instagram** — ángulo: transparencia taxonómica
  > Cada ficha del diccionario incluye el nombre científico en latín, no solo el popular. No es adorno: es lo que te permite verificar cualquier dato por tu cuenta, en cualquier fuente seria. 🌿
  > *(sin claims de curación, sin dato interno expuesto)*

  **LinkedIn** — ángulo: por qué el checklist excluye ciertos temas explícitamente
  > Nuestro checklist editorial excluye por nombre biodescodificación, cristales, reiki y chakras — no por moda, sino porque no hay evidencia que sostenga sus afirmaciones de curación. Preferimos un diccionario más corto y verificable que uno más largo y dudoso.
  > *(sin cifras internas, sin mencionar el backlog de drafts pendiente)*

## Higiene operativa

- Sin volcado de `env` en bloque este ciclo — todas las comprobaciones de secrets se hicieron variable por variable con `[ -n "$VAR" ]`, siguiendo la regla endurecida desde el ciclo de 09:52 UTC del 2026-07-25. Sin incidentes nuevos.
- Token OAuth expuesto en ciclos anteriores (2026-07-25, 06:22 y 09:52 UTC) sigue sin confirmación de rotación en la memoria — escalado de nuevo como prioridad #1.

## Tareas manuales de Papu hoy

1. **Rotar el token OAuth de Claude Code expuesto el 2026-07-25 06:22 UTC y re-expuesto el mismo día a las 09:52 UTC** (`claude_code_oauth_token`, prefijo `sk-ant-oat01-...`) — sigue sin confirmación de rotación. Prioridad máxima mientras el token siga vigente.
2. **Cuenta Gumroad**: generar `NEXT_PUBLIC_GUMROAD_URL` y añadirla a Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) — único bloqueador del primer cobro (€19, ritual-descanso). 17 ciclos consecutivos sin este secret.
3. **Decisión sobre backlog crónico de `blog_posts`** (90 drafts, 17º ciclo consecutivo sin revisión): al menos archivar/borrar los 8 slugs que violan el checklist anti-pseudociencia de forma inequívoca por título (chakras ×4, reiki, cristales, biodescodificación, nutrición cuántica — mismos slugs de ciclos anteriores, ver bitácoras previas para el listado completo).

## Cierre

- Sin commits de código este ciclo (build limpio, nada que arreglar). Bitácora y memoria las commitea el paso dedicado del workflow.
