# Bitácora — 2026-09-20 22:17 UTC (ciclo 275º, MODO CICLO)

## Qué comprobé

- `kimiko_drafts`: sin filas `pendiente` ni `en_curso`, sin orden de Telegram
  este ciclo (las 4 filas históricas siguen `hecho`).
- `npm ci`: limpio, 415 paquetes.
- `npm run build`: exit 0, 36/36 páginas generadas.
- `npm run lint`: sin warnings ni errores.
- `npm audit`: 9 vulnerabilidades (1 moderate, 7 high, 1 critical), sin cambio
  desde el 223º.
- 8/8 rutas del checklist (`/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
  `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`) llegan a
  200 siguiendo redirecciones. `/admin` sin sesión → `/admin/` (308) →
  `/login/?redirect=%2Fadmin%2F` (307) → 200.
- `middleware.ts` confirmado en la raíz del repo, no dentro de `app/`.
- `canonical` y `og:url` de la home apuntan a `https://quantum-holistic.com/`.
- `robots.txt` correcto, `sitemap.xml` con 37 `<loc>`.
- Vercel: últimos 5 despliegues en estado `READY`.
- `plants`: 52 filas. Sexagésima primera pasada del check permanente del
  211º/212º — cruce de hash de `ficha_cientifica` (JSON, comparado con
  `json.dumps(..., sort_keys=True)`) sobre las 52 filas, mismos **7 grupos /
  21 filas** de siempre, sin cambios. 4 publicada+verificada sin cambio
  (`albahaca`, `arnica`, `equinacea`, `hinojo`), las 4 imágenes confirmadas en
  disco. Las 9 peligrosas confirmadas `publicada=false` una a una. `lavanda`
  sigue con `image_cientifica_url` rellena pero sin fichero en disco (abierto
  desde 2026-09-03, 19 días, despublicada, sin blast radius).
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio.
  Los 22 publicados: sin títulos duplicados, `excerpt` <155 car. en todos,
  `image_url` presente en todos, ninguno enlaza a las 9 plantas peligrosas.
  Único slug de diccionario enlazado sigue siendo `hinojo`.
- `citas`: última fila sigue siendo James Thomson (273º), hueco de ~6h36min —
  muy por debajo del umbral de ~24h, sin necesidad de insertar. 45 filas.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Qué arreglé

Nada — sin hallazgos técnicos ni de negocio nuevos.

## Qué encontré sospechoso

Nada nuevo. Sigue abierta la tarea manual de `lavanda` (ver abajo).

## Tareas manuales de Kristian hoy

1. `lavanda`: subir el fichero `public/images/plants/lavanda-cientifica.jpg`
   (o corregir la URL en Supabase) para poder verificar y publicar la ficha —
   abierto desde 2026-09-03, 19 días.
2. Decidir si merece la pena abordar la subida mayor de Next.js (14→16, salta
   la 15, rompe `next-intl` 3→4) para resolver la vulnerabilidad crítica de
   `npm audit` — sigue sin fix no-breaking en la rama 14.x.
3. Ninguna otra pendiente activa esta ronda.
