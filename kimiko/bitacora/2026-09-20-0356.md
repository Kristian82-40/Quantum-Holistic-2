# Bitácora — ciclo 271º, 2026-09-20 03:56 UTC (MODO CICLO)

## Qué comprobé

1. **`kimiko_drafts`**: 4 filas históricas, todas `status='hecho'`, ninguna
   colgada en `en_curso`, sin orden nueva de Telegram este ciclo.
2. **Dependencias/build/lint**: `npm ci` limpio (415 paquetes). `npm run build`
   exit 0, 36/36 páginas generadas. `npm run lint` sin warnings ni errores.
3. **`npm audit`**: 9 vulnerabilidades (1 moderate, 7 high, 1 critical), sin
   cambio desde el ciclo 223º. Sigue sin fix no-breaking (solo `--force`,
   que sube `next` a 16.3.5 y `next-intl` a 4.14.5 — decisión de Kristian por
   blast radius).
4. **Rutas del checklist** (`curl -L` en vivo contra producción): las 8 rutas
   (`/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
   `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`) → 200.
   `/admin` → `/admin/` (308) → `/login/?redirect=%2Fadmin%2F` (307) → 200.
   `middleware.ts` confirmado en la raíz del repo.
5. **SEO técnico**: canonical y `og:url`/`og:image` de la home →
   `https://quantum-holistic.com/`. `sitemap.xml` con 37 `<loc>`. `robots.txt`
   correcto, con referencia al sitemap.
6. **Vercel**: últimos 5 despliegues → `READY`.
7. **`plants`** (52 filas): repetí el cruce de hash por `JSON.stringify` de
   `ficha_cientifica` sobre las 52 filas (no solo publicadas) — mismos
   **7 grupos / 21 filas** de siempre (6 pares de fichas reales + las 9
   peligrosas compartiendo el placeholder). 4 publicada+verificada sin
   cambio (`albahaca`, `arnica`, `equinacea`, `hinojo`), las 4 imágenes
   confirmadas en `public/images/plants/`. Las 9 peligrosas confirmadas
   `publicada=false` una a una. `lavanda` sigue con `image_cientifica_url`
   rellena pero sin fichero en disco (hallazgo abierto desde 2026-09-03,
   despublicada y sin verificar, sin blast radius en vivo).
8. **`blog_posts`**: 109 filas (79 draft / 22 published / 8 rejected), sin
   cambio. Sobre los 22 `published=true`: sin títulos duplicados, ninguno
   >60 car., todos con `excerpt` presente y ≤155 car., todos con
   `image_url`, ninguno enlaza a las 9 plantas peligrosas.
9. **`citas`**: última fila es Emerson (insertada en el 268º), hueco de
   ~12h18min al comprobar — por debajo del umbral de ~24h, sin necesidad de
   insertar. 44 filas, sin cambio.
10. **Negocio**: `leads` en 0, `purchases` en 0, `products` sin cambio (2).
    Estrategia "El Ritual del Descanso" sigue sin tocar (no aprobada).

## Qué arreglé

Nada — sin hallazgos nuevos que requirieran corrección.

## Qué me pareció sospechoso

Nada nuevo. Los dos hallazgos abiertos de siempre siguen igual: `lavanda`
sin imagen en disco (sin blast radius, tarea manual reanotada) y el bloque
de `npm audit` sin fix no-breaking (documentado, pendiente de decisión sobre
la subida mayor de `next`).

## Tareas manuales de Kristian hoy

1. **`lavanda`**: generar o subir la imagen científica
   (`public/images/plants/lavanda-cientifica.jpg`) o decidir dejarla
   despublicada indefinidamente — lleva abierto desde 2026-09-03 (17 días).
2. **`npm audit`**: decidir si autorizar la subida mayor `next` 14→16
   (salta la 15, además sube `next-intl` 3→4) para resolver las 9
   vulnerabilidades pendientes, dado su blast radius.
3. Sin más tareas manuales nuevas este ciclo — checklist limpio de principio
   a fin.

## Sin commits de código ni escrituras en Supabase este ciclo

Cincuenta y siete ciclos consecutivos (212º-271º) sin hallazgos técnicos
nuevos en el checklist mecánico, y sin hallazgos de negocio nuevos.
