# Bitácora — ciclo 231º (2026-09-10, 22:21 UTC, MODO CICLO)

## Comprobado

- `kimiko_drafts`: sin filas `pendiente`, sin `en_curso` colgado. Sin orden
  de Telegram este ciclo (comprobado primero, antes del resto del
  checklist, pese al disparo `schedule`).
- `npm ci` + `npm run build`: limpio, 36/36 páginas generadas.
- `npx next lint`: sin avisos.
- `npm audit`: sin cambio desde el 223º — 1 moderate, 7 high, 1 critical (9
  total), todas requieren `--force` con breaking change (`next@16.3.4`,
  `next-intl@4.14.3`). Sin fix no-breaking disponible.
- 8/8 rutas del checklist en 200 (`-L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` sin sesión → `/admin/` (308) → `/login/?redirect=%2Fadmin%2F`
  (307) → 200. `middleware.ts` confirmado en la raíz del repo.
- `canonical` y `og:url` de home → `https://quantum-holistic.com/`.
  `robots.txt` correcto, apunta a `/sitemap.xml`. `sitemap.xml`: 37
  `<loc>`, sin cambio.
- Vercel: últimos 5 despliegues (`kimiko` y `quantum-holistic-2`) en
  `READY`.

## Diccionario (`plants`)

- 52 filas totales, sin cambio.
- Publicada+verificada: mismas 4 de siempre — `albahaca`, `arnica`,
  `equinacea`, `hinojo`. Las 4 imágenes en `image_cientifica_url`
  confirmadas en disco (`public/images/plants/*.jpg`).
- Las 9 peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) siguen
  `publicada=false`, `ficha_verificada=false`.
- `updated_at` máximo global sigue en 2026-09-01T09:21:58Z → sin auditoría
  visual completa este ciclo (nada ha cambiado desde entonces).
- Check permanente (211º/212º): cruce de hash de `ficha_cientifica` sobre
  las 52 filas — decimonovena pasada, exactamente los mismos 7 grupos
  duplicados que en 212º-230º, sin grupos nuevos. El grupo de las 9
  peligrosas comparte ficha (esperado, contenido placeholder, todas sin
  publicar). Resto de grupos ya documentados en ciclos previos sin acción
  pendiente.
- `echinacea` vs `equinacea` en vivo: `echinacea` → 404, `equinacea` → 200
  (siguiendo redirects). Sigue sin ser un duplicado real en producción.

## Blog (`blog_posts`)

- 109 filas: 79 draft / 22 published / 8 rejected. Sin cambio.
- Draft más reciente sigue en 2026-07-10 (ya evaluado y descartado en
  206º-209º).

## Citas

- Última fila sigue siendo Proverbios 12:25 (insertada en el 229º, ciclo
  16:02 UTC). Hueco actual ~6h20min — muy por debajo del umbral de ~24h.
  Sin inserción este ciclo.

## Negocio

- `leads`: 0 filas. `purchases`: 0 filas. `products`: 2 filas, sin cambio
  (`Quantum Crypto`, `El Ritual del Descanso`). El funnel de pago del
  Ritual del Descanso sigue sin tocarse — no está aprobado.

## Resultado

Sin hallazgos técnicos nuevos, sin hallazgos de negocio nuevos. Décimo
noveno ciclo consecutivo (212º-231º) con el checklist mecánico estable.
Sin commits de código ni escrituras en Supabase este ciclo.

## Tareas manuales de Kristian hoy

1. Ninguna acción urgente. El `npm audit` sigue con 9 vulnerabilidades sin
   fix no-breaking desde el 223º — si en algún momento quieres que
   Kimiko intente el upgrade a `next@16` / `next-intl@4.14` con todas sus
   roturas, dilo explícitamente; por defecto no se toca porque es
   breaking.
2. Si quieres que el diccionario avance más allá de las 4 fichas
   publicadas actuales, decide qué plantas priorizar — Kimiko audita como
   máximo 6 a fondo por ciclo y de momento no hay `updated_at` nuevo que
   dispare una revisión visual completa.
3. Nada más pendiente de tu parte.
