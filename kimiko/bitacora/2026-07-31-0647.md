# Kimiko — Ciclo cloud 2026-07-31 06:47 UTC

## QA (Paso 1)
1. `npm ci && npm run build`: **OK**, sin errores de tipos ni lint (36 rutas, `ƒ Middleware` presente).
2. Producción (`https://quantum-holistic.com`): canonical y `og:url` == `https://quantum-holistic.com/`. OK.
3. Rutas 200 (siguiendo redirect de trailing slash): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`. OK.
4. `/admin` sin sesión → `308` a `/admin/` → `307` a `/login/?redirect=%2Fadmin%2F` (200 final). `middleware.ts` confirmado en la raíz del repo, no en `app/`. OK.
5. Supabase `plants`: **52/52** filas íntegras (`content-range: 0-51/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null` y `ficha_cientifica: {}` — placeholder intacto. Reverificado en producción: `/diccionario/cannabis/` no expone "Uso Ceremonial" ni "Salvia officinalis" en el HTML servido.
6. `leads`: 0 filas. `purchases`: 0 filas. Sin conversiones ni segmentación por dosha que analizar todavía.
7. `sitemap.xml` (200) y `robots.txt` (200): dominio correcto (`Sitemap: https://quantum-holistic.com/sitemap.xml`).

**QA 7/7 OK**, sin fixes de código necesarios este ciclo. `git status` mostró solo `next-env.d.ts` regenerado por el build (comentario de URL de docs de Next, no funcional); revertido sin commitear.

## Hallazgos reconfirmados (sin cambio de estado)

- **Checkout Gumroad sigue roto** — 2 días 12h22min (desde 2026-07-28 18:25:20 UTC), **decimocuarto ciclo consecutivo**. `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a `kristian320.gumroad.com/l/ritual-descanso` → `404` (dominio base también 404), reconfirmado en vivo con el CTA real de `/producto/ritual-descanso/` ("Comprar por 19€" enlaza a esa URL rota). URL previa funcional `kristiantronco.gumroad.com/l/ugsqtg` → `200`, revert sigue siendo viable. Sin tocar la env var sin OK de Papu.
- **`lavanda` sigue con `image_cientifica_url` apuntando a un archivo inexistente** (`/images/plants/lavanda-cientifica.jpg` → 404 en producción, reconfirmado). Sin escritura en `plants` este ciclo.
- **34/52 fichas (25 seguras + 9 peligrosas) siguen con `ficha_mistica`/`ficha_cientifica` contaminada** con contenido idéntico al de otra especie, sin gate de verificación ni corrección de dato — sin cambio respecto al ciclo 2026-07-31 02:57 UTC. Caso testigo `ashwagandha-fruto` reverificado en vivo extrayendo los valores renderizados del payload RSC: Elemento=Agua, Chakra=Corazón, Planeta=Venus — sigue siendo el perfil místico de Saúco (*Sambucus nigra*), no el de *Withania somnifera*.
- Gate `ficha_verificada`: 0/52 verificadas (`content-range: */0`), sin cambio.
- Tabla `citas`: sigue sin poder crearse vía REST (`PGRST205` — tabla no existe en el schema cache), **38º ciclo consecutivo bloqueado** por falta de canal DDL (solo `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`VERCEL_TOKEN` disponibles como secrets).
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio.
- `public/images/plants/`: 71 archivos, sin cambio.

## Monetización (Paso 2)

- Funnel `/regalo/primera-noche` → lead (`RegaloForm.tsx` → `POST /api/leads`) → `/producto/ritual-descanso`: verificado en código y rutas 200, sin cambios.
- "Tu Planta Aliada": sin implementar (ningún componente/ruta la referencia todavía), bloqueada por la misma decisión de producto pendiente sobre las 25 fichas seguras contaminadas.

## Optimización continua (Paso 5)

1. Funnel A/B: `leads`/`purchases` en 0 filas, sin datos suficientes para proponer variantes de CTA.
2. "Tu Planta Aliada": test negativo de las 9 peligrosas sigue pasando (imagen y contenido místico ocultos en el render).
3. Cita diaria: tabla `citas` sigue bloqueada, sin poder insertar (38º ciclo).
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a la URL rota — incidente crítico abierto, no un hito.

## Borradores sociales (solo bitácora, sin publicar)

**Instagram** — ángulo: lo que se ve vs. lo que se sostiene.
> Una web bonita no es lo mismo que una web correcta. Esta semana seguimos con la lupa puesta en cada ficha del diccionario antes de dejarla suelta — preferimos que tarde en salir a que salga mal. #QH #bienestar #hechobien

**LinkedIn** — ángulo: coste de reverificar vs. coste de un dato mal etiquetado.
> Cuando un dato "parece" correcto (nombre, formato, estructura) pero el contenido pertenece a otra fila, ningún test de esquema lo detecta — solo una comparación de contenido campo a campo contra la fuente original. Es más lento que confiar en la forma, pero es la única prueba que realmente distingue "correcto" de "con buena pinta". #QH #calidaddedatos #datagovernance

## Tareas manuales de Papu hoy

1. **Resolver el checkout Gumroad** — roto 2 días 12h22min, decimocuarto ciclo escalándolo. Revert de `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional, verificado 200 este ciclo) o arreglar/republicar el producto en la cuenta `kristian320`. Impacto directo en ventas — cada visitante que pulsa "Comprar por 19€" llega a un 404 en vez de comprar.
2. **Decidir el alcance del problema de `ficha_mistica`/`ficha_cientifica` sin gate** en las 25 fichas seguras contaminadas: (a) extender el gate `ficha_verificada` a las 52 fichas, o (b) corregir/despublicar fila por fila. Bloquea también "Tu Planta Aliada". Abierto desde 2026-07-30 02:36 UTC.
3. **Rotar la service role key y el token OAuth expuestos** (documentado desde 2026-07-25 y 2026-07-29) — sin confirmación de rotación todavía.

---
*Ciclo cloud automático. Sin commits de código este ciclo (build pasa, QA limpio, sin fixes necesarios). Bitácora y memoria commiteadas por el paso dedicado del workflow.*
