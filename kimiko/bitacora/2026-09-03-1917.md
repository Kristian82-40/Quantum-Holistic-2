# Bitácora — 2026-09-03 19:17 UTC (200º ciclo, MODO CICLO)

## Salud del sitio
- `npm ci` limpio (9 vulnerabilidades, sin cambio desde el 185º ciclo).
- `npm run build`: 36/36 páginas generadas sin error. `npx next lint`: sin avisos.
- 8/8 rutas del checklist devuelven 200 en producción siguiendo el 308 de barra
  final: `/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
  `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
- `/admin` sin sesión → cadena completa verificada: 308 → 307 a
  `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts` confirmado en la raíz del
  repo (no dentro de `app/`).
- Home: `canonical` y `og:url` → `https://quantum-holistic.com/`, `og:image` →
  `https://quantum-holistic.com/images/og-default.jpg` (fix del 196º sigue vivo).
- `robots.txt` correcto. `sitemap.xml`: 85 `<loc>`.
- Vercel: últimos 5 despliegues `READY`.

## Diccionario (`plants`)
- 52 filas, `updated_at` máximo sin cambio desde el 189º (mismo timestamp exacto
  `2026-09-01T09:21:58`) → no repetí la auditoría visual completa. 23
  publicada+verificada, 9 peligrosas confirmadas `publicada=false`, `lavanda`
  sigue despublicada, duplicado `equinacea`/`echinacea` sin resolver
  (`echinacea` correctamente despublicada).

## Blog (`blog_posts`) y SEO — hallazgo de este ciclo
- 109 filas (79 draft / 22 published / 8 rejected). **Comprobando los 2 posts
  corregidos en el 199º (enlace roto a `lavanda`), descubrí que `updated_at` no
  se movió tras el `UPDATE` de contenido** — sigue en `2026-05-30T06:38:08`, la
  misma fecha de siempre. Confirma que la tabla no tiene trigger que mantenga
  `updated_at` al día en escrituras vía REST. **Esto invalida, en general, la
  regla usada en ciclos anteriores de "sin `UPDATE` nuevo → no repetir la
  auditoría de `blog_posts`"**: un cambio de contenido puede no reflejarse en
  `updated_at` en absoluto. Detalle completo y check permanente añadido a
  `KIMIKO_MEMORIA.md`.
- Repetí igualmente el barrido completo de enlaces internos rotos (check
  permanente del 199º): de los 22 posts publicados, el único slug de
  diccionario enlazado es `hinojo` (`publicada=true`, `ficha_verificada=true`,
  200 en vivo). Ninguna de las 9 plantas peligrosas aparece mencionada ni
  enlazada en contenido publicado. El fix del 199º (quitar `<a href>` a
  `lavanda` en 2 posts, dejar el anchor text plano) sigue vivo, verificado
  directamente en el `content` de Supabase.
- Sin duplicados de título entre los 22 publicados, ninguno supera 60
  caracteres. QA SEO en vivo de un post al azar
  (`aromaterapia-y-aceites-esenciales-terapeuticos-1780097914`): `title`,
  `meta description`, `canonical`, `og:image`, `og:title` — todos presentes y
  correctos.

## Negocio
- `leads`: 0 filas, sin cambio.
- `citas`: última inserción `2026-09-02T08:35:24 UTC`, ~34h41min sin inserción
  nueva al cierre — sigue creciendo desde el cruce de umbral del 198º
  (24h06min), sin ser un fallo técnico. Observación de negocio, sin tocar el
  funnel.

## Telegram
- `kimiko_drafts`: cola vacía, sin filas colgadas en `en_curso`, sin orden
  nueva este ciclo.

## Resultado
Sin commits de código ni escritura en Supabase este ciclo. Checklist limpio de
principio a fin, pero con un hallazgo metodológico importante sobre la
fiabilidad de `updated_at` en `blog_posts` (ver arriba y memoria).

## Tareas manuales de Kristian hoy
1. Duplicado `equinacea`/`echinacea` en el diccionario sigue sin resolver desde
   hace varios ciclos — decidir cuál de las dos fichas se queda.
2. `citas` lleva ~34h41min sin una reserva nueva (última: 2026-09-02 08:35
   UTC) — probablemente solo refleja el ritmo real de tráfico, pero si
   esperabas más movimiento merece un vistazo.
3. Ninguna otra tarea urgente: build, SEO, rutas, enlaces internos y
   despliegues están todos verdes.
