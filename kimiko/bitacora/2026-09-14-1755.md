# Bitácora — 2026-09-14 17:55 UTC (ciclo 247º, MODO CICLO)

## Comprobado

- `kimiko_drafts`: comprobado primero, antes del resto del checklist. Sin filas
  `pendiente`, sin `en_curso` colgado. Sin orden de Telegram este ciclo pese al
  disparo `schedule`.
- `npm ci` + `npm run build`: 36/36 páginas, limpio. `npx next lint`: sin avisos.
- `npm audit`: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate, 7
  high, 1 critical), sigue sin fix no-breaking disponible.
- 8/8 rutas del checklist en 200 con `-L` (`/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`). `/admin` sin sesión → 308 (redirect a login), correcto.
  `middleware.ts` confirmado en la raíz del repo, no dentro de `app/`.
- `canonical` y `og:url` en `https://quantum-holistic.com/` correctos.
- `sitemap.xml`: 37 `<loc>` en 3 intentos consistentes con `-m 30`. `robots.txt`
  correcto, apunta al sitemap con el dominio bueno. Vercel: últimos 5
  despliegues `READY`.

## Hallazgo: el check permanente del 211º/212º llevaba 33 ciclos citando "7
   grupos" cuando el recuento real es 6

- Repetí el cruce de hash de `ficha_cientifica` sobre las 52 filas de
  `plants`, con el mismo método documentado (`json.dumps(sort_keys=True)`
  + sha256, porque la columna es JSON no texto). Resultado: **6 grupos de
  duplicado exacto, 12 filas implicadas, todos pares de 2**:
  `ashwagandha-fruto`/`sauco`, `cinamomo`/`valeriana`, `echinacea`/`equinacea`,
  `ginseng`/`muerdago`, `loto`/`tulsi`, `manzanilla`/`nigela`.
- Esto coincide exactamente con lo que el propio ciclo 213º había enumerado
  de forma explícita: "los 5 pares conocidos... y `echinacea`/`equinacea`"
  — es decir, 6 grupos. La cifra "7 grupos" aparece por primera vez en la
  entrada del 214º sin una lista nueva que la respalde, y desde ahí se ha
  venido repitiendo mecánicamente ("exactamente los mismos 7 grupos que
  el 212º-246º") en más de 30 cierres consecutivos sin que nadie volviera
  a derivar la lista completa para contrastarla.
- No es un hallazgo de negocio ni de seguridad: los 12 slots implicados
  siguen `publicada=false` (excepto `equinacea`, que es una de las 4 fichas
  correctas — su "duplicado" con `echinacea` no crea duplicado en vivo,
  confirmado de nuevo: `echinacea` 404, `equinacea` 200), así que no hay
  blast radius en producción. Es un error de recuento en la propia bitácora,
  y por el principio de presunción negativa no debía darse por bueno solo
  porque se repitiera; tocaba volver a derivarlo desde cero.
- Corrijo la cifra en `KIMIKO_MEMORIA.md` a "6 grupos" de aquí en adelante y
  dejo la lista completa anotada para que el próximo ciclo pueda contrastar
  directamente sin tener que recalcular.
- `plants`: 52 filas, 4 publicada+verificada sin cambio (`albahaca`,
  `arnica`, `equinacea`, `hinojo`), las 4 imágenes en
  `public/images/plants/` confirmadas en disco. Las 9 peligrosas
  confirmadas `publicada=false`.
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio.
  Draft más reciente sigue en 2026-07-10, ya descartado en 206º-209º.
- `citas`: hueco de **~26h06min** desde la última fila (Proverbios 15:30,
  242º) — por encima del umbral de ~24h usado en ciclos previos (242º
  insertó con ~24h32min). Insertada cita nueva: **Proverbios 18:14
  (Reina-Valera)**, *"El ánimo del hombre soportará su enfermedad; Mas
  ¿quién soportará al ánimo angustiado?"* Verificada con `WebSearch` contra
  varias fuentes independientes (Bible Study Tools, YouVersion,
  BlueLetterBible), texto exacto confirmado, verso no repetido (comprobados
  los 39 autores/versículos previos), sin claim de curación. `citas` pasa
  de 39 a 40 filas.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Cambios este ciclo

- Fila nueva en `citas` (Proverbios 18:14).
- Corrección de recuento en `KIMIKO_MEMORIA.md` (7→6 grupos del check
  permanente de duplicados), sin cambios de código ni de datos en `plants`.

## Tareas manuales de Kristian hoy

1. Ninguna tarea manual urgente esta vez. La cola de vulnerabilidades de
   `npm audit` (9, sin fix no-breaking) sigue ahí desde el 223º si en algún
   momento quiere revisarla con margen para un upgrade mayor.
