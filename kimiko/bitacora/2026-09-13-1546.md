# Bitácora Kimiko — 2026-09-13 15:46 UTC (ciclo 242º, MODO CICLO)

## Comprobado

- `kimiko_drafts`: cola de `pendiente`/`en_curso` vacía — comprobado
  primero, antes del resto del checklist, pese al disparo `schedule`. Sin
  orden de Telegram este ciclo.
- `npm ci` limpio. `npm run build`: 36/36 páginas generadas sin error.
- `npx next lint`: sin avisos.
- `npm audit`: sin cambio desde el 223º — 1 moderate, 7 high, 1 critical
  (9 total), todas requieren `--force` (breaking change en `next`/`next-intl`),
  sigue sin fix no disruptivo.
- 8/8 rutas del checklist en 200 (`-L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` sin sesión → 308 (redirect a login). `middleware.ts` confirmado
  en la raíz del repo, no dentro de `app/`.
- `canonical` y `og:url` de la home apuntan a `https://quantum-holistic.com/`.
- `sitemap.xml` → 200, 37 `<loc>` (sin cambio; un primer `curl` sin
  reintento dio 15 por un corte de red transitorio — falsa alarma,
  descartada con un segundo `curl -m 20` que confirmó las 37). `robots.txt`
  → 200.
- Vercel: últimos 5 despliegues en estado `READY`.

## Diccionario de plantas

- `plants`: 52 filas (sin cambio).
- `publicada=true AND ficha_verificada=true`: 4 filas, sin cambio
  (`albahaca`, `arnica`, `equinacea`, `hinojo`). Las 4 imágenes en
  `image_cientifica_url` confirmadas presentes en `public/images/plants/`.
- Las 9 peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora)
  confirmadas `publicada=false`.
- Trigésima pasada del check permanente del 211º/212º (cruce de hash de
  `ficha_cientifica` sobre las 52 filas): exactamente los mismos 7 grupos
  que en el 212º-241º, sin grupos nuevos:
  `[loto, tulsi]`, `[cinamomo, valeriana]`, `[ginseng, muerdago]`,
  `[ashwagandha-fruto, sauco]`, `[manzanilla, nigela]`,
  `[echinacea, equinacea]`, y el grupo de las 9 peligrosas (comparten
  ficha placeholder, no es un hallazgo nuevo).
- `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual
  completa este ciclo.
- `echinacea`/`equinacea` sin `-L` dan ambas 308 (normalización de barra
  final de Next.js). Siguiendo el redirect (`-L`): `echinacea` → 404,
  `equinacea` → 200. Sigue sin ser un duplicado real en vivo.

## Contenido y negocio

- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio.
  Draft más reciente sigue siendo el de 2026-07-10, ya descartado en
  206º-209º.
- `citas`: última fila era Aristóteles (*Ética a Nicómaco*, Libro I,
  insertada en el 238º), hueco de **~24h32min** desde entonces — por
  encima del umbral de ~24h. Insertada una cita nueva: Proverbios 15:30
  (Reina-Valera), *"La luz de los ojos alegra el corazón, Y la buena
  nueva conforta los huesos."* Verificada con `WebSearch` contra varias
  fuentes independientes (bible.com, blueletterbible, bibliatodo,
  biblia.es, ediciones-biblicas.ch), verso no repetido (comprobados los 38
  autores/versículos previos), sin claim de curación. Un candidato previo
  de Cicerón (*De Senectute*, "It is exercise alone that supports the
  spirits...") fue descartado por no verificar limpio contra las fuentes
  primarias (Perseus, LacusCurtius no reproducen esa formulación exacta),
  mismo motivo que descartó a Pasteur en el 238º. `citas` pasa de 38 a 39
  filas.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Resultado

Único cambio de datos del ciclo: la fila nueva en `citas` (contenido
editorial rutinario dentro de las atribuciones de Kimiko). Sin commits de
código ni cambios en `plants`/`blog_posts`. Trigésimo ciclo consecutivo
(212º-242º) sin hallazgos técnicos nuevos en el checklist mecánico, y sin
hallazgos de negocio nuevos.

## Tareas manuales de Kristian hoy

Ninguna. El sitio, el diccionario y el checklist mecánico siguen estables
sin intervención requerida.
