# Bitácora — 2026-09-18 19:03 UTC (ciclo 264º, MODO CICLO)

## Qué comprobé

- **`kimiko_drafts`** (primero, antes del resto del checklist, pese al disparo
  `schedule`): las 4 filas históricas siguen `status = 'hecho'`, sin ninguna
  `en_curso` colgada. Sin orden de Telegram este ciclo.
- **Build/lint:** `npm run build` sale limpio (exit 0), sin errores de tipos ni
  de rutas.
- **`npm audit`:** 9 vulnerabilidades (1 moderate, 7 high, 1 critical), sin
  cambio desde el ciclo 223º. Reverifiqué con `npm audit fix --dry-run`: sigue
  sin fix no-breaking — la única vía es `--force`, que sube `next` y
  `next-intl` con cambios rotos. No lo aplico.
- **Rutas del checklist (8/8):** `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas` → 200 tras el redirect 308 de trailing-slash de Next.js.
  `/admin` → `/admin/` → `/login/?redirect=%2Fadmin%2F` (sin sesión, correcto).
  `middleware.ts` confirmado en la raíz del repo, no dentro de `app/`.
- **canonical / og:url / sitemap / robots:** ambos apuntan a
  `https://quantum-holistic.com/`. `sitemap.xml` con 37 `<loc>`. `robots.txt`
  con `Allow: /` y referencia al sitemap correcto.
- **Vercel:** últimos 5 despliegues en estado `READY`.
- **Check permanente del 211º/212º (quincuagésima pasada):** rehecho desde
  cero el cruce de hash de la columna `ficha_cientifica` (JSON) sobre las 52
  filas de `plants`. Resultado: los mismos **7 grupos / 21 filas** de siempre
  (`loto`/`tulsi`; `cinamomo`/`valeriana`; `ginseng`/`muerdago`;
  `ashwagandha-fruto`/`sauco`; `manzanilla`/`nigela`; `echinacea`/`equinacea`;
  y las 9 peligrosas compartiendo el mismo placeholder). Sin cambios respecto
  a la corrección del 249º.
- **`plants`:** 52 filas. 4 `publicada=true AND ficha_verificada=true`
  (`albahaca`, `arnica`, `equinacea`, `hinojo`) — las 4 imágenes
  (`*-cientifica.jpg`) confirmadas en disco con `ls`. Las 9 peligrosas
  (`aconito`, `datura`, `datura-metel`, `amanita-muscaria`, `cannabis`,
  `cornezuelo-centeno`, `beleno-negro`, `tejo`, `hierba-mora`) confirmadas
  `publicada=false`. `lavanda` sigue con `image_cientifica_url` rellena en la
  fila pero sin el fichero en disco (`lavanda-cientifica.jpg` no existe) —
  hallazgo abierto desde 2026-09-03, despublicada y sin verificar, sin blast
  radius. `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual
  completa este ciclo (dentro del límite de 6 plantas/ciclo, no tocada por no
  haber cambios que la disparen).
- **`blog_posts`:** 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Repasados los 22 publicados: sin títulos duplicados, todos los
  `excerpt` <155 caracteres, `image_url` presente en los 22.
- **`citas`:** última fila sigue siendo la cita de Cicerón (*De Senectute*,
  insertada en el 262º), hueco de **~10h08min** al comprobar — muy por debajo
  del umbral de ~24h. 43 filas, sin cambio, sin necesidad de insertar.
- **`leads`** en 0, **`purchases`** en 0, **`products`** sin cambio (2 filas).

## Qué arreglé

Nada — quincuagésimo ciclo consecutivo (212º-264º) sin hallazgos técnicos
nuevos en el checklist mecánico de código/build/rutas/Supabase, y sin
hallazgos de negocio nuevos. Sin commits de código ni escrituras en Supabase
este ciclo.

## Qué encontré sospechoso

- `lavanda` sigue con la imagen ausente en disco (abierto desde 2026-09-03,
  cumplió 15 días de antigüedad en el 263º). Sin blast radius porque está
  despublicada, pero es una tarea manual pendiente de resolver de raíz
  (generar o subir la imagen, o limpiar la fila).

## Tareas manuales de Kristian hoy

1. Generar o subir `lavanda-cientifica.jpg` a `public/images/plants/` (estilo
   acuarela botánica de marca) para poder verificar y publicar esa ficha —
   lleva abierto desde 2026-09-03.
2. Decidir si se acepta el `npm audit fix --force` (sube `next` a 16.3.5 y
   `next-intl` a 4.14.5 con cambios rotos) para cerrar las 9 vulnerabilidades
   pendientes, o si se prioriza otra vía; sigue sin existir un fix no-breaking.
3. Ninguna tarea de negocio nueva: `leads` y `purchases` siguen en 0.
