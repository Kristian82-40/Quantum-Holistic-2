# Bitácora — 2026-09-08 16:14 UTC (ciclo 222º, MODO CICLO)

## Comprobado
- `npm ci`: 415 paquetes, 9 vulnerabilidades (1 moderate, 8 high) — sin cambio desde el ciclo 185º.
- `npm run build`: limpio, 36/36 páginas generadas. `npx next lint`: sin avisos.
- `middleware.ts` confirmado en la raíz (no en `app/`).
- Rutas del checklist, todas 200 (con `-L`): `/`, `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
- `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200.
- `canonical` y `og:url` → `https://quantum-holistic.com/`. `sitemap.xml`: 37 `<loc>` (sin cambio). `robots.txt` correcto con referencia al sitemap.
- Vercel: últimos 5 despliegues `READY`.
- `plants`: 52 filas. 4 publicada+verificada sin cambio (`albahaca`, `arnica`, `equinacea`, `hinojo`) — las 4 imágenes en `public/images/plants/` confirmadas en disco (`stat` sobre cada fichero). Las 9 peligrosas confirmadas `publicada=false`.
- Décima pasada del cruce de hash de `ficha_cientifica` sobre las 52 filas: exactamente los mismos 7 grupos que desde el ciclo 212º, sin grupos nuevos (`tulsi`/`loto`, `valeriana`/`cinamomo`, `muerdago`/`ginseng`, `sauco`/`ashwagandha-fruto`, `manzanilla`/`nigela`, `equinacea`/`echinacea`, y las 9 peligrosas compartiendo placeholder). `updated_at` máximo sin cambio (2026-09-01), coherente con no repetir auditoría visual completa este ciclo.
- `echinacea`/`equinacea`: sigue sin duplicado real en vivo (404 vs 200).
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin cambio. Verificado con consulta real (no solo memoria): 22 publicados sin duplicados de título, todos ≤60 car., `excerpt` ≤155 car., todos con `image_url`. 3/22 enlazan a `/diccionario` (sin cambio — backlog conocido). Draft más reciente sigue en 2026-07-10 (`nutricion-km0-herbologia-plantas-medicinales-de-proximidad-...`) — este candidato ya fue investigado a fondo en los ciclos 206º-209º y descartado por título/excerpt fuera de límite y canibalización temática con "Herboristería Europea de Proximidad"; no hace falta repetir esa investigación, solo queda como backlog muerto documentado.
- `citas`: última fila sigue siendo la de Cervantes insertada en el ciclo 221º (2026-09-08T08:46:02 UTC). Hueco actual: **7h28min** — dentro del umbral normal (~24h), sin necesidad de insertar esta vez. Confirmado que sigue siendo la tabla de citas célebres (no un funnel), aplicando el check permanente reforzado en el 221º.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2). `kimiko_drafts`: 4 filas totales, todas `hecho`, cola de `pendiente` vacía — sin orden de Telegram este ciclo.

## Sin commits de código ni escrituras en Supabase este ciclo
Décimo ciclo consecutivo (212º-222º) sin hallazgos técnicos nuevos en el checklist mecánico. Sin hallazgos de negocio nuevos tampoco (la regresión de `citas` del 221º sigue corregida y estable).

## Tareas manuales de Kristian hoy
1. Ninguna con urgencia nueva. El hueco de `citas` (7h28min) está dentro de lo normal, no requiere acción.
2. Backlog de blog (79 drafts) sigue sin candidatos nuevos publicables tras la revisión en bloque de los ciclos 208º-209º — si Kristian quiere contenido nuevo en el blog, hace falta redactar temas distintos a los ~37 ya cubiertos, no seguir puliendo el backlog existente.
3. `npm audit`: 9 vulnerabilidades (1 moderate, 8 high) sin cambio desde hace muchos ciclos — si Kristian quiere que se aborden, decir si prefiere `npm audit fix` (sin romper) o revisar caso a caso las que requieren `--force`.
