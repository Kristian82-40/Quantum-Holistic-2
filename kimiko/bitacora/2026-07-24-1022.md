# Bitácora Kimiko — Ciclo Cloud 2026-07-24 10:22 UTC

## QA — 8/8 checks OK
1. `npm ci && npm run build` → **pasa**, sin fixes necesarios. Mismo aviso persistente: `next@14.2.5` con vulnerabilidad de seguridad conocida (aviso 2025-12-11), `npm audit` reporta 16 vulnerabilidades (1 low, 4 moderate, 10 high, 1 critical). No se actualiza este ciclo — requiere sesión dedicada de validación, no es un bump trivial.
2. Producción: canonical y og:url == `https://quantum-holistic.com/` ✔
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → todas 200 ✔
4. `/admin` sin sesión → 308 → `/admin/` → 307/redirect → `/login/?redirect=%2Fadmin%2F` → 200. Middleware activo en raíz. ✔
5. Supabase tabla `plants`: **52 plantas íntegras** (`content-range: 0-51/52`). ✔
6. Las 9 plantas peligrosas: `image_cientifica_url = null` para las 9 (verificado con la lista completa vía API, sin excepciones). ✔
7. `leads`: 0 filas, `purchases`: 0 filas — sin actividad de usuarios reales desde el ciclo anterior (06:30 UTC).
8. `sitemap.ts` y `robots.ts` presentes, dominio `quantum-holistic.com` correcto en ambos. ✔

Sin diferencias estructurales frente al ciclo anterior del mismo día (06:30 UTC) — sin commits de código entre medias, working tree limpio al inicio y al final.

### Novedad de infraestructura: `VERCEL_TOKEN` ahora disponible como secret en el entorno cloud
Por primera vez en un ciclo cloud, el entorno trae `VERCEL_TOKEN` además de `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`. Usado para consultar en vivo `GET /v9/projects/prj_DASuxCUuV72w8CLpZejVij8XcXvL/env` en vez de depender de lo registrado en memoria — confirma **12 env vars en producción, ninguna llamada `NEXT_PUBLIC_GUMROAD_URL`** (mismo resultado que ciclos anteriores, pero ahora verificado con acceso API real y no solo heredado de memoria). Sigue sin dar canal DDL para Supabase (eso necesita un token distinto, de Supabase Management API o connection string — no lo resuelve `VERCEL_TOKEN`).

## Monetización
- `/producto/ritual-descanso`: `NEXT_PUBLIC_GUMROAD_URL` **sigue sin existir** en Vercel, confirmado en vivo (ver arriba). `RitualCheckout.tsx` cae correctamente al fallback de waitlist. Sin cambios de código necesarios.
- Funnel `/regalo/primera-noche` → `/producto/ritual-descanso`: páginas verificadas 200, sin regresión. **No se repitió el test E2E de POST/DELETE real** — el último test real fue en el ciclo de 06:30 UTC del mismo día (~3h50min antes), por debajo del umbral de 24h documentado en memoria para justificar repetirlo.
- **Hallazgo heredado, sin acción:** `/api/webhooks/btcpay` sigue en el build (`app/api/webhooks/btcpay/route.ts`). No se toca sin instrucción explícita de Papu.
- **"Tu Planta Aliada":** sin cambios respecto a la propuesta ya en bitácora — usar `ficha_mistica.afinidad_ayurvedica` (JSONB ya existente) con filtro hardcoded de exclusión de las 9 peligrosas. Sin migración necesaria. Sigue pendiente de OK de Papu, no implementada.

## Optimización continua
- **Funnel A/B:** `leads` sigue en 0 filas — sin datos reales todavía para proponer variantes de CTA con fundamento.
- **Tu Planta Aliada — verificación de imágenes:** recruzado por script las 43 plantas seguras (52 − 9 peligrosas) contra `public/images/plants/{slug}-cientifica.jpg` en disco → **0 faltantes**.
- **Duplicados de `nombre_latino` (recruzado este ciclo):** mismos dos pares que en el ciclo de 06:30 UTC — `equinacea`/`echinacea` (duplicado real de contenido, pendiente de decisión de slug canónico de Papu) y `ashwagandha`/`ashwagandha-fruto` (ya descartado como duplicado — fichas distintas a propósito, ver memoria). Sin novedad, sin pares nuevos.
- **Cita diaria (`citas`):** sigue bloqueada — confirmado de nuevo que el entorno cloud NO trae ningún secret de Supabase con permiso de DDL (revisado el listado completo de variables de entorno del runner; solo `SUPABASE_URL` y `SUPABASE_SERVICE_ROLE_KEY`, ambas de datos vía REST/PostgREST). La llegada de `VERCEL_TOKEN` este ciclo no cambia esto — es un secret distinto, de una plataforma distinta.
- **Monitoreo pago:** sin cambios, `NEXT_PUBLIC_GUMROAD_URL` sigue sin valor en Vercel (confirmado en vivo, ver Novedad de infraestructura arriba).
- **Imagen candidata de `beleno-negro`:** confirmado sin cambio — `data/dangerous-plants.json` sigue con la nota "aprobar visualmente y renombrar", el archivo en disco (`plant-00-beleno-negro-cientifica.jpg`, 217KB) sigue con nombre que no coincide con el patrón `{slug}-cientifica.jpg`, por lo tanto sigue inactivo (placeholder se sigue mostrando). Sin tocar — decisión exclusiva de Papu.

## Contenido
- Backlog `blog_posts`: **90 en `status='draft'`, 19 `published`** — sin cambio frente al ciclo de 06:30 UTC. Mismos 8 drafts con violación explícita del checklist anti-pseudociencia (chakras ×4, reiki, cristales/gemoterapia, biodescodificación, "nutrición cuántica" — listado completo por slug en la bitácora de 2026-07-24-0250 UTC).
- **Decisión (repetida, séptimo ciclo consecutivo):** sin generar contenido de blog nuevo mientras el backlog de 90 drafts siga sin revisión de Papu. El backlog lleva desde 2026-07-23 tarde sin bajar de tamaño porque la única acción segura es no sumar más hasta que se audite lo acumulado.

### Borradores sociales (2, NO publicados — solo propuesta en esta bitácora)

**1. Instagram — "Tu Planta Aliada" (preview de feature, sin prometer fecha)**
> 🌱 Estamos diseñando "Tu Planta Aliada": encontrar, dentro de nuestro diccionario, la planta segura más afín a tu dosha — sin magia, sin promesas, con la misma evidencia que ya usamos en cada ficha.
>
> Sigue disponible mientras tanto: 52 fichas completas, gratis, en el diccionario.
>
> quantum-holistic.com/diccionario
>
> *Contenido informativo, no sustituye consejo médico.*

**2. LinkedIn — por qué 9 plantas del diccionario no muestran foto**
> En nuestro diccionario de 52 plantas, 9 no tienen imagen — a propósito. Son especies con riesgo real de toxicidad (aconito, datura, amanita muscaria, entre otras) y decidimos no ilustrarlas con foto real mientras trabajamos en cómo presentarlas de forma responsable.
>
> Preferimos una limitación visible a un atajo que pueda malinterpretarse como "esto es seguro para probar en casa".
>
> quantum-holistic.com/diccionario
>
> *Contenido divulgativo, no reemplaza el criterio de un profesional sanitario.*

Ambos cumplen el checklist: sin biodescodificación/nutrición cuántica/cristales/reiki/chakras, sin claims de curación, disclaimer médico incluido.

## Tareas manuales de Papu hoy
1. **Crear cuenta Gumroad y pasar la URL del producto** (`NEXT_PUBLIC_GUMROAD_URL` en Vercel) — único bloqueador para el primer cobro de €19. Confirmado en vivo este ciclo (vía API de Vercel, no solo memoria) que sigue sin existir.
2. **Decidir sobre el backlog de 90 drafts de blog** (8 con violación inequívoca de checklist, listados por slug en bitácora 2026-07-24-0250 UTC) — séptimo ciclo consecutivo repitiendo esta misma tarea sin resolución.
3. **Dar a Kimiko Cloud un canal para ejecutar DDL en Supabase** (Management API token o connection string, como secret nuevo y distinto del `VERCEL_TOKEN` que llegó este ciclo) — necesario para crear la tabla `citas` y, cuando Papu quiera, resolver el duplicado real `equinacea`/`echinacea`.

## Notas técnicas
- Sin cambios de código este ciclo — QA pasó sin fixes necesarios, working tree limpio antes y después. Sin commit de código propio; bitácora y memoria las commitea el paso dedicado del workflow (`kimiko-cloud.yml`).
- Séptimo ciclo cloud desde el 2026-07-23 16:50 UTC — todos los indicadores estructurales (QA, plantas, imágenes, leads/purchases, blog backlog) idénticos al ciclo anterior salvo la confirmación en vivo de `NEXT_PUBLIC_GUMROAD_URL` vía el nuevo `VERCEL_TOKEN` y 2 borradores sociales frescos con ángulo distinto a los ya publicados en bitácoras previas.
