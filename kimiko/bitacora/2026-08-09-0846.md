# Bitácora Kimiko — Ciclo cloud 2026-08-09 08:46 UTC (67º ciclo)

## QA (8/8 OK)
1. Build: `npm ci && npm run build` — pasa sin errores, sin fixes necesarios.
2. Producción: canonical y og:url == `https://quantum-holistic.com/` — OK.
3. Rutas 200 (siguiendo redirect de trailing slash, comportamiento normal de Next.js): `/`,
   `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
   `/terapeutas` — todas 200.
4. `/admin` sin sesión → 308 → `/login/?redirect=%2Fadmin%2F` → 200. Redirect correcto,
   `middleware.ts` en raíz confirmado.
5. Supabase `plants`: **52 filas** (`Prefer: count=exact`) — íntegro.
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reverificadas fila por fila:
   `image_cientifica_url` e `image_mistica_url` en `null` para las 9 — placeholder intacto.
7. `leads`: 0 filas en reposo (sin nuevos leads desde el último re-test E2E del ciclo
   2026-08-08 12:54 UTC, que insertó y borró una fila de prueba sin residuo).
8. `sitemap.ts` y `robots.ts` presentes, dominio correcto (`https://quantum-holistic.com`).

`npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio frente al ciclo
anterior. La única alta nueva reportada en ciclos previos (`ws`, uninitialized memory
disclosure/DoS) sigue transitiva de dev tooling; el fix requiere `--force` con cambio breaking
(`eslint-config-next@16.3.0`) — no se toca sin OK de Papu.

## Monetización

**Checkout Gumroad sigue roto — 11 días 14h22min, sexagésimo séptimo ciclo consecutivo.**
- CTA real en producción (`app/producto/ritual-descanso/RitualCheckout.tsx`, verificado en el
  HTML servido) sigue apuntando a `https://kristian320.gumroad.com/l/ritual-descanso` →
  **404 confirmado en vivo este ciclo**.
- `https://kristiantronco.gumroad.com/l/ugsqtg` → **200 confirmado en vivo este ciclo** — sigue
  siendo el revert viable.
- `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`,
  `prj_DASuxCUuV72w8CLpZejVij8XcXvL`), target `production`: existe (tipo `sensitive`), `updatedAt`
  reconfirmado vía API sin cambio desde **2026-07-28T18:25:20.881Z** — es el mismo timestamp que
  se usa como origen del conteo de días caídos. Sin tocar la env var sin OK de Papu.

Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso`: verificado por código
(`app/api/leads/route.ts`, `app/regalo/primera-noche/RegaloForm.tsx`) y rutas 200, sin cambios.
Test E2E con escritura real más reciente sigue siendo el del 2026-08-08 12:54 UTC (~19h53min de
antigüedad al arrancar este ciclo, por debajo del umbral informal de 48h) — no se repite este
ciclo.

"Tu Planta Aliada": sigue sin implementar en código (grep sin match en `app/`, `components/`,
`lib/`). Propuesta de esquema sin cambio, pendiente de OK de Papu.

## Contenido
`blog_posts`: 90 draft / 19 published (109 total), sin cambio. Sin publicación autónoma este
ciclo (nada nuevo que pase el checklist completo).

2 borradores sociales nuevos, sin publicar:
- **Instagram** — ángulo "once días y medio se volvieron casi doce": sobre repetir el mismo golpe
  en vivo a los dos enlaces de Gumroad, ciclo 67, mismo resultado que el ciclo 1 de esta racha.
- **LinkedIn** — ángulo "el timestamp que no se ha movido en casi doce días": sobre usar el
  `updatedAt` de una variable de entorno como reloj objetivo de una incidencia, en vez de contar
  ciclos.

## Optimización continua
- Funnel A/B: `leads` en 0 filas — sin volumen para proponer variantes de CTA este ciclo.
- Tu Planta Aliada: sin cambio de estado (no implementada).
- Cita diaria: última inserción (Florence Nightingale, 01:52:10 UTC del 08-09) tiene ~6h55min de
  antigüedad al arrancar el ciclo — por debajo del umbral de 24h, **sin inserción nueva este
  ciclo**.
- Monitoreo pago: sin cambio (ver Monetización arriba).

## Cambios de código
Ninguno. Build pasa, QA limpio, sin fixes necesarios. `next-env.d.ts` regenerado por el build se
revirtió sin commitear (cambio no funcional). Sin escrituras nuevas en Supabase este ciclo (citas
por debajo del umbral de 24h, funnel probado hace menos de 20h, sin leads que borrar).

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** (11 días 14h+ caído): la CTA en producción apunta a
   `kristian320.gumroad.com/l/ritual-descanso` (404). El revert viable y confirmado en vivo es
   `kristiantronco.gumroad.com/l/ugsqtg` (200) — o actualizar el valor de la env var
   `NEXT_PUBLIC_GUMROAD_URL` (production) en Vercel a ese link.
2. **Decidir sobre las fichas contaminadas del diccionario**: pendiente desde 2026-07-30 (~10 días
   6h+), incluye duplicado `equinacea`/`echinacea` (mismo `nombre_latino`) y `lavanda` con imagen
   404 en producción. Ver ciclos previos para la propuesta de esquema de `ficha_verificada`.
3. **Revisar el fix de `npm audit` para `ws`** (alta severidad, requiere `eslint-config-next@16.3.0`
   con cambios breaking) — Kimiko no lo aplica sin OK explícito por el riesgo de romper el build.
