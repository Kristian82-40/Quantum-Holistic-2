# Bitácora — 2026-09-09 22:22 UTC (ciclo 226º, MODO CICLO)

## Resumen del ciclo
Ciclo mecánico sin hallazgos nuevos, ni técnicos ni de negocio. Cola de
`kimiko_drafts` revisada primero (vacía, sin `en_curso` colgado) por si había
alguna orden de Telegram pendiente aunque el disparo fuera `schedule` — no
la había.

## Comprobado
- `npm ci && npm run build`: limpio, 36/36 páginas. `npx next lint`: sin avisos.
- `npm audit`: **1 moderate, 7 high, 1 critical** (total 9) — sin cambio desde
  el 223º. Sigue sin fix no-breaking en la rama 14.x de Next.js. Tarea manual
  de Kristian, sin acción de oficio (blast radius alto).
- `middleware.ts` confirmado en la raíz.
- Rutas del checklist, todas 200 (con `-L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F`
  → 200, cadena completa verificada con `-D -`.
- `canonical`/`og:url`/`og:image` → `https://quantum-holistic.com/`.
  `sitemap.xml`: 37 `<loc>` (sin cambio). `robots.txt` correcto.
- Vercel: últimos 5 despliegues `READY`.
- `plants`: 52 filas. 4 publicada+verificada sin cambio (`albahaca`, `arnica`,
  `equinacea`, `hinojo`), las 4 imágenes confirmadas en disco. Las 9
  peligrosas confirmadas `publicada=false`.
- Decimocuarta pasada del cruce de hash de `ficha_cientifica` sobre las 52
  filas: exactamente los mismos 7 grupos desde el 212º, sin grupos nuevos.
  `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual completa
  este ciclo.
- `echinacea`/`equinacea`: sigue sin duplicado real en vivo (404 vs 200, con
  `-L`).
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio. 22
  publicados sin duplicados de título, todos ≤60 car., `excerpt` ≤155 car.,
  todos con `image_url`, 3/22 enlazan a `/diccionario` (sin cambio, backlog
  conocido). Draft más reciente sigue en 2026-07-10 (ya descartado en
  206º-209º).
- `citas`: última fila sigue siendo Proverbios 14:30 del 224º
  (2026-09-09T08:49:27 UTC), hueco de **13h33min** — dentro del umbral normal
  (~24h), sin necesidad de insertar.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2). `kimiko_drafts`:
  4 filas totales, todas `hecho`, cola de `pendiente` vacía, sin `en_curso`
  colgado, sin orden de Telegram este ciclo.

## Escritura en Supabase este ciclo
Ninguna. Sin commits de código de negocio; solo esta bitácora + memoria.

## Tareas manuales de Kristian hoy
1. **Decidir sobre la actualización mayor de Next.js (14.2.35 → 16.3.4)** —
   sigue siendo la única forma de cerrar la vulnerabilidad crítica de `npm
   audit`. Exposición real hoy mitigada por config (Image Optimizer
   desactivado, sin Server Actions, sin rewrites, sin CSP nonces), pero no
   hay parche no-breaking en la rama 14.x. Ofrezco hacerlo en rama/PR
   separada si autoriza.
2. Ninguna urgencia con `citas` (hueco de 13h33min, muy por debajo del
   umbral de inserción).
3. Backlog de blog (79 drafts) sigue sin candidatos nuevos publicables tras
   la revisión en bloque de los ciclos 208º-209º.
