# 2026-08-19 04:38 UTC — Ciclo cloud: QA limpio, sin cambios de estado, Gumroad sigue roto (125º ciclo)

## QA (8/8 OK)
1. Build: `npm ci && npm run build` pasa sin errores (corrido desde la raíz).
2. Producción: canonical y `og:url` en `https://quantum-holistic.com` correctos
   (`https://quantum-holistic.com/`).
3. Rutas del checklist, las 7 en 200 (siguiendo redirect por `trailingSlash: true`, esperado):
   `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
   `/registro`, `/terapeutas`.
4. `/admin` sin sesión → 308 a `/admin/` → 200 en `/login/?redirect=%2Fadmin%2F`. `middleware.ts`
   confirmado en la raíz (no existe `app/middleware.ts`).
5. Supabase: 52 plantas en tabla `plants`, íntegras (`content-range: 0-0/52`).
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url` e
   `image_mistica_url` en `null`, reverificado fila por fila. Placeholder intacto.
7. `leads`: 0 filas, sin cambio. Sin datos para segmentación por dosha ni A/B.
8. `sitemap.ts` y `robots.ts` presentes, dominio correcto (`quantum-holistic.com`).

`npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio.

## Monetización
- **Checkout Gumroad sigue roto**, ~21 días 10h, 125º ciclo consecutivo desde
  2026-07-28T18:25:20.881Z. CTA real en el HTML servido de `/producto/ritual-descanso` apunta a
  `https://kristian320.gumroad.com/l/ritual-descanso` → 404 en vivo (reconfirmado). El revert
  viable sigue siendo `https://kristiantronco.gumroad.com/l/ugsqtg` → 200 (reconfirmado). Entrada
  `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, target `production`, tipo
  `sensitive`) reconfirmada sin cambio vía API de solo lectura (`updatedAt` = 2026-07-28T18:25:20.881Z,
  idéntico). Código de `RitualCheckout.tsx` correcto (usa la env var con fallback de captura de
  email si no existe); el problema es exclusivamente el valor configurado en Vercel. Sin tocar
  ninguna env var sin OK de Papu — es un cambio en un secreto de producción, fuera de mi alcance
  autónomo.
- Funnel `/regalo/primera-noche` → lead → producto verificado por código (rutas 200, lógica
  intacta). `leads` sigue en 0 filas, sin datos para A/B.
- "Tu Planta Aliada": sin implementar (confirmado por grep en `app/`, `components/`, `lib/`),
  pendiente de OK de Papu sobre el esquema propuesto en ciclos previos.

## Contenido
- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los 19 drafts identificados en
  ciclos previos (8 en temas prohibidos del checklist anti-pseudociencia, 11 en categoría de
  contraindicaciones sin resolver) reconfirmados sin cambio, ninguno publicado ni reescrito este
  ciclo.
- Sin borradores sociales nuevos este ciclo (los 2 pendientes de `2026-08-16-0125.md` siguen sin
  publicar; no se duplican en la bitácora para evitar ruido).

## Optimización continua
- Gate `ficha_verificada`: 0/52, sin cambio (pendiente desde 2026-07-30 ~02:36 UTC, ~20 días 2h).
- Duplicado `equinacea` (id 52) / `echinacea` (id 21) sin cambio.
- `lavanda` sigue con imagen `/images/plants/lavanda-cientifica.jpg` en 404 en vivo.
- **Cita diaria**: última (Benjamin Franklin, 12:51:16 UTC del 08-18) con ~15h47min de antigüedad
  al arrancar el ciclo, por debajo del umbral de 24h — sin inserción nueva. 18 citas en la tabla,
  todas de autores distintos. Cruzará el umbral ~12:51 UTC del 08-19, en un ciclo posterior.

## Estado del repo
- `git status` al arrancar: limpio.
- Sin commits de código este ciclo (build pasa, QA limpio, sin fixes necesarios). Sin escrituras
  nuevas en Supabase este ciclo (cita bajo el umbral de 24h, `leads` en 0, sin datos que segmentar).
  Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** (~21 días roto, 125 ciclos): actualizar
   `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, target `production`) al
   enlace vigente. Revert probado y funcional: `https://kristiantronco.gumroad.com/l/ugsqtg`
   (200). El actual, `https://kristian320.gumroad.com/l/ritual-descanso`, da 404.
2. **Decidir el gate `ficha_verificada`** (0/52 desde hace ~20 días 2h): confirmar si las 52
   fichas de plantas están listas para marcarse como verificadas, o indicar qué falta.
3. **Resolver el duplicado `equinacea`/`echinacea`** (ids 52/21) y la imagen 404 de `lavanda`
   (`/images/plants/lavanda-cientifica.jpg`) en el diccionario.
