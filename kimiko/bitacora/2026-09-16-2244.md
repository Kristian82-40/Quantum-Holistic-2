# Bitácora — 2026-09-16 22:44 UTC (ciclo 256º, MODO CICLO)

## Qué comprobé

- `kimiko_drafts`: comprobado primero, antes del resto del checklist, pese al
  disparo `schedule`. Cola de `pendiente` vacía, sin fila `en_curso` colgada,
  sin orden de Telegram este ciclo.
- Build: `npm ci` + `npm run build` limpios, 36/36 páginas generadas, sin
  errores de tipos ni de lint. `npx next lint` sin avisos.
- `npm audit`: sin cambio desde el 223º — 1 moderate, 7 high, 1 critical (9
  total), todas con fix solo disponible vía `--force` (breaking: next-intl
  4.14.5, next 16.3.5). Sigue sin fix no-breaking disponible.
- 8/8 rutas del checklist devuelven 200 tras seguir redirects: `/`,
  `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`,
  `/login`, `/registro`, `/terapeutas`.
- `/admin` sin sesión: sigue redirigiendo hasta `/login/?redirect=%2Fadmin%2F`.
  Correcto.
- `middleware.ts` confirmado en la raíz del repo (no dentro de `app/`).
- Producción: `canonical` y `og:url` en home apuntan a
  `https://quantum-holistic.com/`. `sitemap.xml` con 37 `<loc>`. `robots.txt`
  correcto con referencia al sitemap.
- Vercel: últimos 5 despliegues (`quantum-holistic-2` y `kimiko`) en `READY`.

## Cuadragésima segunda pasada del check permanente (211º/212º)

Rehecho el cruce de hash de `ficha_cientifica` desde cero sobre las 52 filas
de `plants` (sin mirar la cifra anotada en ciclos previos, comparando
`json.dumps(valor, sort_keys=True)` antes de hashear). Resultado: mismos **7
grupos / 21 filas** de la corrección del 249º —

- 6 pares de fichas reales: `ashwagandha-fruto`/`sauco`,
  `cinamomo`/`valeriana`, `echinacea`/`equinacea`, `ginseng`/`muerdago`,
  `loto`/`tulsi`, `manzanilla`/`nigela` (12 filas).
- 1 grupo de 9 filas: las peligrosas (`aconito`, `amanita-muscaria`,
  `beleno-negro`, `cannabis`, `cornezuelo-centeno`, `datura`, `datura-metel`,
  `hierba-mora`, `tejo`) comparten el placeholder.

Sin cambios respecto al baseline. `plants`: 52 filas totales, 4
publicada+verificada sin cambio (`albahaca`, `arnica`, `equinacea`,
`hinojo`) — las 4 imágenes confirmadas presentes en disco
(`public/images/plants/`). Las 9 peligrosas confirmadas `publicada=false`
(ninguna aparece marcada publicada). `lavanda` sigue despublicada y sin
verificar, con `image_cientifica_url` apuntando a
`lavanda-cientifica.jpg` que sigue sin existir en disco — hallazgo ya
documentado desde 2026-09-03, sin blast radius (no publicada). `updated_at`
máximo sin cambio (2026-09-01T09:21:58Z) → sin auditoría visual completa
este ciclo (nada nuevo que auditar).

## Otras tablas

- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin cambio.
- `citas`: última fila sigue siendo Salmos 103:3 (Reina-Valera, insertada en
  el 252º), hueco de **~23h56min** al comprobar — justo por debajo del
  umbral de ~24h (margen de minutos), sin necesidad de insertar este ciclo.
  Si el próximo ciclo tarda más de ~4 minutos en llegar, cruzará el umbral y
  tocará insertar entonces. 41 filas, sin cambio.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Qué arreglé

Nada — no había hallazgos que requirieran cambio.

## Qué encontré sospechoso

Nada nuevo. Los dos hallazgos ya documentados en ciclos anteriores siguen
igual y sin blast radius: `lavanda` sin imagen en disco (no publicada), y
el `npm audit` con 9 vulnerabilidades sin fix no-breaking (dependen de
actualizaciones mayores de next-intl/next). Además, `citas` está muy cerca
del umbral de ~24h (ver arriba) — probable que el próximo ciclo tenga que
insertar una cita nueva.

## Cambios este ciclo

Ninguno — sin commits de código, sin escrituras en Supabase. Cuadragésimo
segundo ciclo consecutivo (212º-256º) sin hallazgos técnicos nuevos en el
checklist mecánico, y sin hallazgos de negocio nuevos.

## Tareas manuales de Kristian hoy

1. Ninguna urgente. El `npm audit` (9 vulnerabilidades, incluida 1 critical)
   sigue esperando una decisión consciente sobre si asumir el upgrade
   breaking de `next` a 16.3.5 y `next-intl` a 4.14.5 — es una decisión de
   producto/arquitectura, no algo que deba forzar por mi cuenta.
2. Si en algún momento hay una imagen real para `lavanda-cientifica.jpg`,
   subirla a `public/images/plants/` para que pueda verificar y publicar
   esa ficha en un ciclo futuro.
3. Nada más pendiente de tu parte hoy.
