# Bitácora — 2026-09-15 09:16 UTC (ciclo 250º, MODO CICLO)

## Comprobado
- `kimiko_drafts`: cola de `pendiente`/`en_curso` vacía (comprobado primero,
  antes del resto del checklist). Sin orden de Telegram este ciclo pese al
  disparo `schedule`.
- `npm ci`: OK, 415 paquetes. `npm audit`: 9 vulnerabilidades (1 moderate,
  7 high, 1 critical), sin cambio desde el 223º, sigue sin fix no-breaking.
- `npm run build`: OK, 36/36 páginas generadas. `npx next lint`: sin avisos.
- Rutas del checklist (8/8) en 200: `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F`.
  `middleware.ts` en la raíz del repo (no dentro de `app/`).
- Canonical (`https://quantum-holistic.com/`) y `og:url` correctos en `/`.
- `sitemap.xml`: 37 `<loc>`. `robots.txt`: `Allow: /` + referencia al
  sitemap, dominio correcto.
- Vercel: últimos 5 despliegues (`quantum-holistic-2` y `kimiko`) en `READY`.

## Diccionario (`plants`)
- 52 filas. `publicada=true AND ficha_verificada=true`: `albahaca`,
  `arnica`, `equinacea`, `hinojo` (sin cambio). Las 4 imágenes
  (`*-cientifica.jpg`) confirmadas en disco en `public/images/plants/`.
- Las 9 peligrosas (aconito, amanita-muscaria, beleno-negro, cannabis,
  cornezuelo-centeno, datura, datura-metel, hierba-mora, tejo) confirmadas
  `publicada=false`.
- Re-derivé desde cero (sin mirar la cifra anotada en ciclos previos) el
  cruce de hash de `ficha_cientifica` sobre las 52 filas: **7 grupos
  duplicados / 21 filas**, igual que la corrección del 249º. Confirma que
  esa corrección fue correcta; no hago cambios en `plants`.
- `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual
  completa este ciclo (no hay fichas nuevas que revisar).

## Blog y contenido
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Draft más reciente sigue en 2026-07-10, ya descartado en 206º-209º.

## Citas
- Última fila: Proverbios 18:14 (insertada en el 247º), hueco de
  ~15h21min desde ahora — por debajo del umbral de ~24h, sin necesidad de
  insertar cita nueva. 40 filas, sin cambio.

## Negocio
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Cambios este ciclo
Ninguno en código ni en Supabase. Trigésimo sexto ciclo consecutivo
(212º-250º, con la corrección puntual del 247º/249º ya resuelta) sin
hallazgos técnicos nuevos en el checklist mecánico, y sin hallazgos de
negocio nuevos.

## Tareas manuales de Kristian hoy
1. Ninguna acción urgente — el sitio, el diccionario y el blog están
   estables. `npm audit` sigue con 9 vulnerabilidades sin fix no-breaking;
   si en algún momento quieres evaluar el fix con cambios breaking, dímelo
   y lo reviso con más detalle.
2. Sigue habiendo 79 posts en `draft` en el blog (incluido uno desde
   2026-07-10) esperando revisión editorial si quieres retomar publicación
   activa; por ahora los descarto de mi checklist automático.
3. La cola de Telegram (`kimiko_drafts`) está vacía: si me has escrito
   algo que no ha llegado, revisa que el buzón de Vercel esté guardando
   bien el mensaje en Supabase.
