# Bitácora Kimiko — Ciclo cloud 2026-08-03 11:29 UTC

## QA (7/7 OK)
1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipo (corrido desde la raíz).
2. Producción: canonical y og:url en `https://quantum-holistic.com/` == `https://quantum-holistic.com/` OK.
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
   `/registro`, `/terapeutas` — todas 200 (con `-L` para las que hacen 308 a slash final).
4. `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts`
   confirmado en raíz (no en `app/`).
5. Supabase: 52/52 plantas íntegras (`content-range: 0-0/52` con `limit=1` + `count=exact`, tabla
   `plants`).
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reconfirmadas fila por fila:
   `image_cientifica_url`/`image_mistica_url` en `null`, placeholder intacto.
7. `leads`: 0 filas (solo lectura este ciclo, sin escritura de prueba).
8. `sitemap.xml`/`robots.txt`: presentes (200/200), dominio correcto (`https://quantum-holistic.com`).

## Monetización
- **Checkout Gumroad sigue roto, 5 días 17h de antigüedad, trigésimo tercer ciclo consecutivo.**
  CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción tras
  seguir el 308 con `-L`) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404
  confirmado en vivo). `NEXT_PUBLIC_GUMROAD_URL` reconfirmada vía API directa de Vercel
  (`prj_DASuxCUuV72w8CLpZejVij8XcXvL`): `updatedAt` de la variable de producción sigue en
  `1785263120881` ms → 2026-07-28T18:25:20.881Z, sin cambio desde el incidente original.
  `kristiantronco.gumroad.com/l/ugsqtg` sigue siendo el revert viable. Sin tocar la env var sin OK
  de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado (código + rutas
  200, PDF de regalo 200), sin cambios.
- "Tu Planta Aliada": sin implementar en código, sin cambios. Cobertura de datos íntegra
  (`ficha_mistica.afinidad_ayurvedica` 43/43 plantas seguras, excluyendo las 9 peligrosas por
  filtro hardcoded, reconfirmado vía filtro JSON `ficha_mistica->>afinidad_ayurvedica`). Sigue
  pendiente de OK de Papu para implementar.

## Contenido
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Duplicado `equinacea`/`echinacea` reconfirmado (dos fichas separadas: `equinacea` "Equinácea" y
  `echinacea` "Echinacea", ambas `Echinacea purpurea`), sin fusionar — pendiente de decisión de
  producto.
- `lavanda` (planta segura): `image_cientifica_url` sigue apuntando a
  `/images/plants/lavanda-cientifica.jpg`, reconfirmado 404 en producción. Hallazgo abierto desde
  2026-07-29, sin cambio.
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio. `uuid` y `ws` con
  fix disponible sin `--force` (sin breaking changes, reconfirmado por dry-run este ciclo);
  `next-intl` y `postcss` solo vía `--force` (`next@16`, breaking). Se mantiene sin aplicar — 33
  ciclos consecutivos deferido a tarea manual de Papu.
- Imágenes en disco: 71 archivos en `public/images/plants`, sin cambio.
- Las 34 fichas contaminadas (25 seguras + 9 peligrosas) siguen sin gate ni corrección — decisión
  de producto pendiente de Papu desde 2026-07-30 02:36 UTC (tarea manual #2). Caso
  `ashwagandha-fruto` reverificado en vivo: sigue con `ficha_mistica.chakra = "Corazón"` (perfil
  de Saúco/Sambucus, planeta regente Venus) bajo el nombre y slug de "Ashwagandha en Fruto", sin
  cambio.

## Cita diaria (tabla `citas`)
- Sin inserción este ciclo: última cita (Maimónides, 2026-08-02 13:28:24 UTC) tiene ~22h de
  antigüedad al momento de la verificación (11:29 UTC) — por debajo del umbral de 24h. Corresponde
  insertar la próxima cita ~2026-08-03 13:28 UTC.

## Optimización continua
- A/B funnel: sin datos suficientes en `leads` (0 filas reales) para proponer variantes de CTA con
  respaldo estadístico — se mantiene la conclusión de ciclos anteriores, sin propuesta nueva.
- "Tu Planta Aliada": sin cambios en el mapeo (feature no implementada en código todavía).
- `/api/webhooks/btcpay`: ruta heredada reconfirmada en el build de producción, sin tocar — no
  evaluada ni usada (pasarela cripto descartada por decisión ya tomada).

## Borradores sociales (sin publicar)
1. **Instagram** — Ángulo: "Cinco días, treinta y tres verificaciones" (gancho: no es que el
   equipo no note el enlace roto — es que cada ciclo lo comprueba en vivo, en el HTML real que ve
   quien compra, no solo en el código. La constancia de la verificación es una promesa silenciosa
   de que nada se corrige "a ojo").
2. **LinkedIn** — Ángulo: "Verificar por dos caminos independientes sigue rindiendo, incluso al
   repetirlo" (gancho: la confirmación cruzada del dato de Gumroad —API de Vercel + HTML servido—
   y de la cobertura de "Tu Planta Aliada" —consulta directa sobre el campo JSON en vez de asumir
   el dato del ciclo anterior— es la misma disciplina aplicada dos veces en el mismo ciclo).

## Tareas manuales de Papu hoy
1. **Reactivar el checkout de Ritual del Descanso**: `NEXT_PUBLIC_GUMROAD_URL` en Vercel apunta a
   `kristian320.gumroad.com/l/ritual-descanso` (404 desde 2026-07-28, 33 ciclos). Si es un error,
   revertir a `kristiantronco.gumroad.com/l/ugsqtg` o publicar la URL nueva correctamente en
   Gumroad. Bloqueador directo del objetivo de €22.000.
2. **Decisión sobre las 34 fichas contaminadas** (25 seguras + 9 peligrosas, incluye caso
   confirmado `ashwagandha-fruto` con contenido completo de Saúco) **y sobre la imagen rota de
   `lavanda`** (404 desde el 2026-07-29): ¿reescribir/corregir manualmente, despublicar, o
   mantener el gate `ficha_verificada` indefinidamente? Pendiente desde 2026-07-30 02:36 UTC.
3. **Aplicar `npm audit fix`** (sin `--force`) para las vulnerabilidades de `uuid` y `ws` (memory
   disclosure + DoS en `ws`, sin breaking changes confirmado por dry-run) — de las 16 totales, son
   las dos con parche directo disponible sin afectar `next`/`next-intl`.

## Cierre
- QA 7/7 OK, sin hallazgos críticos nuevos de código.
- 52 plantas, 9 peligrosas con placeholder intacto, reverificado fila por fila.
- Sin commits de código este ciclo (build limpio, QA sin regresiones); `next-env.d.ts` regenerado
  por el build revertido sin commitear (cambio no funcional).
- Sin escrituras nuevas en Supabase este ciclo (cita diaria por debajo del umbral de 24h, leads en
  modo solo lectura).
- Bitácora y memoria las commitea el paso dedicado del workflow.
