# Bitácora Kimiko — Ciclo cloud 2026-08-10 05:29 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200 (siguiendo redirect de trailing slash, esperado en Next.js): `/`, `/diccionario/`, `/regalo/primera-noche/`, `/producto/ritual-descanso/`, `/login/`, `/registro/`, `/terapeutas/`.
- `/admin` sin sesión → 308 → `/admin/` → 200 sobre `/login/?redirect=%2Fadmin%2F` tras redirect (middleware.ts confirmado en la raíz del repo, no en `app/`).
- `sitemap.xml` y `robots.txt` (200, dominio correcto `quantum-holistic.com`) y `robots.txt` con `Sitemap:` correcto.
- Supabase `plants`: 52 filas íntegras (`Content-Range: 0-0/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila.
- `npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio frente al ciclo anterior.
- `leads`: 0 filas, sin cambio.

## Monetización
- **Checkout Gumroad sigue roto — 72º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~12 días 11h03min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) reconfirmada con sus dos entradas: `development` (`encrypted`, id `vf27pDQT5ZT5iBel`, sin relevancia para el CTA real) y `production` (`sensitive`, id `L6v4bSqxUSFYv5U5`) — la de producción sin cambio desde 2026-07-28T18:25:20.881Z. No se toca la env var sin OK explícito de Papu.
- Código de fallback en `RitualCheckout.tsx` reconfirmado correcto: si `NEXT_PUBLIC_GUMROAD_URL` no existe, muestra captura de email a `/api/leads` con `source: ritual_descanso_waitlist`; como la env var de producción sí existe (aunque rota), el componente sirve el CTA roto en vez del fallback — comportamiento esperado del código, el problema es el valor de la env var, no la lógica.
- Funnel `/regalo/primera-noche` → lead (`POST /api/leads`, `source: primera_noche_regalo`) → éxito enlaza a `/producto/ritual-descanso` — verificado por código (`RegaloForm.tsx`) y rutas 200, sin cambios. Test E2E real con escritura más reciente sigue siendo el del 2026-08-08 12:54 UTC (~1 día 16h35min de antigüedad, por debajo del umbral informal de 48h) — no se repite este ciclo.
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Propuesta de esquema sin cambio (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu para implementar.

## Contenido
- Sin blog nuevo — backlog sin cambio: 90 `draft` / 19 `published` (109 total). Decisión deliberada de no generar contenido nuevo mientras el backlog no revisado siga acumulado — sigue pendiente de Papu.
- Tabla `citas`: última inserción (Ambroise Paré, 2026-08-10 01:56:27 UTC) tiene ~3h32min de antigüedad al arrancar el ciclo — por debajo del umbral de 24h, sin inserción nueva este ciclo. 10 citas en la tabla, todas de autores distintos.
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "72 veces no es lo mismo que 72 intentos": verificar el mismo link roto por septuagésima segunda vez seguida no es repetir un fracaso, es negarse a asumir que "seguía roto ayer" implica "sigue roto hoy" sin comprobarlo. La constancia de la comprobación es el producto, no una molestia.
  - **LinkedIn** — ángulo "descartar el archivo que no importa": cada ciclo el build regenera `next-env.d.ts` y cada ciclo se revierte sin commitear porque no es un cambio funcional. Saber qué diff vale la pena conservar y cuál es ruido del propio proceso de build es una decisión operativa pequeña que, sin disciplina, ensucia el historial de git con nada.

## Higiene de datos (sin recruce completo este ciclo, sin cambio de estado)
- Última reproducción íntegra del cruce de fichas contaminadas fue el ciclo 2026-08-07 16:57 UTC: 34 confirmadas (25 seguras + 9 peligrosas), con contenido de otra especie en `ficha_mistica`. Pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC (~11 días 2h53min).
- Duplicado de contenido `equinacea`/`echinacea` (mismo `nombre_latino`, `Echinacea purpurea`) reconfirmado sin cambio (ids 52 y 21) — pendiente de decisión de slug canónico.
- `lavanda`: `image_cientifica_url` (`/images/plants/lavanda-cientifica.jpg`) reconfirmado 404 en vivo este ciclo — sin reemplazo.
- Gate `ficha_verificada`: 0/52 plantas verificadas, sin cambio.

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. `next-env.d.ts` regenerado por el build se revirtió sin commitear (`git checkout -- next-env.d.ts`, cambio no funcional). Sin escrituras nuevas en Supabase este ciclo (citas por debajo del umbral de 24h, funnel probado hace ~1 día 16h35min, sin leads que segmentar). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — la env var `NEXT_PUBLIC_GUMROAD_URL` de `production` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 12 días 11h03min. `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que sirven `ficha_mistica` de una especie distinta a la real — pendiente desde el 2026-07-30, bloquea "Tu Planta Aliada" y cualquier feature que use ese campo.
3. **Revisar el backlog de 90 drafts de blog** (8 con violación explícita del checklist anti-pseudociencia: chakras, reiki, cristales, biodescodificación, nutrición cuántica, identificados en ciclos previos) — decidir archivar/borrar/publicar antes de que Kimiko genere contenido nuevo.
