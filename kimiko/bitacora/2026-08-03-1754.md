# Bitácora Kimiko — Ciclo cloud 2026-08-03 17:54 UTC

## QA (8/8 OK)
1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipo (corrido desde la raíz).
2. Producción: canonical y og:url en `https://quantum-holistic.com/` == `https://quantum-holistic.com/` OK.
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
   `/registro`, `/terapeutas` — todas 200 (con `-L` para las que hacen 308 a slash final).
4. `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts`
   confirmado en raíz (no en `app/`, sin duplicado).
5. Supabase: 52/52 plantas íntegras (`content-range: 0-0/52` con `limit=1` + `count=exact`, tabla
   `plants`).
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reconfirmadas fila por fila:
   `image_cientifica_url`/`image_mistica_url` en `null`; verificado además que ninguna tiene
   archivo de imagen huérfano en `public/images/plants/` (doble check campo + disco).
7. `leads`: 0 filas (solo lectura este ciclo, sin escritura de prueba).
8. `sitemap.xml`/`robots.txt`: presentes (200/200), dominio correcto (`https://quantum-holistic.com`).

## Monetización
- **Checkout Gumroad sigue roto, ~5 días 23h30min de antigüedad, trigésimo quinto ciclo
  consecutivo.** CTA real servido en `/producto/ritual-descanso/` (`href` extraído del HTML de
  producción) sigue apuntando a `https://kristian320.gumroad.com/l/ritual-descanso` (404
  confirmado en vivo). Vía API de Vercel (`prj_DASuxCUuV72w8CLpZejVij8XcXvL`), `updatedAt` del env
  var de producción sigue siendo `1785263120881` ms (2026-07-28T18:25:20.881Z), sin cambio desde
  el incidente original. `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue
  siendo el revert viable. Sin tocar la env var sin OK de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado (código + rutas
  200), sin cambios.
- "Tu Planta Aliada": sin implementar en código, sin cambios. Cobertura de datos reverificada:
  `ficha_mistica.afinidad_ayurvedica` presente en 43/43 plantas seguras (excluyendo las 9
  peligrosas por filtro hardcoded), 0 faltantes. Sigue pendiente de OK de Papu para implementar.

## Contenido
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Duplicado `equinacea`/`echinacea` reconfirmado (dos fichas separadas, mismo `nombre_latino`),
  sin fusionar — pendiente de decisión de producto.
- `lavanda` (planta segura): `image_cientifica_url` sigue apuntando a
  `/images/plants/lavanda-cientifica.jpg`, reconfirmado 404 en producción. Sin cambio desde
  2026-07-29.
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio frente al ciclo
  anterior. `uuid`/`ws` con parche disponible sin `--force` sigue sin aplicar — 35 ciclos
  consecutivos deferido a tarea manual de Papu.
- Imágenes en disco: 71 archivos en `public/images/plants`, sin cambio.
- Las 34 fichas contaminadas (25 seguras + 9 peligrosas) siguen sin gate ni corrección — pendiente
  de Papu desde 2026-07-30 02:36 UTC.

## Cita diaria (tabla `citas`)
- **Sin inserción este ciclo.** Última cita (Charaka, *Charaka Samhita*, 2026-08-03T14:46:06 UTC)
  tiene ~3h08min de antigüedad al momento de la verificación (17:54 UTC) — no supera el umbral de
  24h. Próxima inserción corresponde al ciclo que supere las 24h (~2026-08-04 14:46 UTC).

## Optimización continua
- A/B funnel: sin datos suficientes en `leads` (0 filas reales) para proponer variantes de CTA con
  respaldo estadístico — se mantiene la conclusión de ciclos anteriores, sin propuesta nueva.
- "Tu Planta Aliada": sin cambios en el mapeo (feature no implementada en código todavía).
- Monitoreo de pago: `NEXT_PUBLIC_GUMROAD_URL` existe en Vercel para `production` (tipo
  `sensitive`) y `development` (tipo `encrypted`), pero el valor servido en producción sigue
  siendo la URL 404 — no corresponde activar el hito de CTA transaccional hasta que el enlace
  resuelva en 200.

## Borradores sociales (sin publicar)
1. **Instagram** — Ángulo: "Treinta y cinco veces la misma comprobación, y ninguna es un trámite"
   (gancho: cada pocas horas se repite el mismo chequeo del enlace de compra, plantas seguras y
   fichas de contenido — no porque se espere que algo cambie, sino porque la única forma de saber
   que nada se rompió en silencio es mirar de nuevo. La constancia aburrida es la que sostiene la
   confianza del catálogo).
2. **LinkedIn** — Ángulo: "Cuando la comprobación no encuentra nada nuevo, sigue siendo la
   comprobación correcta" (gancho: en ciclos de monitoreo automatizado, un resultado idéntico al
   anterior no es un ciclo desperdiciado — es la confirmación de que un incidente detectado hace
   días (el CTA de Gumroad roto) sigue exactamente acotado y no se ha extendido a nada más del
   sitio).

## Tareas manuales de Papu hoy
1. **Reactivar el checkout de Ritual del Descanso**: `NEXT_PUBLIC_GUMROAD_URL` en Vercel (target
   production) sigue sirviendo `kristian320.gumroad.com/l/ritual-descanso` (404 desde 2026-07-28,
   35 ciclos). Si es un error, revertir a `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado) o
   publicar la URL nueva correctamente en Gumroad. Bloqueador directo del objetivo de €22.000.
2. **Decisión sobre las 34 fichas contaminadas** (25 seguras + 9 peligrosas) **y sobre la imagen
   rota de `lavanda`** (404 desde el 2026-07-29): ¿reescribir/corregir manualmente, despublicar, o
   mantener el gate `ficha_verificada` indefinidamente? Pendiente desde 2026-07-30 02:36 UTC.
3. **Aplicar `npm audit fix`** (sin `--force`) para las vulnerabilidades de `uuid` y `ws` (memory
   disclosure + DoS en `ws`, sin breaking changes confirmado por dry-run en ciclos previos) — de
   las 16 totales, son las dos con parche directo disponible sin afectar `next`/`next-intl`.

## Cierre
- QA 8/8 OK, sin hallazgos críticos nuevos de código.
- 52 plantas, 9 peligrosas con placeholder intacto (campo null + sin archivo huérfano),
  reverificado fila por fila.
- Sin inserción de cita (última tiene ~3h de antigüedad, por debajo del umbral de 24h).
- Sin commits de código este ciclo (build pasa, QA sin regresiones); `next-env.d.ts` regenerado
  por el build se revirtió sin commitear (cambio no funcional).
- Sin escrituras nuevas en Supabase este ciclo (leads y citas en modo solo lectura).
- Bitácora y memoria las commitea el paso dedicado del workflow.
