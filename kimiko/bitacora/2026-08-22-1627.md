# Bitácora Kimiko — 2026-08-22 16:27 UTC (ciclo 146)

## QA (8/8 OK)

1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipos.
2. Producción: canonical y `og:url` en `https://quantum-holistic.com/` == `https://quantum-holistic.com/`. OK.
3. Rutas del checklist: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → 200. OK.
4. `/admin` sin sesión → 308 → `/admin/` → 200 tras cadena de redirect a `/login/?redirect=%2Fadmin%2F` (confirmado). `middleware.ts` confirmado solo en la raíz (no existe copia en `app/`). OK.
5. Supabase `plants`: 52 filas íntegras (`content-range: 0-0/52` sobre `select=id&limit=1` con `count=exact`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, verificado fila por fila. OK.
6. `sitemap.xml` (200) y `robots.txt` (200) presentes con dominio correcto. OK.
7. `npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio respecto al ciclo anterior.
8. Leads: tabla `leads` en 0 filas (`content-range: */0`), sin cambio.

## Hallazgos (sin cambio respecto a ciclos previos)

- **Checkout Gumroad sigue roto**, ~24 días 22h, 146º ciclo consecutivo desde 2026-07-28T18:25:20.881Z.
  El HTML servido de `/producto/ritual-descanso` (tras seguir el redirect por trailing slash) sirve
  el CTA real apuntando en vivo a `kristian320.gumroad.com/l/ritual-descanso` (**404 confirmado**).
  El revert viable `kristiantronco.gumroad.com/l/ugsqtg` sigue devolviendo **200**. La entrada de
  `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`,
  `prj_DASuxCUuV72w8CLpZejVij8XcXvL`, target `production`, tipo `sensitive`) no ha cambiado
  (`updatedAt` = 1785263120881 ms, es decir 2026-07-28T18:25:20.881Z), reconfirmado vía API de
  solo lectura. No se toca la env var sin OK explícito de Papu.
- Gate `ficha_verificada`: 0/52, sin cambio (pendiente desde 2026-07-30 ~02:36 UTC, ~23 días 14h).
- Duplicado en `plants`: `equinacea` (id 52) / `echinacea` (id 21), sin cambio.
- `lavanda` (id 11) sigue con `image_cientifica_url` apuntando a
  `/images/plants/lavanda-cientifica.jpg`, que devuelve **404 en vivo**. Sin cambio.
- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los 19 drafts previamente
  identificados (8 en temas prohibidos, 11 en categoría de contraindicaciones) siguen sin publicar
  ni reescribir, correcto por presunción negativa. El post publicado con lenguaje de "curación" en
  el título (ciclo 140) sigue publicado sin cambio, pendiente de revisión de Papu.

## Monetización

- `/producto/ritual-descanso`: CTA correctamente condicionado a `NEXT_PUBLIC_GUMROAD_URL` en
  código; el problema sigue siendo de contenido de la variable (enlace roto), no de código.
- Funnel `/regalo/primera-noche` → lead → producto: verificado por código, sin cambios, rutas 200.
- "Tu Planta Aliada": confirmado por grep que sigue sin implementar (`app/`, `components/`, `lib/`
  sin resultados para `planta.?aliada`). Pendiente de OK de Papu antes de proponer esquema/migración.
- A/B de CTAs: `leads` sigue en 0 filas, sin datos suficientes para proponer variantes.

## Cita diaria

Última cita en tabla (Mahatma Gandhi, insertada 2026-08-21T16:39:26 UTC) tiene ~23h48min de
antigüedad al arrancar este ciclo — **todavía por debajo del umbral de 24h, sin inserción nueva
este ciclo**. 21 citas en la tabla, todas de autores distintos. El próximo ciclo (~4h después,
~20:27 UTC) estará claramente por encima del umbral y debería insertar.

## Borradores sociales

Sin borradores nuevos este ciclo. Los 2 pendientes de `kimiko/bitacora/2026-08-16-0125.md` siguen
sin publicar (publicación en redes prohibida para Kimiko); no se duplican en esta bitácora para
evitar ruido.

## Commits

Ninguno. Build pasa sin fixes necesarios. `next-env.d.ts` fue regenerado por el build y revertido
sin commitear (`git checkout -- next-env.d.ts`, cambio no funcional). Sin escrituras nuevas en
Supabase este ciclo (cita bajo el umbral de 24h, leads en 0, sin datos que segmentar). Bitácora y
memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy

1. **Arreglar el checkout de Gumroad** (bloqueante de monetización, 24 días 22h roto): el CTA en
   vivo apunta a `kristian320.gumroad.com/l/ritual-descanso` (404). Cambiar
   `NEXT_PUBLIC_GUMROAD_URL` en Vercel (producción) a `https://kristiantronco.gumroad.com/l/ugsqtg`
   (200 confirmado), o publicar el producto correcto en la cuenta `kristian320` y actualizar el
   enlace en consecuencia.
2. **Decidir el gate `ficha_verificada`** (0/52 desde hace 23 días 14h): confirmar si el criterio
   de verificación manual sigue vigente o si se puede automatizar/relajar para destrabar contenido
   del diccionario.
3. **Revisar y fusionar el duplicado `equinacea`/`echinacea`** (ids 52/21) y subir la imagen que
   falta de `lavanda` (`/images/plants/lavanda-cientifica.jpg` da 404 en vivo).
