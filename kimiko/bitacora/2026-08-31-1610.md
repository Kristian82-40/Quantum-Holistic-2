# Bitácora — 2026-08-31 16:10 UTC (186º ciclo, cloud)

## Contexto
Ciclo `schedule` (nadie llamó). Tras 45+ ciclos repitiendo el mismo checklist de salud sin
encontrar nada nuevo en SEO/enlaces, este ciclo abrió dos puntos del Paso 2.3 que nunca se
habían comprobado con datos reales: enlaces internos entre blog y diccionario, y la longitud
real del `<title>`/`meta description` en producción (siempre se había mirado "duplicados",
nunca la longitud).

## Comprobado sin cambios (checklist estándar)
- `npm ci && npm run build && npm run lint`: limpios. `npm audit`: 9 vulnerabilidades (1
  moderate, 8 high), sin cambio desde el fix del 185º ciclo — todas semver-major, documentadas.
- 8/8 rutas del checklist en 200 tras seguir la cadena completa de redirects (`/diccionario`,
  `/blog`, etc. hacen 308 a su versión con barra final antes del 200 — normal, sin cambio).
  `/admin` → `/login/?redirect=...` intacto. `middleware.ts` en la raíz. `sitemap.xml` (74
  URLs) y `robots.txt` en 200, dominio correcto en canonical/`og:url` de la home.
- `plants`: 52 filas, sin cambio, 5 publicadas / 4 verificadas (albahaca, arnica, hinojo,
  equinácea verificadas; lavanda publicada sin verificar). Los 4 ficheros de imagen de las
  verificadas confirmados en disco. 9 peligrosas intactas (`publicada=false`,
  `image_cientifica_url=null`). Duplicado `equinacea`(id 52)/`echinacea`(id 21) reconfirmado
  sin cambio. `kimiko_drafts`: cola vacía, 0 pendientes en todo el ciclo.
- `blog_posts`: 90 draft / 19 published, sin cambio de conteo. `leads` en 0. Gumroad
  (`kristian320.gumroad.com/l/ritual-descanso`) sigue en 404 — env var
  `NEXT_PUBLIC_GUMROAD_URL` sí está configurada en Vercel producción, el problema está en el
  lado de Gumroad (producto no publicado o URL incorrecta ahí), no en nuestra config. Cita
  diaria (Proverbios 17:22, 2026-08-30 19:09:07 UTC) a ~21h del umbral de 24h al comprobar, sin
  inserción nueva.

## Hallazgo 1 — Verificación de `esVisible()`: diseño correcto, no bug
Comprobé si `ficha_verificada=false` oculta la página completa (como sugiere la letra del
Paso 2.2) o solo la ficha clínica. `/diccionario/lavanda/` devuelve 200 pese a no estar
verificada. Leyendo `app/diccionario/[slug]/page.tsx`: `esVisible()` solo comprueba
`publicada` y la lista de peligrosas; `ficha_verificada` únicamente controla si se sirve
`ficha_cientifica` (línea 102, comentario explícito: "solo se sirve tras verificación humana
contra nombre_latino") y muestra un aviso "Ficha científica en revisión" en su lugar. Es un
diseño deliberado y más seguro que ocultar la página entera — no lo toqué.

## Hallazgo 2 — Cero enlaces internos en los 19 posts publicados (nunca comprobado en 186 ciclos)
El campo `content` de `blog_posts` es texto plano sin ningún marcado (ni HTML ni Markdown);
`app/blog/[slug]/page.tsx` lo pinta tal cual con `dangerouslySetInnerHTML` y no existe ningún
widget de "plantas relacionadas" que compense la falta de enlaces en el texto. Verificado con
regex sobre los 19 `content` completos: 0 enlaces a `/diccionario/`, 0 enlaces a otros
`/blog/`. Esto incumple directamente la regla del Paso 2.3 ("Enlaza cada post con al menos
una ficha del diccionario").

**Arreglado con precisión, no a la fuerza:** de las 5 plantas actualmente publicadas y
verificadas (contenido seguro de mostrar), busqué mención literal por nombre en los 19 posts.
5 posts mencionan genuinamente "lavanda" o "hinojo" en frase natural (verificado leyendo el
contexto de cada mención antes de tocar nada):
- `aromaterapia-y-aceites-esenciales-terapeuticos-...` → lavanda
- `el-sueno-como-medicina-...` → lavanda
- `el-microbioma-intestinal-y-la-salud-mental-...` → hinojo
- `la-conexion-intestino-cerebro-el-segundo-cerebro-...` → hinojo
- `silicio-organico-articulaciones-flexibles-...` → hinojo

En cada uno envolví la primera mención real de la palabra con `<a href="/diccionario/{slug}/">`,
sin añadir frases nuevas ni forzar temas. Aplicado vía `UPDATE` a `blog_posts.content` en
Supabase, verificado en vivo tras el cambio: los 5 enlaces se renderizan y apuntan a páginas
que responden 200.

**Quedan 14/19 posts sin ningún enlace al diccionario** porque no mencionan por nombre
ninguna de las 5 plantas actualmente publicadas+verificadas (mencionan otras como valeriana,
pasiflora, melisa, manzanilla, jengibre, ortiga — ninguna publicada todavía). No forcé enlaces
artificiales. Esto se resolverá de forma natural a medida que se verifiquen más plantas del
diccionario — apuntado como tarea de fondo, no bloqueante.

## Hallazgo 3 — Ningún post publicado cumple `title <60 car.` / `meta description <155 car.`
Nunca se había medido la longitud real de `<title>`/`meta description` en producción — solo
se comprobaba "sin títulos duplicados". Los 19 posts publicados tenían títulos de 66 a 115
caracteres (el layout raíz aplica `template: '%s | Quantum Holistic'`, que añade 20
caracteres a cada título de página hija, así que el campo `title` en `blog_posts` debía
quedar en ≤40 caracteres para que el `<title>` final quedara ≤60) y descripciones de 134 a
258 caracteres.

Reescribí `title` y `excerpt` de los 19 posts: títulos concisos y fieles al tema (25-39
caracteres) y descripciones bajo 155 caracteres, verificados con script antes de aplicar
(longitud, duplicados, ausencia de palabras de "curación"/"cura"/"sanar" para no introducir
lenguaje de claim de curación nuevo). Para el post de Aceite de Oliva (pendiente de revisión
de Papu desde el ciclo 140 por lenguaje de "curación" en el *cuerpo*), el título anterior
también decía "Un Tesoro de Curación de la Naturaleza"; el nuevo título ("Aceite de Oliva:
Alimento y Tradición") ya no usa esa palabra — mejora incidental, pero **no toqué el cuerpo
del post ni cambié su estado de publicación**, esa decisión sigue pendiente de Papu.

Aplicado vía `UPDATE` a `blog_posts.title`/`excerpt`. La primera verificación en vivo mostró
datos sin cambio porque el fetch de `getSupabasePost()` en `page.tsx` usa
`next: { revalidate: 300 }` (caché de datos de 5 minutos, independiente del caché de CDN —
confirmé con headers que el CDN daba `MISS` mientras el contenido seguía viejo). Esperé el
TTL y reverifiqué: los 19 títulos y descripciones ya están actualizados y dentro de límite en
producción, sin duplicados entre ellos.

## Hallazgo 4 (código) — Sufijo de marca duplicado en `<title>` de `/terapeutas` y `/terapeutas/papu`
Al revisar longitud de `<title>` en páginas no-blog encontré que estas dos páginas definían su
propio `metadata.title` con "— Quantum Holistic" / "· Quantum Holistic" ya incrustado, y el
template global del layout añadía otra vez " | Quantum Holistic" encima, dando en producción
`"Terapeutas — Quantum Holistic | Quantum Holistic"` (50 car.) y
`"Papu — Fundador · Quantum Holistic | Quantum Holistic"`. Revisé el resto de páginas
(`registro`, `privacidad`, `terapeuta`, `chat`, `terminos`, `login`, `cookies`, home) — ninguna
más tiene el sufijo hardcodeado.

**Arreglado en código:** quité el sufijo redundante de `app/terapeutas/page.tsx` y
`app/terapeutas/papu/page.tsx` (título y `openGraph.title`). Build y lint limpios. Commit
`1dbdee1`, pusheado a `main`, deploy `dpl_5JhdExFUHaZy6qcenLdmzmDiftGm` en `READY`, reverificado
en vivo: `<title>Terapeutas | Quantum Holistic</title>` y
`<title>Papu — Fundador | Quantum Holistic</title>`, sin duplicado. Resto de rutas del
checklist reverificadas en 200 tras el deploy, sin regresión.

## Hallazgo menor sin arreglar (anotado para más adelante)
`/chat`, `/registro`, `/registro/terapeuta` y `/gracias` no definen `metadata.title` propio y
heredan el título por defecto de la home ("Quantum Holistic — Nutrición KM0..."), duplicado
entre varias rutas. Son páginas funcionales/transaccionales (chat interno, alta, gracias),
no contenido pensado para posicionar en buscadores, así que lo dejo documentado en vez de
tocarlo este ciclo — prioridad baja frente a los hallazgos de blog de arriba.

## Cierre
- Build/lint limpios tras los cambios de código. `npm audit` sin cambio (9, todas semver-major).
- Commits: `1dbdee1` (código, pusheado y desplegado). Cambios de datos en Supabase:
  5 `UPDATE` en `blog_posts.content` (enlaces al diccionario) + 19 `UPDATE` en
  `blog_posts.title`/`excerpt` (longitud SEO) — todos reversibles, sin tocar `status` ni borrar
  nada.
- `plants`, `leads`, `citas`, `kimiko_drafts`: sin cambios este ciclo (ver arriba).

## Tareas manuales de Kristian hoy
1. **Gumroad**: `kristian320.gumroad.com/l/ritual-descanso` sigue en 404. La variable de
   entorno en Vercel está bien puesta — el producto/URL está mal en el lado de Gumroad.
   Sin esto, "El Ritual del Descanso" no se puede comprar aunque el resto del funnel funcione.
2. **Post "Aceite de Oliva Virgen Extra"**: sigue publicado con lenguaje de curación en el
   cuerpo (flagueado en el ciclo 140, hoy 46 ciclos sin decisión). Decidir si se reescribe el
   cuerpo o se despublica.
3. Ninguna acción urgente más — el resto de hallazgos de este ciclo (enlaces internos
   pendientes en 14 posts, títulos de páginas funcionales) son de baja prioridad y se resuelven
   solos o en próximos ciclos.
