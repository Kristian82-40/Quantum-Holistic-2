# Bitácora — 2026-09-05 03:30 UTC (206º ciclo, MODO CICLO)

## Salud del sitio
- `npm ci`: limpio, 415 paquetes.
- `npm run build`: 36/36 páginas generadas sin errores ni avisos de tipos/lint.
- `npx next lint`: sin avisos.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin cambio
  desde el 185º. Fix solo disponible con `--force` (breaking: next@16, postcss
  vía next) — no lo aplico sin validación de Kristian.
- 8/8 rutas del checklist en 200 (`curl -L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión: cadena completa verificada `308` (`/admin` → `/admin/`)
  → `307` (`/admin/` → `/login/?redirect=%2Fadmin%2F`) → `200`.
- `middleware.ts` confirmado en la raíz del repo (no existe duplicado en `app/`).
- Canonical (`https://quantum-holistic.com/`), `og:url` y `og:image` correctos
  en home. `sitemap.xml`: 85 `<loc>`. `robots.txt` con `Allow: /` y referencia
  al sitemap.
- Vercel: últimos 5 despliegues en estado `READY`.

## Integridad del diccionario (`plants`)
- 52 filas totales, 23 con `publicada=true AND ficha_verificada=true`.
- Las 9 plantas peligrosas confirmadas una a una: todas `publicada=false`.
- `lavanda` sigue `publicada=false, ficha_verificada=false` (imagen inexistente
  en `public/images/plants/`) — despublicación correcta.
- Duplicado `equinacea`/`echinacea` sigue sin resolver — fuera de mi alcance
  (implicaría borrar/fusionar una fila).
- `updated_at` máximo en `plants` sigue siendo `2026-09-01T09:21:58.968322+00:00`,
  sin cambio → no repetí la auditoría visual completa este ciclo.

## SEO (`blog_posts`)
- 109 filas totales (79 `draft`, 22 `published`, 8 `rejected`; `published=true`
  también da 22, coherente).
- 22 publicados: sin títulos duplicados, ninguno >60 caracteres, todos con
  `excerpt` presente y ≤155 caracteres. Único slug de diccionario enlazado
  sigue siendo `hinojo` (`publicada=true, ficha_verificada=true`). Ninguna
  planta peligrosa enlazada en contenido publicado.

### Backlog de 79 borradores — inspección más profunda que el 205º
El 205º dejó anotado que había 79 borradores sin revisar y que varios títulos
tocaban categorías sensibles. Este ciclo entré al contenido real y el cuadro
es más problemático de lo que el recuento por sí solo sugiere:
- **32 de los 79 son fragmentos rotos, no artículos**: sin `excerpt`, con
  `content` de una sola frase suelta (p. ej. una cita de Epicteto de ~150
  caracteres, dos veces con variantes casi idénticas) y `tags=null`. No son
  borradores publicables en ningún estado — parecen residuo de una generación
  automática que se cortó a medias, más 5 filas literalmente de prueba
  (`Test agente nocturno`, `test final` ×2, y dos variantes casi iguales de
  "Microbiota Intestinal" que sí tienen contenido completo pero duplicado).
- **De los 47 restantes con contenido y excerpt reales**, hay 18 títulos que
  se repiten (hasta 4 copias de la misma pieza, p. ej. "la sabiduría del
  bambú japonés" ×4, "Desbloquea el Poder Antiinflamatorio de la Cúrcuma..."
  ×3), y de los títulos únicos que quedan, la inmensa mayoría cae dentro de
  las categorías que el Paso 2.4 marca como sensibles o directamente
  prohibidas: "Ayuno Intermitente" (×3 variantes), "Adaptógenas" (×2),
  "Rasayanas Ayurvédicos", "Desintoxica tu Hígado Naturalmente" — todas
  exigen contraindicaciones obligatorias — y varios usan lenguaje de claim de
  curación directo en el propio título ("El Poder Curativo del...", "Un
  Tesoro Curativo de la Tierra", "Sanar el Cuerpo"), lo que el filtro
  descarta antes de leer el cuerpo del texto.
- No encontré ningún borrador que pueda calificar como "limpio" sin lectura
  línea a línea completa (verificar contraindicaciones exactas o ausencia real
  de claim en el cuerpo, no solo en el título) — y con el volumen de
  duplicados a resolver primero (elegir versión canónica) antes de tocar el
  contenido, esto es una revisión editorial en toda regla, no algo que quepa
  de pasada dentro del checklist de un ciclo. Sigo sin publicar nada por
  iniciativa propia este ciclo — "duda = borrador" aplicado con el detalle
  nuevo no cambia la conclusión del 205º, la refuerza.

## Negocio
- `leads`: 0 filas.
- `citas` (frases célebres): última inserción 2026-09-03T22:36:49 UTC (201º
  ciclo) — al cierre de este ciclo habían pasado ~28h51min, cruzando el
  umbral de 24h documentado desde el 184º-187º. Apliqué la práctica: candidato
  verificado con `WebSearch` contra varias fuentes independientes (Constitución
  de la OMS, 1946, en vigor desde 1948 — texto oficial de dominio público, sin
  lenguaje de curación ni pseudociencia), confirmado que no estaba ya en la
  tabla, e insertado. 33 filas tras la inserción (antes 32).
- `kimiko_drafts`: 4 filas totales, las 4 en `status='hecho'`. Sin filas
  `pendiente` ni `en_curso`, sin orden de Telegram nueva este ciclo (evento
  `schedule`, MODO CICLO).
- Producto "El Ritual del Descanso": sin cambios de funnel ni de checkout por
  iniciativa propia, según límite del Paso 5.

## Resumen
Salud/SEO/diccionario sin hallazgos técnicos nuevos (décimo ciclo consecutivo,
197º–206º). Única escritura en Supabase: la cita diaria en `citas`, verificada
en vivo (32→33). El backlog de blog se investigó a fondo por primera vez desde
que se detectó en el 205º: no es solo "sin revisar", tiene fragmentos rotos,
duplicados extensos y casi todo el contenido real cae en categorías que exigen
o descartan por el filtro del Paso 2.4. Sigue sin publicarse nada — decisión
correcta según el mismo filtro, no parálisis.

## Tareas manuales de Kristian hoy
1. El backlog de 79 borradores de blog necesita una decisión editorial, no solo
   técnica: 32 son fragmentos rotos que probablemente haya que descartar (yo no
   borro filas), y de los 47 restantes hay 18 títulos duplicados que requieren
   elegir una versión canónica antes de que valga la pena revisar el texto
   contra el filtro de contenido. Dime si quieres que dedique un ciclo entero
   solo a esto, o si prefieres filtrar tú primero cuáles sobreviven.
2. Duplicado `equinacea`/`echinacea` en `plants` sigue sin resolver — decidir
   cuál fila fusionar o borrar.
3. Las 9 vulnerabilidades de `npm audit` llevan desde el 185º sin cambio; el
   único fix disponible implica subir a Next 16 (breaking change) — decidir si
   se aborda o se sigue aceptando el riesgo.
