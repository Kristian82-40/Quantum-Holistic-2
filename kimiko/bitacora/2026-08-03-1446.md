# Bitácora Kimiko — Ciclo cloud 2026-08-03 14:46 UTC

## QA (7/7 OK)
1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipo (corrido desde la raíz).
2. Producción: canonical y og:url en `https://quantum-holistic.com/` == `https://quantum-holistic.com/` OK.
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
   `/registro`, `/terapeutas` — todas 200 (con `-L` para las que hacen 308 a slash final).
4. `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts`
   confirmado en raíz (no en `app/`).
5. Supabase: 52/52 plantas íntegras (`content-range: 0-0/52` con `limit=1` + `count=exact`, tabla
   `plants`).
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reconfirmadas fila por fila:
   `image_cientifica_url`/`image_mistica_url` en `null`, placeholder intacto.
7. `leads`: 0 filas (solo lectura este ciclo, sin escritura de prueba).
8. `sitemap.xml`/`robots.txt`: presentes (200/200), dominio correcto (`https://quantum-holistic.com`).

## Monetización
- **Checkout Gumroad sigue roto, 5 días 20h21min de antigüedad, trigésimo cuarto ciclo
  consecutivo.** CTA real servido en `/producto/ritual-descanso/` (`href` extraído del HTML de
  producción tras seguir el 308 con `-L`) apunta a `https://kristian320.gumroad.com/l/ritual-descanso`
  (404 confirmado en vivo). Vía API de Vercel (`prj_DASuxCUuV72w8CLpZejVij8XcXvL`), el env var de
  producción es de tipo `sensitive` (no `encrypted`) por lo que el endpoint `?decrypt=true` no
  devuelve el valor en texto plano (`"decrypted": false`) — el `updatedAt` sigue siendo la señal
  verificable: `1785263120881` ms → 2026-07-28T18:25:20.881Z, sin cambio desde el incidente
  original. `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert
  viable. Sin tocar la env var sin OK de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado (código + rutas
  200, PDF de regalo 200), sin cambios.
- "Tu Planta Aliada": sin implementar en código, sin cambios. Cobertura de datos íntegra
  (`ficha_mistica.afinidad_ayurvedica` 43/43 plantas seguras, excluyendo las 9 peligrosas por
  filtro hardcoded), reconfirmado. Sigue pendiente de OK de Papu para implementar.

## Contenido
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Duplicado `equinacea`/`echinacea` reconfirmado (dos fichas separadas, ambas `Echinacea
  purpurea`), sin fusionar — pendiente de decisión de producto.
- `lavanda` (planta segura): `image_cientifica_url` sigue apuntando a
  `/images/plants/lavanda-cientifica.jpg`, reconfirmado 404 en producción. Hallazgo abierto desde
  2026-07-29, sin cambio.
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio. `uuid` y `ws` con
  fix disponible sin `--force` reconfirmado por dry-run este ciclo; `next-intl`/`postcss` solo vía
  `--force` (`next@16`, breaking). Se mantiene sin aplicar — 34 ciclos consecutivos deferido a
  tarea manual de Papu.
- Imágenes en disco: 71 archivos en `public/images/plants`, sin cambio.
- Las 34 fichas contaminadas (25 seguras + 9 peligrosas) siguen sin gate ni corrección — pendiente
  de Papu desde 2026-07-30 02:36 UTC.

## Cita diaria (tabla `citas`)
- **Insertada.** Última cita previa (Maimónides, 2026-08-02 13:28:24 UTC) tenía ~25h18min de
  antigüedad al momento de la verificación (14:46 UTC), superando el umbral de 24h. Se insertó una
  cita nueva de autor no repetido (los 3 autores previos eran Maimónides, Hipócrates, Paracelso):
  **Charaka** — "No existe sustancia en el universo que no sea medicina, si se usa en el momento
  adecuado, en la dosis correcta y de la forma correcta." (fuente: *Charaka Samhita*, texto
  ayurvédico clásico de dominio público). Pasa el filtro anti-pseudociencia: no hace claims de
  curación específicos, no menciona chakras/reiki/cuántica; coherente temáticamente con el enfoque
  ayurvédico del sitio sin duplicar el ángulo dosis-veneno ya cubierto por la cita de Paracelso
  (esta habla del principio de uso correcto, no de toxicidad).

## Optimización continua
- A/B funnel: sin datos suficientes en `leads` (0 filas reales) para proponer variantes de CTA con
  respaldo estadístico — se mantiene la conclusión de ciclos anteriores, sin propuesta nueva.
- "Tu Planta Aliada": sin cambios en el mapeo (feature no implementada en código todavía).
- Monitoreo de pago: `NEXT_PUBLIC_GUMROAD_URL` existe en Vercel para `production` (tipo
  `sensitive`) y para `development` (tipo `encrypted`), pero el valor servido en producción sigue
  siendo la URL 404 — no corresponde activar el hito de CTA transaccional hasta que el enlace
  resuelva en 200.

## Borradores sociales (sin publicar)
1. **Instagram** — Ángulo: "Una cita, treinta y cuatro verificaciones" (gancho: hoy tocó insertar
   la cita diaria del texto ayurvédico clásico Charaka Samhita — un gesto de contenido pequeño y
   puntual, cuidadosamente filtrado contra pseudociencia, que convive con la misma comprobación
   rigurosa del enlace de compra que lleva más de cinco días roto. Cuidar lo pequeño no baja la
   guardia sobre lo grande).
2. **LinkedIn** — Ángulo: "Por qué un 'no puedo confirmar el valor' también es un dato" (gancho: el
   endpoint de Vercel para la variable de producción devolvió `decrypted: false` porque es de tipo
   `sensitive`, no `encrypted` — en vez de asumir que eso oculta información, se documenta la
   limitación exacta y se usa la señal disponible, el `updatedAt` sin cambios, para seguir
   confirmando el mismo incidente sin inventar certeza donde no la hay).

## Tareas manuales de Papu hoy
1. **Reactivar el checkout de Ritual del Descanso**: `NEXT_PUBLIC_GUMROAD_URL` en Vercel (target
   production) sigue sirviendo `kristian320.gumroad.com/l/ritual-descanso` (404 desde 2026-07-28,
   34 ciclos). Si es un error, revertir a `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado) o
   publicar la URL nueva correctamente en Gumroad. Bloqueador directo del objetivo de €22.000.
2. **Decisión sobre las 34 fichas contaminadas** (25 seguras + 9 peligrosas) **y sobre la imagen
   rota de `lavanda`** (404 desde el 2026-07-29): ¿reescribir/corregir manualmente, despublicar, o
   mantener el gate `ficha_verificada` indefinidamente? Pendiente desde 2026-07-30 02:36 UTC.
3. **Aplicar `npm audit fix`** (sin `--force`) para las vulnerabilidades de `uuid` y `ws` (memory
   disclosure + DoS en `ws`, sin breaking changes confirmado por dry-run) — de las 16 totales, son
   las dos con parche directo disponible sin afectar `next`/`next-intl`.

## Cierre
- QA 7/7 OK, sin hallazgos críticos nuevos de código.
- 52 plantas, 9 peligrosas con placeholder intacto, reverificado fila por fila.
- Cita diaria insertada (Charaka Samhita) tras superar el umbral de 24h.
- Sin commits de código este ciclo (build limpio, QA sin regresiones); `next-env.d.ts` regenerado
  por el build revertido sin commitear (cambio no funcional).
- Única escritura en Supabase este ciclo: 1 fila nueva en `citas`. Leads en modo solo lectura.
- Bitácora y memoria las commitea el paso dedicado del workflow.
