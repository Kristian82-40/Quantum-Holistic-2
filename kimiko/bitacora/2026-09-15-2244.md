# Bitácora — 2026-09-15 22:44 UTC (ciclo 252º, MODO CICLO)

## Comprobado
- `kimiko_drafts`: cola de `pendiente` vacía, sin `en_curso` colgado — comprobado
  primero, antes del resto del checklist, pese al disparo `schedule`.
- `npm ci && npm run build`: limpio, 36/36 páginas.
- `npx next lint`: sin avisos.
- `npm audit`: sin cambio desde el 223º — 1 moderate, 7 high, 1 critical (9
  total), sigue sin fix no-breaking (`npm audit fix --force` implica romper
  `next-intl` 3→4 y `next` 14→16).
- 8/8 rutas del checklist en 200 (`/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`).
- `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=...` → 200.
  `middleware.ts` en la raíz del repo (no dentro de `app/`).
- canonical y `og:url` apuntan a `https://quantum-holistic.com/`.
- `sitemap.xml`: 37 `<loc>`. `robots.txt` correcto (Allow: /, Sitemap
  apuntando al dominio correcto).
- Vercel: últimos 5 despliegues `READY`.
- `plants`: 52 filas, 4 publicada+verificada sin cambio (`albahaca`,
  `arnica`, `equinacea`, `hinojo`), las 4 imágenes confirmadas en disco.
  Las 9 peligrosas confirmadas `publicada=false`.
- Trigesimoctava pasada del check permanente del 211º/212º: cruce de hash de
  `ficha_cientifica` rehecho desde cero sobre las 52 filas, mismos **7
  grupos / 21 filas** de la corrección del 249º/250º/251º (6 pares de
  fichas reales + las 9 peligrosas compartiendo el placeholder). Sin cambios.
  `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual
  completa este ciclo.
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio.
  Draft más reciente sigue en 2026-07-10, ya descartado en 206º-209º.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Hallazgo y acción de este ciclo
- **`citas`: el hueco desde la última fila superó por primera vez el umbral
  de ~24h** (última fila del 251º, Proverbios 18:14, insertada
  2026-09-14T17:55:30 UTC; a las 22:44 UTC de hoy el hueco era de
  ~28h51min). Verifiqué con `WebSearch` un candidato inicial (Hildegard von
  Bingen, "All nature is at the disposal of humankind...") y no encontré
  fuente primaria ni texto latino original con certeza alta — descartado.
  Probé un segundo candidato (Cicerón, "bona valetudo") y la búsqueda lo
  atribuyó en realidad a Séneca (De Clementia 2,2), que ya está en la tabla
  como autor — descartado por posible repetición y por discrepancia de
  atribución.
  **Inserté Salmos 103:3 (Reina-Valera)**: "Él es quien perdona todas tus
  iniquidades, El que sana todas tus dolencias." — verificado el texto
  exacto por `WebSearch` contra RVR1960 (bible.com, bibliatodo.com), autor
  ("Salmos 103:3") no repetido en la tabla (40 filas previas no incluían
  ningún Salmo), sin lenguaje de pseudociencia ni claim comercial: es cita
  de dominio público en el mismo género que las entradas de Proverbios y
  Eclesiástico 38:4 ya presentes. Confirmado el insert: 41 filas, última
  fila con `created_at` 2026-09-15T22:48:27 UTC.

## Cambios este ciclo
- 1 INSERT en Supabase (`citas`, 1 fila nueva). Sin commits de código.

## Tareas manuales de Kristian hoy
- Ninguna nueva. Gumroad sigue sin cuenta conectada (bloqueador antiguo, sin
  cambio este ciclo). `npm audit`: la única corrección de fondo (Next
  14→16) sigue pendiente de tu decisión explícita por el blast radius.
