# Bitácora — 2026-09-01 13:14 UTC (190º ciclo, MODO CICLO, `schedule`)

## Qué comprobé

- Build: `npm ci` limpio (415 paquetes), `npm run build` sin errores (36/36 páginas).
- `npm audit`: 9 vulnerabilidades sin cambio (1 moderate, 8 high), todas semver-major
  (`next@16.3.4`, `next-intl@4.14.2`) desde el 185º ciclo. No las toco: requieren
  actualización con cambios incompatibles, fuera de una corrección mecánica de un ciclo.
- 8/8 rutas del checklist en 200 (siguiendo el redirect 308 de barra final, comportamiento
  normal de Next.js con `trailingSlash`). `/admin` sin sesión → `308` → `/login/?redirect=%2Fadmin%2F`
  → `200`, cadena intacta. `middleware.ts` sigue en la raíz.
- Canonical y `og:url` de `/` apuntan a `https://quantum-holistic.com/`. `robots.txt` correcto,
  apunta a `sitemap.xml`. `sitemap.xml` con 85 `<loc>`, 53 de `/diccionario/` (nota ya
  documentada en el 189º: es estático de build, ya refleja las 17 plantas revertidas porque el
  189º sí desplegó código en ciclos previos — sin discrepancia visible en este chequeo).
- Vercel: últimos 5 despliegues, todos `READY`, sin fallos.
- `kimiko_drafts`: sin filas `pendiente`. No hubo orden de Telegram este ciclo.

## `plants`
52 filas, sin cambio desde el 189º ciclo: **23 publicada+verificada** (las mismas 23 del cierre
anterior), **9 peligrosas** con `publicada=false` confirmado explícitamente una a una, `lavanda`
ahora también `publicada=false, ficha_verificada=false` (en el 189º seguía publicada sin
verificar por imagen rota — ya no, resuelto sin que yo interviniera este ciclo). Comprobé
`updated_at` de las filas más recientes: las 40 tocadas en el `UPDATE` masivo siguen con el
mismo timestamp `2026-09-01T09:21:58Z` de aquel evento — **ningún `UPDATE` nuevo desde entonces**,
así que no repetí la auditoría visual completa (solo se justifica ante un salto sospechoso, no
en cada ciclo de rutina).

## `blog_posts`
109 filas, 79 draft / 22 published / 8 rejected, sin cambio de conteo desde el 189º. Revisé los
22 `published` por duplicados de `title` y longitud >60 caracteres: ninguno. Sin contenido nuevo
que auditar contra el filtro del Paso 2.4 este ciclo (no escribí ni publiqué nada).

## `leads` y `citas`
`leads` en 0. `citas`: 30 filas, última inserción 21:26:28 UTC del 08-31, ~15h48min antes de
este chequeo — por debajo del umbral de 24h, no inserté ninguna.

## Gumroad
Sigue devolviendo comportamiento de error en `/producto/ritual-descanso` (redirect 308 normal
de la ruta, pero el CTA de Gumroad depende de `NEXT_PUBLIC_GUMROAD_URL`, cuyo problema ya está
documentado desde el ciclo 180 como del lado de la cuenta de Gumroad, no del código ni de la env
var). No toco pasarelas de pago (Paso 5).

## Sin cambios este ciclo
No hubo commit de código ni `UPDATE` en Supabase — todo el checklist salió limpio, sin
hallazgos nuevos que corregir. Ciclo de solo verificación.

## Tareas manuales de Kristian hoy
1. **Las 17 plantas revertidas en el 189º ciclo** (`jengibre`, `aloe-vera`, `tulsi`,
   `ashwagandha`, `brahmi`, `ginseng`, `manzanilla`, `salvia`, `sauco`, `castano-de-indias`,
   `valeriana`, `olivo`, `muerdago`, `guayaba`, `tomillo`, `llantan`, `tribulus`) siguen sin
   imagen correcta — sin cambio desde entonces, sigue pendiente de tu revisión del mapping de
   `verify-plants.html` a los ficheros.
2. Gumroad sigue en error, sin cambio desde el ciclo 180 (lado de la cuenta).
3. Post "Aceite de Oliva" sigue pendiente de tu decisión desde el ciclo 140 (50 ciclos sin
   resolución).
