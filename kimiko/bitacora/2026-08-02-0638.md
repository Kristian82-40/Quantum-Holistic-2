# Bitácora Kimiko — Ciclo cloud 2026-08-02 06:38 UTC

## QA (7/7 OK)
1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipo.
2. Producción: canonical y og:url en `https://quantum-holistic.com/` == `https://quantum-holistic.com` OK.
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
   `/registro`, `/terapeutas` — todas 200 (con `-L` para las que hacen 308 a slash final).
4. `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts`
   en raíz confirmado, ausente en `app/`.
5. Supabase: 52/52 plantas íntegras (`content-range: 0-0/52` con `limit=1` + `count=exact`).
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reconfirmadas fila por fila:
   `image_cientifica_url`/`image_mistica_url` en `null`, placeholder intacto.
7. `leads`/`purchases`: 0 filas (solo lectura este ciclo). Último test E2E real con escritura fue
   el ciclo de 2026-07-31 21:10 UTC, ~9h28min antes, bajo el umbral de 24h — no se repite este
   ciclo.
8. `sitemap.xml`/`robots.txt`: presentes (200/200), dominio correcto (`https://quantum-holistic.com`).

## Monetización
- **Checkout Gumroad sigue roto, 4 días 12h9min de antigüedad, vigésimo sexto ciclo consecutivo.**
  CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción, tras
  seguir el 308 con `-L`) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404
  confirmado, dominio base también 404). `NEXT_PUBLIC_GUMROAD_URL` reconfirmada vía API de
  Vercel (`prj_DASuxCUuV72w8CLpZejVij8XcXvL`): `updatedAt` de la variable de producción sigue en
  `1785263120881` ms → 2026-07-28 18:25:20.881 UTC (`updatedBy: null`), sin cambio desde el
  incidente original. URL previa funcional `kristiantronco.gumroad.com/l/ugsqtg` (200) sigue
  siendo el revert viable. Sin tocar la env var sin OK de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado (código + rutas
  200), sin cambios.
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`
  para "planta aliada"/"planta-aliada"/"plantaAliada"). Sigue pendiente de OK de Papu para
  implementar — cobertura de datos íntegra (`ficha_mistica.afinidad_ayurvedica` 43/43 plantas
  seguras desde el 2026-07-29), sin migración pendiente.

## Contenido
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Duplicado `equinacea`/`echinacea` reconfirmado (dos fichas separadas, sin fusionar — pendiente
  de decisión de producto).
- **Reconfirmado explícitamente este ciclo (drift de varias bitácoras sin mencionarlo por nombre):
  `lavanda` (planta segura) sigue con `image_cientifica_url` apuntando a
  `/images/plants/lavanda-cientifica.jpg`, que devuelve 404 en producción.** El archivo fue
  retirado en una limpieza local el 2026-07-28 17:40 UTC (contenido real era de cornezuelo) y la
  fila nunca se actualizó. Es un hallazgo abierto desde 2026-07-29 21:03 UTC, distinto de las 34
  fichas contaminadas pero en la misma categoría de "esperando decisión de Papu, sin tocar datos
  de producto sin OK". Se restablece la mención explícita para no perder el hallazgo por drift de
  bitácora en bitácora.
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio. Confirmado con
  `npm audit fix --dry-run`: `uuid` y `ws` tienen fix disponible sin `--force` (sin breaking
  changes); `next-intl` y `postcss` solo tienen fix vía `--force` (breaking changes, `next@16`).
  Se mantiene sin aplicar — 26 ciclos consecutivos deferido a tarea manual de Papu, se mantiene la
  consistencia con ese criterio en vez de decidir unilateralmente aplicar el cambio de
  dependencias.
- Imágenes en disco: 71 archivos en `public/images/plants`, sin cambio.
- Las 34 fichas contaminadas (25 seguras + 9 peligrosas) siguen sin gate ni corrección — decisión
  de producto pendiente de Papu desde 2026-07-30 02:36 UTC (tarea manual #2). Caso
  `ashwagandha-fruto` reverificado en vivo: sigue con `ficha_mistica.chakra = "Corazón"`
  (perfil de Saúco/Sambucus) bajo el nombre y slug de "Ashwagandha en Fruto", sin cambio.

## Cita diaria (tabla `citas`)
- Sin inserción este ciclo: última cita (Hipócrates, 2026-08-01 13:27:37 UTC) tiene ~17h11min de
  antigüedad al momento de la verificación (06:38 UTC) — por debajo del umbral de 24h. Corresponde
  insertar la próxima cita ~2026-08-02 13:27 UTC.

## Optimización continua
- A/B funnel: sin datos suficientes en `leads` (0 filas reales) para proponer variantes de CTA con
  respaldo estadístico — se mantiene la conclusión de ciclos anteriores, sin propuesta nueva.
- "Tu Planta Aliada": sin cambios en el mapeo (feature no implementada en código todavía).

## Borradores sociales (sin publicar)
1. **Instagram** — Ángulo: "Encontramos un enlace roto que llevábamos días sin mencionar en voz
   alta" (gancho: la imagen de Lavanda lleva rota desde el 28 de julio; contarlo es más honesto
   que dejar que se pierda entre reportes repetidos — sin dramatizar, mostrando el proceso de
   revisión que lo recuperó).
2. **LinkedIn** — Ángulo: "El punto ciego más peligroso de un sistema de QA automatizado no es lo
   que nunca detecta, es lo que detectó una vez y dejó de repetir" (gancho: usar el caso de
   `lavanda` — detectado el 29 de julio, ausente de las bitácoras recientes por pura omisión de
   redacción — como ejemplo concreto de por qué la verificación recurrente necesita releer el
   historial completo, no solo el ciclo anterior).

## Tareas manuales de Papu hoy
1. **Reactivar el checkout de Ritual del Descanso**: `NEXT_PUBLIC_GUMROAD_URL` en Vercel apunta
   a `kristian320.gumroad.com/l/ritual-descanso` (404 desde 2026-07-28, 26 ciclos). Si es un
   error, revertir a `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) o publicar
   la URL nueva correctamente en Gumroad. Bloqueador directo del objetivo de €22.000.
2. **Decisión sobre las 34 fichas contaminadas** (25 seguras + 9 peligrosas, incluye caso
   confirmado `ashwagandha-fruto` con contenido completo de Saúco) **y sobre la imagen rota de
   `lavanda`** (404 desde el 2026-07-28): ¿reescribir/corregir manualmente, despublicar, o
   mantener el gate `ficha_verificada` indefinidamente? Pendiente desde 2026-07-30 02:36 UTC.
3. **Aplicar `npm audit fix`** (sin `--force`) para las vulnerabilidades de `uuid` y `ws`
   (memory disclosure + DoS en `ws`, sin breaking changes confirmado por dry-run) — de las 16
   totales, son las dos con parche directo disponible sin afectar `next`/`next-intl`.

## Cierre
- QA 7/7 OK, sin hallazgos críticos nuevos de código.
- 52 plantas, 9 peligrosas con placeholder intacto, reverificado fila por fila.
- Sin commits de código este ciclo (build limpio, QA sin regresiones); `next-env.d.ts`
  regenerado por el build revertido sin commitear (cambio no funcional).
- Sin escrituras nuevas en Supabase este ciclo (cita diaria por debajo del umbral de 24h, leads
  en modo solo lectura).
- Bitácora y memoria las commitea el paso dedicado del workflow.
