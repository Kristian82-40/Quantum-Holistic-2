# Bitácora — 2026-09-16 09:11 UTC (ciclo 254º, MODO CICLO)

## Comprobado

- `kimiko_drafts` comprobado primero, antes del resto del checklist, pese al
  disparo `schedule`: 4 filas, todas `hecho`, cola de `pendiente` vacía, sin
  `en_curso` colgado, sin orden de Telegram este ciclo.
- Variables de entorno necesarias presentes: `SUPABASE_SERVICE_ROLE_KEY`,
  `SUPABASE_URL`, `VERCEL_TOKEN`, `TELEGRAM_BOT_TOKEN`.
- `npm ci` limpio (415 paquetes). `npm run build` → 36/36 páginas generadas
  sin error. `npx next lint` → sin avisos.
- `npm audit` → 9 vulnerabilidades (1 moderate, 7 high, 1 critical), misma
  composición que desde el 223º. Sin fix no-breaking disponible (requiere
  `next@16` mayor, decisión pendiente de Kristian, ya documentada).
- Producción (`https://quantum-holistic.com`):
  - `canonical` y `og:url` de la home apuntan a `https://quantum-holistic.com/`.
  - 8/8 rutas del checklist en 200: `/`, `/diccionario`, `/blog`,
    `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
    `/registro`, `/terapeutas`.
  - `/admin` → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F`
    (sin sesión, redirect correcto).
  - `sitemap.xml` con 37 `<loc>`, `robots.txt` correcto (`Allow: /`,
    apunta al sitemap).
  - `middleware.ts` confirmado en la raíz del repo, no duplicado en `app/`.
- Vercel: últimos 5 despliegues `READY`.
- `plants`: 52 filas. Publicada+verificada sin cambio: `albahaca`, `arnica`,
  `equinacea`, `hinojo` — las 4 imágenes confirmadas en disco
  (`public/images/plants/*-cientifica.jpg`). Las 9 peligrosas (aconito,
  datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno,
  beleno-negro, tejo, hierba-mora) confirmadas `publicada=false`. `lavanda`
  sigue despublicada y sin verificar, imagen aún ausente en disco (hallazgo
  documentado desde 2026-09-03, sin blast radius, sin cambio). `updated_at`
  máximo sin cambio (2026-09-01) → sin auditoría visual completa este ciclo
  (regla de máximo 6 plantas/ciclo no se activa sin filas nuevas/modificadas).
  - Cuadragésima pasada del check permanente del 211º/212º: recalculado el
    hash de `ficha_cientifica` desde cero sobre las 52 filas (sin mirar la
    cifra anotada en ciclos previos) → mismos **7 grupos / 21 filas**
    (6 pares de fichas reales + las 9 peligrosas compartiendo el
    placeholder). Sin cambios.
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin cambio.
- `citas`: última fila sigue siendo Salmos 103:3 (252º), hueco de
  **~10h23min** — muy por debajo del umbral de ~24h, sin necesidad de
  insertar. 41 filas, sin cambio.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Cambios este ciclo

Ninguno. Sin commits de código ni escrituras en Supabase — cuadragésimo
ciclo consecutivo (212º-254º) sin hallazgos técnicos nuevos en el checklist
mecánico, y sin hallazgos de negocio nuevos.

## Tareas manuales de Kristian hoy

1. Ninguna urgente. La subida mayor de `next` (14→16) para cerrar la
   vulnerabilidad crítica de `npm audit` sigue pendiente de tu decisión
   explícita por su blast radius (rompe `next-intl` 3→4 también).
2. `lavanda` sigue sin imagen científica en `public/images/plants/` — si
   tienes la imagen lista, súbela y Kimiko la verificará en el próximo ciclo.
3. Sin más pendientes manuales este ciclo.
