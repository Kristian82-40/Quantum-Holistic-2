# Ciclo 213º — 2026-09-06 15:08 UTC (MODO CICLO, `schedule`)

## 2.1 Salud del sitio
- `npm ci` limpio. `npm run build`: 36/36 páginas, sin errores de tipos ni lint.
- `npm audit`: 9 vulnerabilidades (1 moderate, 8 high), sin cambio desde el 185º
  (todas semver-major).
- Producción (`https://quantum-holistic.com`):
  - `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`.
    `og:image` presente.
  - 8/8 rutas del checklist devuelven 200 tras seguir el redirect 308 de i18n
    (`/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
    `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`).
  - `/admin` sin sesión → cadena completa hasta
    `/login/?redirect=%2Fadmin%2F` (200).
  - `middleware.ts` confirmado en la raíz del repo (no dentro de `app/`).
  - `sitemap.xml`: 37 `<loc>`, sin cambio desde el fix del 211º. Confirmado
    que ninguna de las 9 plantas peligrosas ni sus variantes de slug aparece.
  - `robots.txt`: `Allow: /` + referencia correcta al sitemap.
- Vercel: últimos 5 despliegues (`quantum-holistic-2` y `kimiko`) en `READY`.

## 2.2 Integridad del diccionario
- `plants`: 52 filas (sin cambio).
- Apliqué el check permanente del 211º/212º: cruce de hash de
  `ficha_cientifica` (el campo es JSON, se compara por `JSON.stringify` +
  hash, no como string plano) sobre las 52 filas completas.
- Resultado: los mismos grupos de duplicado exacto que en el 211º/212º, sin
  grupos nuevos:
  - Las 9 peligrosas comparten contenido idéntico entre sí (es el
    placeholder, no cruce real — todas siguen `publicada=false, image=null`).
  - `ashwagandha-fruto`/`sauco`, `cinamomo`/`valeriana`, `ginseng`/`muerdago`,
    `loto`/`tulsi`, `manzanilla`/`nigela`: los 5 pares ya conocidos, las 10
    filas siguen `publicada=false, ficha_verificada=false`. Sin blast radius.
  - `echinacea`/`equinacea`: duplicado conocido sin resolver de fondo;
    `echinacea` sigue `publicada=false`, no crea duplicado en vivo.
- **publicada+verificada sigue en 4**: `albahaca`, `arnica`, `equinacea`,
  `hinojo`. Confirmé en disco que las 4 imágenes de
  `image_cientifica_url` existen en `public/images/plants/`.
- No hubo ningún `UPDATE` de Kristian sobre `plants` desde el 212º (no hay
  transición `false→true` que auditar de nuevo).

## 2.3 SEO
- 22 posts `published`: sin títulos duplicados, ninguno >60 caracteres,
  todos con `excerpt` (usado como meta description vía
  `generateMetadata` en `app/blog/[slug]/page.tsx`) ≤155 caracteres y con
  `image_url` (alimenta `og:image`).
- Barrido de enlaces internos `/diccionario/` en el contenido de los 22
  posts: 3 enlaces, los 3 a `hinojo` (una de las 4 fichas correctas). Cero
  enlaces rotos o hacia plantas despublicadas.

## 2.4 Contenido
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio
  desde el 209º-212º. El cruce en bloque del 209º ya calificó ese backlog
  como no aprovechable sin reescritura; sin candidatos nuevos que evaluar
  este ciclo — no publiqué nada.
- Redes sociales: sigue bloqueado, sin cambios.

## 2.5 Negocio
- `leads`: 0 filas, sin cambio.
- `citas`: última inserción a las 2026-09-05T03:28:37Z, ~35h39min antes del
  cierre de este ciclo — dentro del rango de huecos habituales (24-40h)
  confirmado en el 211º. Sin acción.
- `purchases`: 0 filas. `products`: 2 filas, sin cambio.
- `kimiko_drafts`: cola vacía (`pendiente`/`en_curso` = 0), sin orden de
  Telegram este ciclo.
- "El Ritual del Descanso": sin tocar, sigue sin aprobación de Kristian.

## Cierre
- **Sin commits de código ni escrituras en Supabase este ciclo** (solo
  bitácora y memoria) — segundo ciclo consecutivo sin hallazgos técnicos
  nuevos tras el 212º (que a su vez encontró los 2 duplicados ya
  despublicados). Todos los checks permanentes acumulados hasta el 212º se
  corrieron y no detectaron nada nuevo.

### Tareas manuales de Kristian hoy
- Ninguna. Todo lo revisable desde este ciclo está en verde o ya
  documentado como conocido y sin blast radius (duplicado
  `echinacea`/`equinacea` de fondo, backlog de blog sin aprovechar).
