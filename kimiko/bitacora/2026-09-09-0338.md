# Bitácora — 2026-09-09 03:38 UTC (ciclo 223º, MODO CICLO)

## Hallazgo del ciclo: `npm audit` escaló a 1 crítica (antes 1 moderate/8 high, sin crítica, desde el 185º)

- `npm ci` sigue instalando 415 paquetes, pero `npm audit` ahora reporta **1 moderate, 7 high, 1 critical** (total 9, igual que antes — cambia la composición, no el conteo). La nueva entrada crítica es el bloque de advisories de `next` (rango `9.3.4-canary.0 - 16.3.0-preview.10`, incluye ahora "Unauthenticated Remote Code Execution on windows-hosted servers" y "Unauthenticated Remote Code Execution in Image Optimization API when AVIF files are used", entre ~23 GHSA distintos acumulados bajo el mismo paquete).
- Verificado que **no existe fix no-breaking**: `npm view next versions` confirma que `14.2.35` (la versión pinneada en `package.json`) es la última release de la rama `14.2.x` — no hay parche 14.x más nuevo. `npm audit --json` solo ofrece `next@16.3.4` (`isSemVerMajor: true`) como fix, saltándose la v15 entera. Mismo patrón en `next-intl` (fix solo vía `4.14.2`, semver-major).
- **Exposición real revisada, no solo el titular del advisory**, antes de decidir no tocar nada en caliente:
  - `next.config.js` tiene `images.unoptimized: true` → el Image Optimizer está desactivado, así que las CVEs de "Image Optimization API" (RCE con AVIF, DoS de caché, DoS por `remotePatterns`) no aplican a este despliegue.
  - Hosting es Vercel, no Windows → la RCE "on windows-hosted servers" no aplica.
  - `grep -rn "'use server'"` sobre `app/`: sin resultados → no se usan Server Actions, así que las CVEs de DoS/SSRF/disclosure de Server Actions no aplican.
  - `next.config.js` sin `rewrites()` → las CVEs de smuggling/SSRF vía rewrites no aplican.
  - Sin `nonce`/CSP en el código → la CVE de XSS con nonces de CSP no aplica.
  - Sigue aplicando en teoría el resto (cache poisoning de RSC, bypass de middleware en apps con i18n de Pages Router — este proyecto usa App Router con `next-intl`, no el i18n nativo de Pages Router, así que tampoco aplica ese concreto).
- **No he tocado `next`/`next-intl`**: una subida de versión mayor (14→16, saltándose la 15) es exactamente el tipo de cambio irreversible/de alto blast radius que el Paso 5 pide documentar y no ejecutar sin decisión de Kristian — requeriría probar build, App Router, middleware, `next-intl` v4 (breaking respecto a v3) y todo el checkout/leads antes de desplegar. Lo dejo como tarea manual prioritaria de este ciclo.

## Comprobado (resto del checklist, sin cambios)
- `npm run build`: limpio, 36/36 páginas. `npx next lint`: sin avisos.
- `middleware.ts` confirmado en la raíz.
- Rutas del checklist, todas 200 (con `-L`): `/`, `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
- `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200.
- `canonical`/`og:url` → `https://quantum-holistic.com/`. `sitemap.xml`: 37 `<loc>` (sin cambio). `robots.txt` correcto.
- Vercel: últimos 5 despliegues `READY`.
- `plants`: 52 filas. 4 publicada+verificada sin cambio (`albahaca`, `arnica`, `equinacea`, `hinojo`), imágenes confirmadas en disco. Las 9 peligrosas confirmadas `publicada=false`.
- Undécima pasada del cruce de hash de `ficha_cientifica` (JSON, `json.dumps(sort_keys=True)`) sobre las 52 filas: exactamente los mismos 7 grupos desde el 212º, sin grupos nuevos (`loto`/`tulsi`, `cinamomo`/`valeriana`, `ginseng`/`muerdago`, `ashwagandha-fruto`/`sauco`, `manzanilla`/`nigela`, `echinacea`/`equinacea`, y las 9 peligrosas compartiendo `ficha_cientifica` vacía). `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual completa este ciclo.
- `echinacea`/`equinacea`: sigue sin duplicado real en vivo (404 vs 200).
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio. 22 publicados sin duplicados de título, todos ≤60 car., `excerpt` ≤155 car., todos con `image_url`, 3/22 enlazan a `/diccionario` (sin cambio, backlog conocido). Draft más reciente sigue en 2026-07-10 (ya descartado en 206º-209º).
- `citas`: última fila sigue siendo la de Cervantes del 221º (2026-09-08T08:46:02 UTC). Hueco: **18h52min** — dentro del umbral normal (~24h), sin necesidad de insertar. Confirmado que sigue siendo la tabla de citas célebres, no un funnel.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2). `kimiko_drafts`: cola de `pendiente` vacía, sin `en_curso` colgado, sin orden de Telegram este ciclo.

## Sin commits de código ni escrituras en Supabase este ciclo
El hallazgo del `npm audit` es de diagnóstico, no de arreglo — la única acción disponible (subida mayor de Next.js) es la que el Paso 5 pide dejar a decisión de Kristian, no ejecutar por iniciativa propia dado el blast radius.

## Tareas manuales de Kristian hoy
1. **Decidir sobre la actualización mayor de Next.js (14.2.35 → 16.3.4).** Es la única forma de cerrar la vulnerabilidad crítica nueva en `npm audit`; la exposición real hoy es baja (Image Optimizer desactivado, sin Server Actions, sin rewrites, sin CSP nonces — verificado arriba), pero no hay parche no-breaking en la rama 14.x. Si autorizas, lo hago en una rama/PR separada para probar build + `next-intl` v4 (también breaking) antes de tocar `main`.
2. Ninguna urgencia con `citas` (18h52min, dentro de lo normal).
3. Backlog de blog (79 drafts) sigue sin candidatos nuevos publicables tras la revisión en bloque de los ciclos 208º-209º.
