# Bitácora Kimiko — Ciclo Cloud 2026-07-29 06:35 UTC

## 🔴 Checkout Gumroad: sigue caído, ya ~12h10min sin cobrar

Reverificado desde cero (Vercel API + comprobación directa de las dos URLs con user-agent de
navegador real):

- `NEXT_PUBLIC_GUMROAD_URL` en Vercel (producción): la API devuelve el valor cifrado en blanco
  (tipo `sensitive`, esperado — no es indicio de cambio), pero `createdAt`/`updatedAt` de la
  variable **siguen idénticos** al ciclo anterior (`2026-07-28 18:25:20/24 UTC`, `updatedBy: null`).
  Papu no ha tocado la env var.
- `/producto/ritual-descanso` en producción confirmado sirviendo
  `https://kristian320.gumroad.com/l/ritual-descanso` como único CTA ("Comprar por 19€").
- Esa URL sigue en **404**. La URL anterior (`kristiantronco.gumroad.com/l/ugsqtg`) sigue viva,
  **200**.
- **Ventana de checkout roto: ~12h10min** en el momento de este chequeo (06:35 UTC), desde las
  18:25 UTC del 28-jul. Sigue sin tocarse la env var sin OK de Papu — es su decisión (terminar de
  publicar en la cuenta `kristian320` vs. revertir a la cuenta anterior), no un bug de código.
- Sin fixes de código posibles aquí: el problema vive en una env var de Vercel, no en el repo.
  `RitualCheckout.tsx` está haciendo exactamente lo que debe — sirve el CTA de Gumroad cuando la
  env var existe, fallback a captura de email cuando no — el fallo es el valor de la env var, no
  la lógica.

## QA — 8/8 checks OK (aparte del hallazgo de arriba)

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.5, 36 rutas, `ƒ Middleware`
   presente).
2. **Producción:** canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (`-L`, siguiendo el 308 de trailing slash de Next): `/`, `/diccionario`,
   `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` →
   todas `200` ✅.
4. **`/admin` sin sesión** → `308` → `307` → `/login/?redirect=%2Fadmin%2F` ✅. `middleware.ts`
   confirmado en raíz, no en `app/` ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-51/52`) ✅.
6. **9 plantas peligrosas con placeholder** ✅: `ficha_cientifica={}`, `image_cientifica_url` e
   `image_mistica_url` en `null`, `ficha_verificada:false`, `publicada:false` en las 9 (slugs
   verificados uno a uno vía REST).
7. **`leads`/`purchases`:** `content-range: */0` en ambas, 0 filas ✅ (sin cambio — sin datos aún
   para el análisis de funnel A/B del Paso 5.1).
8. **`sitemap.ts`/`robots.ts`:** dominio correcto en ambos ✅.

## Gate `ficha_verificada` — sigue conteniendo el problema, sin cambios

- 43/52 plantas con `ficha_cientifica`, **0 con `ficha_verificada = true`**. Sin movimiento desde
  la sesión interactiva del 29-jul (recuperación de 18 fichas por especie, ver `handoff.md`).
- Tabla `citas` (Paso 5.3): sigue sin existir (`/rest/v1/citas` → `PGRST205`, tabla no encontrada
  en el schema cache). Canal DDL bloqueado: ni `DATABASE_URL` ni `SUPABASE_DB_URL` ni
  `SUPABASE_ACCESS_TOKEN` en el entorno cloud — más de 30 ciclos consecutivos.

## Monetización

- **Checkout Gumroad: roto, ver hallazgo crítico arriba.** Pausado hasta decisión de Papu.
- Funnel `/regalo/primera-noche`: página 200, sin cambios estructurales frente a ciclos anteriores.
- **"Tu Planta Aliada":** sigue en pausa — gate `ficha_verificada` en 0 filas reales, mapear
  dosha→planta sobre datos no re-verificados seguiría siendo prematuro. Propuesta de esquema
  (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9
  peligrosas) sigue vigente sin cambios, ver `KIMIKO_MEMORIA.md`.

## Contenido

- Backlog `blog_posts`: 90 drafts / 19 published, sin cambio. Sin publicación este ciclo.
- `npm audit --json`: 0 critical / 11 high / 4 moderate / 1 low — sin cambio.
- `public/images/plants/`: 71 archivos — sin cambio.
- 2 borradores sociales nuevos (sin publicar, sin mención a "ya puedes comprar" dado el checkout
  roto):
  - **Instagram:** "12 horas. Es lo que lleva el botón de compra apuntando a un enlace que da 404.
    Podríamos arreglarlo solos cambiando una línea. No lo hacemos porque esa línea la cambió una
    persona, a mano, y no sabemos si estaba a mitad de algo. Preferimos parar la venta que adivinar
    una decisión que no es nuestra."
  - **LinkedIn:** "Doce horas de checkout roto y la reparación técnica cabe en un `git revert` de
    treinta segundos. No lo hacemos. La regla que seguimos no distingue entre 'esto es urgente' y
    'esto es reversible sin consultar' — son preguntas distintas, y solo la segunda decide si
    actuamos solos. Cuando coinciden urgencia y ambigüedad, gana la ambigüedad."

## Tareas manuales de Papu hoy

1. **🔴 URGENTE (escalado, ya ~12h10min) — El checkout de Gumroad sigue roto en producción.**
   `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a `https://kristian320.gumroad.com/l/ritual-descanso`
   (404). La URL anterior (`https://kristiantronco.gumroad.com/l/ugsqtg`) sigue funcionando (200).
   O se publica el producto en la cuenta `kristian320`, o se revierte la env var en Vercel —
   cualquiera de las dos toma dos minutos y ya lleva más de 12 horas de venta perdida.
2. **Decidir el plan de re-verificación del diccionario** (heredado de la sesión interactiva del
   29-jul): 18 fichas ya recuperadas por especie correcta, pero el gate `ficha_verificada` sigue
   en 0. Sin al menos una planta verificada, "Tu Planta Aliada" sigue en pausa.
3. **Rotar la service role key** expuesta en claro en `/Volumes/Papu Ext/scripts/agente-plantas.sh`
   y `scripts/runner.sh` (heredado, sigue sin confirmación de rotación), y el token OAuth expuesto
   el 2026-07-25.

*(Nota: `equinacea`/`echinacea`, `ashwagandha`/`ashwagandha-fruto`, las 29 imágenes huérfanas y el
canal DDL para `citas` siguen pendientes de decisión de Papu, sin cambio de estado.)*

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios; el problema del checkout vive
en una env var de Vercel, no en el repo). Bitácora y memoria las commitea el paso dedicado del
workflow.
