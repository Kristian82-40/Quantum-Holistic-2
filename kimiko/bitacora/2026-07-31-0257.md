# Kimiko — Ciclo cloud 2026-07-31 02:57 UTC

## QA (Paso 1)
1. `npm ci && npm run build`: **OK**, sin errores de tipos ni lint.
2. Producción (`https://quantum-holistic.com`): canonical y `og:url` == `https://quantum-holistic.com/`. OK.
3. Rutas 200 (siguiendo redirect de trailing slash, todos `308→200`): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`. OK.
4. `/admin` sin sesión → `308` a `/login/?redirect=%2Fadmin%2F` (200 final). `middleware.ts` confirmado en la raíz del repo, no en `app/`. OK.
5. Supabase `plants`: **52/52** filas íntegras (`content-range: 0-0/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) tienen `image_cientifica_url`/`image_mistica_url` en `null` — placeholder intacto, reconfirmado. Su `ficha_mistica` sigue oculta en el render (fix del ciclo 2026-07-30 10:26 UTC, commit `46f1022`), reverificado en producción: `/diccionario/cannabis/` no expone "Uso Ceremonial" ni datos de Salvia officinalis.
6. `leads`: 0 filas. `purchases`: 0 filas. Sin conversiones ni segmentación por dosha que analizar todavía.
7. `sitemap.xml` y `robots.txt`: ambos 200, dominio correcto (`Sitemap: https://quantum-holistic.com/sitemap.xml`).

**QA 7/7 OK**, sin fixes de código necesarios este ciclo. `git status` mostró solo `next-env.d.ts` regenerado por el build (comentario no funcional); revertido sin commitear.

## Hallazgos reconfirmados (sin cambio de estado)
- **Checkout Gumroad sigue roto** — ~2 días 8h32min (desde 2026-07-28 18:25:20 UTC), **decimotercer ciclo consecutivo**. `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a `kristian320.gumroad.com/l/ritual-descanso` → 404 (dominio base también 404). URL previa funcional `kristiantronco.gumroad.com/l/ugsqtg` → 200, revert sigue siendo viable. Sin tocar la env var sin OK de Papu.
- **`lavanda` sigue con `image_cientifica_url` apuntando a un archivo inexistente** (`/images/plants/lavanda-cientifica.jpg` → 404 en producción, reconfirmado). Sin escritura en `plants` este ciclo.
- **Las 25 fichas seguras contaminadas de `ficha_mistica` (más las 9 peligrosas, ya mitigadas por el fix de UI) siguen sin gate de verificación ni corrección de dato.** Recorrí de nuevo el cruce por `id` contra `app/fichas-50-valid.json` comparando `ficha_cientifica`/`ficha_mistica` byte a byte junto con `nombre_latino`: **34/52 filas totales** siguen con contenido idéntico al origen de otra especie (25 de las 43 seguras + las 9 peligrosas), sin cambio respecto al ciclo del 2026-07-30 06:34/10:26 UTC. Caso testigo `ashwagandha-fruto` reconfirmado en vivo sirviendo el perfil místico de Saúco (Elemento Agua / Chakra Corazón / Planeta Venus / "Rituales de protección... estacional").
- Gate `ficha_verificada`: 0/52 verificadas, 43/52 con `ficha_cientifica` no vacía (las 9 peligrosas tienen `ficha_cientifica: {}`), sin cambio.
- Tabla `citas`: sigue sin poder crearse vía REST (`PGRST205` — la tabla no existe en el schema cache), **37º ciclo consecutivo bloqueado** por falta de canal DDL (solo llegan `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`VERCEL_TOKEN` como secrets, ninguno habilita `CREATE TABLE`).
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio.
- `public/images/plants/`: 71 archivos, sin cambio.

## Monetización (Paso 2)
- Funnel `/regalo/primera-noche` → lead (`RegaloForm.tsx` → `POST /api/leads`) → `/producto/ritual-descanso`: verificado en código y rutas 200, sin cambios.
- "Tu Planta Aliada": sin implementar (`grep` confirma ningún componente/ruta la referencia todavía), bloqueada por la misma decisión de producto pendiente sobre las 25 fichas contaminadas.

## Optimización continua (Paso 5)
1. Funnel A/B: `leads`/`purchases` en 0 filas, sin datos suficientes para proponer variantes de CTA.
2. "Tu Planta Aliada": test negativo de las 9 peligrosas sigue pasando (imagen y contenido místico ocultos), reverificado este ciclo con el cruce completo por `id`.
3. Cita diaria: tabla `citas` sigue bloqueada, sin poder insertar (37º ciclo).
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a la URL rota — incidente crítico abierto, no un hito.

## Borradores sociales (solo bitácora, sin publicar)

**Instagram** — ángulo: la transparencia como parte del producto.
> No todo lo que publicamos es una novedad brillante — a veces es "seguimos revisando esto hasta que quede bien". Preferimos ese ritmo silencioso a prometer algo que todavía no podemos sostener. #QH #bienestar #procesoreal

**LinkedIn** — ángulo: por qué "sin cambios" también es una entrega (evergreen).
> En control de calidad continuo, un ciclo sin hallazgos nuevos no es un ciclo vacío: es la confirmación de que ningún incidente conocido empeoró y ninguno silencioso apareció. La disciplina de reverificar lo ya sabido, no solo buscar lo nuevo, es lo que sostiene la confianza en el sistema entre los picos de actividad. #QH #calidaddedatos #operaciones

## Tareas manuales de Papu hoy

1. **Resolver el checkout Gumroad** — roto ~2 días 8h32min, decimotercer ciclo escalándolo. Revert de `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional, verificado 200 este ciclo) o arreglar/republicar el producto en la cuenta `kristian320`. Impacto directo en ventas — cada visitante que pulsa "Comprar por 19€" llega a un 404 en vez de comprar o dejar su email.
2. **Decidir el alcance del problema de `ficha_mistica` sin gate** en las 25 fichas seguras contaminadas: (a) extender el gate de verificación a las 52 fichas místicas, o (b) corregir/despublicar fila por fila. Bloquea también "Tu Planta Aliada". Abierto desde 2026-07-30 02:36 UTC.
3. **Rotar la service role key y el token OAuth expuestos** (documentado desde 2026-07-25 y 2026-07-29) — sin confirmación de rotación todavía. Mientras no se rote, cada ciclo repite el mismo riesgo latente.

---
*Ciclo cloud automático. Sin commits de código este ciclo (build pasa, QA limpio, sin fixes necesarios). Bitácora y memoria commiteadas por el paso dedicado del workflow.*
