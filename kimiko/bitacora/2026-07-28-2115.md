# Bitácora Kimiko — Ciclo Cloud 2026-07-28 21:15 UTC

## 🔴 Hallazgo crítico nuevo: checkout Gumroad roto en producción

Entre el ciclo cloud anterior (17:40 UTC, que confirmó el checkout en vivo) y este ciclo, alguien
cambió `NEXT_PUBLIC_GUMROAD_URL` en Vercel directamente (sin commit de código — `git log` no
muestra nada nuevo desde `35a6546`, el de la sesión interactiva de auditoría del diccionario).

- Env var `NEXT_PUBLIC_GUMROAD_URL` (producción) actualizada a las **18:25:20 UTC**, redeploy
  automático a las **18:26:13 UTC** (confirmado vía API de Vercel, `deployments`).
- Nuevo valor apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (antes:
  `https://kristiantronco.gumroad.com/l/ugsqtg` — dominio y slug distintos, cuenta de Gumroad
  distinta).
- **La URL nueva devuelve `404` en Gumroad** (`<title>Page not found (404) - Gumroad</title>`),
  verificado con user-agent de navegador real, no solo `curl` por defecto.
- `/producto/ritual-descanso` en producción sirve ese enlace roto como único CTA — **no hay
  fallback activo** porque la env var sí existe (solo está vacía la lógica de fallback cuando
  la var no existe, no cuando existe pero el destino está muerto).
- **La URL antigua (`kristiantronco.gumroad.com/l/ugsqtg`) sigue viva y devuelve `200`** — no se
  ha tocado nada de ese lado, solo cambió a qué apunta el sitio.
- **Impacto estimado: el checkout ha estado roto en producción durante ~2h49min** en el momento
  de este chequeo (21:14 UTC). Cualquier visita a `/producto/ritual-descanso` en esa ventana
  llevó a un 404 de Gumroad en vez de una compra.
- **No se ha tocado la env var.** Revertir a la URL antigua sin saber si Papu está migrando de
  cuenta de Gumroad deliberadamente (y aún no ha terminado de publicar el producto nuevo) sería
  una acción dudosa e irreversible sin su OK — se documenta y se escala como tarea manual #1,
  por delante de todo lo demás.

## QA — 8/8 checks OK (aparte del hallazgo de arriba)

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.35, 36 rutas, `ƒ Middleware`
   presente).
2. **Producción:** canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (`-L`): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`,
   `/login`, `/registro`, `/terapeutas` → todas `200` ✅.
4. **`/admin` sin sesión** → `308` → `307` → `/login/?redirect=%2Fadmin%2F` ✅. `middleware.ts`
   en raíz, no en `app/` ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`) ✅.
6. **9 plantas peligrosas con placeholder** ✅: `ficha_cientifica={}`, `image_cientifica_url` e
   `image_mistica_url` en `null`, `ficha_verificada:false` en las 9.
7. **`leads`/`purchases`:** `content-range: */0` en ambas, 0 filas ✅. Último test E2E real de
   escritura: 06:31 UTC del mismo día (~14h41min antes, bajo el umbral de 24h) — lectura suficiente.
8. **`sitemap.ts`/`robots.ts`:** dominio correcto en ambos ✅.

## Gate `ficha_verificada` (heredado de la sesión interactiva de hoy) — sigue conteniendo el problema

La sesión interactiva de hoy (commit `b99e6d8` + `35a6546`) añadió `plants.ficha_verificada
boolean default false` tras encontrar que **38 de 41 fichas restantes pertenecen a otra especie**
(93% de error) y que 3 especies tóxicas fuera de la lista de exclusión de 9 (`muerdago`,
`cinamomo`, `abedul`) también servían posología ajena.

- Verificado en producción este ciclo con un barrido específico (excluyendo la frase del aviso
  de revisión, que contiene la palabra "posología" y generaba falsos positivos en un primer
  grep): **0 coincidencias reales** de patrones de dosis/posología en las 9 tóxicas + `muerdago`,
  `cinamomo`, `abedul`, `valeriana`, `lavanda` (14 slugs de control).
- `ficha_verificada=true`: **0 filas** — nadie ha empezado la re-verificación planta a planta
  todavía. Sigue siendo trabajo de contenido pendiente (no de código), como quedó anotado en
  `handoff.md` de la sesión interactiva.

## Monetización

- **Checkout Gumroad: roto, ver hallazgo crítico arriba.** Pausado hasta decisión de Papu.
- Funnel `/regalo/primera-noche`: página 200, formulario de lead presente (componente cliente,
  `fetch` no visible en el HTML estático servido, confirmado estructuralmente igual que ciclos
  anteriores).
- **"Tu Planta Aliada":** sigue en pausa — con el gate `ficha_verificada` en 0 filas verdaderas,
  mapear dosha→planta sobre datos aún no re-verificados seguiría siendo prematuro.

## Contenido

- Backlog `blog_posts`: 90 drafts / 19 published, sin cambio.
- Duplicados `equinacea`/`echinacea` (mismo `nombre_latino` *Echinacea purpurea*) y
  `ashwagandha`/`ashwagandha-fruto` (mismo `nombre_latino` *Withania somnifera*) reconfirmados
  sin cambios, pendientes de decisión de Papu.
- Sin publicación de blog ni cambios de contenido del diccionario este ciclo — se mantiene la
  pausa mientras el gate de re-verificación sigue en 0 fichas confirmadas.
- 2 borradores sociales nuevos (sin publicar). Se evitó deliberadamente cualquier mención a
  "ya puedes comprar" o CTA transaccional, dado que el checkout está roto ahora mismo:
  - **Instagram:** "Esta semana auditamos nuestro propio diccionario de plantas dos veces.
    La primera vez encontramos 9 fichas tóxicas con la posología de otra planta. La segunda
    vez encontramos que el problema era mucho más grande: 38 de las 41 fichas restantes también
    venían de otra especie. En vez de borrar el trabajo, lo bloqueamos ficha a ficha hasta
    verificarlo de verdad. Preferimos un diccionario más pequeño y honesto que uno grande y
    dudoso."
  - **LinkedIn:** "Una lección de auditoría de datos: cuando sospechamos que el contenido de una
    tabla estaba barajado entre filas, la forma más barata de confirmarlo no fue leer la prosa
    de cada ficha, sino cotejar un campo taxonómico redundante (familia botánica) contra la
    identidad real de cada fila. Una sola query nos dio el veredicto de 41 fichas: 38 pertenecían
    a otra especie. Cuando el dato tiene un checksum natural escondido en otro campo, úsalo antes
    de auditar texto libre."

## Imágenes y vulnerabilidades

- `public/images/plants/`: **71 archivos**, **29 huérfanos** — sin cambio frente al ciclo
  anterior y la auditoría interactiva de hoy.
- `npm audit --json`: **0 critical / 11 high / 4 moderate / 1 low** — sin cambio.
- `/api/webhooks/btcpay` heredado (pasarela cripto descartada por directiva) sigue en el build,
  sin tocar.
- Canal DDL para `citas` (paso 5.3) sigue sin `DATABASE_URL`/`SUPABASE_DB_URL`/
  `SUPABASE_ACCESS_TOKEN` en el entorno cloud (`/rest/v1/citas` → `404`). Bloqueado de forma
  consecutiva desde hace más de 30 ciclos.
- Token OAuth expuesto (2026-07-25, 06:22 y 09:52 UTC) sigue sin confirmación de rotación.

## Tareas manuales de Papu hoy

1. **🔴 URGENTE — El checkout de Gumroad está roto en producción ahora mismo.**
   `NEXT_PUBLIC_GUMROAD_URL` se cambió hoy a las 18:25 UTC a
   `https://kristian320.gumroad.com/l/ritual-descanso`, que da 404 en Gumroad. La URL anterior
   (`https://kristiantronco.gumroad.com/l/ugsqtg`) sigue funcionando. Si el cambio de cuenta fue
   intencional, falta publicar el producto nuevo; si no, revertir la env var en Vercel. Cada
   minuto que pase con esto roto es venta perdida — es la tarea #1 del día, por delante de todo.
2. **Decidir el plan de re-verificación del diccionario:** el gate `ficha_verificada` sigue en
   0 filas confirmadas. Sin una planta verificada todavía, "Tu Planta Aliada" y cualquier
   contenido nuevo del diccionario siguen en pausa.
3. **Rotar el token OAuth expuesto** (2026-07-25, 06:22 y 09:52 UTC) — sigue sin confirmación de
   rotación.

*(Nota: `equinacea`/`echinacea`, `ashwagandha`/`ashwagandha-fruto`, las 29 imágenes huérfanas y
el canal DDL para `citas` siguen pendientes de decisión de Papu, sin cambio de estado.)*

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios); bitácora y memoria las
commitea el paso dedicado del workflow.
