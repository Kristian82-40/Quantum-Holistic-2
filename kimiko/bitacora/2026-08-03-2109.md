# Bitácora Kimiko — Ciclo cloud 2026-08-03 21:09 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200: `/`, `/diccionario/`, `/regalo/primera-noche/`, `/producto/ritual-descanso/`, `/login/`, `/registro/`, `/terapeutas/`.
- `/admin/` sin sesión → 307 a `/login/?redirect=%2Fadmin%2F`. `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` con dominio correcto (`quantum-holistic.com`), `lastmod` reciente.
- Supabase `plants`: 52 filas íntegras. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila, y sin archivo huérfano `{slug}-cientifica.jpg` en disco. 71 archivos de imagen en total, sin cambio.
- `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio frente al ciclo anterior; parche de `uuid`/`ws` sigue disponible sin breaking changes vía dry-run, deferido a Papu (36 ciclos).

## Hallazgo del ciclo: test E2E real del funnel de leads llevaba ~48h sin repetirse
El último test E2E real con escritura (`POST /api/leads/` → verificación en Supabase → `DELETE` de limpieza) fue el ciclo de **2026-07-31 21:10 UTC** — el mismo ciclo donde ya se había corregido una vez un drift de 2.5 días en este rastreo. Entre ese ciclo y este (2026-08-03 21:09 UTC) pasaron **~13 ciclos sin repetir la escritura real**, solo lecturas de `Content-Range` confirmando `leads`/`purchases` en 0 filas — es decir, el mismo patrón de drift volvió a colarse pese al aprendizaje ya documentado. Corregido este ciclo: `POST` real → verificado por `id` en Supabase → `DELETE` de limpieza → tabla de vuelta a 0 filas. Sin incidencias; el funnel sigue sano. Ver Aprendizajes en `KIMIKO_MEMORIA.md` para el detalle de causa raíz (ningún ciclo entre el 08-01 13:27 UTC y este recalculó el umbral contra la fecha real, solo copiaron "bajo el umbral" de la bitácora anterior sin volver a verificar el número).

## Monetización
- **Checkout Gumroad sigue roto — 36º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~6 días 2h44min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) confirmada de tipo `sensitive` en producción, `updatedAt` sin cambio. No se toca la env var sin OK explícito de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` reverificado extremo a extremo con escritura real (ver hallazgo arriba). `leads`: 0 filas (sin actividad real de usuarios todavía).
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Cobertura de datos íntegra: 52/52 plantas con `ficha_mistica.afinidad_ayurvedica` poblado (incluye las 9 peligrosas, dato ya existente, sin migración necesaria). Propuesta de esquema sigue siendo la misma de ciclos anteriores (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu para implementar.

## Contenido
- Sin blog nuevo — backlog sin cambio: 90 `draft` / 19 `published`, mismos 8 drafts con violación explícita del checklist anti-pseudociencia (chakras/reiki/cristales/biodescodificación/"nutrición cuántica" en el título). Bloqueador crónico, ya son más de 10 ciclos con el mismo pendiente sin revisión de Papu.
- Tabla `citas`: última inserción sigue siendo Charaka (14:46:06 UTC, ~6h23min de antigüedad) — por debajo del umbral de 24h, sin inserción nueva este ciclo. Corresponde al ciclo que supere las 24h (~2026-08-04 14:46 UTC).
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "una cita al día, sin adornos": invita a leer la cita filosófico-medicinal del día en el diccionario, conecta con la idea de que cada entrada del diccionario tiene fuente y contexto, sin promesas de curación.
  - **LinkedIn** — ángulo "el propio sistema de verificación también se audita": narra sin tecnicismos que esta sesión encontró que una de las propias comprobaciones automáticas llevaba casi dos días sin repetirse de verdad, y se corrigió apenas se detectó — ejemplo de que la disciplina de verificación aplica también hacia adentro.

## Higiene de datos (sin cambio, reconfirmado)
- Duplicado de contenido `equinacea`/`echinacea` (misma especie, dos slugs) — pendiente de decisión de Papu sobre slug canónico.
- `lavanda`: `image_cientifica_url` apunta a un archivo que devuelve 404 en producción — pendiente de reemplazo.
- 34 fichas contaminadas (25 seguras + 9 peligrosas) con contenido pseudocientífico en `ficha_mistica` (chakra/planeta regente/elemento) sin gate ni corrección — pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC.

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. `next-env.d.ts` regenerado por el build revertido sin commitear (cambio no funcional). Única escritura de datos: el ciclo de prueba POST/DELETE en `leads` (limpiado, tabla de vuelta a 0 filas). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — `NEXT_PUBLIC_GUMROAD_URL` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 6 días. El link `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que tienen contenido pseudocientífico (chakra, planeta regente, elemento) en `ficha_mistica` — pendiente desde el 2026-07-30, bloquea poder activar cualquier feature que use ese campo (incl. "Tu Planta Aliada").
3. **Higiene del diccionario**: decidir slug canónico entre `equinacea`/`echinacea` (contenido duplicado) y aprobar el reemplazo de la imagen rota de `lavanda` (404 en producción).
