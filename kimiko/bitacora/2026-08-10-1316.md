# Bitácora Kimiko — Ciclo cloud 2026-08-10 13:16 UTC (74º ciclo)

## QA (8/8 OK)
1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipos. `npm audit`: 17
   vulnerabilidades (1 low, 4 moderate, 12 high), sin cambio frente al ciclo anterior.
2. Producción: canonical y `og:url` en `https://quantum-holistic.com` correctos
   (`<link rel="canonical" href="https://quantum-holistic.com/"/>`,
   `<meta property="og:url" content="https://quantum-holistic.com/"/>`).
3. Rutas 200 (siguiendo redirects de trailing slash, todos 308→200): `/`, `/diccionario`,
   `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
4. `/admin` sin sesión → redirect a `/login/?redirect=%2Fadmin%2F` (200 final). `middleware.ts`
   confirmado en raíz del repo, no en `app/`.
5. Supabase tabla `plants`: 52 filas íntegras (`content-range: 0-51/52`). Las 9 plantas peligrosas
   (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro,
   tejo, hierba-mora) reverificadas fila por fila: `image_cientifica_url` e `image_mistica_url` en
   `null` para las 9, sin excepción.
6. Tabla `leads`: 0 filas en reposo (después del test E2E de este ciclo, ver abajo). Sin
   segmentación por dosha posible (sin datos acumulados).
7. `sitemap.ts` y `robots.ts` presentes en `app/`, dominio `https://quantum-holistic.com` correcto
   en ambos (verificado también en vivo sobre `/sitemap.xml` y `/robots.txt`).

## Monetización
- **Checkout Gumroad sigue roto**, ~12 días 18h50min, septuagésimo cuarto ciclo consecutivo. CTA
  real (`RitualCheckout.tsx`, `process.env.NEXT_PUBLIC_GUMROAD_URL`) sigue apuntando a
  `kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo este ciclo);
  `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable.
  Ambas entradas de `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`,
  `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) reconfirmadas sin cambio: `development` (id
  `vf27pDQT5ZT5iBel`) y `production` (id `L6v4bSqxUSFYv5U5`, sin cambio desde
  2026-07-28T18:25:20.881Z, la que sirve el CTA real). Sin tocar ninguna env var sin OK de Papu.
- **Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` re-testeado con escritura
  real** (la prueba anterior, 2026-08-08 12:54 UTC, ya llevaba ~2 días 0h22min de antigüedad, sobre
  el umbral informal de 48h). `POST /api/leads/` (con barra final) en producción devolvió
  `200 {"ok":true}`; fila apareció en `leads` con `id bf0d1a30-1028-4c05-a589-2f2dc28cefce`,
  `source: kimiko_e2e_test`, `dosha: vata`; se borró con `DELETE` (204), sin residuo. `leads`: 0
  filas en reposo.
- "Tu Planta Aliada": sigue sin implementar en código (sin coincidencias de `planta aliada` /
  `planta_aliada` en `app/`, `components/`, `lib/`); propuesta de esquema sin cambio, pendiente de
  OK de Papu desde 2026-07-30.
- **Chequeo nuevo este ciclo — imágenes de las 43 plantas seguras:** las 43 tienen
  `image_cientifica_url` no nulo; de esas, 42 responden 200 en producción y 1 (`lavanda`) responde
  404 (hallazgo ya conocido, sin cambio). Las 43 tienen `image_mistica_url` en `null`, sin
  excepción — a diferencia de las 9 peligrosas (donde el `null` es el placeholder de seguridad
  intencional), este `null` en las seguras es simplemente un campo no usado: `grep` confirma que
  ni `app/diccionario/page.tsx` ni `app/diccionario/[slug]/page.tsx` leen `image_mistica_url` en
  ningún punto del código actual. No es un bug funcional (nada en producción depende de ese campo
  hoy), pero queda anotado por si "Tu Planta Aliada" u otra feature futura llegara a necesitarlo —
  en ese caso habría que poblar la columna para las 43 seguras antes de usarla en UI.

## Contenido
- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los mismos 8 drafts con
  violación de checklist (chakras, reiki, cristales, biodescodificación, nutrición cuántica)
  reconfirmados por `ilike` sobre `title` — no se publican. Los 19 published reconfirmados sin
  ninguna coincidencia de esos mismos términos (0 resultados) — el filtro sigue íntegro en lo ya
  publicado.
- Gate `ficha_verificada`: 0/52, sin cambio. Fichas contaminadas sin recruce completo este ciclo
  (última reproducción íntegra desde cero fue el ciclo 2026-08-07 16:57 UTC, 34 confirmadas: 25
  seguras + 9 peligrosas), pendientes de decisión de Papu desde 2026-07-30 02:36 UTC
  (~11 días 10h40min). `lavanda` (imagen `lavanda-cientifica.jpg` → 404 en producción,
  reconfirmado en vivo este ciclo) y duplicado `equinacea` (id 52) / `echinacea` (id 21) — mismo
  `nombre_latino`, `Echinacea purpurea` — reconfirmados sin cambio.

## Cita diaria
- Tabla `citas`: última inserción (Ambroise Paré, 2026-08-10T01:56:27 UTC) tiene ~11h19min de
  antigüedad al arrancar el ciclo — por debajo del umbral de 24h, sin inserción nueva este ciclo.
  10 citas en la tabla, todas de autores distintos.

## Borradores sociales (NO publicados, solo bitácora)
1. **Instagram** — ángulo "un campo que nadie lee todavía no es un bug, pero tampoco es nada":
   sobre el hallazgo de este ciclo — las 43 plantas seguras tienen su imagen "mística" vacía, pero
   ninguna pantalla la pide hoy — y por qué documentar ese tipo de vacío importa antes de que una
   feature futura tropiece con él en silencio.
2. **LinkedIn** — ángulo "repetir la prueba cuando el reloj lo pide, no cuando conviene": sobre
   volver a escribir y borrar una fila de prueba real en `leads` al cruzar el umbral de 48 horas,
   en vez de asumir que el resultado de hace dos días sigue siendo válido — la diferencia entre un
   sistema que confía en su propio historial y uno que lo vuelve a comprobar.

## Cambios de código
- Ninguno. Build pasa, QA limpio, sin fixes necesarios. `next-env.d.ts` regenerado por el build se
  revirtió sin commitear (cambio no funcional).

## Escrituras en Supabase
- 1 fila de prueba en `leads` (insertada y borrada en el mismo ciclo, sin residuo, para el re-test
  E2E del funnel — umbral de 48h superado). Sin inserción de cita (por debajo del umbral de 24h).

## Tareas manuales de Papu hoy (máx. 3)
1. **Decidir sobre el checkout de Gumroad roto (74º ciclo, ~12 días 19h):** o corregir el valor de
   `NEXT_PUBLIC_GUMROAD_URL` (production) en Vercel al link viable
   `kristiantronco.gumroad.com/l/ugsqtg`, o confirmar que el link actual
   (`kristian320.gumroad.com/l/ritual-descanso`) se va a arreglar por otra vía. Cada día que pasa
   es venta perdida hacia el objetivo de €22.000.
2. **Decidir sobre las 34 fichas de plantas reproducidas** (25 seguras + 9 peligrosas, ciclo
   2026-08-07) para poder avanzar el gate `ficha_verificada` (pendiente desde 2026-07-30, ~11 días).
3. **Revisar los 8 drafts de blog con violación de checklist** (chakras/reiki/cristales/
   biodescodificación/nutrición cuántica) para decidir si se reescriben o se descartan — llevan
   varios ciclos sin movimiento y ocupan espacio en el pipeline de contenido.
