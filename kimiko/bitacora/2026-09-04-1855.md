# Bitácora — 2026-09-04 18:55 UTC (204º ciclo, MODO CICLO)

## Salud del sitio
- `npm ci`: limpio, 415 paquetes.
- `npm run build`: 36/36 páginas generadas sin errores ni avisos de tipos/lint.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin cambio
  desde el 185º. Fix solo disponible con `--force` (breaking: next@16, postcss
  vía next) — no lo aplico sin validación de Kristian.
- Rutas del checklist, todas 200 (con `curl -L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión: cadena completa verificada `308` (`/admin` → `/admin/`)
  → `307` (`/admin/` → `/login/?redirect=%2Fadmin%2F`) → `200`.
- `middleware.ts` confirmado en la raíz del repo, no dentro de `app/`.
- Canonical (`https://quantum-holistic.com/`) y `og:url` correctos en home.
  `sitemap.xml`: 85 `<loc>`. `robots.txt` con `Allow: /` y referencia al sitemap.
- Vercel: últimos 5 despliegues en estado `READY`.

## Integridad del diccionario (`plants`)
- 52 filas totales, 23 con `publicada=true AND ficha_verificada=true`.
- Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) confirmadas
  una a una: todas `publicada=false`, `ficha_verificada=false`.
- `lavanda` sigue `publicada=false` (imagen ausente, sin cambio).
- Duplicado `equinacea`/`echinacea` sigue sin resolver — fuera de mi alcance
  (implicaría borrar/fusionar una fila).
- `updated_at` máximo en `plants` sigue siendo `2026-09-01T09:21:58.968322+00:00`,
  igual que en ciclos anteriores → no repetí la auditoría visual completa de
  imágenes (presupuesto de 6/ciclo reservado para cuando haya indicio de cambio).

## SEO (`blog_posts`)
- 109 filas totales (79 `draft`, 22 `published`, 8 `rejected`).
- 22 publicados: sin títulos duplicados, ninguno >60 caracteres, todos con
  `excerpt` presente y ≤155 caracteres.
- Único slug de diccionario enlazado en contenido publicado: `hinojo`
  (confirmado `publicada=true, ficha_verificada=true`). Ninguna planta
  peligrosa enlazada en contenido publicado.
- (Nota metodológica del 200º: no condiciono estos chequeos a `updated_at`,
  que no es fiable en esta tabla para escrituras vía REST — los repito enteros
  cada ciclo.)

## Negocio
- `leads`: 0 filas.
- `citas` (frases célebres): última inserción 2026-09-03T22:36:49 UTC (201º
  ciclo), ~20h18min antes del cierre de este ciclo — dentro del umbral de 24h,
  sin acción necesaria. 32 filas totales.
- `kimiko_drafts`: cola vacía (`pendiente`/`en_curso` = 0 filas), sin orden de
  Telegram este ciclo.
- Producto "El Ritual del Descanso": sin cambios de funnel ni de checkout por
  iniciativa propia, según límite del Paso 5.

## Resumen
Checklist completo sin hallazgos nuevos respecto al 203º. Sin commits de
código ni escrituras en Supabase este ciclo — solo lectura y verificación.

## Tareas manuales de Kristian hoy
1. Las 9 vulnerabilidades de `npm audit` llevan desde el 185º sin cambio; el
   único fix disponible implica subir a Next 16 (breaking change) — decidir
   si se aborda o se sigue aceptando el riesgo.
2. Duplicado `equinacea`/`echinacea` en `plants`: decidir cuál fila fusionar o
   borrar (Kimiko no borra filas por su cuenta).
3. `lavanda` sigue sin imagen en `public/images/plants/`: si hay una imagen
   verificada de la especie, subirla para poder reactivar la ficha.
