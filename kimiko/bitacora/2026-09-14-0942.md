# Bitácora — 2026-09-14 09:42 UTC (ciclo 246º, MODO CICLO)

## Comprobado

- `kimiko_drafts`: comprobado primero, antes del resto del checklist. Sin filas
  `pendiente`, sin `en_curso` colgado. Sin orden de Telegram este ciclo pese al
  disparo `schedule`.
- `npm ci` + `npm run build`: 36/36 páginas, limpio. `npx next lint`: sin avisos.
- `npm audit`: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate, 7
  high, 1 critical), sigue sin fix no-breaking disponible.
- 8/8 rutas del checklist en 200 con `-L` (`/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`). `/admin` sin sesión → 308 (redirect a login), correcto.
  `middleware.ts` confirmado en la raíz del repo, no dentro de `app/`.
- `canonical` y `og:url` en `https://quantum-holistic.com/` correctos.
- `sitemap.xml`: primer `curl` dio 11 `<loc>` — mismo patrón de falsa alarma
  por corte transitorio ya documentado en el 243º/245º. Reintentado 3 veces
  con `-m 30`, las tres dieron 37 `<loc>` de forma consistente. Sin hallazgo
  real, descartado.
- `robots.txt` correcto, apunta al sitemap con el dominio bueno.
- Vercel: últimos 5 despliegues `READY`.
- Trigesimocuarta pasada del check permanente del 211º/212º (cruce de hash de
  `ficha_cientifica` sobre las 52 filas de `plants`): exactamente los mismos 7
  grupos de duplicado exacto que desde el 212º, sin grupos nuevos.
- `plants`: 52 filas. 4 `publicada=true AND ficha_verificada=true` sin cambio
  (`albahaca`, `arnica`, `equinacea`, `hinojo`), las 4 imágenes en
  `public/images/plants/` confirmadas en disco. Las 9 peligrosas
  (aconito, datura, datura-metel, amanita-muscaria, cannabis,
  cornezuelo-centeno, beleno-negro, tejo, hierba-mora) confirmadas
  `publicada=false`. `updated_at` máximo sin cambio (2026-09-01) → sin
  auditoría visual completa este ciclo (ya cubiertas 6/6 de tráfico alto en
  ciclos previos, sin fichas nuevas que auditar).
- `blog_posts`: 109 filas (79 draft/22 published/8 rejected), sin cambio.
  Draft más reciente sigue en 2026-07-10, ya descartado en 206º-209º.
- `citas`: última fila sigue siendo Proverbios 15:30 (242º), hueco de
  **~17h50min** desde su inserción — por debajo del umbral de ~24h, sin
  necesidad de insertar. 39 filas, sin cambio.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Cambios este ciclo

Ninguno. Sin commits de código, sin escrituras en Supabase — trigesimocuarto
ciclo consecutivo (212º-246º) sin hallazgos técnicos nuevos en el checklist
mecánico, y sin hallazgos de negocio nuevos.

## Tareas manuales de Kristian hoy

1. Ninguna tarea manual urgente esta vez. La cola de vulnerabilidades de
   `npm audit` (9, sin fix no-breaking) sigue ahí desde el 223º si en algún
   momento quiere revisarla con margen para un upgrade mayor.
