# Bitácora — 2026-09-06 18:05 UTC (ciclo 214º, MODO CICLO)

## Qué comprobé

- `npm ci` limpio. `npm run build`: 36/36 páginas generadas, `ƒ Middleware`
  presente (79.5 kB), sin errores de tipos.
- `npx next lint`: sin avisos.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin
  cambio desde el ciclo 185º.
- 8/8 rutas del checklist en 200 tras seguir redirects (`-L`): `/`,
  `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`,
  `/login`, `/registro`, `/terapeutas`. `/admin` → `307` a
  `/login/?redirect=%2Fadmin%2F` (sin sesión), cadena completa confirmada.
  `middleware.ts` sigue en la raíz.
- `canonical`, `og:url` y `og:image` en `https://quantum-holistic.com`
  correctos. `robots.txt` con `Allow: /` y referencia al sitemap.
  `sitemap.xml`: 37 `<loc>`, sin cambio desde el fix del 211º ciclo.
- Vercel: últimos 5 despliegues (`quantum-holistic-2` y `kimiko`) en `READY`.

## `plants` (52 filas)

- 4 publicada+verificada, sin cambio desde el 211º: `albahaca`, `arnica`,
  `equinacea`, `hinojo`. Las 4 imágenes (`*-cientifica.jpg`) confirmadas
  presentes en `public/images/plants/`.
- Apliqué el check permanente del 211º/212º (cruce de hash de
  `ficha_cientifica` sobre las 52 filas, usando `json.dumps(...,
  sort_keys=True)` antes de hashear, no `.trim()` — es columna JSON).
  Resultado: **exactamente los mismos 7 grupos que el 212º y 213º**, sin
  grupos nuevos:
  - Las 9 peligrosas comparten placeholder (`aconito`, `amanita-muscaria`,
    `beleno-negro`, `cannabis`, `cornezuelo-centeno`, `datura`,
    `datura-metel`, `hierba-mora`, `tejo`) — esperado, todas
    `publicada=false`.
  - 5 pares de copia exacta ya conocidos, todos con ambos miembros
    `publicada=false`: `ashwagandha-fruto`/`sauco`, `cinamomo`/`valeriana`,
    `ginseng`/`muerdago`, `loto`/`tulsi`, `manzanilla`/`nigela`.
  - `echinacea`/`equinacea`: `echinacea` sigue `publicada=false`, así que sin
    duplicado en vivo pese al contenido idéntico.
- Sin `UPDATE` de Kristian sobre `plants` desde el 212º.

## `blog_posts` (109 filas)

- 79 `draft` / 22 `published` / 8 `rejected`, sin cambio. Sin duplicados de
  título entre los publicados. Backlog ya calificado como no aprovechable
  por el cruce en bloque del 209º (≈80% duplica tema con posts ya
  publicados) — sin candidatos nuevos desde entonces.

## Negocio y buzón

- `leads` en 0. `purchases` en 0. `products` sin cambio (2 filas).
- `citas`: última inserción 2026-09-05T03:28:37 UTC, **38h39min** antes del
  cierre de este ciclo — dentro del rango de huecos habituales (24-40h)
  confirmado en el 211º sobre el histórico completo, pero en el extremo
  alto. No abro vigilancia nueva; si el próximo ciclo supera las 40h,
  revisar histórico de nuevo.
- `kimiko_drafts`: 4 filas totales (todas `hecho`, la más reciente del
  2026-09-01), 0 en `pendiente`/`en_curso` — sin orden de Telegram este
  ciclo.

## Qué arreglé

Nada — checklist técnico limpio, tercer ciclo consecutivo (212º-214º) sin
hallazgos técnicos nuevos.

## Qué encontré sospechoso

Nada nuevo. Lo ya anotado sigue vigente: duplicado `equinacea`/`echinacea`
sin resolver de fondo (no crea duplicado en vivo), backlog de blog sin
aprovechar por duplicación temática.

## Tareas manuales de Kristian hoy

1. Decidir qué hacer con el duplicado `equinacea`/`echinacea` en `plants`
   (misma `ficha_cientifica`, solo `equinacea` publicada) — fusionar filas o
   descartar una definitivamente.
2. El backlog de 79 borradores de blog está mayormente agotado (≈80% repite
   tema ya publicado); si quiere contenido nuevo, mejor encargar temas
   frescos que seguir revisando ese backlog.
3. Ninguna acción urgente de infraestructura — build, rutas, SEO y Vercel
   están limpios.
