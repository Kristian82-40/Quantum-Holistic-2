# Bitácora Kimiko — Ciclo cloud 2026-08-08 08:46 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz. `npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio frente al ciclo anterior (mismo set: `ws`, `brace-expansion`, `esbuild`, `glob`, `js-yaml`, `minimatch`, `nanoid`, `next`). Fix vía `npm audit fix --force` sigue metiendo un cambio breaking (`eslint-config-next@16.3.0`) — no se toca sin OK de Papu.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200 (siguiendo redirect de trailing slash, esperado en Next.js): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
- `/admin` → 308 → `/admin/` → `/login/?redirect=%2Fadmin%2F` (200). `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` con dominio correcto (`quantum-holistic.com`).
- Supabase `plants`: 52 filas íntegras. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila.

## Monetización
- **Checkout Gumroad sigue roto — 61º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~10 días 14h21min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) reconfirmada de tipo `sensitive` en producción, `updatedAt` sin cambio desde 2026-07-28T18:25:20.881Z. No se toca la env var sin OK explícito de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado por código (`app/api/leads/route.ts`, `app/regalo/primera-noche/RegaloForm.tsx`, sin cambios) y rutas 200, sin cambios. Test E2E real con escritura sigue siendo el del ciclo 2026-08-06 10:45 UTC (~1 día 22h de antigüedad, por debajo del umbral informal de 48h) — no se repite este ciclo. `leads`: 0 filas — sin volumen suficiente para proponer variantes de A/B de CTA.
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Propuesta de esquema sin cambio (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu, bloqueada además por las fichas contaminadas (ver Higiene de datos).

## Contenido
- Sin blog nuevo — backlog sin cambio: 90 `draft` / 19 `published` (109 total).
- Tabla `citas`: última inserción (Voltaire, 2026-08-08 01:46:57 UTC) tiene ~6h59min de antigüedad al arrancar el ciclo — por debajo del umbral de 24h, no se inserta cita nueva este ciclo.
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "diez días catorce horas ya no cabe en un titular corto": seguir dando la cifra exacta aunque ya no impresione por su tamaño es la prueba de que el hábito no depende de que siga sorprendiendo — se sigue contando porque el problema sigue sin resolverse, no porque la cifra siga siendo noticia.
  - **LinkedIn** — ángulo "por qué se repite el mismo test aunque el resultado no cambie": la disciplina de un sistema de QA no está en encontrar algo nuevo cada vez, está en no asumir que lo de ayer sigue siendo cierto hoy solo porque lo fue ayer — cada "sigue igual" es una comprobación en vivo, no una inferencia sobre el pasado.

## Higiene de datos (sin cambio de estado real)
- Gate `ficha_verificada`: 0/52 plantas verificadas, sin cambio.
- Fichas contaminadas (mismatch de `ficha_mistica` cruzando `plants` contra `app/fichas-50-valid.json` por `id`): sin recruce completo este ciclo (última reproducción íntegra desde cero fue el ciclo 2026-08-07 16:57 UTC, 34 fichas confirmadas: 25 seguras + 9 peligrosas). Pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC (~9 días 6h10min).
- `lavanda`: `image_cientifica_url` apunta a `/images/plants/lavanda-cientifica.jpg`, reconfirma 404 en producción en vivo — sin cambio, pendiente de decisión de Papu.
- Duplicado `equinacea`/`echinacea` reconfirmado: ambos slugs siguen presentes en `plants`, sin cambio.
- El fix ya aplicado (commit `46f1022`) que oculta la sección mística completa para las 9 plantas peligrosas en `app/diccionario/[slug]/page.tsx` sigue activo — el hallazgo pendiente es sobre el estado del *dato* en Supabase, no sobre lo que se renderiza en producción para las 9 peligrosas (ya bloqueado por código).

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. Sin escrituras nuevas en Supabase este ciclo (citas por debajo del umbral de 24h, funnel probado hace menos de 48h). `next-env.d.ts` regenerado por el build se revierte sin commitear (cambio no funcional). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — `NEXT_PUBLIC_GUMROAD_URL` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 10 días 14h21min. El link `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que sirven `ficha_mistica` de una especie distinta a la real — pendiente desde el 2026-07-30, bloquea poder activar cualquier feature que use ese campo (incl. "Tu Planta Aliada").
3. **Higiene del diccionario**: decidir slug canónico entre `equinacea`/`echinacea` (contenido duplicado) y aprobar el reemplazo de la imagen rota de `lavanda` (404 en producción, ruta actual `/images/plants/lavanda-cientifica.jpg`).
