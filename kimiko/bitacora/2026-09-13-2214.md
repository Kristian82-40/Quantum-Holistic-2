# Bitácora — 2026-09-13 22:14 UTC (ciclo 244º, MODO CICLO)

## Kimiko_drafts (Telegram)
- Cola `pendiente` vacía, sin `en_curso` colgado. Sin orden de Telegram este
  ciclo, comprobado primero pese al disparo `schedule`.

## Salud del sitio
- `npm ci` limpio. `npm run build`: 36/36 páginas OK. `npx next lint`: sin
  avisos.
- `npm audit`: sin cambio desde el 223º — 1 moderate, 7 high, 1 critical
  (total 9), sin fix no-breaking disponible.
- 8/8 rutas del checklist en 200 (`curl -L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` sin sesión → 308 → sigue redirect hasta `/login/?redirect=%2Fadmin%2F`
  (200). `middleware.ts` en la raíz, no existe copia en `app/`.
- `canonical` y `og:url` en `https://quantum-holistic.com/`. `sitemap.xml`
  con 37 `<loc>`, sin cambio. `robots.txt` correcto (Allow: /, referencia al
  sitemap).
- Vercel: últimos 5 despliegues `READY`.

## Integridad del diccionario
- `plants`: 52 filas. 4 publicada+verificada sin cambio (`albahaca`,
  `arnica`, `equinacea`, `hinojo`); las 4 imágenes de `image_cientifica_url`
  confirmadas en `public/images/plants/`.
- Las 9 peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) confirmadas
  `publicada=false`.
- Trigesimosegunda pasada del check permanente del 211º/212º (cruce de hash
  de `ficha_cientifica` sobre las 52 filas): exactamente los mismos 7 grupos
  que en los ciclos 212º-243º, sin grupos nuevos:
  - las 9 peligrosas (ficha genérica compartida)
  - `echinacea` / `equinacea`
  - `muerdago` / `ginseng`
  - `nigela` / `manzanilla`
  - `sauco` / `ashwagandha-fruto`
  - `tulsi` / `loto`
  - `valeriana` / `cinamomo`
- `updated_at` máximo sin cambio (2026-09-01) → sin auditoría visual
  completa este ciclo.
- `echinacea`/`equinacea` verificado en vivo: `/diccionario/echinacea` → 404
  tras redirect, `/diccionario/equinacea` → 200. Sigue sin duplicado real
  publicado, solo comparten plantilla de ficha en base de datos.

## SEO
- Sin cambios detectados en el estado SEO general (canonical, OG, sitemap,
  robots) respecto al 243º.

## Contenido
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Draft más reciente sigue en 2026-07-10 (ya descartado en
  206º-209º).
- `citas`: 39 filas, sin cambio. Última fila sigue siendo Proverbios 15:30
  (Reina-Valera), insertada hace ~13 min al cierre del ciclo anterior (242º)
  — hueco muy por debajo del umbral de ~24h, sin necesidad de insertar
  esta vez.

## Negocio
- `leads` en 0, `purchases` en 0, `products` sin cambio (2 filas).
- Estrategia "El Ritual del Descanso" sigue sin tocar: no aprobada, no se
  empuja checkout ni se rediseña el funnel.

## Resultado del ciclo
- Sin commits de código ni escrituras en Supabase este ciclo (solo
  bitácora y memoria) — trigesimosegundo ciclo consecutivo (212º-244º) sin
  hallazgos técnicos nuevos en el checklist mecánico, y sin hallazgos de
  negocio nuevos.

## Tareas manuales de Kristian hoy
- Ninguna. El checklist mecánico sigue estable; nada requiere su
  intervención hoy.
