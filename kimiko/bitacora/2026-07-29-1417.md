# Bitácora Kimiko — Ciclo Cloud 2026-07-29 14:17 UTC

## 🔴 Checkout Gumroad: sigue caído, ya ~19h50min sin cobrar

Cuarto ciclo consecutivo confirmando el mismo estado, esta vez sin novedades adicionales:

- `NEXT_PUBLIC_GUMROAD_URL` en Vercel (producción): `updatedAt` sigue **idéntico** a los tres
  ciclos anteriores (`2026-07-28 18:25:20 UTC`, `updatedBy: null`). Papu no ha tocado la env var.
- `/producto/ritual-descanso` sigue sirviendo `https://kristian320.gumroad.com/l/ritual-descanso`
  como único CTA (verificado en el HTML servido, no solo en la env var). Esa URL sigue en **404**
  (confirmado con user-agent de navegador real). La URL anterior
  (`https://kristiantronco.gumroad.com/l/ugsqtg`) sigue viva, **200**.
- Ventana de checkout roto: **~19h50min** en el momento de este chequeo (14:15 UTC), desde las
  18:25:20 UTC del 28-jul.
- `RitualCheckout.tsx` sin cambios de código: el fallback de captura de email sigue sin activarse
  porque la env var existe (aunque apunte a un enlace roto). Sin tocar la env var sin OK de Papu.

## QA — 8/8 checks OK (aparte del hallazgo de arriba)

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.5, 36 rutas, `ƒ Middleware`
   presente).
2. **Producción:** canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (siguiendo el 308 de trailing slash): `/`, `/diccionario`,
   `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` →
   todas `200` ✅.
4. **`/admin` sin sesión** → `308` → `307` → `/login/?redirect=%2Fadmin%2F` (200 en login, no en
   admin) ✅. `middleware.ts` confirmado en raíz ✅, ausente en `app/` ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-51/52`) ✅.
6. **9 plantas peligrosas con placeholder** ✅: `image_cientifica_url` e `image_mistica_url` en
   `null` en las 9, `ficha_cientifica` vacía en exactamente esas 9.
7. **`leads`/`purchases`:** 0 filas en ambas ✅ (sin datos aún para funnel A/B del Paso 5.1).
8. **`sitemap.xml`/`robots.txt`:** dominio correcto en ambos ✅.

## Gate `ficha_verificada` — sin cambios

- 43/52 plantas con `ficha_cientifica`, **0 con `ficha_verificada = true`**. Sin movimiento desde
  la sesión interactiva del 29-jul (18 fichas recuperadas por especie).
- Reconfirmado el hallazgo ya documentado (`olivo` con ficha de cardo mariano, familia
  `Asteraceae`, posología de silimarina) — sin cambio, sigue bloqueado por el mismo motivo (sin
  fuente en disco para `Olea europaea`, gate impide que llegue a producción como dato clínico).
- Tabla `citas` (Paso 5.3): sigue sin existir (`PGRST205`). Canal DDL bloqueado (sin
  `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` en el entorno cloud) — más de 30 ciclos
  consecutivos.

## Monetización

- **Checkout Gumroad: roto, ver hallazgo crítico arriba.** Pausado hasta decisión de Papu.
- Funnel `/regalo/primera-noche`: página 200, formulario de email presente, publica a
  `/api/leads`. Sin cambios estructurales.
- **"Tu Planta Aliada":** propuesta de esquema sigue vigente sin cambios (cruce `profiles.dosha` ×
  `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas). Gate
  `ficha_verificada` en 0 sigue siendo el bloqueo real para lanzarla con datos clínicos de por
  medio. Ver `KIMIKO_MEMORIA.md`.

## Contenido

- `blog_posts`: 90 `status=draft` / 19 `status=published`, sin cambio. Sin publicación este
  ciclo.
- `npm audit --json`: 0 crítico / 11 alto / 4 moderado / 1 bajo — sin cambio.
- `public/images/plants/`: 71 archivos — sin cambio.
- 2 borradores sociales nuevos, ángulos distintos a los de ciclos anteriores (evitando repetir
  "esperamos autorización" y "el fallback no se activa"):
  - **Instagram** (ángulo: lo que un 200 OK no cuenta): "Cada chequeo automático de este ciclo
    dijo que todo estaba bien: la web carga, las páginas responden, el sitio está en pie. Ese
    checklist llevaba casi veinte horas siendo cierto y a la vez irrelevante, porque la única
    cosa que de verdad importaba —que alguien pudiera terminar de comprar— llevaba caída desde
    ayer por la tarde. Un sistema puede estar "sano" en todas sus métricas y aun así no cumplir
    su propósito. Por eso no basta con medir que algo responde: hay que medir que hace lo que
    promete."
  - **LinkedIn** (ángulo: por qué un agente autónomo no revierte configuración de producción):
    "Llevamos casi veinte horas viendo el mismo diagnóstico: una variable de entorno de producción
    apunta a un enlace que da error, y la anterior sigue funcionando. Revertirla nos tomaría
    segundos. No lo hacemos, y no es por falta de acceso — es porque no sabemos, desde fuera, si
    ese cambio fue un error o el primer paso de una migración de cuenta a medio terminar. Un
    agente que actúa con la mitad del contexto y toda la capacidad de ejecutar es exactamente el
    tipo de riesgo que un buen diseño de automatización tiene que anticipar, no descubrir en
    producción. Preferimos escalar veinte veces a arreglar mal una vez."

## Tareas manuales de Papu hoy

1. **🔴 URGENTE (escalado, 4º ciclo seguido, ya ~19h50min) — El checkout de Gumroad sigue roto en
   producción.** `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a
   `https://kristian320.gumroad.com/l/ritual-descanso` (404). La URL anterior
   (`https://kristiantronco.gumroad.com/l/ugsqtg`) sigue funcionando (200). Revertir o corregir
   toma segundos; cada hora que pasa es venta perdida y quien hace clic ve un 404 directo, sin
   formulario de respaldo.
2. **Decidir el plan de re-verificación del diccionario**, incluyendo qué hacer con las 25 fichas
   sin fuente en disco (ejemplo ya documentado: `olivo` con datos de cardo mariano). Sin al menos
   una planta verificada, "Tu Planta Aliada" sigue en pausa.
3. **Rotar la service role key** expuesta en claro en `/Volumes/Papu Ext/scripts/agente-plantas.sh`
   y `scripts/runner.sh` (heredado, sigue sin confirmación), y el token OAuth expuesto el
   2026-07-25.

*(Nota: `equinacea`/`echinacea`, `ashwagandha`/`ashwagandha-fruto`, las imágenes huérfanas y el
canal DDL para `citas` siguen pendientes de decisión de Papu, sin cambio de estado.)*

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios; el problema del checkout vive
en una env var de Vercel, no en el repo). Bitácora y memoria las commitea el paso dedicado del
workflow.
