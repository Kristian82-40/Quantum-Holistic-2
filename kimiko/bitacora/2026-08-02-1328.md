# Bitácora Kimiko — Ciclo cloud 2026-08-02 13:28 UTC

## QA (7/7 OK)
1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipo.
2. Producción: canonical y og:url en `https://quantum-holistic.com/` == `https://quantum-holistic.com` OK.
3. Rutas 200: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
   `/registro`, `/terapeutas` — todas 200 (con `-L` para las que hacen 308 a slash final).
4. `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts`
   en raíz confirmado, ausente en `app/`.
5. Supabase: 52/52 plantas íntegras (`content-range: 0-0/52` con `limit=1` + `count=exact`).
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reconfirmadas fila por fila:
   `image_cientifica_url`/`image_mistica_url` en `null`, placeholder intacto.
7. `leads`/`purchases`: 0 filas (solo lectura este ciclo, sin escritura de prueba).
8. `sitemap.xml`/`robots.txt`: presentes (200/200), dominio correcto (`https://quantum-holistic.com`).

## Monetización
- **Checkout Gumroad sigue roto, 4 días 19h de antigüedad, vigésimo octavo ciclo consecutivo.**
  CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción)
  apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado). `NEXT_PUBLIC_GUMROAD_URL`
  reconfirmada vía API de Vercel (`prj_DASuxCUuV72w8CLpZejVij8XcXvL`): `updatedAt` de la variable
  de producción sigue en `1785263120881` ms → 2026-07-28 18:25:20.881 UTC, sin cambio desde el
  incidente original. URL previa funcional `kristiantronco.gumroad.com/l/ugsqtg` (200) sigue
  siendo el revert viable. `RitualCheckout.tsx` confirmado en código: usa `NEXT_PUBLIC_GUMROAD_URL`
  si existe (por eso se sirve el CTA roto en vez del fallback de email, la env var sí existe pero
  con URL incorrecta). Sin tocar la env var sin OK de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado (código + rutas
  200), sin cambios.
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`).
  Esquema propuesto en ciclos anteriores (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`,
  filtro hardcoded de las 9 peligrosas) sigue pendiente de OK de Papu.

## Contenido
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Duplicado `equinacea`/`echinacea` reconfirmado (dos fichas separadas, sin fusionar).
- `lavanda` (planta segura) sigue con `image_cientifica_url` → `/images/plants/lavanda-cientifica.jpg`,
  404 en producción confirmado de nuevo — hallazgo abierto desde 2026-07-29, sin cambio.
- `blog_posts`: 90 draft / 19 published, sin cambio.
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio (confirmado con `npm audit --json`
  tras `npm ci` limpio). `uuid` y `ws` con fix disponible sin `--force`; `next-intl`/`postcss` solo
  vía `--force` (breaking). Sin aplicar — 28 ciclos consecutivos deferido a Papu.
- Imágenes en disco: 71 archivos en `public/images/plants`, sin cambio.
- Cobertura `afinidad_ayurvedica`: 43/43 plantas seguras (verificado fila por fila excluyendo las
  9 peligrosas), sin cambio.
- Las 34 fichas contaminadas (25 seguras + 9 peligrosas) siguen sin gate ni corrección — decisión
  de producto pendiente de Papu desde 2026-07-30. Caso `ashwagandha-fruto` reverificado en vivo:
  sigue con `ficha_mistica.chakra = "Corazón"` (perfil de Saúco/Sambucus) bajo el nombre y slug de
  "Ashwagandha en Fruto", sin cambio.

## Cita diaria (tabla `citas`)
- **Insertada** (umbral de 24h superado: última cita, Hipócrates, tenía 24h00min de antigüedad al
  momento de la verificación). Nueva cita: *"No debe el médico tratar la enfermedad sino al enfermo
  que la sufre."* — Maimónides (aforismo médico, dominio público, sin claims de curación, sin
  vocabulario pseudocientífico). Tabla `citas` ahora con 3 filas totales (Paracelso 2026-07-31,
  Hipócrates 2026-08-01, Maimónides 2026-08-02).

## Optimización continua
- A/B funnel: sin datos suficientes en `leads` (0 filas reales) para proponer variantes de CTA con
  respaldo estadístico — sin propuesta nueva.
- "Tu Planta Aliada": sin cambios en el mapeo (feature no implementada en código todavía).

## Borradores sociales (sin publicar)
1. **Instagram** — Ángulo: "Tres citas en cinco días: por qué no publicamos una cada pocas horas"
   (gancho: explicar la diferencia entre automatizar una tarea y automatizarla con criterio — la
   cadencia de 24h para la cita diaria es una regla explícita, no un límite técnico, y respetarla
   incluso cuando sería "más fácil" publicar más seguido es parte de la disciplina del sistema).
2. **LinkedIn** — Ángulo: "Tres bloqueadores, veintiocho ciclos, cero decisiones unilaterales"
   (gancho: usar el aniversario del cuarto día del incidente de Gumroad junto con las 34 fichas
   contaminadas y las vulnerabilidades de `npm audit` sin aplicar como ejemplo de qué significa
   gobernanza real en automatización: detectar rápido, documentar con precisión, y no tocar nada
   que sea irreversible o de negocio sin autorización humana explícita).

## Tareas manuales de Papu hoy
1. **Reactivar el checkout de Ritual del Descanso**: `NEXT_PUBLIC_GUMROAD_URL` en Vercel apunta
   a `kristian320.gumroad.com/l/ritual-descanso` (404 desde 2026-07-28, 28 ciclos). Si es un
   error, revertir a `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) o publicar
   la URL nueva correctamente en Gumroad. Bloqueador directo del objetivo de €22.000.
2. **Decisión sobre las 34 fichas contaminadas** (25 seguras + 9 peligrosas, incluye caso
   confirmado `ashwagandha-fruto` con contenido completo de Saúco) **y sobre la imagen rota de
   `lavanda`** (404 desde el 2026-07-28): ¿reescribir/corregir manualmente, despublicar, o
   mantener el gate `ficha_verificada` indefinidamente? Pendiente desde 2026-07-30.
3. **Aplicar `npm audit fix`** (sin `--force`) para las vulnerabilidades de `uuid` y `ws`
   (memory disclosure + DoS en `ws`, sin breaking changes) — de las 16 totales, son las dos con
   parche directo disponible sin afectar `next`/`next-intl`.

## Cierre
- QA 7/7 OK, sin hallazgos críticos nuevos de código.
- 52 plantas, 9 peligrosas con placeholder intacto, reverificado fila por fila.
- Sin commits de código este ciclo (build limpio, QA sin regresiones); `next-env.d.ts`
  regenerado por el build revertido sin commitear (cambio no funcional).
- Única escritura en Supabase este ciclo: inserción de la cita diaria en `citas` (dato permanente,
  no de prueba). `leads`/`purchases` en modo solo lectura.
- Bitácora y memoria las commitea el paso dedicado del workflow.
