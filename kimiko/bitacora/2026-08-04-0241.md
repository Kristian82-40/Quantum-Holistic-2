# Bitácora Kimiko — Ciclo cloud 2026-08-04 02:41 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200 (siguiendo redirect de trailing slash, esperado en Next.js): `/`, `/diccionario/`, `/regalo/primera-noche/`, `/producto/ritual-descanso/`, `/login/`, `/registro/`, `/terapeutas/`.
- `/admin/` sin sesión → 307 a `/login/?redirect=%2Fadmin%2F`. `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` con dominio correcto (`quantum-holistic.com`), `lastmod` reciente.
- Supabase `plants`: 52 filas íntegras. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila. 71 imágenes en `public/images/plants` (conteo anterior de "71 archivos" se refería solo a esa carpeta, no a `public/images` completo — reconfirmado, sin cambio real).
- `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio frente al ciclo anterior; parche de `uuid`/`ws` sigue disponible sin breaking changes, deferido a Papu (37 ciclos).

## Monetización
- **Checkout Gumroad sigue roto — 37º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~6 días 8h15min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) confirmada de tipo `sensitive` en producción, `updatedAt` sin cambio desde 2026-07-28T18:25:20.881Z. No se toca la env var sin OK explícito de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado por código y rutas 200; test E2E real con escritura (POST/DELETE) se hizo el ciclo anterior (2026-08-03 21:09 UTC, ~5h30min de antigüedad) — no se repite este ciclo por estar reciente. `leads`: 0 filas (sin actividad real de usuarios todavía).
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Cobertura de datos íntegra: 52/52 plantas con `ficha_mistica.afinidad_ayurvedica` poblado (incluye las 9 peligrosas, dato ya existente, sin migración necesaria). Propuesta de esquema sigue siendo la misma de ciclos anteriores (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu para implementar.

## Contenido
- Sin blog nuevo — backlog sin cambio: 90 `draft` / 19 `published`, mismos 8 drafts con violación explícita del checklist anti-pseudociencia (chakras/reiki/cristales/biodescodificación/"nutrición cuántica" en el título). Bloqueador crónico, más de 10 ciclos con el mismo pendiente sin revisión de Papu.
- Tabla `citas`: última inserción sigue siendo Charaka (2026-08-03 14:46:06 UTC, ~11h55min de antigüedad) — por debajo del umbral de 24h, sin inserción nueva este ciclo. Corresponde al ciclo que supere las 24h (~2026-08-04 14:46 UTC).
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "seis días, treinta y siete comprobaciones, cero atajos": narra que el checkout de un producto real lleva más de seis días roto y que la respuesta no fue improvisar un parche sino seguir verificando y esperando la decisión correcta, con foco en que "rápido" y "correcto" a veces son cosas distintas.
  - **LinkedIn** — ángulo "por qué un sistema automatizado no se autoriza a sí mismo a tocar el dinero": explica sin tecnicismos que aunque el sistema tiene acceso técnico de sobra para cambiar el link de pago roto, la decisión se queda fuera de su alcance por diseño — ejemplo concreto de límites que se sostienen incluso cuando serían fáciles de saltar.

## Higiene de datos (sin cambio, reconfirmado)
- Duplicado de contenido `equinacea`/`echinacea` (ambos slugs existen en `plants`) — pendiente de decisión de Papu sobre slug canónico.
- `lavanda`: `image_cientifica_url` (`/images/plants/lavanda-cientifica.jpg`) devuelve 404 en producción — pendiente de reemplazo.
- 34 fichas contaminadas (25 seguras + 9 peligrosas) con contenido pseudocientífico en `ficha_mistica` (chakra/planeta regente/elemento) sin gate ni corrección — pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC.
- Gate `ficha_verificada`: 0/52 plantas verificadas, sin cambio.

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. `next-env.d.ts` regenerado por el build revertido sin commitear (cambio no funcional). Sin escrituras nuevas en Supabase este ciclo (citas por debajo del umbral de 24h, leads probado recientemente en el ciclo anterior). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — `NEXT_PUBLIC_GUMROAD_URL` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 6 días 8h. El link `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que tienen contenido pseudocientífico (chakra, planeta regente, elemento) en `ficha_mistica` — pendiente desde el 2026-07-30, bloquea poder activar cualquier feature que use ese campo (incl. "Tu Planta Aliada").
3. **Higiene del diccionario**: decidir slug canónico entre `equinacea`/`echinacea` (contenido duplicado) y aprobar el reemplazo de la imagen rota de `lavanda` (404 en producción).
