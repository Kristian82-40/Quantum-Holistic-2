# Bitácora Kimiko — Ciclo cloud 2026-08-06 10:45 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200 (siguiendo redirect de trailing slash, esperado en Next.js): `/`, `/diccionario/`, `/regalo/primera-noche/`, `/producto/ritual-descanso/`, `/login/`, `/registro/`, `/terapeutas/`.
- `/admin/` sin sesión → 308 → `/login/?redirect=%2Fadmin%2F` (200). `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` con dominio correcto (`quantum-holistic.com`).
- Supabase `plants`: 52 filas íntegras. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila.
- `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio; parche de `ws` disponible vía `npm audit fix`, deferido a Papu (49 ciclos).
- **Falso positivo propio descartado en vivo**: un primer `grep` de la URL de Gumroad en el HTML de producción usó una clase de caracteres sin guion (`[a-zA-Z0-9]*`), que truncó `ritual-descanso` en `ritual` y pareció sugerir un cambio de URL. Recomprobado con `curl` sin filtrar y con cache-bust: la URL real sigue siendo `kristian320.gumroad.com/l/ritual-descanso` (404), sin cambio. Se documenta para no repetir el error de regex en ciclos futuros.

## Monetización
- **Checkout Gumroad sigue roto — 51º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~8 días 16h20min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción, sin filtrar por regex esta vez) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) reconfirmada de tipo `sensitive` en producción, `updatedAt` sin cambio desde 2026-07-28T18:25:20.881Z. No se toca la env var sin OK explícito de Papu.
- **Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` re-verificado con escritura real (POST/DELETE)** este ciclo: `POST /api/leads/` en producción devolvió `200 {"ok":true}`, la fila apareció en `leads` con `source: kimiko_e2e_test`, `dosha: vata`, y se limpió con `DELETE` directo (204). Última prueba real anterior había sido el 2026-08-03 21:09 UTC (~2 días 13h de antigüedad, ya sobre el umbral informal de 48h sin re-testear) — corregido este ciclo. `leads`: 0 filas en reposo (sin actividad real de usuarios todavía; la fila de prueba se borró).
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Propuesta de esquema sin cambio (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu para implementar.

## Contenido
- **Corrección de una cifra crónica arrastrada por varios ciclos**: la bitácora venía repitiendo "9 títulos distintos (10 filas)" con violación de checklist por keyword en el título. Recontado desde cero este ciclo con query directa (no regex de shell): son **8 filas / 7 títulos distintos** que violan por keyword de título — 4 con "chakra", 1 "reiki", 1 "cristales/gemoterapia", 1 "biodescodificación", 1 "cuántica" — más los 2 casos ya conocidos de contenido heredado (falso positivo, no esotérico real: `echinacea-guia-1779978659`, `sidr-espino-de-cristo-guia-1779978766`, con "chakra" solo en el cuerpo por farmacología de otra especie). No se encontró contenido nuevo; es una recontabilización, no un cambio de estado. `blog_posts`: 90 draft / 19 published (109 total), sin cambio en el total.
- Los ~82 drafts restantes sin keyword prohibida obvia en título siguen pendientes de revisión de contraindicaciones fila por fila (ayuno intermitente, rasayanas ayurvédicos, adaptógenos, "detox" hepático/primaveral) antes de poder publicar — sin publicación autónoma este ciclo, mismo criterio conservador de los 50 ciclos previos. Contenido de prueba mezclado en la tabla (`Test agente nocturno`, `test final` ×2) y varios títulos duplicados exactos, sin accionar, solo señalado.
- Tabla `citas`: última inserción (William Osler, `2026-08-05T21:13:16Z`) tiene ~13h32min de antigüedad — por debajo del umbral de 24h, sin inserción nueva este ciclo. Corresponde al ciclo que supere las 24h (~2026-08-06 21:13 UTC).
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "el error era mío, no del enlace": cuenta que esta vez la sorpresa fue un falso positivo propio (un filtro de texto mal escrito que recortó una URL), no un cambio real en el problema de fondo — y que revisar el propio trabajo dos veces antes de reportarlo es la misma disciplina que aplicamos a todo lo demás.
  - **LinkedIn** — ángulo "recontar en vez de repetir": explica por qué se volvió a contar desde cero una cifra que llevaba semanas copiándose de bitácora en bitácora (9 títulos con violación de checklist), y que el resultado real fue 7 — un ejemplo de que la memoria operativa debe re-verificarse periódicamente, no solo heredarse.

## Higiene de datos (sin cambio, reconfirmado)
- Duplicado de contenido `equinacea`/`echinacea` (ambos slugs existen en `plants`, mismo `nombre_latino` `Echinacea purpurea`) — pendiente de decisión de Papu sobre slug canónico.
- `lavanda`: `image_cientifica_url` (`/images/plants/lavanda-cientifica.jpg`) devuelve 404 en producción — pendiente de reemplazo.
- 34 fichas contaminadas (25 seguras + 9 peligrosas) con contenido pseudocientífico en `ficha_mistica` (chakra/planeta regente/elemento) sin gate ni corrección — pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC (~7 días 8h).
- Gate `ficha_verificada`: 0/52 plantas verificadas, sin cambio.

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. `next-env.d.ts` regenerado por el build revertido sin commitear (cambio no funcional). Escrituras en Supabase este ciclo: 1 fila de prueba en `leads` (insertada y borrada en el mismo ciclo para el test E2E). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — `NEXT_PUBLIC_GUMROAD_URL` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 8 días 16h. El link `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que tienen contenido pseudocientífico (chakra, planeta regente, elemento) en `ficha_mistica` — pendiente desde el 2026-07-30, bloquea poder activar cualquier feature que use ese campo (incl. "Tu Planta Aliada").
3. **Higiene del diccionario**: decidir slug canónico entre `equinacea`/`echinacea` (contenido duplicado) y aprobar el reemplazo de la imagen rota de `lavanda` (404 en producción).
