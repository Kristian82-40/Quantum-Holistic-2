# Bitácora — 2026-09-02 16:11 UTC (ciclo 195º, MODO CICLO)

## Comprobado

- `npm ci`: 415 paquetes, `npm audit` 9 vulnerabilidades (1 moderate, 8 high),
  sin cambio desde el 185º ciclo (semver-major, requieren `--force`).
- `npm run build`: limpio, todas las páginas generadas sin errores. `npm run
  lint`: sin warnings ni errores.
- Producción (`curl -L`):
  - `canonical` y `og:url` en `/`, `/diccionario/hinojo` y `/blog` →
    `https://quantum-holistic.com/...` correctos, sin regresión.
  - 8/8 rutas del checklist → 200 (vía redirect 308 de barra final): `/`,
    `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`,
    `/login`, `/registro`, `/terapeutas`.
  - `/admin` sin sesión → cadena completa `308 → /admin/` → `307 →
    /login/?redirect=...` → `200`. Intacto.
  - `middleware.ts` confirmado en la raíz del repo (no dentro de `app/`).
  - `sitemap.xml` → 85 `<loc>` (sin cambio desde el 194º). `robots.txt` → 200
    con `Sitemap:` correcto.
- Vercel: últimos 5 despliegues en `READY`.
- Supabase `plants`: 52 filas. `updated_at` de las 5 más recientes idéntico al
  incidente del 189º ciclo (`2026-09-01T09:21:58.968322Z`) — sin `UPDATE`
  nuevo, no repetí la auditoría visual completa (check permanente del 190º
  ciclo). 23 publicada+verificada, las 9 peligrosas reconfirmadas una a una
  con `publicada=false, ficha_verificada=false`, `lavanda` sigue
  despublicada, duplicado `equinacea`/`echinacea` reconfirmado (solo
  `equinacea` publicada).
- Supabase `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected).
  `updated_at` más reciente idéntico al del ciclo anterior
  (`09:22:44.977084Z`) — sin `UPDATE` nuevo, no repetí revisión de
  SEO/duplicados. Post de Aceite de Oliva sigue pendiente de la decisión de
  fondo de Papu (ciclo 140, 50 ciclos sin cambio).
- Supabase `leads`: 0 filas.
- Supabase `citas`: última inserción hace 7h36min (08:35:24 UTC, cita de
  Plinio el Viejo del ciclo anterior) — por debajo del umbral de 24h, no
  procede insertar una nueva. 31 filas sin cambio.
- Supabase `kimiko_drafts`: cola de pendientes vacía, sin filas colgadas en
  `en_curso`, sin orden de Telegram este ciclo.

## Hallazgos

Ninguno nuevo de código ni de datos. Octavo ciclo seguido sin hallazgos tras
el incidente del 189º (17 plantas con imagen cruzada, corregido en su
momento).

## Acciones

Ninguna escritura en Supabase ni commit de código este ciclo — checklist
limpio de principio a fin.

## Tareas manuales de Kristian hoy

Ninguna. No hay nada bloqueado ni pendiente de su decisión que no estuviera
ya anotado (post de Aceite de Oliva, ciclo 140).
