# Bitácora Kimiko — Ciclo Cloud 2026-07-27 03:23 UTC (Vigesimotercer ciclo)

## QA — 8/8 checks OK

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.5, 36 rutas generadas, `ƒ Middleware` presente).
2. **Producción:** `curl -s https://quantum-holistic.com` → canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (con `-L`): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → todas `200` ✅.
4. **`/admin` sin sesión** → redirect a `/login/?redirect=%2Fadmin%2F` (`200` final) ✅. `middleware.ts` confirmado en raíz, no en `app/` ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`) ✅.
6. **9 plantas peligrosas con placeholder:** `image_cientifica_url`/`image_mistica_url` = `null` para las 9 (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) ✅. Nunca activadas.
7. **`leads`/`purchases`:** 0 filas en ambas tablas al cierre. Umbral de 24h superado desde el último test E2E real (02:54 UTC del 26/07, ~24h25min antes) → se repitió el test real: `POST /api/leads` con `-L` y email de prueba (`kimiko-e2e-test-...@quantum-holistic.com`, `source: kimiko_e2e_test`), confirmado `{"ok":true}` y fila creada en Supabase, luego **eliminada** (`DELETE` por `id`) y reverificado `content-range: */0`. Funnel de escritura sano, sin residuo.
8. **`sitemap.ts` y `robots.ts`:** presentes en `app/`, dominio `https://quantum-holistic.com` correcto en ambos ✅.

## Monetización

- `NEXT_PUBLIC_GUMROAD_URL` sigue ausente en Vercel (12 env vars totales, confirmado vía project ID directo `prj_DASuxCUuV72w8CLpZejVij8XcXvL`). `RitualCheckout.tsx` mantiene el fallback correcto: sin Gumroad, formulario de captura de email a `/api/leads` con `source: 'ritual_descanso_waitlist'`. Vigesimotercer ciclo consecutivo sin el secret.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso`: sin cambios estructurales. El test E2E real de este ciclo (ver QA punto 7) cubre el tramo de escritura del funnel (`/api/leads`), que es compartido por ambas páginas.
- **"Tu Planta Aliada":** sin implementar (solo existe `/api/perfil/dosha` escribiendo a `profiles.dosha`). Propuesta de esquema sin cambios: cruce `profiles.dosha` × `plants.ficha_mistica->>afinidad_ayurvedica` (campo JSONB dentro de `plants`, no tabla separada), filtro hardcoded de las 9 peligrosas, sin necesitar migración. Pendiente de OK de Papu para migrar/activar. Cobertura reconfirmada: 41/43 plantas seguras con `afinidad_ayurvedica` (faltan `manzanilla` y `equinacea`, sin cambios).
- Canal DDL para tabla `citas` (paso 5.3) sigue bloqueado: sin `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` en el entorno cloud (`/rest/v1/citas` → `404`, tabla no existe). 24º ciclo consecutivo bloqueado.

## Contenido

- Backlog `blog_posts`: 90 drafts / 19 published, sin cambio.
- **8 drafts con violación de checklist**, mismo total y mismo desglose que el ciclo anterior: 4 chakras (`chakras-y-alimentacion-colores-que-sanan-498063`, `los-7-chakras-principales-y-como-equilibrarlos-1780097836`, `chakras-y-alimentacion-colores-que-sanan`, `chakras-y-colores-que-sanan-557229`) + reiki (1) + cristales (1) + biodescodificación (1) + nutrición cuántica (1). Ninguno publicado.
- Duplicados reconfirmados sin cambios: `equinacea`/`echinacea` (real, `Echinacea purpurea`, pendiente decisión de Papu) y `ashwagandha`/`ashwagandha-fruto` (descartado, son partes distintas de la planta).
- **Funnel A/B (`leads`):** sin datos — tabla en 0 filas reales al cierre de ciclo (la única fila de este ciclo fue de prueba y se eliminó). No hay volumen suficiente para proponer variantes de CTA todavía; se retoma cuando existan leads reales que analizar.
- 2 borradores sociales nuevos (sin publicar, solo aquí):
  - **Instagram:** "Hoy tocaba probar de verdad el formulario de la web — no simularlo, escribirlo. Así que mandamos un lead de prueba, confirmamos que llegó, y lo borramos antes de cerrar. Probar en producción sin dejar rastro no es un truco: es la única forma honesta de decir 'funciona' sin ensuciar los datos reales que todavía no existen."
  - **LinkedIn:** "Hay un aviso que llevamos repitiendo ciclo tras ciclo desde hace más de una semana: un token que debería haberse rotado y no sabemos si se rotó. No dejamos de mencionarlo porque asumamos que nadie lo vio la primera vez — lo repetimos porque un sistema que dice de forma automática 'ya está resuelto' sin confirmación es más peligroso que uno pesado que insiste."

## Imágenes huérfanas y vulnerabilidades

- `public/images/plants/`: 73 archivos totales, **30 huérfanos** sin slug correspondiente en las 52 plantas del catálogo (1 ya conocido — `plant-00-beleno-negro-cientifica.jpg`, candidato peligroso sin resolver — + 29 nombres de plantas comunes no catalogadas). Recontado archivo por archivo este ciclo cruzando contra los 52 slugs actuales de la DB — sin cambios desde el 20º ciclo, ningún archivo tocado.
- `npm audit --json`: 1 critical / 10 high / 4 moderate / 1 low — mismo desglose que ciclos anteriores, sin cambio.

## Tareas manuales de Papu hoy

1. **Rotar el token OAuth expuesto en ciclos anteriores** (2026-07-25, 06:22 y 09:52 UTC) — sigue sin confirmación de rotación, ya son 9 ciclos escalándolo.
2. **Decidir sobre las 30 imágenes huérfanas en `public/images/plants/`**: confirmar si son assets a integrar a un futuro ampliado del diccionario o basura a eliminar — y en particular, revisar/rechazar `plant-00-beleno-negro-cientifica.jpg` (candidato con posible error de identificación botánica, ver ciclo del 06:41 UTC del 26/07).
3. **Activar Gumroad** (`NEXT_PUBLIC_GUMROAD_URL` en Vercel) para desbloquear el CTA transaccional de `/producto/ritual-descanso` — bloqueo de monetización desde hace 23 ciclos consecutivos.

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios); bitácora y memoria las commitea el paso dedicado del workflow.
