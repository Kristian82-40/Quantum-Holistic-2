# Bitácora — 2026-09-21 18:01 UTC (ciclo 279º, MODO CICLO)

## Qué comprobé
- `kimiko_drafts`: sin filas `pendiente` ni `en_curso`, sin orden de Telegram nueva
  este ciclo (la última, 278º, sigue `hecho`).
- `npm ci`: limpio, 415 paquetes.
- `npm run build`: exit 0, 36/36 páginas.
- `npm run lint`: sin warnings ni errores.
- `npm audit`: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate, 7
  high, 1 critical), fix solo vía `--force` (breaking: next 14→16, postcss).
- 8/8 rutas del checklist del Paso 2.1 en 200 siguiendo redirecciones: `/`,
  `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`,
  `/login`, `/registro`, `/terapeutas`.
- `/admin` → `/admin/` (308) → `/login/?redirect=%2Fadmin%2F` (307) → 200.
  `middleware.ts` en la raíz.
- canonical y `og:url` en `https://quantum-holistic.com/`. `sitemap.xml` (200,
  37 `<loc>`) y `robots.txt` (200) correctos.
- Vercel: últimos 5 despliegues `READY`.
- Sexagésima cuarta pasada del check permanente del 211º/212º: cruce de hash
  de `ficha_cientifica` sobre las 52 filas de `plants` (columna JSON completa,
  `json.dumps(sort_keys=True)` antes de hashear). Mismos **7 grupos / 21
  filas** de siempre, sin cambios.
- `plants`: 52 filas, 4 publicada+verificada sin cambio (`albahaca`, `arnica`,
  `equinacea`, `hinojo`), las 4 imágenes confirmadas en disco
  (`public/images/plants/*-cientifica.jpg`). Las 9 peligrosas confirmadas
  `publicada=false` una a una. `lavanda` sigue con `image_cientifica_url`
  rellena pero sin fichero `lavanda*` en disco — reconfirmado, 20 días abierto,
  sin blast radius (despublicada).
- `blog_posts`: 109 filas totales, 22 publicados (`published=true`), sin
  títulos duplicados, ninguno >60 car., `excerpt` <155 car., `image_url`
  presente en todos. Barrido de enlaces internos al diccionario sobre el
  `content` completo de los 22: único slug enlazado sigue siendo `hinojo`, sin
  ninguna planta peligrosa enlazada.
- `citas`: última inserción antes de este ciclo era del 273º (James Thomson,
  2026-09-20T15:41:18), ~26h20min de antigüedad al comprobar — **cruza el
  umbral de ~24h**.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Qué arreglé
- Inserté una cita nueva en `citas`: Izaak Walton, *The Compleat Angler*,
  Parte I, Cap. 21 (1653-1655) — "Look to your health; and if you have it,
  praise God, and value it next to a good conscience; for health is the
  second blessing that we mortals are capable of; a blessing that money
  cannot buy." Verificada por `WebSearch` contra varias fuentes independientes
  (azquotes.com, libquotes.com), texto consistente entre ellas. Autor no
  repetido entre las 45 filas previas, tema de salud, sin lenguaje de curación
  ni pseudociencia. `citas` pasa de 45 a 46 filas.

## Qué encontré sospechoso
- Nada nuevo. El desajuste "romero" del chip de `ChatBot.tsx` (sin planta
  `romero` en `plants`, anotado en el 278º) y la imagen ausente de `lavanda`
  siguen abiertos como tareas manuales, sin blast radius.

## Tareas manuales de Kristian hoy
1. Subir la imagen científica de `lavanda` a `public/images/plants/` o
   limpiar `image_cientifica_url` en Supabase — 20 días abierto, sin urgencia
   por estar despublicada.
2. El chip de sugerencia "Romero y memoria" en `ChatBot.tsx` no tiene planta
   `romero` en la tabla `plants` — decidir si se crea la ficha o se quita el
   chip.
3. `npm audit`: la única corrección de fondo (next 14→16, salta la 15, rompe
   `next-intl` 3→4) requiere tu decisión explícita por el blast radius —
   sigue sin aplicarse de oficio.

Sexagésimo cuarto ciclo consecutivo (212º-279º) sin hallazgos técnicos nuevos
en el checklist mecánico. Sin commits de código este ciclo; única escritura
fue el `INSERT` en `citas`.
