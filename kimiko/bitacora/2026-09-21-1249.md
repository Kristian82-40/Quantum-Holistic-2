# Bitácora — 2026-09-21 12:49 UTC (278º ciclo, MODO ORDEN vía Telegram)

## Orden recibida

Kristian escribió por Telegram a las 12:40:02 UTC (draft
`69eafe6f-b261-494f-a7a4-f16a47e3cbd1`, `chat_id` 8902055800, autorizado):

> "Hey kimiko. Dime si el bot de la página funciona correctamente!, y si ves
> que no funciona, lo reparas! Para que en el mismo chat contestes tu
> kimiko!"

Lo interpreté como: el chatbot embebido en la web (`components/ui/ChatBot.tsx`,
llama a `/api/chat`), no el buzón de Telegram. Marqué la fila `en_curso` antes
de empezar.

## Qué comprobé

- `POST https://quantum-holistic.com/api/chat` respondía 200 pero siempre con
  `"En este momento no puedo responder. Inténtalo en unos minutos."` — el
  fallback genérico del `catch`, en cualquier mensaje.
- `app/api/chat/route.ts`: la ruta intenta primero `N8N_CHAT_WEBHOOK_URL`
  (webhook de n8n) y si falla cae a `OLLAMA_URL` (chat de Ollama). Ambos
  valores por defecto apuntan a `localhost` si la env var no existe.
- Vercel (`vercel env pull`, proyecto `quantum-holistic-2`, `production`):
  `N8N_CHAT_WEBHOOK_URL` no está configurada en absoluto; `OLLAMA_URL` está
  configurada pero apunta literalmente a `http://localhost:11434`. Según
  `app/CLAUDE.md`, n8n y Ollama son servicios que corren solo en la máquina
  local de Kristian ("Papu Ext"), nunca en Vercel — así que desde una función
  serverless de Vercel, `localhost` es el propio contenedor de la función, sin
  nada escuchando ahí. **No es una caída puntual: el chat no podía funcionar
  nunca en producción tal como estaba desplegado**, desde que se desplegó.
- No hay `ANTHROPIC_API_KEY` ni `GEMINI_API_KEY` en Vercel (coherente con
  coste-cero), así que no hay ninguna IA de pago disponible como alternativa
  para esta ruta en tiempo real (distinto de mí: yo soy el modelo, pero corro
  en GitHub Actions, no puedo servir la ruta `/api/chat` de la web en vivo).

## Qué arreglé

- `app/api/chat/route.ts`: cuando n8n y Ollama fallan (siempre, hoy), en vez
  de devolver el mensaje de error muerto, ahora se hace una búsqueda por
  palabras clave (sin acentos, con lista de stopwords) sobre el contenido ya
  verificado del sitio — `plants` (`publicada=true AND ficha_verificada=true`,
  excluyendo siempre las 9 peligrosas vía `PLANTAS_PELIGROSAS`) y
  `blog_posts` (`status=eq.published`) — y responde con la ficha o el
  artículo más relevante, enlazando a `/diccionario/{slug}` o `/blog/{slug}`,
  con el aviso de consulta médica ya presente en el tono del asistente. Si no
  hay match, invita a explorar `/diccionario` o `/blog` en vez de un
  callejón sin salida. Reduje también el timeout de la llamada a Ollama de
  90s a 15s (no hacía falta esperar tanto para un `localhost` inalcanzable).
- Mantuve intactos los intentos a n8n/Ollama: si Kristian expone alguno de
  los dos públicamente y actualiza la env var en Vercel, el chat los usará
  igual, sin tocar código.
- Verificado: `npm ci` limpio, `npm run build` exit 0 (36/36 páginas, sin
  errores de tipos ni lint), y en local (`npm run start`, con
  `NEXT_PUBLIC_SUPABASE_URL` explícita) probé varios mensajes — respuestas
  coherentes para consultas que matchean plantas publicadas, respuesta
  educada para mensajes vacíos/irrelevantes, 400 para mensaje inválido o
  >1000 caracteres, sin excepciones sin capturar.
- Commit `e957d8a` en `main`, desplegado en Vercel (`READY` confirmado por
  API), y reprobado en caliente contra `https://quantum-holistic.com/api/chat`
  con el mismo resultado que en local.

## Qué encontré sospechoso

- Solo **4 de las 52 plantas** están `publicada=true AND ficha_verificada=true`
  ahora mismo (`albahaca`, `arnica`, `equinacea`, `hinojo` — coincide con el
  277º), así que el fallback nuevo tiene poco contenido del que tirar. El
  chip de sugerencia "Romero y memoria" del propio `ChatBot.tsx` no tiene
  ninguna planta `romero` en la tabla `plants` (confirmado, cero filas) — no
  es un bug de mi fix, es un desajuste de contenido preexistente entre la UI
  y los datos. No lo toco (no voy a inventar ni publicar una ficha de
  `romero` sin pasar por la regla de hierro del Paso 2.2 con imagen
  verificada en disco).
- La causa raíz de fondo (dar al chat una IA real y disponible 24/7 en
  producción) sigue sin resolver: requeriría exponer Ollama o n8n vía un
  túnel público estable (p.ej. Cloudflare Tunnel) desde la máquina de
  Kristian y apuntar `OLLAMA_URL`/`N8N_CHAT_WEBHOOK_URL` en Vercel a esa URL
  pública — decisión de infraestructura de Kristian, fuera de lo que puedo
  desplegar yo desde GitHub Actions sin acceso a su red local, y no hay
  presupuesto para una API de pago (coste cero es innegociable).

## Cierre de la orden

Marqué la fila `hecho` en `kimiko_drafts` con un resumen en `copy`, y envié
el mismo resumen por Telegram al `chat_id` 8902055800 vía `sendMessage`. Sin
más filas `pendiente` en la cola tras esta.

## Tareas manuales de Kristian hoy

1. Si quiere una IA real (no solo el fallback de contenido) respondiendo en
   el chat de la web 24/7, exponer Ollama y/o el webhook de n8n con una URL
   pública estable y decirme cuál es, para apuntar `OLLAMA_URL` /
   `N8N_CHAT_WEBHOOK_URL` en Vercel — sin eso, el fallback nuevo es lo mejor
   que puede dar el chat en producción.
2. `lavanda`: sigue sin `public/images/plants/lavanda-cientifica.jpg` en
   disco — abierto desde 2026-09-03, 18 días.
3. Decidir sobre la subida mayor de Next.js (14→16) para la vulnerabilidad
   crítica de `npm audit` — sin cambio desde el 223º.
