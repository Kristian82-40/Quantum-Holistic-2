# Bitácora — 2026-09-18 03:43 UTC (ciclo 261º, MODO CICLO)

## Qué comprobé

- **`kimiko_drafts`**: comprobado primero, antes del resto del checklist,
  pese al disparo `schedule`. Las 4 filas históricas siguen `status = 'hecho'`,
  sin `en_curso` colgado, sin orden de Telegram nueva este ciclo.
- **Build**: `npm ci` + `npm run build` limpios, 36/36 páginas generadas, sin
  errores de lint ni de tipos.
- **`npm audit`**: sin cambio desde el ciclo 223º — 1 moderate, 7 high, 1
  critical (total 9). El bloque crítico sigue siendo `next@14.2.35` sin
  parche en la rama 14.x; la única corrección de fondo (subida mayor 14→16)
  sigue pendiente de decisión explícita de Kristian por su blast radius.
- **Rutas**: `/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
  `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` — las 8
  resuelven a 200 (algunas vía redirect 308 de trailing-slash de
  next-intl, comprobado el chain completo). `/admin` sin sesión →
  308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts`
  confirmado en la raíz del repo (no dentro de `app/`).
- **SEO técnico**: `canonical` y `og:url` de la portada apuntan a
  `https://quantum-holistic.com/`. `sitemap.xml` con 37 `<loc>`.
  `robots.txt` correcto, con referencia al sitemap. `app/sitemap.ts` y
  `app/robots.ts` presentes.
- **Vercel**: últimos 5 despliegues en estado `READY`.
- **Check permanente del 211º/212º (47ª pasada)**: rehecho desde cero el
  cruce de hash sobre la columna `ficha_cientifica` (JSON) de las 52 filas
  de `plants`. Mismos **7 grupos / 21 filas** de siempre: 6 pares de fichas
  reales con contenido idéntico (`tulsi`/`loto`, `valeriana`/`cinamomo`,
  `muerdago`/`ginseng`, `sauco`/`ashwagandha-fruto`, `manzanilla`/`nigela`,
  `equinacea`/`echinacea`) más las 9 peligrosas compartiendo el placeholder.
  Sin cambios.
- **`plants`**: 52 filas. 4 publicada+verificada sin cambio (`albahaca`,
  `arnica`, `equinacea`, `hinojo`), las 4 imágenes (`*-cientifica.jpg`)
  confirmadas en disco en `public/images/plants/`. Las 9 peligrosas
  confirmadas `publicada = false`. `lavanda` sigue con
  `image_cientifica_url` rellena en la fila pero sin fichero en disco
  (hallazgo documentado desde 2026-09-03, despublicada y sin verificar, sin
  blast radius).
- **`blog_posts`**: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Repasados los 22 publicados: sin títulos duplicados, `excerpt`
  <155 caracteres en todos, `image_url` presente en todos.
- **`citas`**: última fila sigue siendo 3 Juan 1:2 (insertada en el 257º,
  2026-09-17T03:56:39 UTC). Hueco de **~23h47min** al comprobar — por
  debajo del umbral de ~24h, pero con margen mínimo. Es muy probable que el
  próximo ciclo cruce el umbral y toque insertar una cita nueva (pública,
  verificada, temática de salud, no duplicada). 42 filas, sin cambio.
- **Negocio**: `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Qué arreglé

Nada — no se detectó ningún hallazgo que requiriera cambio en código, en
Supabase ni en configuración.

## Qué encontré sospechoso

Nada nuevo. Los dos hallazgos ya documentados (`lavanda` sin imagen en
disco, `npm audit` crítico sin fix no-breaking) siguen igual, sin blast
radius activo, y no piden acción de oficio bajo el Paso 5.

## Tareas manuales de Kristian hoy

1. Decidir si autoriza la subida mayor de Next.js (14→16, rompe
   `next-intl` 3→4) para resolver la vulnerabilidad crítica de `npm audit`,
   o si se mantiene la postura actual de no tocarlo por su blast radius.
2. Aportar o autorizar la imagen científica de `lavanda`
   (`public/images/plants/lavanda-cientifica.jpg`) si se quiere publicar
   esa ficha; sigue despublicada y sin verificar desde el 2026-09-03.
3. Ninguna tarea urgente más — sin hallazgos de negocio, sin órdenes de
   Telegram pendientes, sin bloqueos nuevos.
