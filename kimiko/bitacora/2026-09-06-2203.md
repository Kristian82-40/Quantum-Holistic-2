# Bitácora — 2026-09-06 22:03 UTC (ciclo 215º, MODO CICLO)

## Salud del sitio
- `npm ci` limpio (415 paquetes). `npm run build`: 36/36 páginas, sin errores.
  `npx next lint`: sin avisos.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin cambio
  desde el 185º.
- 8/8 rutas del checklist devuelven 200 tras seguir redirects (`/`,
  `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`,
  `/login`, `/registro`, `/terapeutas`); todas pasan por un 308 de
  trailing-slash/locale antes del 200 final, comportamiento esperado.
- `/admin` → cadena de redirect hasta `/login` confirmada (308 a `/admin/`,
  luego a login). `middleware.ts` confirmado en la raíz del repo (no dentro
  de `app/`).
- `canonical`, `og:url`, `og:image`, sitemap (37 `<loc>`, sin cambio desde
  el fix del 211º) y `robots.txt` correctos en producción.
- Vercel: últimos 5 despliegues (`quantum-holistic-2` y `kimiko`) en `READY`.

## Diccionario (`plants`)
- 52 filas totales, 4 `publicada=true AND ficha_verificada=true`
  (`albahaca`, `arnica`, `equinacea`, `hinojo`), sin cambio desde el 211º.
  Las 4 imágenes correspondientes confirmadas en disco en
  `public/images/plants/`.
- Las 9 peligrosas (`aconito`, `datura`, `datura-metel`, `amanita-muscaria`,
  `cannabis`, `cornezuelo-centeno`, `beleno-negro`, `tejo`, `hierba-mora`)
  confirmadas una a una: `publicada=false, ficha_verificada=false`.
- Check permanente del 211º/212º (cruce de hash de `ficha_cientifica` sobre
  las 52 filas, con `json.dumps(sort_keys=True)` para evitar el `TypeError`
  de comparar JSON como texto, según la nota técnica del 213º): **cuarta
  pasada, exactamente los mismos 7 grupos que el 212º-214º, sin grupos
  nuevos.** El grupo de las 9 peligrosas comparte el mismo placeholder
  (esperado). Los 5 pares de contenido cruzado real
  (`ashwagandha-fruto`/`sauco`, `cinamomo`/`valeriana`, `ginseng`/`muerdago`,
  `loto`/`tulsi`, `manzanilla`/`nigela`) y `echinacea`/`equinacea` siguen
  todos `publicada=false`, sin blast radius en vivo.
- Sin `UPDATE` de Kristian sobre `plants` desde el 212º.

## SEO / blog
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Los 22 publicados: sin duplicados de título, todos ≤60 car.,
  todos con `excerpt` ≤155 car., todos con `image_url`. 3 enlaces internos
  a `/diccionario/`, los 3 a `hinojo` (una de las 4 fichas correctas), sin
  enlaces rotos.
- Backlog de borradores sigue calificado como no aprovechable por el cruce
  en bloque del 209º (≈80% duplica tema de posts ya publicados); sin
  candidatos nuevos desde entonces.

## Negocio
- `leads` en 0. `purchases` en 0. `products` sin cambio (2 filas).
- `kimiko_drafts`: 4 filas totales, todas `hecho`, cola vacía — sin orden de
  Telegram este ciclo.
- **`citas`: la última inserción es de hace 42h33min** (2026-09-05T03:28:37
  UTC), la primera vez que el hueco supera el máximo histórico observado.
  Revisé las 33 filas más recientes de la tabla: el hueco más largo entre
  inserciones consecutivas hasta ahora era de 38.0h (el que el 214º ya
  marcó como "extremo alto" y dejó para vigilar si se superaban las 40h).
  Este ciclo lo supera (42.57h y sigue subiendo, la fila no ha llegado
  todavía). No hay ninguna acción de código o dato que tomar — no es un
  bug reproducible, es una caída real en el ritmo de reservas — así que lo
  dejo documentado como aviso para Kristian en vez de intervenir sobre el
  funnel (bloqueado por instrucción permanente).

## Resumen de cambios este ciclo
- **Sin commits de código ni escrituras en Supabase** — cuarto ciclo
  consecutivo (212º-215º) sin hallazgos técnicos nuevos en el checklist
  mecánico. El hallazgo de este ciclo es de negocio, no técnico: la
  cadencia de `citas` se ha roto por primera vez desde que se viene
  vigilando.

## Tareas manuales de Kristian hoy
1. **Revisar por qué no ha entrado ninguna cita nueva en 42h33min** — es el
   hueco más largo desde que hay registro (el máximo previo era 38h). Puede
   ser estacional (fin de semana) o una señal real de caída en el funnel de
   reservas; solo tú puedes juzgar el contexto de negocio detrás.
2. Duplicado de slug `equinacea`/`echinacea` en `plants`: comparten
   contenido idéntico y ninguno está publicado, así que no hay urgencia,
   pero sigue pendiente de que decidas cuál de los dos slugs es el bueno
   para consolidar.
3. Backlog de blog (87 borradores/rechazados): ≈80% duplica tema de posts
   ya publicados según el cruce del 209º. Si quieres contenido nuevo en el
   blog hace falta generar candidatos frescos, no seguir tirando de esta
   cola.
