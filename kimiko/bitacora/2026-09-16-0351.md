# Bitácora — 2026-09-16 03:51 UTC (ciclo 253º, MODO CICLO)

## Comprobado

- **`kimiko_drafts`**: consultado antes que nada pese al disparo `schedule`. Cola sin
  filas en `pendiente` ni `en_curso`, sin orden de Telegram este ciclo. Las 4 filas
  históricas siguen en `hecho`.
- **Build**: `npm ci` + `npm run build` limpios, 36/36 páginas generadas.
- **Lint**: `npx next lint` sin avisos.
- **`npm audit`**: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate, 7 high,
  1 critical), todas en `postcss`/`next` vía `next-intl`, solo resolubles con
  `--force` (breaking change). Sin fix no-breaking disponible.
- **Rutas** (siguiendo redirects): `/` → 200 directo; `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas` → 308 (trailing slash) → 200. `/admin` → 308 → 200 en
  `/login/?redirect=%2Fadmin%2F` (sin sesión, correcto). `middleware.ts` confirmado
  en la raíz del repo (no dentro de `app/`).
- **SEO técnico**: `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`.
  `robots.txt` correcto con referencia al sitemap. `sitemap.xml`: 37 `<loc>`.
- **Vercel**: últimos 5 despliegues (`kimiko` y `quantum-holistic-2`) en `READY`.
- **Diccionario — check permanente del 211º/212º (39ª pasada)**: rehecho desde cero
  el hash del contenido JSON de `ficha_cientifica` sobre las 52 filas de `plants`
  (sin mirar la cifra anotada en ciclos previos). Resultado: **7 grupos / 21 filas**
  con contenido idéntico —
  - 9 peligrosas compartiendo el mismo placeholder de ficha (`hierba-mora`,
    `cannabis`, `datura`, `datura-metel`, `amanita-muscaria`, `beleno-negro`,
    `tejo`, `aconito`, `cornezuelo-centeno`)
  - 6 pares de fichas reales duplicadas: `tulsi`/`loto`, `valeriana`/`cinamomo`,
    `muerdago`/`ginseng`, `sauco`/`ashwagandha-fruto`, `manzanilla`/`nigela`,
    `equinacea`/`echinacea`

  Coincide exactamente con la cifra ya consolidada en el 249º-252º. Sin cambios.
- **`plants`**: 52 filas, 4 `publicada=true AND ficha_verificada=true`
  (`albahaca`, `arnica`, `equinacea`, `hinojo`), las 4 imágenes confirmadas en disco.
  Las 9 peligrosas confirmadas `publicada=false`. `lavanda` sigue despublicada y sin
  verificar (imagen `lavanda-cientifica.jpg` sigue ausente en disco, hallazgo ya
  documentado desde 2026-09-03, sin blast radius porque nunca se activó). `updated_at`
  máximo sin cambio (2026-09-01) → sin auditoría visual nueva este ciclo.
- **`blog_posts`**: 79 draft / 22 published / 8 rejected (109 filas), sin cambio.
- **`citas`**: última fila sigue siendo la insertada en el 252º (Salmos 103:3,
  Reina-Valera), hueco de **~5h03min** al comprobar — muy por debajo del umbral de
  ~24h, sin necesidad de insertar. 41 filas, sin cambio.
- **`leads`** en 0, **`purchases`** en 0, **`products`** sin cambio (2).

## Hallazgos

Ninguno nuevo. Trigesimonoveno ciclo consecutivo (212º-253º) sin hallazgos técnicos
nuevos en el checklist mecánico, y sin hallazgos de negocio nuevos.

## Cambios este ciclo

Ninguno. Sin commits de código, sin escrituras en Supabase.

## Tareas manuales de Kristian hoy

1. Las 9 vulnerabilidades de `npm audit` (1 moderate, 7 high, 1 critical, en
   `postcss`/`next` vía `next-intl`) solo tienen fix vía `npm audit fix --force`,
   que es breaking change (`next-intl@4.14.5`, `next@16.3.5`). Requiere tu decisión
   y pruebas manuales antes de que yo lo toque.
2. Si quieres activar `lavanda`, hace falta subir `public/images/plants/lavanda-cientifica.jpg`
   (o generar una ficha nueva) — sigue sin fichero desde hace 13 días.
3. Ninguna más: sin hallazgos de negocio nuevos (`leads`/`purchases` en 0).
