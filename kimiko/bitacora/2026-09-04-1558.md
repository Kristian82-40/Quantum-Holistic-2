# Bitácora — 2026-09-04 15:58 UTC (ciclo 203º, MODO CICLO)

## Comprobado
- `npm ci` limpio. `npm run build`: 36/36 páginas, sin errores. `npx next lint`: sin avisos.
- `npm audit` (sin flags, vía la salida de `npm ci`): 9 vulnerabilidades (1 moderate, 8 high), sin
  cambio desde el 185º.
- 8/8 rutas del checklist en 200 tras seguir redirecciones (`curl -L`): `/`, `/diccionario`,
  `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`. `/admin` → `/login` con la cadena completa verificada (308 trailing-slash + 307
  auth redirect + 200). `middleware.ts` en la raíz. `canonical`/`og:url` en `/` apuntan a
  `https://quantum-holistic.com/`. `sitemap.xml` con 85 `<loc>` y `robots.txt` correctos.
- Vercel: últimos 5 despliegues `READY`.
- `plants`: 52 filas, 23 publicada+verificada, 0 publicada-sin-verificar. 9 peligrosas confirmadas
  `publicada=false`. `lavanda` sigue despublicada — confirmé de nuevo que no existe ningún fichero
  `lavanda*` en `public/images/plants/`, así que la regla de hierro sigue aplicada correctamente.
  Duplicado `equinacea`/`echinacea` sigue sin resolver (mismo estado que ciclos anteriores). No
  repetí la auditoría visual completa de imágenes este ciclo (sin señal de cambio en los datos y
  presupuesto de 6/ciclo mejor gastado si hay indicio de rotura).
- `blog_posts`: 22 publicados, sin duplicados de título, ninguno >60 car., todos con `excerpt`
  presente y ≤155 car. Barrido de enlaces internos a `/diccionario/{slug}/`: único slug enlazado
  sigue siendo `hinojo` (publicada+verificada). Ninguna de las 9 plantas peligrosas aparece
  enlazada en contenido publicado.
- `leads` en 0. `citas`: última fila insertada por el ciclo 201º (2026-09-03T22:36:49 UTC), hace
  ~17h22min al cierre de este ciclo — dentro del umbral normal (<24h), sin acción necesaria.
  `kimiko_drafts`: las 4 filas existentes están en `status='hecho'`, cola vacía, sin filas
  colgadas en `en_curso`, sin orden de Telegram nueva este ciclo.

## Sin cambios aplicados
Ni commit de código ni escritura en Supabase este ciclo — checklist limpio de principio a fin, sin
hallazgos nuevos respecto al 202º.

## Tareas manuales de Kristian hoy
1. Duplicado `equinacea`/`echinacea` en `plants` sigue sin resolver desde hace varios ciclos —
   decidir cuál de las dos filas es la correcta y borrar o fusionar la otra (fuera de mi alcance:
   no borro filas por iniciativa propia).
2. `lavanda` sigue despublicada por falta de imagen en `public/images/plants/` — si quieres esa
   ficha activa, hace falta subir la imagen correspondiente.
3. Sin más pendientes accionables hoy — sitio, build y datos en buen estado.
