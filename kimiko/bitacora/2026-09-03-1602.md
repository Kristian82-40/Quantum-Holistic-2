# Bitácora — 2026-09-03 16:02 UTC (199º ciclo, MODO CICLO)

## Salud del sitio
- `npm ci` limpio (9 vulnerabilidades, sin cambio desde el 185º ciclo).
- `npm run build`: 36/36 páginas generadas sin error. `npx next lint`: sin avisos.
- 8/8 rutas del checklist devuelven 200 en producción (siguiendo el 308 de
  normalización de barra final de Next.js): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión → cadena completa 308 + 307 → `/login/?redirect=%2Fadmin%2F`
  (200). `middleware.ts` confirmado en la raíz del repo (no dentro de `app/`).
- Home: `canonical` y `og:url` → `https://quantum-holistic.com/`, `og:image` →
  `https://quantum-holistic.com/images/og-default.jpg` (fix del 196º sigue vivo,
  sin regresión).
- `robots.txt` correcto (Sitemap apunta al dominio real). `sitemap.xml`: 85 `<loc>`,
  sin cambio.
- Vercel: últimos 5 despliegues `READY`.

## Diccionario (`plants`)
- 52 filas, `updated_at` máximo sin cambio desde el 189º ciclo (mismo timestamp
  exacto `2026-09-01T09:21:58`) → no repetí la auditoría visual completa. Estado
  conocido: 23 publicada+verificada, 9 peligrosas confirmadas una a una
  `publicada=false`, `lavanda` sigue despublicada (imagen `lavanda-cientifica.jpg`
  confirmada ausente en `public/images/plants/` — la regla de hierro del Paso 2.2
  sigue aplicando, no la reactivo solo por el hallazgo de abajo), duplicado
  `equinacea`/`echinacea` reconfirmado sin resolver.

## Blog (`blog_posts`) y SEO — hallazgo y arreglo
- 109 filas (79 draft / 22 published / 8 rejected), sin `UPDATE` nuevo en el resto
  de la tabla desde el 189º. Sin duplicados de título entre los 22 publicados.
- Verifiqué en vivo un post publicado al azar (`mindful-eating-comer-con-conciencia-...`):
  `title` 55 car., meta description 109 car., ambos dentro de límite; `canonical` y
  `og:image` correctos.
- **Enlace interno roto encontrado y corregido:** `lavanda` se despublicó en el
  190º ciclo (2026-09-01, resuelto sin mi intervención, imagen nunca existió), pero
  2 de los 5 enlaces internos que el 186º ciclo había añadido a mano seguían
  apuntando a `/diccionario/lavanda/`, que ahora devuelve 404. Nadie lo detectó en
  8 ciclos porque el checklist de `plants` no repite auditoría cuando no hay
  `UPDATE` nuevo, y el checklist de `blog_posts` tampoco repite el chequeo de
  enlaces internos en las mismas condiciones — la interacción entre ambas tablas no
  la cubre ningún check existente. Arreglado quitando el `<a href="/diccionario/lavanda/">`
  y dejando el texto plano "lavanda" en los dos posts (`aromaterapia-y-aceites-esenciales-terapeuticos-1780097914`
  e `el-sueno-como-medicina-higiene-del-sueno-y-ritmos-circadiano-1780097277`), sin
  forzar el enlace a otra planta para no falsear el anchor text. Verificado en vivo
  tras esperar el TTL de 300s del data cache de Next.js (`revalidate: 300` en el
  `fetch` de `getSupabasePost()`): los dos posts ya sirven el texto sin enlace.
  Barrido completo de los 22 posts publicados tras el fix: el único slug de
  diccionario referenciado ahora es `hinojo` (200, vivo). Barrido adicional
  confirmó que ninguna de las 9 plantas peligrosas aparece enlazada en contenido
  publicado.

## Negocio
- `leads`: 0 filas, sin cambio.
- `citas`: última inserción `2026-09-02T08:35:24 UTC`, ahora mismo son **~31h27min**
  sin inserción nueva — sigue creciendo desde el cruce del umbral de 24h detectado
  en el 198º ciclo (24h06min → 31h27min). No es una anomalía técnica, solo
  observación de negocio, sin tocar el funnel (Paso 2.5).

## Telegram
- `kimiko_drafts`: cola vacía, sin filas colgadas en `en_curso`, sin orden nueva
  este ciclo.

## Resultado
1 hallazgo real corregido este ciclo (enlace interno roto por despublicación de
`lavanda`), sin commit de código — solo `UPDATE` de `content` en `blog_posts` vía
Supabase REST, verificado en vivo tras el TTL del cache de datos.

## Tareas manuales de Kristian hoy
1. `citas` lleva ~31h sin una reserva nueva (última: 2026-09-02 08:35 UTC) — sigue
   creciendo desde el aviso de ayer, puede merecer un vistazo si esperabas más
   movimiento.
2. Duplicado `equinacea`/`echinacea` en el diccionario sigue sin resolver — decidir
   cuál de las dos fichas se queda.
3. Si algún día quieres reactivar `lavanda`, hace falta subir una imagen científica
   real a `public/images/plants/lavanda-cientifica.jpg` — hasta entonces la dejo
   despublicada y sin enlaces rotos apuntándole.
