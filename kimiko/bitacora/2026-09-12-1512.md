# Bitácora — ciclo 238º (2026-09-12, ~15:12 UTC, MODO CICLO)

## Comprobado

- `kimiko_drafts`: 4 filas totales, todas `hecho`. Sin `pendiente` ni
  `en_curso` colgado. Sin orden de Telegram este ciclo (comprobado primero,
  antes del resto del checklist, pese al disparo `schedule`).
- `npm ci` + `npm run build`: limpio, 36/36 páginas generadas.
- `npx next lint`: sin avisos.
- `npm audit`: sin cambio desde el 223º — 1 moderate, 7 high, 1 critical (9
  total), todas requieren `--force` con breaking change. Sin fix
  no-breaking disponible.
- 8/8 rutas del checklist en 200 (`-L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` sin sesión → 308 → 200 siguiendo redirects hasta login.
  `middleware.ts` confirmado en la raíz del repo.
- `canonical` y `og:url` de home → `https://quantum-holistic.com/`.
  `robots.txt` correcto. `sitemap.xml`: 37 `<loc>`, sin cambio.
- Vercel: últimos 5 despliegues en `READY`.

## Diccionario (`plants`)

- 52 filas totales, sin cambio.
- Publicada+verificada: mismas 4 de siempre — `albahaca`, `arnica`,
  `equinacea`, `hinojo`. Las 4 imágenes en `image_cientifica_url`
  confirmadas en disco (`public/images/plants/*.jpg`).
- Las 9 peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) siguen
  `publicada=false`.
- Check permanente (211º/212º): cruce de hash de `ficha_cientifica` sobre
  las 52 filas — vigesimosexta pasada, exactamente los mismos 7 grupos
  duplicados que en 212º-237º, sin grupos nuevos.
- `echinacea` vs `equinacea` en vivo: sin `-L` ambas dan 308 (normalización
  de barra final, comportamiento normal de Next.js) — siguiendo el
  redirect, `echinacea` sigue en 404 y `equinacea` en 200. No es un
  duplicado real, coincide con ciclos anteriores. Anotado para no
  confundir el 308 crudo con un hallazgo nuevo en próximos ciclos.

## Blog (`blog_posts`)

- 109 filas: 79 draft / 22 published / 8 rejected. Sin cambio.
- Draft más reciente sigue en 2026-07-10 (ya evaluado y descartado en
  206º-209º).

## Citas

- Última fila antes de este ciclo: Proverbios 16:24 (234º, insertada
  2026-09-11 16:05:36 UTC). Hueco de **~23h07min** al comprobarlo —
  rozando el umbral de ~24h, y con el patrón de cadencia entre ciclos
  (~5-7h) el próximo ciclo habría caído ya claramente por encima del
  umbral. Decidí insertar ahora en vez de esperar.
- Cita nueva verificada con `WebSearch` antes de insertar: Aristóteles,
  *Ética a Nicómaco*, Libro I — "Así, el fin de la medicina es la salud;
  el de la construcción naval, el navío; el de la estrategia, la victoria,
  y el de la ciencia económica, la riqueza." Fuente clásica, dominio
  público, sin lenguaje de curación ni pseudociencia, autor no repetido
  (revisados los 37 autores previos antes de elegir). Insertada correctamente
  — `citas` pasa de 37 a 38 filas, confirmado por `Content-Range`.

## Negocio

- `leads`: 0 filas. `purchases`: 0 filas. `products`: 2 filas, sin cambio.
  El funnel de pago del Ritual del Descanso sigue sin tocarse — no está
  aprobado.

## Resultado

Checklist mecánico estable (vigesimosexto ciclo consecutivo, 212º-238º,
sin hallazgos técnicos nuevos). Única escritura del ciclo: una fila nueva
en `citas` (contenido editorial rutinario, dentro de las atribuciones de
Kimiko, sin tocar código ni tablas protegidas).

## Tareas manuales de Kristian hoy

1. Ninguna acción urgente. El `npm audit` sigue con 9 vulnerabilidades sin
   fix no-breaking desde el 223º — si quieres que Kimiko intente el upgrade
   a `next@16` / `next-intl@4.14` con sus roturas, dilo explícitamente; por
   defecto no se toca porque es breaking.
2. Si quieres que el diccionario avance más allá de las 4 fichas publicadas
   actuales, decide qué plantas priorizar.
3. Nada más pendiente de tu parte.
