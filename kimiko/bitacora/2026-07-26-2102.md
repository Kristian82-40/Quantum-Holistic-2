# Bitácora Kimiko — Ciclo Cloud 2026-07-26 21:02 UTC (Vigesimosegundo ciclo)

## QA — 8/8 checks OK

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.5, 36 rutas generadas, `ƒ Middleware` presente).
2. **Producción:** `curl -s https://quantum-holistic.com` → canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (con `-L`): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → todas `200` ✅.
4. **`/admin` sin sesión** → `308` (trailing slash) → `307`/`302` a `/login/?redirect=%2Fadmin%2F` → `200` ✅ (`url_effective` confirmado). `middleware.ts` en raíz, no en `app/` ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`) ✅.
6. **9 plantas peligrosas con placeholder:** `image_cientifica_url`/`image_mistica_url` = `null` para las 9 (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) ✅. Nunca activadas.
7. **`leads`/`purchases`:** 0 filas ambas tablas (verificado con `Prefer: count=exact`, `content-range: */0`). Último test E2E real de escritura: 02:54 UTC del mismo día (~18h09min antes), bajo el umbral de 24h — no repetido, solo lectura.
8. **`sitemap.ts` y `robots.ts`:** presentes en `app/`, dominio `https://quantum-holistic.com` correcto en ambos ✅.

## Monetización

- `NEXT_PUBLIC_GUMROAD_URL` sigue ausente en Vercel (12 env vars totales, confirmado vía project ID directo `prj_DASuxCUuV72w8CLpZejVij8XcXvL`). `RitualCheckout.tsx` mantiene el fallback correcto: sin Gumroad, formulario de captura de email a `/api/leads` con `source: 'ritual_descanso_waitlist'`. Vigesimosegundo ciclo consecutivo sin el secret.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso`: sin cambios estructurales, último test E2E real dentro del umbral de 24h, no repetido este ciclo.
- **"Tu Planta Aliada":** sin implementar (solo existe `/api/perfil/dosha` escribiendo a `profiles.dosha`). Propuesta de esquema sin cambios: cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin necesitar migración. Pendiente de OK de Papu para migrar/activar. Cobertura reconfirmada: 41/43 plantas seguras con `afinidad_ayurvedica` (faltan `manzanilla` y `equinacea`, sin cambios).
- Canal DDL para tabla `citas` (paso 5.3) sigue bloqueado: sin `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` en el entorno cloud (`/rest/v1/citas` → `404`, tabla no existe).

## Contenido

- Backlog `blog_posts`: 90 drafts / 19 published, sin cambio.
- **8 drafts con violación de checklist**, mismo total y mismo desglose que el ciclo anterior: 4 chakras (`chakras-y-alimentacion-colores-que-sanan-498063`, `los-7-chakras-principales-y-como-equilibrarlos-1780097836`, `chakras-y-alimentacion-colores-que-sanan`, `chakras-y-colores-que-sanan-557229`) + reiki (1) + cristales (1) + biodescodificación (1) + nutrición cuántica (1). Ninguno publicado.
- Duplicados reconfirmados sin cambios: `equinacea`/`echinacea` (real, `Echinacea purpurea`, pendiente decisión de Papu) y `ashwagandha`/`ashwagandha-fruto` (descartado, son partes distintas de la planta).
- 2 borradores sociales nuevos (sin publicar, solo aquí):
  - **Instagram:** "Cada ciclo volvemos a comprobar cosas que 'ya sabíamos' que estaban bien — las 52 fichas, los 9 placeholders, el dominio del sitemap. No es desconfianza porfiada: es que lo que no se puede probar hoy se trata como sospechoso hasta que se prueba, no como correcto por defecto."
  - **LinkedIn:** "Hay una variable de entorno que activaría el cobro real en la tienda. Podríamos simularla, mockearla, o inventar un flag temporal — pero no lo hacemos. Si la decisión de negocio (activar pagos) no está tomada por quien tiene que tomarla, el sistema no la toma por su cuenta aunque técnicamente pudiera."

## Imágenes huérfanas y vulnerabilidades

- `public/images/plants/`: 73 archivos totales, **30 huérfanos** sin slug correspondiente en las 52 plantas del catálogo (1 ya conocido — `plant-00-beleno-negro-cientifica.jpg`, candidato peligroso sin resolver — + 29 nombres de plantas comunes no catalogadas). Recontado archivo por archivo este ciclo cruzando contra los 52 slugs actuales de la DB (no solo contra el conteo del ciclo anterior) — sin cambios desde el 20º ciclo, ningún archivo tocado.
- `npm audit --json`: 1 critical / 10 high / 4 moderate / 1 low — mismo desglose que ciclos anteriores, sin cambio.

## Tareas manuales de Papu hoy

1. **Rotar el token OAuth expuesto en ciclos anteriores** (2026-07-25, 06:22 y 09:52 UTC) — sigue sin confirmación de rotación, ya son 8 ciclos escalándolo.
2. **Decidir sobre las 30 imágenes huérfanas en `public/images/plants/`**: confirmar si son assets a integrar a un futuro ampliado del diccionario o basura a eliminar — y en particular, revisar/rechazar `plant-00-beleno-negro-cientifica.jpg` (candidato con posible error de identificación botánica, ver ciclo del 06:41 UTC).
3. **Activar Gumroad** (`NEXT_PUBLIC_GUMROAD_URL` en Vercel) para desbloquear el CTA transaccional de `/producto/ritual-descanso` — bloqueo de monetización desde hace 22 ciclos consecutivos.

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios); bitácora y memoria las commitea el paso dedicado del workflow.
