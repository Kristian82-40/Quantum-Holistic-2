# Kimiko — Ciclo cloud 2026-07-31 17:42 UTC

## QA (Paso 1)
1. `npm ci && npm run build` (corrido desde la raíz del repo): **OK**, sin errores de tipos ni lint (37 rutas, `ƒ Middleware` presente, 79.5 kB).
2. Producción (`https://quantum-holistic.com`): canonical y `og:url` == `https://quantum-holistic.com/`. OK.
3. Rutas 200 (siguiendo redirect de trailing slash): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`. OK.
4. `/admin` sin sesión → `308` a `/admin/` → `307` a `/login/?redirect=%2Fadmin%2F` (200 final). `middleware.ts` confirmado en la raíz del repo, no en `app/`. OK.
5. Supabase `plants`: **52/52** filas íntegras (`content-range: 0-51/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null` — placeholder intacto, reverificado fila por fila.
6. `leads`: 0 filas. `purchases`: 0 filas. Sin conversiones ni segmentación por dosha que analizar todavía.
7. `sitemap.xml` (200) y `robots.txt` (200): dominio correcto (`Sitemap: https://quantum-holistic.com/sitemap.xml`). `app/sitemap.ts` y `app/robots.ts` presentes en la raíz.

**QA 7/7 OK**, sin fixes de código necesarios este ciclo. `git status` mostró solo `next-env.d.ts` regenerado por el build (comentario de URL de docs de Next, no funcional); revertido sin commitear.

## Hallazgos reconfirmados (sin cambio de estado)

- **Checkout Gumroad sigue roto** — ~2 días 23h17min (desde 2026-07-28 18:25:20 UTC), **decimoséptimo ciclo consecutivo**. El CTA real servido en `/producto/ritual-descanso/` sigue apuntando a `https://kristian320.gumroad.com/l/ritual-descanso` → `404` (dominio base `kristian320.gumroad.com` también `404`). URL previa funcional `kristiantronco.gumroad.com/l/ugsqtg` → `200`, revert sigue siendo viable. Sin tocar `NEXT_PUBLIC_GUMROAD_URL` sin OK de Papu.
- **`lavanda` sigue con `image_cientifica_url` apuntando a un archivo inexistente** (`/images/plants/lavanda-cientifica.jpg` → `404`). Sin escritura en `plants` este ciclo.
- **34/52 fichas (25 seguras + 9 peligrosas) siguen con `ficha_mistica`/`ficha_cientifica` contaminada** con contenido idéntico al de otra especie, sin gate ni corrección. Caso testigo `ashwagandha-fruto` reverificado en vivo (label + valor extraído del HTML, no grep literal de la frase completa): Elemento=Agua, Chakra=Corazón — sigue siendo el perfil místico de Saúco (*Sambucus nigra*), no el de *Withania somnifera*.
- **Duplicado `equinacea`/`echinacea`** (mismo `nombre_latino = Echinacea purpurea`, dos slugs) reconfirmado sin cambio, pendiente de decisión de Papu.
- Gate `ficha_verificada`: 0/52 verificadas, sin cambio.
- Tabla `citas`: última cita (Paracelso, insertada 2026-07-31 10:43 UTC) tiene ~7h de antigüedad (< 24h) — **no corresponde insertar una nueva este ciclo**. Se insertará en el próximo ciclo que supere las 24h desde esa inserción (a partir de 2026-08-01 ~10:43 UTC).
- `blog_posts`: 90 draft / 19 published, sin cambio. La desalineación `published` boolean vs. `status` (8/19 filas) reportada el ciclo de las 14:15 UTC sigue igual — no afecta al sitio (ambas páginas de blog filtran por `status`), solo higiene de datos.
- `npm audit`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio.
- `public/images/plants/`: 71 archivos, sin cambio.

## Monetización (Paso 2)

- Funnel `/regalo/primera-noche` → lead (`RegaloForm.tsx` → `POST /api/leads`) → `/producto/ritual-descanso`: verificado en código y en producción. PDF de descarga (`/downloads/primera-noche-tranquila.pdf`) → `200`. Sin cambios.
- "Tu Planta Aliada": sin implementar (ningún componente/ruta la referencia todavía). Esquema propuesto en ciclos anteriores (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin migración necesaria) sigue vigente, bloqueada por la misma decisión de producto pendiente sobre las 25 fichas seguras contaminadas.

## Optimización continua (Paso 5)

1. Funnel A/B: `leads`/`purchases` en 0 filas, sin datos suficientes para proponer variantes de CTA.
2. "Tu Planta Aliada": test negativo de las 9 peligrosas sigue pasando (imagen y contenido místico ocultos en el render vía filtro hardcoded en `app/diccionario/[slug]/page.tsx`).
3. Cita diaria: sin inserción este ciclo (< 24h desde la anterior, ver arriba).
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a la URL rota — incidente crítico abierto, no un hito.

## Borradores sociales (solo bitácora, sin publicar)

**Instagram** — ángulo: lo que sí se sostiene mientras algo más está roto.
> El regalo sigue en pie: guía "Primera Noche Tranquila" gratis en quantum-holistic.com/regalo/primera-noche. Si el resto del camino tarda un poco más de lo esperado, esto no. #QH #fitoterapia #regalo

**LinkedIn** — ángulo: qué significa "sin cambio" cuando se repite 17 veces.
> Diecisiete ciclos consecutivos reportando el mismo checkout roto no es lo mismo que diecisiete ciclos sin hacer nada: cada uno reverifica la URL rota, la URL de respaldo, y el CTA real servido en producción — no el dato de hace tres días. La verificación repetida no es ruido, es la única forma honesta de decir "sigue igual". #QH #operaciones #QA

## Tareas manuales de Papu hoy

1. **Resolver el checkout Gumroad** — roto ~2 días 23h17min, decimoséptimo ciclo escalándolo. Revert de `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional, verificado 200 este ciclo) o arreglar/republicar el producto en la cuenta `kristian320`. Impacto directo en ventas — cada visitante que pulsa "Comprar por 19€" llega a un 404.
2. **Decidir el alcance del problema de `ficha_mistica`/`ficha_cientifica` sin gate** en las 25 fichas seguras contaminadas: (a) extender el gate `ficha_verificada` a las 52 fichas, o (b) corregir/despublicar fila por fila. Dataset de recuperación ya preparado en `kimiko/recuperacion/fichas-recuperables-2026-07-29.json` y `restaurar-fichas-2026-07-29.sql`. Bloquea también "Tu Planta Aliada". Abierto desde 2026-07-30 02:36 UTC.
3. **Rotar `SUPABASE_SERVICE_ROLE_KEY` y el token OAuth expuestos** (documentado desde 2026-07-25), sin confirmación de rotación todavía. De paso, decidir el duplicado `equinacea`/`echinacea` (mismo `nombre_latino`, dos slugs) y si se quiere una UI pública para la tabla `citas`.

---
*Ciclo cloud automático. Sin commits de código este ciclo (build pasa, QA limpio, sin fixes necesarios). Bitácora y memoria commiteadas por el paso dedicado del workflow.*
