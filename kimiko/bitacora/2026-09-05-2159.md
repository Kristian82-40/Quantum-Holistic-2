# Bitácora — 2026-09-05 21:59 UTC (210º ciclo, MODO CICLO)

## Salud del sitio
- `npm ci`: limpio, 415 paquetes.
- `npm run build`: 36/36 páginas generadas sin errores.
- `npx next lint`: sin avisos.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin cambio
  desde el 185º. Fix solo disponible con `--force` (breaking: next@16,
  next-intl@4). No lo aplico sin validación de Kristian.
- 8/8 rutas del checklist en 200 (`curl -L`): `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`.
- `/admin` sin sesión: cadena completa verificada `308` (`/admin` → `/admin/`)
  → `307` (`/admin/` → `/login/?redirect=%2Fadmin%2F`) → `200`.
- `middleware.ts` confirmado en la raíz del repo.
- Canonical (`https://quantum-holistic.com/`), `og:url` y `og:image` correctos
  en home. `sitemap.xml`: 85 `<loc>`. `robots.txt` con `Allow: /` y referencia
  al sitemap.
- Vercel: últimos 5 despliegues en estado `READY`.

## Integridad del diccionario (`plants`)
- 52 filas totales, 23 con `publicada=true AND ficha_verificada=true`.
- Las 9 plantas peligrosas confirmadas una a una: todas `publicada=false`.
- `updated_at` máximo sigue en `2026-09-01T09:21:58.968322+00:00` (sin cambio
  desde el 189º) → no repetí la auditoría visual completa este ciclo.
- Duplicado `equinacea`/`echinacea` sigue sin resolver — fuera de mi alcance.

## SEO (`blog_posts`)
- 22 publicados: sin títulos duplicados, ninguno >60 caracteres, todos con
  `excerpt` ≤155 caracteres.
- Backlog no publicado: 87 filas (79 draft + 8 rejected), sin cambio desde el
  209º — el registro más reciente sigue siendo del 2026-07-10, es decir el
  pipeline no ha generado borradores nuevos desde entonces. No repetí el
  cruce en bloque contra publicados porque no hay candidatos nuevos que
  auditar; el resultado del 209º (≈80% duplica tema) sigue vigente.

## Negocio
- `leads`: 0 filas.
- `citas`: última inserción 2026-09-05T03:28:37 UTC (206º ciclo) — ~18h31min
  antes del cierre de este, dentro del umbral normal de 24h pero acercándose;
  vigilar en el próximo ciclo.
- `kimiko_drafts`: 4 filas totales, 0 en `pendiente`/`en_curso` — cola vacía,
  sin orden de Telegram nueva este ciclo (evento `schedule`, MODO CICLO).
- Producto "El Ritual del Descanso": sin cambios de funnel ni de checkout por
  iniciativa propia, según límite del Paso 5.

## Resumen
Decimocuarto ciclo consecutivo (197º-210º) sin hallazgos técnicos nuevos.
Backlog de blog sin cambios desde el cruce en bloque del 209º (no hay
borradores nuevos que auditar). Sin commits de código ni escrituras en
Supabase este ciclo.

## Tareas manuales de Kristian hoy
1. El backlog de 79 borradores de blog sigue mayoritariamente repitiendo
   temas ya publicados (≈80% de los títulos únicos, cuantificado en el
   209º). Vale la pena decidir si se borra en bloque o se usa como material
   de referencia para actualizar los posts ya publicados.
2. Duplicado `equinacea`/`echinacea` en `plants` sigue sin resolver — decidir
   cuál fila fusionar o borrar.
3. Las 9 vulnerabilidades de `npm audit` llevan desde el 185º sin cambio; el
   único fix disponible implica subir a Next 16 (breaking change) — decidir
   si se aborda o se sigue aceptando el riesgo.
