# Bitácora — 2026-09-08 08:46 UTC (ciclo 221º, MODO CICLO)

## Checklist mecánico (limpio, 9º ciclo consecutivo — pero ver hallazgo de negocio abajo)

- `npm ci` limpio.
- `npm run build`: 36/36 páginas generadas sin error.
- `npx next lint`: sin avisos.
- `npm audit`: 9 vulnerabilidades (1 moderate, 8 high), sin cambio desde el
  185º. Fix disponible solo con `--force` (breaking: `next@16`, `postcss`
  vía `next-intl@4.14.2`) — no aplicado, pendiente de decisión explícita.
- 8/8 rutas del checklist devuelven 200 con `-L`: `/`, `/diccionario`,
  `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas`.
- `/admin` sin sesión: `308` → `/admin/` → `/login/?redirect=%2Fadmin%2F`
  (200). Cadena completa correcta. `middleware.ts` en la raíz del repo.
- `canonical` y `og:url` en `https://quantum-holistic.com/` correctos.
- `sitemap.xml`: 37 `<loc>`, sin cambio. `robots.txt` correcto (Allow: /,
  referencia al sitemap).
- Vercel: últimos 5 despliegues en estado `READY`.

## Diccionario de plantas

- `plants`: 52 filas totales.
- Publicada + `ficha_verificada`: 4 (`albahaca`, `arnica`, `equinacea`,
  `hinojo`) — sin cambio. Las 4 imágenes en `image_cientifica_url`
  confirmadas presentes en `public/images/plants/`.
- Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria,
  cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) confirmadas
  `publicada = false`.

## Blog / SEO

- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio de conteo.
- 22 publicados: sin duplicados de título, todos con título ≤60 caracteres,
  `excerpt` ≤155 caracteres, todos con `image_url`.
- Enlaces internos a `/diccionario`: solo 3 de 22 posts enlazan (todos a
  `hinojo`). Sin cambio respecto a ciclos anteriores — no hay fichas nuevas
  publicada+verificada que ofrecer como destino, así que no es una regresión
  nueva, sigue como backlog conocido.

## Hallazgo del ciclo: regresión real en `citas` (72h+ sin la cita diaria)

La tabla `citas` no es un funnel de reservas de terapia — es una tabla de
citas célebres de dominio público sobre salud (Hipócrates, Avicena,
Maimónides, la OMS, ahora Cervantes...) que Kimiko puebla ella misma,
insertando una fila nueva cuando pasan ~24h desde la última, verificando el
texto con `WebSearch` cuando no hay certeza alta.

Esta misma confusión ("citas" = reservas de negocio, por ambigüedad del
nombre en español) ya se había detectado y corregido en el ciclo 201º
(2026-09-03). Volvió a colarse desde el ciclo 215º: los cierres 215º-220º
leyeron el hueco creciente (42h → 72h04min sin fila nueva) como "señal de
negocio... sin tocar el funnel, bloqueado por el Paso 5" — el mismo error
que el 201º ya había dejado documentado como check permanente, pero que no
sobrevivió porque `KIMIKO_MEMORIA.md` es puramente cronológico (pasaba de
800 KB / 9700 líneas antes de este ciclo) y sin índice: un ciclo que solo
lee la cola reciente ve la corrección del 219º sobre "no narrar el delta
como tendencia" y da por buena la premisa de fondo sin volver a
verificarla contra la tabla real.

Verificación hecha este ciclo antes de actuar:
- Columnas reales de la fila más reciente: `texto`, `autor`, `fuente`,
  `fecha_publicacion`, `created_at` — nada de cliente, servicio ni estado de
  reserva.
- Listado completo de tablas del schema vía PostgREST (24 tablas): `citas`
  es la única con ese nombre, no existe una tabla de reservas separada.
- `grep` sobre `app/`, `lib/`, `components/`: ninguna referencia a `citas`
  como tabla de reservas en el código del sitio.

Corregido: inserté una cita nueva, verificada con `WebSearch` contra varias
fuentes independientes con texto coincidente:

> "Come poco y cena menos, que la salud de todo el cuerpo se fragua en la
> oficina del estómago." — Miguel de Cervantes, *Don Quijote de la Mancha*,
> Segunda Parte, cap. 43 (1615). Dominio público.

Autor nuevo (no estaba entre los 33 anteriores). Pasa el filtro
anti-pseudociencia: moderación dietética general, sin ningún claim de
curación. 34 citas en la tabla tras la inserción.

**Corrección estructural aplicada a la memoria:** añadí una sección "Checks
críticos" al principio de `KIMIKO_MEMORIA.md` (antes de la primera entrada
cronológica) con las reglas que ya han causado una desviación de
comportamiento real más de una vez, para que un ciclo futuro las vea aunque
solo lea el principio y la cola del archivo. Es un mecanismo nuevo — habrá
que revisar en ciclos futuros si de verdad ayuda.

## Negocio

- `leads`: 0 filas. `purchases`: 0 filas. `products`: sin cambio (2 filas).
- `kimiko_drafts`: 4 filas totales, todas `hecho`, cola vacía — sin orden de
  Telegram pendiente este ciclo.
- La estrategia "El Ritual del Descanso" sigue sin aprobación explícita de
  Kristian — no se ha tocado el funnel de checkout.

## Escrituras de este ciclo

- Supabase: 1 fila nueva en `citas` (Cervantes).
- Repo: corrección en `KIMIKO_MEMORIA.md` (sección "Checks críticos" +
  entrada del ciclo 221º) y esta bitácora. Sin cambios de código de
  producto.

## Tareas manuales de Kristian hoy

1. **Ninguna acción de negocio nueva que requiera tu decisión este ciclo** —
   el hallazgo de hoy era un error de interpretación de Kimiko sobre una
   tabla de contenido propio, no un problema real del sitio ni del embudo de
   ventas. Ya está corregido.
2. `leads` y `purchases` siguen en 0 desde hace muchos ciclos — si esperabas
   tráfico o conversión real por otro canal (fuera de lo que audita este
   checklist), avisa para que lo cruce contra algo concreto.
3. Las 9 vulnerabilidades de `npm audit` (1 moderate, 8 high) siguen sin
   resolver porque el único fix disponible es `--force` con upgrade breaking
   de `next`/`next-intl`/`postcss` — decide si quieres que lo intente en una
   rama aparte o lo dejamos como está.
