# Bitácora — 2026-09-17 16:29 UTC (ciclo 259º, MODO CICLO)

## Qué comprobé

- `kimiko_drafts`: comprobado primero, antes del resto del checklist, pese al
  disparo `schedule`. Cola de `status = 'pendiente'` vacía, sin fila
  `en_curso` colgada, sin orden de Telegram este ciclo.
- Build: `npm ci` + `npm run build` limpios, 36/36 páginas generadas, sin
  errores de tipos ni de lint.
- `npm audit`: sin cambio desde el 223º — 1 moderate, 7 high, 1 critical (9
  total), todas con fix solo disponible vía `--force` (breaking: next-intl
  4.14.5, next 16.3.5). Sigue sin fix no-breaking disponible.
- 8/8 rutas del checklist devuelven 200 tras seguir redirects: `/`,
  `/diccionario`, `/blog`, `/regalo/primera-noche`, `/producto/ritual-descanso`,
  `/login`, `/registro`, `/terapeutas`.
- `/admin` sin sesión: redirige a `/login/?redirect=%2Fadmin%2F`. Correcto.
- `middleware.ts` confirmado en la raíz del repo (no dentro de `app/`).
- Producción: `canonical` y `og:url` en home apuntan a
  `https://quantum-holistic.com/`. `sitemap.xml` con 37 `<loc>`. `robots.txt`
  correcto con referencia al sitemap.
- Vercel: últimos 5 despliegues en `READY`.

## Cuadragésima quinta pasada del check permanente (211º/212º)

Rehecho el cruce de hash de `ficha_cientifica` (columna JSON, no los
ficheros de `image_cientifica_url`) desde cero sobre las 52 filas de
`plants`, sin mirar la cifra anotada en ciclos previos. Mismo resultado que
el baseline: **7 grupos / 21 filas** —

- 6 pares de fichas reales: `ashwagandha-fruto`/`sauco`,
  `cinamomo`/`valeriana`, `echinacea`/`equinacea`, `ginseng`/`muerdago`,
  `loto`/`tulsi`, `manzanilla`/`nigela` (12 filas).
- 1 grupo de 9 filas: las peligrosas (`aconito`, `amanita-muscaria`,
  `beleno-negro`, `cannabis`, `cornezuelo-centeno`, `datura`, `datura-metel`,
  `hierba-mora`, `tejo`) comparten el placeholder.

Sin cambios respecto al baseline. `plants`: 52 filas totales, 4
publicada+verificada sin cambio (`albahaca`, `arnica`, `equinacea`,
`hinojo`) — las 4 imágenes confirmadas presentes en disco
(`public/images/plants/`). Las 9 peligrosas confirmadas `publicada=false`.
`lavanda` tiene `image_cientifica_url` en la fila pero el fichero sigue
ausente en disco (hallazgo ya documentado desde 2026-09-03, despublicada,
sin blast radius). `updated_at` máximo sin cambio (2026-09-01) → sin
auditoría visual completa este ciclo.

## Otras tablas

- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Repasados los 22 publicados: sin títulos duplicados, todos con
  `excerpt` <155 car. y `image_url` presente.
- `citas`: última fila sigue siendo 3 Juan 1:2 (insertada en el 257º), hueco
  de **~12h33min** — por debajo del umbral de ~24h, sin necesidad de
  insertar. 42 filas, sin cambio.
- `leads` en 0, `purchases` en 0, `products` sin cambio (2).

## Qué arreglé

Nada — sin escrituras en Supabase ni commits de código este ciclo.

## Qué encontré sospechoso

Nada nuevo. Ningún hallazgo técnico ni de negocio distinto de lo ya
documentado (audit de dependencias pendiente de decisión de producto,
`lavanda` sin imagen).

## Cambios este ciclo

Ninguno en el sitio ni en Supabase. Cuadragésimo quinto ciclo consecutivo
(212º-259º) sin hallazgos técnicos nuevos en el checklist mecánico, y sin
hallazgos de negocio nuevos.

## Tareas manuales de Kristian hoy

1. Ninguna urgente. El `npm audit` (9 vulnerabilidades, incluida 1 critical)
   sigue esperando una decisión consciente sobre si asumir el upgrade
   breaking de `next` a 16.3.5 y `next-intl` a 4.14.5 — es una decisión de
   producto/arquitectura, no algo que deba forzar por mi cuenta.
2. Nada más pendiente de tu parte hoy.
