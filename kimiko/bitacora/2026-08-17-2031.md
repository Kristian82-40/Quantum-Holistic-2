# Ciclo cloud 2026-08-17 20:31 UTC — 118º ciclo

## QA (8/8 OK)

1. **Build**: `npm ci && npm run build` pasa sin errores ni warnings de tipos, corrido desde la raíz.
2. **Producción**: `curl -s https://quantum-holistic.com` → canonical y `og:url` == `https://quantum-holistic.com/`. OK.
3. **Rutas** (siguiendo redirects 308 de trailing slash, normales en Next.js): `/`, `/diccionario`,
   `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → 200
   todas.
4. **`/admin` sin sesión** → 308 → `/admin/` → 307 → `/login/?redirect=%2Fadmin%2F` → 200.
   `middleware.ts` confirmado en la raíz del repo (no en `app/`).
5. **Supabase — tabla `plants`**: 52 filas (`content-range: 0-51/52`). Las 9 plantas peligrosas
   (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro,
   tejo, hierba-mora) reverificadas fila por fila: `image_cientifica_url` e `image_mistica_url` en
   `null` en las 9, sin excepción.
6. `npm audit`: 17 vulnerabilidades (1 low, 4 moderate, 12 high), sin cambio frente al ciclo
   anterior.
7. `sitemap.xml` (200, 74 referencias al dominio) y `robots.txt` presentes, dominio correcto
   (`https://quantum-holistic.com`).
8. Git: única diferencia tras el build fue `next-env.d.ts` (regenerado, no funcional) — revertida
   sin commitear, como en ciclos anteriores.

## Checkout Gumroad — sigue roto

~20 días 2h, **118º ciclo consecutivo** desde 2026-07-28T18:25:20.881Z. CTA real reconfirmado en
el HTML servido de `/producto/ritual-descanso/`: apunta a
`https://kristian320.gumroad.com/l/ritual-descanso` (404 en vivo, reconfirmado). El revert viable
sigue siendo `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado). Consultada la API de
Vercel (`GET /v9/projects/prj_DASuxCUuV72w8CLpZejVij8XcXvL/env`, solo lectura): `updatedAt` de
`NEXT_PUBLIC_GUMROAD_URL` en `production` (id `L6v4bSqxUSFYv5U5`) sigue en `1785263120881`
(2026-07-28T18:25:20.881Z), sin cambio. Sin tocar ninguna env var — requiere OK explícito de Papu.

## Contenido — sin cambios

- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los 19 drafts en violación del
  checklist identificados en ciclos previos (8 en temas prohibidos: biodescodificación, nutrición
  cuántica, cristales, reiki, chakras; 11 en categoría de contraindicaciones sin cubrir: ayuno ×4,
  adaptógenos ×4, detox ×2, rasayanas ×1) reconfirmados sin cambio, ninguno publicado ni
  reescrito. Los ≥71 drafts restantes (títulos aparentemente limpios, duplicados de título y
  posts de prueba identificados en ciclos previos) siguen sin auditoría frase a frase — sin
  publicar nada sin esa verificación íntegra.
- Sin borradores sociales nuevos este ciclo. Los 2 pendientes de `2026-08-16-0125.md` (IG: "107
  revisiones del mismo semáforo en rojo"; LinkedIn: "tres decisiones de Papu, tres bloqueos
  distintos") siguen sin publicar y no se duplican, para no generar ruido en la bitácora.

## Gate `ficha_verificada` y datos sucios del diccionario — sin cambio

- 0/52 plantas verificadas (pendiente desde 2026-07-30 ~02:36 UTC, ~18 días 18h).
- Duplicado `equinacea` (id 52) / `echinacea` (id 21) sin resolver, mismo `nombre_latino`
  (`Echinacea purpurea`).
- `lavanda` (id 11) sigue sirviendo `image_cientifica_url` que devuelve 404 en vivo
  (`/images/plants/lavanda-cientifica.jpg`).

## Funnel y "Tu Planta Aliada"

- `/regalo/primera-noche` → lead → producto verificado por código (`RegaloForm.tsx`,
  `RitualCheckout.tsx` presentes) y rutas 200, sin cambios.
- E2E con escritura real: última vez 2026-08-17 04:46 UTC (~15h45min, bajo el umbral informal de
  48h) — no repetido este ciclo. `leads`: 0 filas en reposo, sin datos nuevos para segmentación
  A/B por dosha.
- "Tu Planta Aliada": sin implementación en código (confirmado por grep), sigue pendiente de OK de
  Papu sobre el esquema propuesto y del gate `ficha_verificada`.

## Cita diaria — sin inserción

Última cita (Francis Bacon, `created_at` 2026-08-17T08:47:20.36Z) con ~11h44min de antigüedad al
arrancar el ciclo, por debajo del umbral de 24h. 17 citas en la tabla, todas de autores distintos.
Cruza el umbral ~08:47 UTC del 2026-08-18, después de este ciclo.

## Resumen de escrituras este ciclo

Ninguna. Sin commits de código (build pasa sin fixes necesarios; `next-env.d.ts` revertido sin
commitear). Sin escrituras en Supabase (cita bajo el umbral de 24h, funnel probado hace <48h, sin
leads que segmentar). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy

1. **Arreglar el checkout Gumroad** (20d2h roto, 118 ciclos consecutivos): decidir entre publicar
   el producto en `kristian320.gumroad.com/l/ritual-descanso` (URL actualmente configurada, 404)
   o revertir `NEXT_PUBLIC_GUMROAD_URL` en Vercel (`production`, id `L6v4bSqxUSFYv5U5`) a
   `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado). Bloquea todo ingreso — máxima
   prioridad, sin cambio desde 2026-07-28.
2. **Decidir el gate `ficha_verificada`** (18d18h pendiente): 0/52 plantas verificadas
   manualmente; bloquea el diccionario y "Tu Planta Aliada".
3. **Resolver el duplicado `equinacea`/`echinacea`** (ids 52/21) y la imagen 404 de `lavanda` —
   limpieza de datos de baja complejidad pero acumulada desde hace semanas.
