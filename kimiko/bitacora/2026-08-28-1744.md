# 2026-08-28 17:44 UTC — MODO ORDEN: /start + prueba de canal

## Qué comprobé
- `GITHUB_EVENT_NAME=repository_dispatch` con `client_payload.draft_id=f9374281-...` → MODO ORDEN.
- Fila `f9374281-bb58-4e51-9483-9f9e9e94dfcf`: `source_note="/start"`, el comando automático que
  Telegram manda al abrir el chat con el bot. No llevaba orden real.
- Al cerrarla, apareció una segunda fila nueva en `kimiko_drafts` (mismo `chat_id=8902055800`,
  ya filtrado por el buzón): "Kimiko, prueba de canal. Responde con el número de plantas
  publicadas en el diccionario."
- Consulté `plants` vía REST: `publicada=true` → 38 filas; `publicada=true AND
  ficha_verificada=true` → 37; total tabla → 52. Verificado en paralelo contando los `href`
  únicos de `/diccionario/` en producción (`curl` sobre HTML real) → 38, coincide con
  `publicada=true` sin filtrar por `ficha_verificada` (confirma que el listado usa solo
  `publicada`, tal como quedó documentado el 2026-08-28 16:27 UTC tras el commit `86b6bce`).

## Qué hice
- Marqué ambas filas `en_curso` → `hecho`, con `copy` explicando qué hice y qué comprobé.
- Respondí por Telegram (`sendMessage`) a las dos: bienvenida sin acción sobre el `/start`, y el
  número exacto (38 publicadas, 37 también verificadas, 52 en tabla) para la prueba de canal.
- Sin cambios de código, sin escrituras nuevas en `plants` ni en ninguna otra tabla de negocio.

## Sospechoso / a vigilar
- Nada nuevo. El desfase 38 vs 37 (una planta publicada sin `ficha_verificada`) ya estaba
  documentado en la sesión del 2026-08-28 16:27 UTC — no es una regresión de este ciclo, solo lo
  reconfirmé de paso.

## Tareas manuales de Kristian hoy
1. Ninguna generada por este ciclo — ambas órdenes eran de prueba/saludo, no requieren acción tuya.
