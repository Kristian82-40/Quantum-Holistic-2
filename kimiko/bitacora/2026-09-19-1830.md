# Bitácora — 2026-09-19 18:30 UTC (ciclo 269º, MODO CICLO)

## Qué comprobé

- **`kimiko_drafts`**: 4 filas históricas, todas `status = 'hecho'`. Sin filas
  colgadas en `en_curso`. Sin orden de Telegram nueva este ciclo, pese al
  disparo `schedule`.
- **Build**: `npm ci` + `npm run build` → exit 0, 36/36 páginas generadas
  correctamente.
- **Lint**: `npm run lint` → sin warnings ni errores.
- **`npm audit`**: sin cambio desde el 223º — 9 vulnerabilidades (1 moderate,
  7 high, 1 critical), bloque de `next`/`postcss`/`next-intl`, sigue sin fix
  no-breaking (`--force` sube `next` a 16.3.5 y `next-intl` a 4.14.5, rotura
  mayor pendiente de decisión de Kristian).
- **Rutas del checklist (8/8)**: `/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`,
  `/registro`, `/terapeutas` → todas 200. `/admin` → redirige a
  `/login/?redirect=%2Fadmin%2F` (200 tras seguir redirecciones), sin sesión.
- `middleware.ts` confirmado en la raíz del repo (no dentro de `app/`).
- `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`.
  `sitemap.xml` con 37 `<loc>`. `robots.txt` correcto, referenciando el
  sitemap.
- **Vercel**: últimos 5 despliegues en estado `READY`.
- **`plants`**: 52 filas. 4 publicada+verificada sin cambio (`albahaca`,
  `arnica`, `equinacea`, `hinojo`). Check permanente del 211º/212º (55ª
  pasada): cruce de hash sobre `ficha_cientifica` (JSON, comparado con
  `json.dumps(..., sort_keys=True)`) en las 52 filas → mismos **7 grupos /
  21 filas** de siempre (6 pares de fichas reales con contenido cruzado + las
  9 peligrosas compartiendo el mismo placeholder). Sin cambios. Las 9
  peligrosas confirmadas `publicada = false`. `lavanda` sigue con
  `image_cientifica_url` rellena (`/images/plants/lavanda-cientifica.jpg`)
  pero despublicada y sin verificar — hallazgo abierto desde 2026-09-03 (17
  días), reanotado como tarea manual. `updated_at` máximo sin cambio
  (2026-09-01) → sin auditoría visual completa este ciclo.
- **`blog_posts`**: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio de conteo. Repasados los 22 publicados: sin títulos duplicados,
  todos con `excerpt` <155 car. e `image_url` presente.
- **`citas`**: última fila sigue siendo Ralph Waldo Emerson (insertada en el
  268º, 2026-09-19T15:37:57 UTC). Gap de ~2h52min al comprobar — muy por
  debajo del umbral de ~24h, sin necesidad de insertar. 44 filas, sin
  cambio.
- **Negocio**: `leads` en 0, `purchases` en 0, `products` sin cambio (2
  filas). Funnel de "El Ritual del Descanso" no tocado (sigue sin aprobar).

## Qué arreglé

Nada — quincuagésimo cuarto ciclo consecutivo (212º-269º) sin hallazgos
técnicos ni de negocio nuevos.

## Qué encontré sospechoso

- Nada nuevo. Los dos hallazgos abiertos de ciclos anteriores siguen igual:
  `lavanda` sin fichero de imagen en disco (17 días abierto, sin blast
  radius por estar despublicada), y las 9 CVEs de `npm audit` sin fix
  no-breaking disponible.

## Tareas manuales de Kristian hoy

1. Decidir si autoriza la subida mayor de `next` 14→16 (salta la 15, rompe
   `next-intl` 3→4 también) para cerrar las 9 vulnerabilidades de `npm
   audit` (1 moderate, 7 high, 1 critical) — requiere su aprobación por el
   blast radius, no la voy a ejecutar de oficio.
2. Generar o subir la imagen científica de `lavanda`
   (`public/images/plants/lavanda-cientifica.jpg`) si se quiere reactivar
   esa ficha — lleva 17 días abierto sin fichero en disco.
3. Ninguna tarea de negocio nueva: `leads` y `purchases` siguen en 0, sin
   señal que requiera su intervención esta vez.

Commits de código: ninguno. Escrituras en Supabase: ninguna (el gap de
`citas` no cruzó el umbral de inserción este ciclo).
