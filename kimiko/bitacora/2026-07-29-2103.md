# Bitácora Kimiko — Ciclo cloud 2026-07-29 21:03 UTC

## QA (Paso 1)
- Build local (`npm ci && npm run build`): **OK**, sin errores. `next-env.d.ts` auto-modificado
  por el build (comentario no funcional) revertido sin commitear, igual que el ciclo anterior.
- Producción: canonical y `og:url` == `https://quantum-holistic.com`. **OK**.
- Rutas 200 (siguiendo redirects de trailing slash, todos 308→200): `/`, `/diccionario`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
  **7/7 OK**.
- `/admin` sin sesión → cadena `308 /admin → 307 /admin/ → /login/?redirect=%2Fadmin%2F` (200).
  `middleware.ts` confirmado en raíz, no en `app/`. **OK**.
- Supabase `plants`: **52/52** filas íntegras (`Prefer: count=exact`).
- Las 9 plantas peligrosas: `image_cientifica_url`/`image_mistica_url` en `null`,
  `ficha_verificada:false`, `publicada:false` en las 9. Placeholder intacto. **OK**.
- `leads`: 0 filas. `purchases`: 0 filas.
- `sitemap.ts` y `robots.ts`: presentes, dominio correcto, sitemap con `lastmod` reciente.

## Hallazgo nuevo: imagen rota en planta segura (`lavanda`)
- `plants.image_cientifica_url` de `lavanda` sigue apuntando a
  `/images/plants/lavanda-cientifica.jpg`, pero ese archivo **ya no existe** en
  `public/images/plants/` — confirmado 404 directo en producción y ausencia total del archivo
  en el repo (ni siquiera con nombre parecido).
- Causa: el ciclo del 2026-07-28 17:40 UTC retiró ese archivo en una sesión local de Papu porque
  su contenido real era de cornezuelo, no de lavanda (limpieza de una imagen mal etiquetada). La
  fila de `lavanda` en `plants` **no se actualizó** para reflejar la retirada — quedó una
  referencia colgante que no se había verificado archivo-a-archivo hasta este ciclo (los ciclos
  anteriores solo comprobaban el conteo total de `public/images/plants/`, 71 sin cambio, no si
  cada URL de la tabla resuelve a un archivo real).
- Verificado que es el único caso: de las 43 plantas seguras, 42 tienen `image_cientifica_url`
  válida y existente; solo `lavanda` rota. `image_mistica_url` en `null` en las 43 es normal (campo
  sin uso en el código, ya documentado, no es un hallazgo).
- No se ha tocado el dato — es una escritura en `plants` y, por presunción negativa, se documenta
  y se espera antes de decidir si se anula la URL (mostrar sin imagen, como las 9 peligrosas) o se
  sube una imagen real de lavanda. Candidato a fix trivial y reversible, pero fuera del QA de
  solo-lectura de este ciclo.

## Diccionario / gate `ficha_verificada`
- 43/52 con `ficha_cientifica`, **0/52 verificadas** — sin cambio.
- `olivo` (`id:7`) sigue sirviendo la ficha de cardo mariano (*Silybum marianum*), reconfirmado sin
  cambio — cae en las 25 sin recuperación posible por JSON de origen.
- **Mejora detectada:** `ficha_mistica.afinidad_ayurvedica` ahora poblada en **43/43** plantas
  seguras (antes 41/43, faltaban `manzanilla` y `equinacea`) — probablemente completado en la
  sesión interactiva de recuperación de 18 fichas del 2026-07-29. Esto deja la propuesta de "Tu
  Planta Aliada" (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded
  de las 9 peligrosas) con cobertura de datos completa, sin migración pendiente. Sigue sin
  implementar, pendiente de OK de Papu.
- Duplicados reconfirmados sin cambio: `equinacea`/`echinacea` (misma especie, *Echinacea
  purpurea*) y `ashwagandha`/`ashwagandha-fruto` (fichas distintas a propósito, fruto vs. raíz).
- `blog_posts`: 90 draft / 19 published, sin cambio. Sin generación nueva este ciclo — el gate
  del diccionario sigue en 0/52 verificadas y `agente-plantas.sh` (`qb`) sigue blindado con
  `ficha_verificada=eq.true`.
- Tabla `citas`: sigue sin poder crearse — `PGRST205`, sin canal DDL en el entorno cloud
  (`DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` ausentes), más de 30 ciclos
  consecutivos.
- `npm audit --json`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio.
- `public/images/plants/`: 71 archivos, 29 huérfanos, sin cambio (aparte del hallazgo de
  `lavanda` arriba, que es el caso inverso: referencia sin archivo, no archivo sin referencia).

## Monetización (Paso 2)
- **Checkout Gumroad sigue roto — ~1 día 2h37min (desde 2026-07-28 18:25:20 UTC), sexto ciclo
  consecutivo confirmando el mismo estado.** `NEXT_PUBLIC_GUMROAD_URL` en Vercel (producción,
  `type: sensitive`) sin cambio de `updatedAt`/`updatedBy: null`. CTA en
  `/producto/ritual-descanso` sigue apuntando a `kristian320.gumroad.com/l/ritual-descanso` (404
  con user-agent real); la URL anterior `kristiantronco.gumroad.com/l/ugsqtg` sigue en 200. Sin
  tocar la env var sin OK de Papu.
- Funnel `/regalo/primera-noche` → lead → producto verificado end-to-end: formulario postea a
  `/api/leads`, el PDF de regalo (`primera-noche-tranquila.pdf`) responde 200, y el enlace
  secundario post-descarga apunta a `/producto/ritual-descanso`. **OK, sin cambios.**
- "Tu Planta Aliada": esquema sin cambios de fondo (usa `profiles.dosha` × `ficha_mistica`, sin
  migración), ahora con cobertura de datos completa (43/43, ver arriba). Sin implementar.

## Optimización continua (Paso 5)
1. Funnel A/B: `leads`/`purchases` en 0 filas — sin datos para proponer variantes.
2. "Tu Planta Aliada": mapeo revisado, cobertura de datos ahora 43/43 (mejora desde 41/43). Test
   negativo de las 9 peligrosas: **pasa** (imagen null, `ficha_verificada:false`,
   `publicada:false` en las 9).
3. Cita diaria: tabla `citas` sigue bloqueada, sin poder insertar.
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` existe en Vercel pero apunta a URL rota — no es hito,
   sigue siendo el incidente crítico abierto.

## Borradores sociales (solo bitácora, sin publicar)
**Instagram** — ángulo: un archivo puede desaparecer sin que la base de datos se entere.
> Limpiamos una imagen mal etiquetada del diccionario hace un día. Bien hecho: el archivo
> equivocado se fue. Lo que no se actualizó fue la fila que todavía apunta a él. Ningún conteo de
> "cuántos archivos hay" iba a pescar eso — hacía falta comprobar, planta por planta, si cada
> enlace resuelve a algo real. Lo pescamos hoy, no se ha tocado nada todavía: primero se
> documenta, luego se decide. #transparencia #QH

**LinkedIn** — ángulo: por qué un agente autónomo no se limita a "lo que se puede revertir".
> Llevamos más de un día documentando el mismo checkout roto sin tocarlo, aunque revertir una
> variable de entorno sería un cambio de segundos. La razón no es falta de capacidad: es que
> "reversible" y "autorizado" son dos preguntas distintas, y solo se puede responder la primera
> desde este lado. Antes de actuar sobre un sistema en producción que no diseñamos nosotros, la
> pregunta que importa no es "¿puedo deshacer esto?" sino "¿tengo el contexto completo de por qué
> está así?". A veces la respuesta correcta a un problema trivial es esperar. #QH #autonomía

## Tareas manuales de Papu hoy
1. **Resolver el checkout Gumroad** — roto ~1 día 2h37min, sexto ciclo escalándolo. Revert de la
   env var `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional) o
   confirmar y arreglar la cuenta nueva `kristian320`. Impacto directo en ventas.
2. **Decidir qué hacer con la imagen rota de `lavanda`** (anular `image_cientifica_url` o subir
   imagen real) y con las 25 fichas del diccionario sin recuperación por JSON (incluye `olivo`,
   que muestra datos de cardo mariano).
3. **Rotar la service role key y el token OAuth expuestos** (documentado desde 2026-07-25 y
   2026-07-29), sin confirmación de rotación todavía.

---
*Ciclo cloud automático. Sin commits de código (build pasa, sin fixes necesarios). Bitácora y
memoria commiteadas por el paso dedicado del workflow.*
