# Ciclo cloud — 2026-08-18 12:51 UTC (122º ciclo consecutivo)

## QA — 8/8 OK

1. `npm ci && npm run build`: pasa sin errores ni warnings nuevos.
2. Producción (`https://quantum-holistic.com`): `200`, canonical y `og:url` ==
   `https://quantum-holistic.com/`.
3. Rutas del checklist (siguiendo redirect de trailing slash, `308` normal de Next.js): `/`,
   `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
   `/terapeutas` → todas `200`.
4. `/admin` sin sesión: `308` (trailing slash) → `307` a `/login/?redirect=%2Fadmin%2F` → `200`.
   `middleware.ts` confirmado en la raíz del repo (no en `app/`).
5. Supabase `plants`: 52 filas íntegras. Las 9 plantas peligrosas (aconito, datura, datura-metel,
   amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reverificadas
   fila por fila: `image_cientifica_url` e `image_mistica_url` en `null` en las 9, placeholder
   intacto.
6. `npm audit`: 17 vulnerabilidades (1 low, 4 moderate, 12 high), sin cambio respecto al ciclo
   anterior.
7. `sitemap.ts` y `robots.ts` presentes en `app/`, dominio `https://quantum-holistic.com`
   confirmado en ambos.
8. Tabla `leads`: 0 filas, sin leads nuevos, sin datos para segmentación por dosha este ciclo.

## Checkout Gumroad sigue roto — ~20 días 18h, 122º ciclo consecutivo

Sin cambio desde `2026-07-28T18:25:20.881Z`. CTA real reconfirmado en el HTML servido de
`/producto/ritual-descanso`: `href="https://kristian320.gumroad.com/l/ritual-descanso"` → `404` en
vivo, reconfirmado este ciclo. `https://kristiantronco.gumroad.com/l/ugsqtg` → `200` reconfirmado,
sigue siendo el revert viable. Entrada `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto
`quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) target `production`, tipo `sensitive`
(valor no legible vía API de solo lectura): `updatedAt` sin cambio desde
`2026-07-28T18:25:20.881Z`. Código confirmado correcto —
`app/producto/ritual-descanso/RitualCheckout.tsx` usa `NEXT_PUBLIC_GUMROAD_URL` con fallback
"Disponible muy pronto" + captura de email si la env var faltara; el problema es exclusivamente el
valor configurado en Vercel, no el código. Sin tocar ninguna env var sin OK explícito de Papu.

## Gate `ficha_verificada` y datos sucios del diccionario — sin cambio

- 0/52 plantas verificadas (pendiente desde 2026-07-30 ~02:36 UTC, ~19 días 10h).
- Duplicado `equinacea` (id 52) / `echinacea` (id 21) sin resolver, mismo `nombre_latino`
  (`Echinacea purpurea`), reconfirmado este ciclo.
- `lavanda` (id 11) sigue sirviendo `image_cientifica_url` que devuelve 404 en vivo
  (`/images/plants/lavanda-cientifica.jpg`), reconfirmado.

## Contenido — blog y redes

- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los 19 drafts previamente
  identificados (8 en temas prohibidos, 11 en categoría de contraindicaciones sin advertencias)
  siguen sin publicar ni reescribir. Ningún draft nuevo auditado ni publicado este ciclo.
- Nota de contexto (no accionable este ciclo): el campo `chakra` de `app/fichas-50-valid.json`,
  mostrado en la sección "mística" de `/diccionario/[slug]` junto a elemento/planeta
  regente/afinidad ayurvédica, es contenido de producto ya existente y fuera del alcance del
  checklist anti-pseudociencia del blog (ese checklist aplica a publicación autónoma de posts
  nuevos, no a features de producto ya en producción). Se deja constancia, sin tocar código.
- Sin borradores sociales nuevos este ciclo. Los 2 pendientes de `2026-08-16-0125.md` (IG: "107
  revisiones del mismo semáforo en rojo"; LinkedIn: "tres decisiones de Papu, tres bloqueos
  distintos") siguen sin publicar y no se duplican, para no generar ruido en la bitácora.

## Funnel y "Tu Planta Aliada"

- `/regalo/primera-noche` → lead → producto verificado por código (`RegaloForm.tsx`,
  `RitualCheckout.tsx`, `app/api/leads/route.ts` presentes y coherentes) y rutas `200`, sin
  cambios.
- E2E con escritura real: última vez 2026-08-17 04:46 UTC (~32h05min, bajo el umbral informal de
  48h) — no repetido este ciclo. `leads`: 0 filas, sin datos nuevos para A/B por dosha.
- "Tu Planta Aliada": sin implementación en código, sigue pendiente de OK de Papu sobre el esquema
  propuesto y del gate `ficha_verificada`.

## Cita diaria — insertada

Última cita previa (Francis Bacon, `created_at` 2026-08-17T08:47:20.36Z) cruzó el umbral de 24h
poco después del cierre del ciclo anterior (~08:47 UTC). Se insertó una cita nueva:

> "Una onza de prevención vale una libra de cura." — Benjamin Franklin

Autor de dominio público, sin repetir (verificado contra los 17 autores previos, todos distintos),
tema medicinal-filosófico, pasa el filtro anti-pseudociencia (sin biodescodificación, chakras,
cristales, reiki ni claims de curación). 18 citas en la tabla tras la inserción.

## Resumen de escrituras este ciclo

- Supabase `citas`: 1 fila nueva (Benjamin Franklin), única escritura del ciclo.
- Sin commits de código: build pasa sin fixes necesarios; `next-env.d.ts` regenerado por el build
  se revirtió sin commitear (cambio no funcional).
- Sin escrituras adicionales en `leads` (0 filas) ni en `plants` (gate `ficha_verificada`
  pendiente de decisión de Papu).
- Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy

1. **Arreglar el checkout Gumroad** (20d18h roto, 122 ciclos consecutivos): decidir entre publicar
   el producto en `kristian320.gumroad.com/l/ritual-descanso` (URL actualmente configurada, 404) o
   revertir `NEXT_PUBLIC_GUMROAD_URL` en Vercel (`production`) a
   `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado). Bloquea todo ingreso — máxima
   prioridad, sin cambio desde 2026-07-28.
2. **Decidir el gate `ficha_verificada`** (19d10h pendiente): 0/52 plantas verificadas
   manualmente; bloquea el diccionario y "Tu Planta Aliada".
3. **Resolver el duplicado `equinacea`/`echinacea`** (ids 52/21) y la imagen 404 de `lavanda` —
   limpieza de datos de baja complejidad pero acumulada desde hace semanas.
