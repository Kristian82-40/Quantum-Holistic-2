# Bitácora Kimiko Cloud — 2026-07-26 10:04 UTC (Decimonoveno ciclo)

## QA — 8/8 checks OK

1. **Build**: `npm ci && npm run build` pasa limpio, sin errores. `npm audit --json` volvió a funcionar este ciclo (mismo desglose de siempre: 1 critical/10 high/4 moderate/1 low, dominado por `next@14.2.5`, sin bump) — el `400 Bad Request` del ciclo de las 06:41 UTC en el endpoint legacy (`/-/npm/v1/security/audits/quick`) fue transitorio, no un cambio real de infraestructura; usar `npm audit --json` directamente evita depender de ese endpoint.
2. **Producción**: canonical `https://quantum-holistic.com/` y `og:url` correctos.
3. **Rutas 200**: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` — todas 200 siguiendo redirect (`-L`).
4. **`/admin` sin sesión**: cadena `308 → /admin/` → `307 → /login/?redirect=%2Fadmin%2F` → `200`. Middleware activo (raíz, `ƒ Middleware`, 80.2 kB).
5. **Supabase — 52 plantas íntegras** (`content-range: 0-51/52`).
6. **9 plantas peligrosas**: `image_cientifica_url` e `image_mistica_url` en `null` para las 9 (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) — ficha pública sigue en placeholder para todas.
7. **`leads`/`purchases`**: 0/0 filas, verificado solo por lectura (`Content-Range: */0`) — último test E2E real con escritura fue el mismo día a las 02:54 UTC (~7h10min antes), por debajo del umbral de 24h, no se repite la escritura de prueba.
8. **`sitemap.ts`/`robots.ts`**: presentes, dominio correcto, `Sitemap: https://quantum-holistic.com/sitemap.xml` en robots.txt, `sitemap.xml` → 200.

## Monetización

- **Gumroad**: `NEXT_PUBLIC_GUMROAD_URL` sigue sin existir en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) — 12 env vars totales, ninguna Gumroad. **Decimonoveno ciclo consecutivo** sin este secret. `RitualCheckout.tsx` mantiene el fallback correcto (captura de email vía `/api/leads` cuando la var no está) — código reconfirmado línea por línea este ciclo, sin cambios.
- **Funnel `/regalo/primera-noche` → `/producto/ritual-descanso`**: sin cambios, sano (último test real 02:54 UTC, dentro del umbral de 24h).
- **"Tu Planta Aliada"**: sigue sin implementar. Cobertura de `ficha_mistica.afinidad_ayurvedica` en plantas seguras: 41/43, sin cambio. Propuesta de esquema sin cambios (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin migración necesaria). Pendiente de OK explícito de Papu.

## Datos — chequeos recurrentes (sin hallazgos nuevos)

- **Duplicados por `nombre_latino`**: `equinacea`/`echinacea` (real, pendiente Papu) y `ashwagandha`/`ashwagandha-fruto` (descartado). Sin cambios.
- **43 plantas seguras**: 73 archivos totales en `public/images/plants/` (43 seguras + 29 duplicados/variantes + 1 candidato de planta peligrosa) — mismo conteo que el ciclo anterior, sin archivos nuevos.
- **Candidato `plant-00-beleno-negro-cientifica.jpg`**: sigue sin renombrar, sin wireado a la DB (`image_cientifica_url` de `beleno-negro` sigue `null`), sigue accesible por URL directa. Sin cambios desde el hallazgo del ciclo de las 06:41 UTC (recomendación de no aprobar por posible error de identificación botánica). No se tocó el archivo.
- **Backlog `blog_posts`**: **90 drafts / 19 published**, sin cambio — decimonoveno ciclo consecutivo con el mismo pendiente crónico.
- **Tabla `citas`**: sigue sin existir (`404 PGRST205`) — bloqueada por falta de canal DDL, mismo bloqueador desde 2026-07-23 16:50 UTC (19 ciclos consecutivos). Sin `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN`.

## Contenido

- **Blog**: sin borrador nuevo — backlog de 90 drafts sin revisar por Papu sigue siendo la razón para no generar más.
- **Borradores sociales** (NO publicados, solo bitácora):

  **Instagram** — ángulo: por qué el catálogo no crece más rápido
  > 52 plantas en el diccionario, no 500. Podríamos añadir más rápido, pero cada ficha nueva pasa por el mismo filtro que las 9 que no muestran foto: si no estamos seguros, no publicamos. Preferimos un catálogo más pequeño y confiable. 🌿
  > *(sin claims de curación, sin dato interno expuesto)*

  **LinkedIn** — ángulo: una función técnicamente lista que sigue sin lanzarse
  > "Tu Planta Aliada" (recomendación de planta según dosha) está lista a nivel técnico desde hace varios ciclos. No la hemos activado porque falta una aprobación explícita del criterio editorial detrás del cruce de datos. A veces lo más rápido de construir no es lo más rápido de lanzar, y está bien que sea así.
  > *(sin cifras internas, sin mencionar el hallazgo de ciclos anteriores)*

## Higiene operativa

- Sin volcado de `env` en bloque este ciclo — todas las comprobaciones de secrets se hicieron variable por variable con `[ -n "$VAR" ]`. Sin incidentes nuevos.
- Token OAuth expuesto en ciclos anteriores (2026-07-25, 06:22 y 09:52 UTC) sigue sin confirmación de rotación — escalado de nuevo como prioridad #1.

## Tareas manuales de Papu hoy

1. **Rotar el token OAuth de Claude Code expuesto el 2026-07-25** (06:22 UTC y re-expuesto 09:52 UTC, `claude_code_oauth_token`, prefijo `sk-ant-oat01-...`) — sigue sin confirmación de rotación. Prioridad máxima mientras el token siga vigente.
2. **Decidir sobre el candidato de imagen `plant-00-beleno-negro-cientifica.jpg`** (`public/images/plants/`, catalogado en `data/dangerous-plants.json`): recomendación de ciclos anteriores (revisión visual) sigue siendo no aprobarla — la ilustración no parece corresponder botánicamente a *Hyoscyamus niger*. Si se decide descartarla, lo más limpio es borrarla del repo en vez de dejarla huérfana y accesible por URL directa.
3. **Cuenta Gumroad**: generar `NEXT_PUBLIC_GUMROAD_URL` y añadirla a Vercel (proyecto `quantum-holistic-2`) — único bloqueador del primer cobro (€19, ritual-descanso). 19 ciclos consecutivos sin este secret.

## Cierre

- Sin commits de código este ciclo (build limpio, nada que arreglar). Bitácora y memoria las commitea el paso dedicado del workflow.
