# Bitácora Kimiko — 2026-08-21 01:23 UTC (ciclo 136)

## QA (8/8 OK)

1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipos.
2. Producción: canonical y `og:url` en `https://quantum-holistic.com` == `https://quantum-holistic.com/`. OK.
3. Rutas del checklist: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → 200 tras 308 (trailingSlash, esperado). OK.
4. `/admin` sin sesión → 308 → `/login/?redirect=%2Fadmin%2F` → 200. `middleware.ts` confirmado solo en la raíz (no existe copia en `app/`). OK.
5. Supabase `plants`: 52 filas íntegras (`content-range: 0-51/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, verificado fila por fila. OK.
6. `sitemap.xml` (200) y `robots.txt` presentes con dominio correcto (`Sitemap: https://quantum-holistic.com/sitemap.xml`). OK.
7. `npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio respecto al ciclo anterior. No crítico para el QA funcional, queda anotado.
8. Leads: tabla `leads` en 0 filas (`content-range: */0`), sin cambio.

## Hallazgos (sin cambio respecto a ciclos previos)

- **Checkout Gumroad sigue roto**, ~23 días 7h, 136º ciclo consecutivo desde 2026-07-28T18:25:20.881Z.
  El HTML servido de `/producto/ritual-descanso` sirve el CTA real vía `RitualCheckout.tsx`
  (`process.env.NEXT_PUBLIC_GUMROAD_URL`), que apunta en vivo a
  `kristian320.gumroad.com/l/ritual-descanso` (**404 confirmado**). El revert viable
  `kristiantronco.gumroad.com/l/ugsqtg` sigue devolviendo **200**. La entrada de
  `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, target `production`, tipo
  `sensitive`) no ha cambiado (`updatedAt` = 1785263120881 ms, es decir 2026-07-28T18:25:20.881Z),
  reconfirmado vía API de solo lectura. No se toca la env var sin OK explícito de Papu.
- Gate `ficha_verificada`: 0/52, sin cambio (pendiente desde 2026-07-30 ~02:36 UTC, ~21 días 22h).
- Duplicado en `plants`: `equinacea` (id 52, `nombre_latino` "Echinacea purpurea") / `echinacea`
  (id 21, mismo `nombre_latino`), sin cambio.
- `lavanda` (id 11) tiene `image_cientifica_url` seteada a
  `/images/plants/lavanda-cientifica.jpg` pero el archivo devuelve **404 en vivo**. Sin cambio.
- `blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los 19 drafts previamente
  identificados (8 en temas prohibidos — biodescodificación, nutrición cuántica, cristales, reiki,
  chakras — y 11 en categoría de contraindicaciones sin advertencia completa) siguen sin publicar
  ni reescribir, correcto por presunción negativa.

## Monetización

- `/producto/ritual-descanso`: CTA correctamente condicionado a `NEXT_PUBLIC_GUMROAD_URL` en
  código (fallback a "disponible muy pronto" si la env var faltara, no es el caso — la env var
  existe pero apunta a un enlace roto). El problema es de contenido de la variable, no de código.
- Funnel `/regalo/primera-noche` → lead → producto: verificado por código, sin cambios, rutas 200.
- "Tu Planta Aliada": confirmado por grep que sigue sin implementar (`app/`, `components/`, `lib/`
  sin resultados para `planta.?aliada`). Pendiente de OK de Papu antes de proponer esquema/migración.
- A/B de CTAs: `leads` sigue en 0 filas, sin datos suficientes para proponer variantes.

## Cita diaria

Última cita en tabla (Arthur Schopenhauer, insertada 2026-08-20T12:54:42 UTC) tiene ~12h29min de
antigüedad al arrancar este ciclo, por debajo del umbral de 24h → **sin inserción nueva este
ciclo**. 20 citas en la tabla, todas de autores distintos.

## Borradores sociales

Sin borradores nuevos este ciclo. Los 2 pendientes de `kimiko/bitacora/2026-08-16-0125.md` siguen
sin publicar (publicación en redes prohibida para Kimiko); no se duplican en esta bitácora para
evitar ruido.

## Commits

Ninguno. Build pasa sin fixes necesarios. `next-env.d.ts` fue regenerado por el build y revertido
sin commitear (cambio no funcional, `git checkout -- next-env.d.ts`). Sin escrituras nuevas en
Supabase este ciclo (cita bajo el umbral de 24h, leads en 0, sin datos que segmentar).

## Tareas manuales de Papu hoy

1. **Arreglar el checkout de Gumroad** (bloqueante de monetización, 23 días 7h roto): el CTA en
   vivo apunta a `kristian320.gumroad.com/l/ritual-descanso` (404). Cambiar
   `NEXT_PUBLIC_GUMROAD_URL` en Vercel (producción) a `https://kristiantronco.gumroad.com/l/ugsqtg`
   (200 confirmado), o publicar el producto correcto en la cuenta `kristian320` y actualizar el
   enlace en consecuencia.
2. **Decidir el gate `ficha_verificada`** (0/52 desde hace 21 días 22h): confirmar si el criterio
   de verificación manual sigue vigente o si se puede automatizar/relajar para destrabar contenido
   del diccionario.
3. **Revisar y fusionar el duplicado `equinacea`/`echinacea`** (ids 52/21) y subir la imagen que
   falta de `lavanda` (`/images/plants/lavanda-cientifica.jpg` da 404 en vivo).
