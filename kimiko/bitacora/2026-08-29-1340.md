# Ciclo cloud — 2026-08-29 13:40 UTC (178º ciclo)

## Salud del sitio
- `npm ci && npm run build`: pasa sin fixes (corrido desde la raíz).
- Rutas del checklist (8/8) en 200: `/`, `/diccionario`, `/blog`, `/regalo/primera-noche`,
  `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
- `/admin` sin sesión → 307 a `/login/?redirect=%2Fadmin%2F`. `middleware.ts` confirmado en la raíz
  (no existe `app/middleware.ts`).
- `canonical` y `og:url` en `/` apuntan a `https://quantum-holistic.com/`. `sitemap.xml` y
  `robots.txt` responden 200 con el dominio correcto.
- `npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio respecto al ciclo
  anterior.
- Cambio no funcional de `next-env.d.ts` regenerado por el build, revertido sin commitear.

## Auditoría de imágenes — las 6 pendientes del ciclo 177 (límite de 6/ciclo agotado)
Se auditaron a fondo `hinojo`, `jengibre`, `sauco`, `tomillo`, `tribulus`, `tulsi` — las 6 que
quedaban de las 12 fichas con texto ya verificado en el ciclo 176. Texto (`familia_botanica` +
`principios_activos` contra `nombre_latino`) confirmado coherente en las 6 antes de mirar imagen.

**Resultado: 5 de 6 imágenes mal emparejadas, solo `hinojo` correcta.**

- `hinojo-cientifica.jpg`: lámina histórica etiquetada "Gebräuchlicher Fenchel" (hinojo común, alemán)
  — correcta, coincide con *Foeniculum vulgare*.
- `tulsi-cientifica.jpg`: flor de loto (*Nelumbo nucifera*), no albahaca sagrada.
- `sauco-cientifica.jpg`: árbol con frutos redondos pegados al tronco (aspecto de amla/*Phyllanthus
  emblica*), no *Sambucus nigra* (arbusto de racimos de bayas).
- `tomillo-cientifica.jpg`: planta rastrera de hoja carnosa junto al agua (aspecto de *Bacopa
  monnieri*/brahmi), no *Thymus vulgaris* (arbusto leñoso mediterráneo).
- `jengibre-cientifica.jpg`: abedul (corteza blanca, amentos), no *Zingiber officinale*.
- `tribulus-cientifica.jpg`: albahaca sagrada real (espigas de flor púrpura, entorno de templo) —
  de hecho es la imagen que le correspondería a `tulsi`, no a *Tribulus terrestris*.

Ninguna de las 5 imágenes intrusas corresponde a las 9 plantas peligrosas (a diferencia del
hallazgo de Datura del ciclo 177). Aun así, el patrón de fallo (5/6, tras 5/6 del ciclo 175 y
25/37 de texto del ciclo 176) sigue apuntando a un problema sistémico en el proceso de carga de
`plants`, no a errores aislados fila a fila.

**Acción:** las 5 mal emparejadas bajadas en Supabase (`publicada=false, ficha_verificada=false`):
`tulsi`, `sauco`, `tomillo`, `jengibre`, `tribulus`. Verificado en vivo: las 5 devuelven 404 en
`/diccionario/<slug>/`, `hinojo` sigue en 200, y el listado de `/diccionario/` muestra ahora
5 `href` (`albahaca`, `arnica`, `equinacea`, `hinojo`, `lavanda`), coincidiendo con las 5 filas
`publicada=true` restantes (antes 10).

## Gate `ficha_verificada` tras el ciclo
- **5 publicadas / 4 verificadas**: `albahaca`, `arnica`, `equinacea`, `hinojo` verificadas;
  `lavanda` sigue publicada pero sin verificar (imagen `lavanda-cientifica.jpg` ausente en
  `public/images/plants/`, caso ya documentado desde el ciclo 175).
- **Con esto se agota la lista completa de las 37 fichas que llegaron con `ficha_verificada=true`
  al inicio del ciclo 175** (fuera de Kimiko, sin commit asociado): 25 bajadas por texto (ciclo
  176), 8 bajadas por imagen (5 del ciclo 175 + `aloe-vera`/`brahmi`/`ginseng` del ciclo 177) +
  5 de este ciclo = 13 por imagen en total, quedan 4 verificadas y correctas
  (`albahaca`, `arnica`, `equinacea`, `hinojo`). No quedan fichas de esa remesa pendientes de
  auditar — cualquier `ficha_verificada=true` futura será obra de un ciclo de Kimiko o de Papu,
  y en ambos casos debe tratarse con la misma prudencia (ver check permanente del ciclo 175).

## 9 plantas peligrosas
Reconfirmadas las 9 (`aconito`, `datura`, `datura-metel`, `amanita-muscaria`, `cannabis`,
`cornezuelo-centeno`, `beleno-negro`, `tejo`, `hierba-mora`) con `publicada=false`,
`ficha_verificada=false`, `image_cientifica_url`/`image_mistica_url` en `null`. Sin cambio.

## Negocio y contenido
- **Gumroad sigue roto**, ~32 días. CTA real en `/producto/ritual-descanso/` sigue apuntando a
  `kristian320.gumroad.com/l/ritual-descanso` (404 en vivo); `kristiantronco.gumroad.com/l/ugsqtg`
  (200 confirmado) sigue siendo el revert viable. Sin tocar la env var sin OK de Papu.
- `leads` en 0 filas, sin datos para A/B.
- **Cita diaria**: la última (John Locke, 10:09:53 UTC del 08-28) superó el umbral de 24h
  (~27h30min de antigüedad al arrancar el ciclo) — se insertó una nueva: Marco Aurelio
  (Meditaciones, Libro VII), "Muy poca cosa se necesita para hacer una vida feliz; todo está
  dentro de ti mismo, en tu manera de pensar." (dominio público, autor no repetido en las últimas
  15 citas, sin claim de salud, pasa el filtro anti-pseudociencia). 28 citas en la tabla `citas`
  tras la inserción (antes 27).
- `blog_posts`: 90 draft / 19 published (109 total), sin cambio, sin contenido nuevo (la
  auditoría de imágenes consumió el ciclo). Post con lenguaje de "curación" (Aceite de Oliva
  Virgen Extra) sigue publicado, pendiente de revisión de Papu, sin cambio.
- Sin borradores sociales nuevos este ciclo (los 2 pendientes de `2026-08-16-0125.md` siguen sin
  publicar, no se duplican para evitar ruido en la bitácora).

## MODO ORDEN
`kimiko_drafts` con 0 filas `pendiente`/`en_curso` al arrancar (evento `schedule`, no
`repository_dispatch`) — sin órdenes que procesar este ciclo.

## Escrituras en Supabase este ciclo
- `plants`: 5 filas actualizadas (`tulsi`, `sauco`, `tomillo`, `jengibre`, `tribulus`) →
  `publicada=false, ficha_verificada=false`.
- `citas`: 1 inserción (Marco Aurelio, id `0ee0b9a4-9475-4dc8-ba3c-140728d9432e`).
- Sin commits de código este ciclo (build pasa sin fixes necesarios).

## Tareas manuales de Kristian hoy
1. **Gumroad sigue roto, ~32 días.** Revertir `NEXT_PUBLIC_GUMROAD_URL` en Vercel a
   `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado) o corregir el enlace
   `kristian320.gumroad.com/l/ritual-descanso` (404) — checkout inoperativo todo este tiempo.
2. **Revisar el origen del cruce planta↔contenido en la tabla `plants`.** Tres ciclos seguidos
   (175, 176, 177, 178) han encontrado fallos sistemáticos: 5/6, 25/37, 3/6 y ahora 5/6 de las
   fichas auditadas tenían imagen y/o texto de otra especie, incluyendo un caso de planta
   peligrosa camuflada (Datura bajo el nombre de Aloe Vera, ciclo 177). El patrón indica un bug
   de asignación en la carga/generación masiva original, no errores aislados — merece revisión
   del proceso, no solo seguir auditando fila a fila.
3. **Duplicado `equinacea`/`echinacea`** (ids 52/21) sigue sin resolver desde antes del ciclo 176 —
   decidir cuál eliminar o fusionar.
