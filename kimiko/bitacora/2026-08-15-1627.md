# Ciclo cloud — 2026-08-15 16:27 UTC (105º ciclo)

## QA (8/8 OK)
1. Build: `npm ci && npm run build` pasa sin errores ni warnings de tipos.
2. Producción: canonical y og:url de `/` == `https://quantum-holistic.com/`, correcto.
3. Rutas 200 (siguiendo redirect de locale/trailing-slash, comportamiento esperado de next-intl):
   `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
   `/terapeutas` — las 7 en 200.
4. `/admin` sin sesión → 308 a `/admin/` → 307 a `/login/?redirect=%2Fadmin%2F` → 200. Cadena de
   redirect intacta, `middleware.ts` sigue en la raíz del repo (no en `app/`).
5. Supabase: 52 plantas en `plants` (`content-range: 0-0/52` con `count=exact`), íntegras.
6. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis,
   cornezuelo-centeno, beleno-negro, tejo, hierba-mora) reverificadas fila por fila: las 9 con
   `image_cientifica_url` y `image_mistica_url` en `null`. Ninguna imagen activada.
7. `leads`: 0 filas, sin cambio. Sin conversiones ni segmentación por dosha que reportar.
8. `sitemap.xml` (200) y `robots.txt` presentes, dominio `https://quantum-holistic.com` correcto
   en ambos.

`npm audit`: 17 vulns (1 low / 4 moderate / 12 high / 0 critical), sin cambio frente al ciclo
anterior.

## Monetización

**Checkout Gumroad sigue roto** — 17 días 22h2min, **centésimo quinto ciclo consecutivo** desde
2026-07-28T18:25:20.881Z. CTA real reconfirmado en el HTML servido de `/producto/ritual-descanso/`:
apunta a `kristian320.gumroad.com/l/ritual-descanso`, **404 en vivo** reconfirmado. La URL anterior,
`kristiantronco.gumroad.com/l/ugsqtg`, sigue en **200** — revert viable, no aplicado. Ambas entradas
de `NEXT_PUBLIC_GUMROAD_URL` en Vercel (`production` id `L6v4bSqxUSFYv5U5`, `development` id
`vf27pDQT5ZT5iBel`) reconfirmadas vía API sin cambio: `updatedAt` de `production` sigue en
2026-07-28T18:25:20.881Z. No se ha tocado ninguna env var sin OK de Papu — sigue siendo una decisión
de negocio (posible migración de cuenta Gumroad a medias), no un bug de código.

Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado por código
(formulario de captura de email presente en el fallback de `RitualCheckout.tsx`) y rutas 200, sin
cambios estructurales. Test E2E con escritura real más reciente sigue siendo el del 2026-08-14
20:35 UTC (~19h53min de antigüedad, por debajo del umbral informal de 48h) — no se repite este
ciclo. `leads` en 0 filas.

"Tu Planta Aliada" sigue sin implementar en código; propuesta de esquema sin cambio, pendiente de
OK de Papu.

## Gate del diccionario y contenido

`ficha_verificada`: 0/52, sin cambio (pendiente de decisión de Papu desde 2026-07-30 02:36 UTC,
~16 días 13h52min). `lavanda` (`/images/plants/lavanda-cientifica.jpg` → 404 reconfirmado en vivo
este ciclo) y duplicado `equinacea`/`echinacea` (mismo `nombre_latino`, `Echinacea purpurea`, ids
52 y 21) reconfirmados sin cambio.

`blog_posts`: 90 draft / 19 published (109 total), sin cambio. Los ~80 drafts restantes siguen
pendientes de revisión de contraindicaciones fila por fila, nunca hecha por completo; por
presunción negativa, siguen sin publicarse. Sin publicaciones nuevas este ciclo.

Revisión de imágenes de "Tu Planta Aliada": las 43 plantas seguras tienen `image_cientifica_url`
no nulo (salvo la excepción ya conocida de `lavanda`, con URL no nula pero 404 en vivo — no
computa como null, pero tampoco sirve). Test negativo repetido: las 9 peligrosas confirmadas con
ambas imágenes en `null`.

## Cita diaria

Última inserción (Sushruta, 08:32:49 UTC del 08-15) tiene ~7h55min de antigüedad al arrancar el
ciclo — por debajo del umbral de 24h. Sin inserción nueva este ciclo. 15 citas en la tabla, todas
de autores distintos.

## Borradores sociales (sin publicar)

1. **Instagram** — ángulo "105 ciclos después, la métrica que no cambia": sobre la diferencia
   entre monitoreo activo y monitoreo que ya confirmó lo que necesitaba confirmar; sugiere que a
   partir de cierto punto reconfirmar cada 4h deja de generar información nueva, aunque el proceso
   siga siendo correcto.
2. **LinkedIn** — ángulo "cuándo un agente debería dejar de preguntar lo mismo": sobre la tensión
   entre disciplina de verificación (nunca asumir que algo sigue OK) y ruido operativo (reportar lo
   mismo 105 veces); propone que la solución no es que el agente decida solo, sino que el propio
   checklist reconozca "sin cambio desde hace N ciclos" como una categoría distinta de "verificado
   este ciclo", sin dejar de verificar.

## Tareas manuales de Papu hoy

1. **Arreglar el checkout Gumroad** (17d22h roto, 105 ciclos): decidir entre publicar el producto
   en `kristian320.gumroad.com/l/ritual-descanso` o revertir `NEXT_PUBLIC_GUMROAD_URL` en Vercel
   (`production`, id `L6v4bSqxUSFYv5U5`) a `kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado).
   Bloquea todo ingreso — es el ítem de mayor prioridad, sin cambio desde 2026-07-28.
2. **Decidir el gate `ficha_verificada`** (16d13h pendiente): 0/52 plantas verificadas manualmente;
   sin esa decisión, el diccionario no puede activar fichas ni "Tu Planta Aliada" no puede usar
   `plants` como fuente completa.
3. **Arreglar imagen de `lavanda`** (`/images/plants/lavanda-cientifica.jpg` → 404) y decidir qué
   hacer con el duplicado `equinacea`/`echinacea` (ids 52 y 21, mismo `nombre_latino`) — ambos de
   corrección rápida pero fuera del alcance de fixes automáticos sin más contexto sobre cuál id es
   el correcto a conservar.

## Notas de cierre

Sin commits de código este ciclo (build pasa, QA limpio, sin fixes necesarios); `next-env.d.ts`
regenerado por el build se revirtió sin commitear (cambio no funcional). Sin escrituras nuevas en
Supabase este ciclo (cita por debajo del umbral de 24h, funnel probado hace ~19h53min, sin leads
que segmentar). Bitácora y memoria las commitea el paso dedicado del workflow.
