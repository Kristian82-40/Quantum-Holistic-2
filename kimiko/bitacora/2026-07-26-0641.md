# Bitácora Kimiko Cloud — 2026-07-26 06:41 UTC (Decimoctavo ciclo)

## QA — 8/8 checks OK

1. **Build**: `npm ci && npm run build` pasa limpio, sin errores. `npm audit` esta vez devolvió `400 Bad Request` — el endpoint legacy de auditoría (`/-/npm/v1/security/audits/quick`) está en proceso de retirada según el propio aviso de npm ("This endpoint is being retired. Use the bulk advisory endpoint instead"). No se pudo confirmar el desglose exacto de vulnerabilidades este ciclo por este cambio de infraestructura de npm, no por un cambio real en el proyecto — sigue dominado por `next@14.2.5`, sin bump este ciclo.
2. **Producción**: canonical `https://quantum-holistic.com/` y `og:url` correctos.
3. **Rutas 200**: `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` — todas responden 308 (trailing slash) → 200 siguiendo el redirect, sin cambios de comportamiento.
4. **`/admin` sin sesión**: cadena `308 → /admin/` → `307 → /login/?redirect=%2Fadmin%2F` → `200`. Middleware activo (`ƒ Middleware`, 80.2 kB, en el output del build).
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`).
6. **9 plantas peligrosas** (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora): `image_cientifica_url = null` y `image_mistica_url = null` en las 9 — la ficha pública sigue mostrando placeholder para todas. **Ver hallazgo nuevo abajo** sobre un archivo candidato fuera de la DB.
7. **`leads`/`purchases`**: 0/0 filas, verificado solo por lectura (`Content-Range: */0`) — último test E2E real con escritura fue el mismo ciclo de las 02:54 UTC (~3h47min antes), por debajo del umbral de 24h, no se repite la escritura de prueba.
8. **`sitemap.ts`/`robots.ts`**: presentes, dominio correcto, `Sitemap: https://quantum-holistic.com/sitemap.xml` en robots.txt, `sitemap.xml` → 200.

## Hallazgo nuevo — candidato de imagen para planta peligrosa fuera de la DB (sin activar, revisión recomendada)

- `public/images/plants/plant-00-beleno-negro-cientifica.jpg` existe en el repo y es un archivo estático servido por Next.js — **confirmado accesible en producción, `200`** en `https://quantum-holistic.com/images/plants/plant-00-beleno-negro-cientifica.jpg`. No está wireado a la app: `plants.image_cientifica_url` para `beleno-negro` sigue `null` en Supabase, y el nombre de archivo no sigue el patrón `{slug}-cientifica.jpg` que usa el código, así que **la ficha del diccionario sigue mostrando el placeholder** — no es una "activación" en el sentido de la UI. Está catalogado en `data/dangerous-plants.json` (nuevo en el repo), que documenta la política explícita: *"NO generar a ciegas. Requieren aprobación visual manual antes de renombrar/publicar"* — el resto de las 8 plantas peligrosas tiene `source: null` (sin archivo), solo esta tiene un candidato generado (etiquetado "SuperGrok").
- **Revisé la imagen visualmente este ciclo** (Read tool, no requiere DDL ni credenciales) y hay un problema de fondo, no solo de proceso: la ilustración muestra una planta de espigas florales moradas/blancas tipo labiada (hojas dentadas opuestas, tallo aparentemente cuadrado, flores en verticilos apilados) — morfología consistente con *Salvia* o afín (Lamiaceae), **no con *Hyoscyamus niger*** (beleño negro real: flores acampanadas amarillo-crema con venas púrpura oscuro muy características, hojas grandes vellosas y pegajosas, sin espigas verticiladas). Es decir, **el candidato parece botánicamente incorrecto para la especie que pretende ilustrar**, más allá de si se aprueba o no publicarlo.
- **Riesgo si se aprobara sin corregir esto**: un lector podría usar esta imagen para "reconocer" beleño negro en el campo y equivocarse de planta — con una especie tóxica, el error de identificación visual es justo el tipo de daño que el placeholder busca evitar. No es una exposición de datos, es un riesgo de contenido.
- **No tomé ninguna acción sobre el archivo** (no lo renombré, no lo wireé a la DB, no lo borré) — la propia política del archivo pide aprobación visual manual, y borrar/mover algo que otro ciclo dejó en curso no me corresponde sin indicación. Queda documentado como hallazgo y como tarea de Papu abajo.

## Monetización

- **Gumroad**: `NEXT_PUBLIC_GUMROAD_URL` sigue sin existir en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) — 12 env vars totales, ninguna Gumroad. **Decimoctavo ciclo consecutivo** sin este secret. `RitualCheckout.tsx` mantiene el fallback correcto (captura de email vía `/api/leads` cuando la var no está).
- **Funnel `/regalo/primera-noche` → `/producto/ritual-descanso`**: sin cambios, sano (último test real 02:54 UTC, dentro del umbral).
- **"Tu Planta Aliada"**: sigue sin implementar. Propuesta de esquema sin cambios (cruce `profiles.dosha` × `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin migración necesaria). Cobertura: 41/43 plantas seguras. Pendiente de OK explícito de Papu.

## Datos — chequeos recurrentes (sin hallazgos nuevos salvo lo anterior)

- **Duplicados por `nombre_latino`**: `equinacea`/`echinacea` (real, pendiente Papu) y `ashwagandha`/`ashwagandha-fruto` (descartado). Sin cambios.
- **43 plantas seguras**: las 43 con imagen válida y nombre de archivo correcto en `public/images/plants/` (73 archivos totales: 43 seguras + 29 duplicados/variantes de las mismas + 1 candidato de planta peligrosa, ver hallazgo arriba).
- **Backlog `blog_posts`**: **90 drafts / 19 published**, sin cambio — decimoctavo ciclo consecutivo con el mismo pendiente crónico.
- **Tabla `citas`**: sigue sin existir (`404 PGRST205`) — bloqueada por falta de canal DDL, mismo bloqueador desde 2026-07-23 16:50 UTC (18 ciclos consecutivos). Sin `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN`.

## Contenido

- **Blog**: sin borrador nuevo — backlog de 90 drafts sin revisar por Papu sigue siendo la razón para no generar más.
- **Borradores sociales** (NO publicados, solo bitácora):

  **Instagram** — ángulo: actualización de fichas ante nueva evidencia
  > Las fichas del diccionario no son estáticas: cuando aparece evidencia nueva (o mejor evidencia) sobre una planta, la ficha se actualiza. Preferimos corregirnos en público a quedarnos con la primera versión por comodidad. 🌿
  > *(sin claims de curación, sin dato interno expuesto)*

  **LinkedIn** — ángulo: qué se automatiza y qué espera aprobación humana
  > No todo lo que se puede automatizar se automatiza. Contenido, imágenes de plantas y decisiones de producto pasan por una capa de revisión humana antes de publicarse — más lento, pero es la diferencia entre un catálogo confiable y uno que solo parece serlo.
  > *(sin cifras internas, sin mencionar el hallazgo de este ciclo)*

## Higiene operativa

- Sin volcado de `env` en bloque este ciclo — todas las comprobaciones de secrets se hicieron variable por variable con `[ -n "$VAR" ]`. Sin incidentes nuevos.
- Token OAuth expuesto en ciclos anteriores (2026-07-25, 06:22 y 09:52 UTC) sigue sin confirmación de rotación en la memoria — escalado de nuevo como prioridad #1.

## Tareas manuales de Papu hoy

1. **Rotar el token OAuth de Claude Code expuesto el 2026-07-25** (06:22 UTC y re-expuesto 09:52 UTC, `claude_code_oauth_token`, prefijo `sk-ant-oat01-...`) — sigue sin confirmación de rotación. Prioridad máxima mientras el token siga vigente.
2. **Revisar el candidato de imagen `plant-00-beleno-negro-cientifica.jpg`** (`public/images/plants/`, catalogado en `data/dangerous-plants.json`): mi lectura visual este ciclo sugiere que la ilustración generada no corresponde botánicamente a *Hyoscyamus niger* (parece una labiada tipo Salvia, no beleño negro) — recomiendo **no aprobarla** y regenerar o descartar, en vez de renombrarla al patrón `{slug}-cientifica.jpg`. Mientras tanto el archivo ya es accesible por URL directa aunque no esté wireado a la ficha — si se decide descartarla, lo más limpio es borrarla del repo en vez de dejarla huérfana.
3. **Cuenta Gumroad**: generar `NEXT_PUBLIC_GUMROAD_URL` y añadirla a Vercel (proyecto `quantum-holistic-2`) — único bloqueador del primer cobro (€19, ritual-descanso). 18 ciclos consecutivos sin este secret.

## Cierre

- Sin commits de código este ciclo (build limpio, nada que arreglar; no toqué el archivo de imagen del hallazgo). Bitácora y memoria las commitea el paso dedicado del workflow.
