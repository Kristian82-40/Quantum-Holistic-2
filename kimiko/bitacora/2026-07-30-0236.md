# Bitácora Kimiko — Ciclo cloud 2026-07-30 02:36 UTC

## QA (Paso 1)
- Build local (`npm ci && npm run build`): **OK**, sin errores ni warnings nuevos.
- Producción: canonical y `og:url` == `https://quantum-holistic.com`. **OK**.
- Rutas 200 (siguiendo redirects de trailing slash, todos 308→200): `/`, `/diccionario`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`.
  **7/7 OK**.
- `/admin` sin sesión → cadena `308 /admin → 307 /admin/ → /login/?redirect=%2Fadmin%2F` (200).
  `middleware.ts` confirmado en raíz, no en `app/`. **OK**.
- Supabase `plants`: **52/52** filas íntegras (`Prefer: count=exact`).
- Las 9 plantas peligrosas: `image_cientifica_url`/`image_mistica_url` en `null`,
  `ficha_verificada:false`, `publicada:false` en las 9. Placeholder intacto. **OK**.
- `leads`: 0 filas. `purchases`: 0 filas.
- `sitemap.ts` y `robots.ts`: presentes, dominio correcto, sitemap con `lastmod` reciente.

## Hallazgo nuevo y significativo: contenido místico sin gate + identidad ajena en vivo (`ashwagandha-fruto`)

- **El gate `ficha_verificada` solo protege `ficha_cientifica`, nunca `ficha_mistica`.**
  Confirmado leyendo el código, no solo infiriendo del HTML: `app/diccionario/[slug]/page.tsx`
  línea 91 asigna `fc = plant.ficha_verificada ? plant.ficha_cientifica : null`, pero línea 92
  asigna `fm = plant.ficha_mistica` sin condición alguna. El campo `publicada` no se usa en
  ninguna parte del código de listado ni de detalle — es una columna vestigial. Esto significa
  que la sección "Tradición & Sabiduría Ancestral" (elemento, chakra, planeta, energía, dosha,
  simbolismo) se sirve en producción para las 52 fichas sin pasar por ninguna revisión, mientras
  todo el ciclo se ha estado verificando (correctamente) solo el lado científico.
- **Consecuencia real, no hipotética:** aplicando el mismo cruce que destapó `olivo`/cardo
  mariano (comparar `nombre_latino` actual contra `app/fichas-50-valid.json` por `id`, esta vez
  cotejando género→familia botánica en las 43 plantas seguras), apareció un segundo caso:
  `ashwagandha-fruto` (`id:36`, `nombre_latino: Withania somnifera`) tiene `ficha_cientifica` y
  `ficha_mistica` **idénticas palabra por palabra** a la entrada `id:36` del JSON de origen, que
  es **Saúco** (*Sambucus nigra*, familia Adoxaceae — "Flores y bayas", "Resfriados, Gripe,
  Fiebre, Sinusitis", "Árbol de la protección... tradiciones europeas", chakra Corazón, planeta
  Venus, afinidad Kapha y Pitta). No existe ninguna entrada de "ashwagandha fruto" en el dataset
  de origen: las únicas 50 fichas originales tienen un solo Withania somnifera (`id:4`, que
  corresponde al `ashwagandha` actual, ya recuperado). **La nota de memoria del 2026-07-24 que
  describía `ashwagandha-fruto` como "ficha distinta a propósito, fruto vs. raíz" queda corregida
  por este hallazgo: no hay base en el origen para esa distinción — es el mismo bug de identidad
  por `id` que ya se documentó en `olivo`, no una ficha deliberada.**
- **Diferencia crítica con `olivo`:** esta fila tiene `publicada:true` (aunque ese campo no
  gatea nada) y, al no estar protegida por el gate de `ficha_verificada`, su `ficha_mistica` se
  sirve ahora mismo en `https://quantum-holistic.com/diccionario/ashwagandha-fruto` — verificado
  extrayendo el texto renderizado: "Elemento Agua · Chakra Corazón · Planeta Venus · Energía
  Protectora y purificadora · Dosha Kapha y Pitta · Simbolismo: Árbol de la protección..." bajo el
  nombre "Ashwagandha en Fruto". Es contenido de otra especie, visible a cualquier visitante,
  desde que se creó la fila (`created_at: 2026-04-29`). El caso de `olivo` estaba contenido por el
  gate; este no lo está.
- No se ha tocado el dato ni el código de gating — ambas son escrituras/decisiones de producto
  (¿se corrige solo esta fila, o se extiende el gate `ficha_verificada` a `ficha_mistica` para las
  52?) fuera del alcance de este QA de solo-lectura. Documentado y a la espera, mismo criterio que
  `lavanda` y `olivo`.

## Diccionario / gate `ficha_verificada`
- 43/52 con `ficha_cientifica`, **0/52 verificadas** — sin cambio.
- `olivo` (`id:7`) sigue sirviendo la ficha de cardo mariano, reconfirmado sin cambio.
- `afinidad_ayurvedica`: sigue en 43/43 plantas seguras, pero **al menos una de esas 43
  (`ashwagandha-fruto`) es contenido contaminado de otra especie** (ver hallazgo arriba) — la
  cobertura de datos para "Tu Planta Aliada" es completa en cantidad, no verificada en calidad.
  El resto del mapeo dosha→planta sigue sin implementar, pendiente de OK de Papu.
- Duplicados reconfirmados sin cambio: `equinacea`/`echinacea` (misma especie a propósito).
- `blog_posts`: 90 draft / 19 published, sin cambio. Sin generación nueva (gate en 0/52
  verificadas, `agente-plantas.sh`/`qb` sigue blindado con `ficha_verificada=eq.true`).
- Tabla `citas`: sigue sin poder crearse — `PGRST205`, sin canal DDL en el entorno cloud, más de
  31 ciclos consecutivos.
- `npm audit --json`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio.
- `public/images/plants/`: 71 archivos, 29 huérfanos, sin cambio. `lavanda` sigue con
  `image_cientifica_url` apuntando a un archivo inexistente (404 directo), sin cambio desde el
  ciclo anterior.

## Monetización (Paso 2)
- **Checkout Gumroad sigue roto — ~1 día 8h (desde 2026-07-28 18:25:20 UTC), séptimo ciclo
  consecutivo confirmando el mismo estado.** Verificado vía endpoint de proyecto Vercel
  (`NEXT_PUBLIC_GUMROAD_URL`, producción, `type: sensitive`, `updatedAt` sin cambio,
  `updatedBy: null`) y directamente en el HTML servido de `/producto/ritual-descanso`: el CTA
  sigue apuntando a `https://kristian320.gumroad.com/l/ritual-descanso` (404 con user-agent real);
  `kristiantronco.gumroad.com/l/ugsqtg` (la URL funcional anterior) sigue en 200. Sin tocar la env
  var sin OK de Papu.
- Funnel `/regalo/primera-noche` → lead → producto verificado end-to-end: formulario postea a
  `/api/leads`, el PDF de regalo (`/downloads/primera-noche-tranquila.pdf`) responde 200, enlace
  secundario post-descarga apunta a `/producto/ritual-descanso`. **OK, sin cambios.**
- "Tu Planta Aliada": esquema sin cambios de fondo, cobertura de datos 43/43 en cantidad pero con
  la reserva de calidad documentada arriba. Sin implementar.

## Optimización continua (Paso 5)
1. Funnel A/B: `leads`/`purchases` en 0 filas — sin datos para proponer variantes.
2. "Tu Planta Aliada": mapeo revisado; nuevo hallazgo de contaminación en `ashwagandha-fruto`
   afecta directamente la fiabilidad de este mapeo si se implementara hoy. Test negativo de las 9
   peligrosas: **pasa**.
3. Cita diaria: tabla `citas` sigue bloqueada, sin poder insertar.
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` existe en Vercel pero apunta a URL rota — sigue
   siendo el incidente crítico abierto, no un hito.

## Borradores sociales (solo bitácora, sin publicar)
**Instagram** — ángulo: el candado que protege la ciencia no protege la mística.
> Hay una ficha en el diccionario que lleva desde abril mostrando el elemento, el planeta y el
> chakra de otra planta — en vivo, ahora mismo, sin ningún filtro de por medio. La parte
> científica sí pasa por un candado que llevamos ciclos verificando; la parte simbólica nunca lo
> tuvo, porque nadie preguntó si hacía falta. Documentado hoy, sin tocar todavía: primero se
> entiende el alcance, luego se decide. #transparencia #QH

**LinkedIn** — ángulo: verificar un candado no es lo mismo que preguntar qué puertas no tiene.
> Llevamos días confirmando que un filtro de calidad funciona. Hoy encontramos que ese filtro solo
> cubre la mitad del contenido de cada página — la otra mitad se sirve sin pasar por nada, y al
> menos un caso real lo demuestra mostrando la identidad equivocada en producción desde hace
> meses. La lección no es "el filtro falló": el filtro hizo exactamente lo que se le pidió. La
> lección es que verificar un control no basta si nunca se pregunta qué queda fuera de su
> alcance. #QH #calidaddedatos

## Tareas manuales de Papu hoy
1. **Resolver el checkout Gumroad** — roto ~1 día 8h, séptimo ciclo escalándolo. Revert de
   `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional) o arreglar la
   cuenta nueva `kristian320`. Impacto directo en ventas.
2. **Decidir sobre el contenido místico sin gate**: (a) si se corrige solo `ashwagandha-fruto`
   (contenido real de Saúco, `id:36`, visible en producción bajo nombre equivocado desde abril) o
   (b) si se extiende el gate `ficha_verificada` a `ficha_mistica` para las 52 fichas — ambas
   opciones cambian qué ve un visitante hoy mismo. Incluye también la imagen rota de `lavanda` y
   las 24 fichas restantes (aparte de `ashwagandha-fruto` y `olivo`) sin recuperación por JSON de
   origen.
3. **Rotar la service role key y el token OAuth expuestos** (documentado desde 2026-07-25 y
   2026-07-29), sin confirmación de rotación todavía.

---
*Ciclo cloud automático. Sin commits de código (build pasa, sin fixes necesarios). Bitácora y
memoria commiteadas por el paso dedicado del workflow.*
