# Bitácora Kimiko — Ciclo cloud 2026-08-07 05:45 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200 (siguiendo redirect de trailing slash, esperado en Next.js): `/`, `/diccionario/`, `/regalo/primera-noche/`, `/producto/ritual-descanso/`, `/login/`, `/registro/`, `/terapeutas/`.
- `/admin/` sin sesión → 307 → `/login/?redirect=%2Fadmin%2F` (200). `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` con dominio correcto (`quantum-holistic.com`).
- Supabase `plants`: 52 filas íntegras. Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila.
- `npm audit`: 16 vulnerabilidades (1 low / 4 moderate / 11 high), sin cambio frente al ciclo anterior.

## Monetización
- **Checkout Gumroad sigue roto — 54º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~9 días 11h20min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción, sin el error de regex sin guion de ciclos previos) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) reconfirmada de tipo `sensitive` en producción, `updatedAt` sin cambio desde 2026-07-28T18:25:20.881Z. No se toca la env var sin OK explícito de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado por código (`app/api/leads/route.ts`, `app/regalo/primera-noche/RegaloForm.tsx`) y rutas 200. Test E2E real con escritura (POST/DELETE) más reciente fue hace ~19h (ciclo 2026-08-06 10:45 UTC) — por debajo del umbral informal de 48h, no se repite este ciclo. `leads`: 0 filas (sin actividad real de usuarios todavía).
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Propuesta de esquema sigue siendo la misma de ciclos anteriores (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu para implementar.

## Contenido
- Sin blog nuevo — backlog sin cambio: 90 `draft` / 19 `published` (109 total), 7 títulos distintos / 8 filas con violación explícita de checklist por keyword de título (todos en `draft`, ninguno publicado con violación), recontado directo desde la tabla este ciclo, sin cambio frente al 2026-08-06 10:45 UTC.
- Tabla `citas`: última inserción (Galeno, 2026-08-07 00:55:55 UTC) tiene ~4h50min de antigüedad — sin inserción nueva este ciclo (no supera el umbral de 24h); corresponde al ciclo que lo supere (~2026-08-07 21:00 UTC aprox).
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "casi reporto un número que no era": cuenta en primera persona que un chequeo rápido (buscar "chakra"/"elemento" en las fichas) iba a arrojar 52 fichas contaminadas en vez de las 34 reales, y que solo revisar el método correcto en la propia memoria evitó reportarlo mal — la disciplina no es solo verificar, es verificar el verificador.
  - **LinkedIn** — ángulo "un falso positivo que no llegó a la bitácora": plantea que en sistemas autónomos de larga duración, el riesgo no es solo actuar sobre datos incorrectos sino *reportar* conclusiones mal derivadas que luego otros (humanos u otros agentes) heredan como verdad — y que el hábito de re-derivar cifras desde la fuente antes de escribirlas es lo que lo evita.

## Higiene de datos (reverificado con método correcto, sin cambio de estado)
- **Nota metodológica de este ciclo**: un primer chequeo ingenuo (buscar presencia de `chakra`/`planeta_regente`/`elemento` en `ficha_mistica`) arrojó 52/52 plantas "contaminadas" — resultado falso, porque esos campos existen por diseño del esquema en las 52 filas (son parte de la estructura `ficha_mistica`, no un indicador de contaminación). El método correcto, documentado en `KIMIKO_MEMORIA.md` (ciclo 2026-07-30 06:34 UTC), es cruzar por `id` contra `app/fichas-50-valid.json` y filtrar filas cuyo `ficha_mistica` sea *idéntico byte a byte* al de otra especie (`nombre_latino` distinto). Reejecutado ese cruce exacto este ciclo: **34 fichas contaminadas confirmadas (25 seguras + 9 peligrosas), sin cambio real** frente a lo documentado desde 2026-07-30. Detalle completo de las 34 filas (slug, `nombre_latino` real vs. contenido servido) verificado y coincide fila por fila con el listado histórico.
- Duplicado de contenido `equinacea`/`echinacea` (ambos slugs existen en `plants`, ninguno tiene `id` coincidente en `app/fichas-50-valid.json` — igual que `manzanilla`, quedan fuera del cruce de las 34 por ser plantas añadidas después del dataset original de 50) — pendiente de decisión de Papu sobre slug canónico.
- `lavanda`: `image_cientifica_url` (`/images/plants/lavanda-cientifica.jpg`) devuelve 404 en producción — pendiente de reemplazo.
- 34 fichas contaminadas (25 seguras + 9 peligrosas) con contenido de otra especie en `ficha_mistica` sin gate ni corrección — pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC (~8 días 3h10min).
- Gate `ficha_verificada`: 0/52 plantas verificadas, sin cambio.

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. Sin escrituras nuevas en Supabase este ciclo (citas por debajo del umbral de 24h, funnel probado hace ~19h). `next-env.d.ts` regenerado por el build se revirtió sin commitear (cambio no funcional). Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — `NEXT_PUBLIC_GUMROAD_URL` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 9 días 11h20min. El link `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que sirven `ficha_mistica` de una especie distinta a la real — pendiente desde el 2026-07-30, bloquea poder activar cualquier feature que use ese campo (incl. "Tu Planta Aliada").
3. **Higiene del diccionario**: decidir slug canónico entre `equinacea`/`echinacea` (contenido duplicado) y aprobar el reemplazo de la imagen rota de `lavanda` (404 en producción).
