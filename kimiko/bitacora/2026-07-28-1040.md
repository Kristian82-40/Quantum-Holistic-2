# Bitácora Kimiko — Ciclo Cloud 2026-07-28 10:40 UTC (Trigésimo primer ciclo)

## QA — 8/8 checks OK

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.5, 36 rutas generadas, `ƒ Middleware` presente). Mismo `npm warn deprecated` de `next@14.2.5` con enlace a `nextjs.org/blog/security-update-2025-12-11`, sin cambio.
2. **Producción:** `curl -s https://quantum-holistic.com` → canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (con `-L`): `/`, `/diccionario`, `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` → todas `200` ✅.
4. **`/admin` sin sesión** → cadena `308` (`/admin` → `/admin/`) → `307` (→ `/login/?redirect=%2Fadmin%2F`) → `200` final ✅. `middleware.ts` confirmado en raíz, no en `app/` ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`) ✅.
6. **9 plantas peligrosas con placeholder:** `image_cientifica_url`/`image_mistica_url` = `null` para las 9 (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) ✅. Nunca activadas.
7. **`leads`/`purchases`:** `content-range: */0` en ambas tablas, 0 filas ✅. Último test E2E real de escritura fue a las 06:31 UTC del mismo día (~4h09min antes, por debajo del umbral de 24h) — bastó con lectura de `Content-Range`, sin gastar una escritura de prueba innecesaria.
8. **`sitemap.ts` y `robots.ts`:** presentes en `app/`, dominio `https://quantum-holistic.com` correcto en ambos ✅.

## Monetización

- `NEXT_PUBLIC_GUMROAD_URL` sigue ausente en Vercel (12 env vars totales, confirmado vía project ID directo `prj_DASuxCUuV72w8CLpZejVij8XcXvL`). `RitualCheckout.tsx` mantiene el fallback correcto: sin Gumroad, formulario de captura de email a `/api/leads` con `source: 'ritual_descanso_waitlist'`. **31º ciclo consecutivo** sin el secret.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso`: sin cambios estructurales, páginas verificadas 200 en el QA de este ciclo.
- **"Tu Planta Aliada":** sin implementar, sin cambios en la propuesta (cruce `profiles.dosha` × `plants.ficha_mistica->>afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas, sin necesitar migración). Cobertura reconfirmada: 41/43 plantas seguras con `afinidad_ayurvedica` (faltan `manzanilla` y `equinacea`, sin cambios). Pendiente OK de Papu para migrar/activar.
- Canal DDL para tabla `citas` (paso 5.3) sigue bloqueado: sin `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` en el entorno cloud (`/rest/v1/citas` → `404`, tabla no existe). **32º ciclo consecutivo bloqueado**, sin herramienta MCP de Supabase disponible en este entorno para saltar el bloqueo.

## Contenido

- Backlog `blog_posts`: 90 drafts / 19 published, sin cambio en el total. Sin contenido nuevo generado este ciclo.
- Checklist de contenido (escaneo `title + content` completo): **10 drafts con violación reconfirmados sin cambios** — 6 chakra (4 explícitos en título + 2 enterrados en el cuerpo de fichas de planta: "Echinacea..." y "El Poder Curativo del Sidr...") + 1 reiki + 1 cristales + 1 biodescodificación + 1 nutrición cuántica. Ninguno publicado (`status=draft` confirmado en los 10) → sin violación en vivo.
- Duplicados reconfirmados sin cambios: `equinacea`/`echinacea` (real, mismo `nombre_latino` "Echinacea purpurea", pendiente decisión de Papu) y `ashwagandha`/`ashwagandha-fruto` (descartado, partes distintas de la planta).
- **Funnel A/B (`leads`):** sin datos reales acumulados (tabla en 0 filas). No hay volumen todavía para proponer variantes de CTA.
- 2 borradores sociales nuevos (sin publicar, solo aquí), ángulo de experiencia de producto / rigor editorial de datos — rotando desde el ángulo de transparencia/seguridad del diccionario del ciclo 30 y el de marca/producto del ciclo 29:
  - **Instagram:** "El ritual de descanso no es una rutina de 10 pasos: es una guía breve pensada para la noche, cuando lo último que necesitas es más decisiones que tomar. 19€, sin promesas de cura milagrosa — solo un acompañamiento sencillo para bajar el ritmo antes de dormir."
  - **LinkedIn:** "Detrás de las 52 fichas de nuestro diccionario hay un proceso de curación de datos poco visible: detectar cuando dos entradas describen la misma planta bajo nombres distintos. Encontramos que 'equinacea' y 'echinacea' comparten el mismo nombre científico (Echinacea purpurea) — probablemente un duplicado real que merece fusionarse, no dos fichas independientes. Cuidar la integridad del catálogo es tan parte del trabajo como escribir el contenido."

## Imágenes huérfanas y vulnerabilidades

- `public/images/plants/`: 73 archivos totales, **30 huérfanos** (no referenciados por ninguna URL en `plants`) — 1 ya conocido (`plant-00-beleno-negro-cientifica.jpg`, candidato peligroso sin resolver) + 29 nombres de plantas comunes no catalogadas. Sin cambios.
- `npm audit --json`: 1 critical / 10 high / 4 moderate / 1 low — mismo desglose que ciclos anteriores. El critical sigue siendo `next@14.2.5` (fix disponible `14.2.35`, sin salto de versión mayor) — no actualizado de forma autónoma, ver "Tareas manuales de Papu".
- Hallazgo heredado reconfirmado sin acción: `/api/webhooks/btcpay` sigue en el build (pasarela cripto explícitamente descartada por directiva). No se toca sin instrucción explícita de Papu.

## Tareas manuales de Papu hoy

1. **Rotar el token OAuth expuesto en ciclos anteriores** (2026-07-25, 06:22 y 09:52 UTC) — sigue sin confirmación de rotación, ya son **17 ciclos** escalándolo.
2. **Activar Gumroad** (`NEXT_PUBLIC_GUMROAD_URL` en Vercel) para desbloquear el CTA transaccional de `/producto/ritual-descanso` — bloqueo de monetización desde hace **31 ciclos** consecutivos.
3. **Decidir sobre `equinacea`/`echinacea`** (mismo `nombre_latino`, dos fichas separadas en el diccionario) y sobre los 2 drafts de plantas con sección "elemento, chakra y dosha" enterrada en el contenido antes de considerar publicarlos.

*(Nota: la actualización de `next` a `14.2.35`, las 30 imágenes huérfanas en `public/images/plants/`, el candidato `plant-00-beleno-negro-cientifica.jpg` y el canal DDL para `citas` siguen pendientes de decisión de Papu, sin cambio de estado — ver secciones anteriores.)*

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios); bitácora y memoria las commitea el paso dedicado del workflow.
