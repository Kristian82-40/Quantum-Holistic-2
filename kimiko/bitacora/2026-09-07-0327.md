# Bitácora — 2026-09-07 03:27 UTC (ciclo 216º, MODO CICLO)

## Salud del sitio
- `npm ci` limpio (415 paquetes). `npm run build`: 36/36 páginas, sin errores.
  `npx next lint`: sin avisos.
- `npm audit` (sin flags): 9 vulnerabilidades (1 moderate, 8 high), sin cambio
  desde el 185º.
- 8/8 rutas del checklist devuelven 200 (`/`, `/diccionario`, `/blog`,
  `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`,
  `/terapeutas`).
- `/admin` → cadena de redirect confirmada hasta `/login/?redirect=%2Fadmin%2F`
  (308 → 200). `middleware.ts` confirmado en la raíz del repo (no dentro de
  `app/`).
- `canonical`, `og:url`, `og:image`, sitemap (37 `<loc>`, sin cambio desde el
  fix del 211º, 4 de `/diccionario/`) y `robots.txt` correctos en producción.
- Vercel: últimos 5 despliegues de `quantum-holistic-2` en `READY`.

## Diccionario (`plants`)
- 52 filas totales, 4 `publicada=true AND ficha_verificada=true`
  (`albahaca`, `arnica`, `equinacea`, `hinojo`), sin cambio desde el 211º.
  Las 4 imágenes correspondientes confirmadas en disco en
  `public/images/plants/`.
- Las 9 peligrosas (`aconito`, `datura`, `datura-metel`, `amanita-muscaria`,
  `cannabis`, `cornezuelo-centeno`, `beleno-negro`, `tejo`, `hierba-mora`)
  confirmadas una a una: `publicada=false`.
- Check permanente del 211º/212º (cruce de hash de `ficha_cientifica` sobre
  las 52 filas, `json.dumps(sort_keys=True)`): **quinta pasada, exactamente
  los mismos 7 grupos que el 212º-215º, sin grupos nuevos.** El grupo de las
  9 peligrosas comparte el mismo placeholder (esperado). Los 5 pares de
  contenido cruzado real (`ashwagandha-fruto`/`sauco`, `cinamomo`/`valeriana`,
  `ginseng`/`muerdago`, `loto`/`tulsi`, `manzanilla`/`nigela`) y
  `echinacea`/`equinacea` siguen todos `publicada=false`, sin blast radius
  en vivo. `echinacea` sigue devolviendo 404 en producción, `equinacea` 200
  — sin duplicado real en vivo.
- Sin `UPDATE` de Kristian sobre `plants` desde el 212º.

## SEO / blog
- `blog_posts`: 109 filas (79 draft / 22 published / 8 rejected), sin
  cambio. Los 22 publicados: sin duplicados de título, todos ≤60 car.,
  todos con `excerpt` ≤155 car., todos con `image_url`. 3 enlaces internos
  a `/diccionario/`, los 3 a `hinojo`, sin enlaces rotos.
- Backlog de borradores sigue calificado como no aprovechable por el cruce
  en bloque del 209º; sin candidatos nuevos.

## Negocio
- `leads` en 0. `purchases` en 0. `products` sin cambio (2 filas).
- `kimiko_drafts`: 4 filas totales, todas `hecho`, cola vacía — sin orden de
  Telegram este ciclo.
- **`citas`: la última inserción sigue siendo la misma fila del 215º
  (2026-09-05T03:28:37 UTC) — el hueco ha seguido creciendo y al cierre de
  este ciclo son 47h58min sin ninguna inserción nueva**, frente al máximo
  histórico real recalculado de 38.02h entre las 33 filas más recientes (el
  mismo cálculo que hizo el 215º, repetido en este ciclo tal como exige su
  check permanente). Es la segunda escalada consecutiva del mismo hueco
  (38h→42h33min en el 215º→47h58min ahora), no una fila nueva con un hueco
  distinto. Sigue sin haber ninguna corrección de código o dato aplicable;
  se documenta y se sube de prioridad como tarea manual, sin tocar el
  funnel (bloqueado por instrucción permanente del Paso 5).

## Resumen de cambios este ciclo
- **Sin commits de código ni escrituras en Supabase** — quinto ciclo
  consecutivo (212º-216º) sin hallazgos técnicos nuevos en el checklist
  mecánico. El hallazgo de este ciclo es de negocio: el mismo hueco de
  `citas` que el 215º dejó "a vigilar" no se ha cerrado, sigue creciendo.

## Tareas manuales de Kristian hoy
1. **`citas` lleva 47h58min sin ninguna reserva nueva** (última: 2026-09-05
   03:28 UTC) — es la misma alerta del ciclo anterior (42h33min) que no se
   ha resuelto y ha seguido subiendo casi 6h más. El máximo histórico previo
   a esta racha era 38h. Merece revisión de negocio directa (¿funnel roto,
   caída real de demanda, o algo estacional que ya debería haber remontado?).
2. Duplicado de slug `equinacea`/`echinacea` en `plants`: comparten
   contenido idéntico y ninguno está publicado en el caso de `echinacea`,
   sigue pendiente de que decidas cuál de los dos slugs es el bueno para
   consolidar.
3. Backlog de blog (87 borradores/rechazados): ≈80% duplica tema de posts
   ya publicados según el cruce del 209º. Si quieres contenido nuevo en el
   blog hace falta generar candidatos frescos, no seguir tirando de esta
   cola.
