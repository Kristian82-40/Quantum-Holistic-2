# Bitácora Kimiko — Ciclo cloud 2026-08-09 16:41 UTC

## QA
- Build (`npm ci && npm run build`) pasa sin fixes, corrido desde la raíz.
- Producción: canonical y `og:url` de `/` == `https://quantum-holistic.com/` (verificado en vivo).
- Rutas 200 (siguiendo redirect de trailing slash, esperado en Next.js): `/`, `/diccionario/`, `/regalo/primera-noche/`, `/producto/ritual-descanso/`, `/login/`, `/registro/`, `/terapeutas/`.
- `/admin` sin sesión → 308 → `/admin/` → 307 → `/login/?redirect=%2Fadmin%2F` (200). `middleware.ts` confirmado en la raíz del repo (no en `app/`).
- `sitemap.xml` y `robots.txt` (200, 74 `<url>`, dominio correcto `quantum-holistic.com`) y `robots.txt` con `Sitemap:` correcto.
- Supabase `plants`: 52 filas íntegras (`Content-Range: 0-51/52`). Las 9 plantas peligrosas (aconito, datura, datura-metel, amanita-muscaria, cannabis, cornezuelo-centeno, beleno-negro, tejo, hierba-mora) con `image_cientifica_url`/`image_mistica_url` en `null`, reverificado fila por fila.
- `npm audit`: 17 vulnerabilidades (1 low / 4 moderate / 12 high), sin cambio frente al ciclo anterior.
- `leads`: 0 filas, sin cambio.

## Monetización
- **Checkout Gumroad sigue roto — 69º ciclo consecutivo** (desde 2026-07-28 18:25:20 UTC, ~11 días 22h16min). El CTA real servido en `/producto/ritual-descanso/` (verificado en el HTML de producción) apunta a `https://kristian320.gumroad.com/l/ritual-descanso` (404 confirmado en vivo). `https://kristiantronco.gumroad.com/l/ugsqtg` (200 confirmado en vivo) sigue siendo el revert viable. `NEXT_PUBLIC_GUMROAD_URL` en Vercel (proyecto `quantum-holistic-2`, `prj_DASuxCUuV72w8CLpZejVij8XcXvL`) reconfirmada vía API de Vercel, `updatedAt` sin cambio desde 2026-07-28T18:25:20Z (production, tipo `sensitive`). No se toca la env var sin OK explícito de Papu.
- Funnel `/regalo/primera-noche` → lead (`POST /api/leads`, `source: primera_noche_regalo`) → éxito enlaza a `/producto/ritual-descanso` — verificado por código (`RegaloForm.tsx`) y rutas 200, sin cambios. Test E2E real con escritura más reciente sigue siendo el del 2026-08-08 12:54 UTC (~27h47min de antigüedad, por debajo del umbral informal de 48h) — no se repite este ciclo.
- "Tu Planta Aliada": sin implementar en código (sin match en `app/`, `components/`, `lib/`). Propuesta de esquema sin cambio (mapping dosha → planta segura, filtro hardcoded excluyendo las 9 peligrosas) — pendiente de OK de Papu para implementar.

## Contenido
- Sin blog nuevo — backlog sin cambio: 90 `draft` / 19 `published` (109 total). Reconfirmados por `ilike` sobre título los mismos 8 drafts con violación explícita de checklist (4 chakras + reiki + cristales + biodescodificación + nutrición cuántica). Decisión deliberada de no generar contenido nuevo mientras el backlog no revisado siga acumulado — sigue pendiente de Papu.
- Tabla `citas`: última inserción (Florence Nightingale, 2026-08-09 01:52:10 UTC) tiene ~14h49min de antigüedad al arrancar el ciclo — por debajo del umbral de 24h, sin inserción nueva este ciclo.
- 2 borradores sociales nuevos, sin publicar:
  - **Instagram** — ángulo "casi doce días, y la pregunta se sigue haciendo igual": sobre por qué repetir la misma comprobación de un link roto ciclo tras ciclo no es desperdicio sino la prueba de que el sistema no se acostumbra a un fallo conocido — la alternativa (asumir que "sigue igual" sin comprobar) es la que abre la puerta a que un cambio real pase desapercibido.
  - **LinkedIn** — ángulo "un casi-incidente documentado vale más que un incidente evitado por suerte": retoma la nota operativa del ciclo anterior (uso indebido de un patrón de volcado de `env` con filtro, sin fuga real) para plantear que la honestidad operativa de un sistema autónomo se mide en cómo registra sus propios desvíos de procedimiento, no solo en si el resultado final salió bien.

## Higiene de datos (sin recruce completo este ciclo, sin cambio de estado)
- Última reproducción íntegra del cruce de fichas contaminadas fue el ciclo 2026-08-07 16:57 UTC: 34 confirmadas (25 seguras + 9 peligrosas), con contenido de otra especie en `ficha_mistica`. Pendiente de decisión de producto de Papu desde 2026-07-30 02:36 UTC (~10 días 14h05min).
- Duplicado de contenido `equinacea`/`echinacea` (mismo `nombre_latino`, `Echinacea purpurea`) reconfirmado sin cambio — pendiente de decisión de slug canónico.
- `lavanda`: `image_cientifica_url` (`/images/plants/lavanda-cientifica.jpg`) sigue apuntando a un recurso que devolvía 404 en producción en ciclos anteriores — sin reemplazo, sin recheck en vivo este ciclo.
- Gate `ficha_verificada`: 0/52 plantas verificadas, sin cambio.

## Sin commits de código este ciclo
Build limpio, QA sin hallazgos que requieran fix de código. Sin escrituras nuevas en Supabase este ciclo (citas por debajo del umbral de 24h, funnel probado hace ~27h47min, `leads` en 0 sin actividad que segmentar). `next-env.d.ts` regenerado por el build se revirtió sin commitear (cambio no funcional, `git checkout -- next-env.d.ts`). Ruta heredada `/api/webhooks/btcpay` (pasarela cripto descartada por directiva) reconfirmada en el build, sin tocar. Bitácora y memoria las commitea el paso dedicado del workflow.

## Tareas manuales de Papu hoy
1. **Arreglar el checkout de Gumroad** — `NEXT_PUBLIC_GUMROAD_URL` en Vercel sigue apuntando a un link 404 (`kristian320.gumroad.com/l/ritual-descanso`) desde hace 11 días 22h16min. `kristiantronco.gumroad.com/l/ugsqtg` (200, confirmado en vivo) es un revert viable si es el correcto — decisión y cambio de env var requieren su OK.
2. **Decidir qué hacer con las 34 fichas contaminadas** (25 plantas seguras + las 9 peligrosas) que sirven `ficha_mistica` de una especie distinta a la real — pendiente desde el 2026-07-30, bloquea "Tu Planta Aliada" y cualquier feature que use ese campo.
3. **Revisar el backlog de 90 drafts de blog** (8 con violación explícita del checklist anti-pseudociencia: chakras, reiki, cristales, biodescodificación, nutrición cuántica) — decidir archivar/borrar/publicar antes de que Kimiko genere contenido nuevo.
