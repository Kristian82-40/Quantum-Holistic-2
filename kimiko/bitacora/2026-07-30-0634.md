# Bitácora Kimiko — Ciclo cloud 2026-07-30 06:34 UTC

## QA (Paso 1) — 8/8 OK
1. Build local (`npm ci && npm run build`): **OK**, sin errores ni warnings nuevos.
2. Producción: canonical y `og:url` == `https://quantum-holistic.com`. **OK**.
3. Rutas 200 (siguiendo redirects de trailing slash): `/`, `/diccionario`, `/regalo/primera-noche`,
   `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas`. **7/7 OK**.
4. `/admin` sin sesión → `308 /admin → /admin/ → /login/?redirect=%2Fadmin%2F` (200 final).
   `middleware.ts` confirmado en raíz, no en `app/`. **OK**.
5. Supabase `plants`: **52/52** filas íntegras (`content-range: 0-0/52`).
6. Las 9 plantas peligrosas: `image_cientifica_url`/`image_mistica_url` en `null`,
   `ficha_verificada:false`, `publicada:false`, `ficha_cientifica:{}` en las 9. Placeholder
   intacto. **OK**.
7. `leads`: 0 filas. `purchases`: 0 filas.
8. `sitemap.ts` y `robots.ts`: presentes en `app/`, dominio correcto en ambos.

## Hallazgo nuevo: el gap de `ficha_mistica` sin gate afecta a 25 fichas, no a 1

El ciclo anterior (02:36 UTC) documentó que `ficha_mistica` se sirve sin ningún filtro y que
`ashwagandha-fruto` (contenido real de Saúco) era un caso confirmado en vivo. Este ciclo se
generalizó el cruce a las 43 plantas seguras completas, cruzando `ficha_cientifica`/`ficha_mistica`
actuales contra `app/fichas-50-valid.json` por `id` (la clave que causó el barajado original,
commit `8b84912`) y comprobando cuáles filas tienen contenido **idéntico byte a byte** al de la
especie equivocada del origen — es decir, filas que nunca se recuperaron en la sesión del
2026-07-29 (que arregló 18 de las 43 emparejando por `nombre_latino`).

**Resultado: 25 de las 43 plantas seguras (58%) tienen tanto `ficha_cientifica` como
`ficha_mistica` idénticas a una especie distinta de su propio `nombre_latino`.** Lista completa:
`olivo`, `loto`, `ajo`, `castano-de-indias`, `muerdago`, `abedul`, `granada`, `higuera`,
`rosa-de-jerico`, `frankenia`, `amla`, `neem`, `azafran`, `llantan`, `boldo`, `sidr`, `milenrama`,
`roble`, `cinamomo`, `nigela`, `ashwagandha-fruto`, `azufaifo`, `incienso`, `arbol-bodhi`,
`guayaba`.

- **Lado científico: contenido.** El gate `ficha_verificada` (0/52 en `true`) bloquea que la
  `ficha_cientifica` de estas 25 llegue a producción — verificado en el código
  (`fc = plant.ficha_verificada ? plant.ficha_cientifica : null`). Sin cambio de riesgo aquí.
- **Lado místico: sin contención, confirmado en producción, no solo por código.** Se
  spot-comprobó el HTML servido de dos slugs de la lista (`loto`, `ajo`) además del ya conocido
  `ashwagandha-fruto`: `/diccionario/loto/` muestra Chakra "Coronilla", Planeta "Sol", Elemento
  "Fuego" — que es la ficha mística de `tulsi`/*Ocimum sanctum* (id 42 en el origen), no la del
  loto sagrado. `/diccionario/ajo/` muestra Chakra "Plexo solar", Planeta "Venus" — ficha de
  `granada`/*Punica granatum* (id 32 en el origen), no la del ajo. Ambos coinciden exactamente con
  lo que predice el cruce por `id`.
- **Consecuencia para "Tu Planta Aliada":** la cobertura "43/43 con `afinidad_ayurvedica`"
  reportada como completa no es utilizable tal cual — **25 de esas 43 filas (58%) recomendarían
  la afinidad dosha de una especie distinta a la que el usuario vería en pantalla**, no solo la
  única fila ya conocida. Esto convierte el hallazgo de "cobertura completa, calidad sin
  verificar" en "cobertura completa, mayoría contaminada, verificada ahora fila por fila".
- No se ha tocado ningún dato ni el gating este ciclo — sigue siendo una decisión de producto
  (¿gate para `ficha_mistica` en las 52, o corrección fila por fila de las 25?) fuera del QA de
  solo-lectura. La lista completa de 25 slugs queda documentada aquí para que la decisión de Papu
  no dependa de repetir el cruce.

## Diccionario / gate `ficha_verificada`
- 43/52 con `ficha_cientifica`, 0/52 verificadas — sin cambio.
- `olivo` (`id:7`, cardo mariano) y `lavanda` (imagen rota, 404 directo) reconfirmados sin cambio,
  ambos ya dentro de la lista de 25 documentada arriba (`olivo`) o aparte (`lavanda`, que sí tiene
  ficha propia correcta, solo la imagen rota).
- `afinidad_ayurvedica`: 43/43 en cantidad, con la reserva de calidad de arriba (25/43
  contaminadas).
- `blog_posts`: 90 draft / 19 published, sin cambio. **10 drafts con violación de checklist**
  (6 chakra + 1 reiki + 1 cristales + 1 biodescodificación + 1 nutrición cuántica), ninguno
  publicado, sin cambio en la lista de IDs respecto al ciclo anterior.
- Tabla `citas`: sigue sin poder crearse (`PGRST205`, sin `DATABASE_URL`/`SUPABASE_DB_URL`/
  `SUPABASE_ACCESS_TOKEN` en el entorno cloud), 32º ciclo consecutivo bloqueado.
- `npm audit --json`: 0 critical / 11 high / 4 moderate / 1 low, sin cambio.
- `public/images/plants/`: 71 archivos, 29 huérfanos, sin cambio. `lavanda` sigue con
  `image_cientifica_url` apuntando a archivo inexistente.

## Monetización (Paso 2)
- **Checkout Gumroad sigue roto — ~1 día 12h (desde 2026-07-28 18:25:20 UTC), octavo ciclo
  consecutivo confirmando el mismo estado.** Reverificado vía endpoint de proyecto Vercel
  (`NEXT_PUBLIC_GUMROAD_URL` producción, `updatedAt` sin cambio, `updatedBy: null`) y directamente:
  `kristian320.gumroad.com/l/ritual-descanso` → 404 con user-agent real;
  `kristiantronco.gumroad.com/l/ugsqtg` (la URL funcional anterior) → 200. Sin tocar la env var sin
  OK de Papu.
- Funnel `/regalo/primera-noche` → lead → `/producto/ritual-descanso` verificado end-to-end: PDF
  (`/downloads/primera-noche-tranquila.pdf`) → 200, formulario postea a `/api/leads`, enlace
  secundario apunta a `/producto/ritual-descanso`. **OK, sin cambios.**
- "Tu Planta Aliada": sin implementar. Cobertura de datos 43/43 en cantidad, pero con el hallazgo
  de arriba (25/43 contaminadas) el mapeo no puede activarse de forma responsable sin corrección
  previa o sin excluir explícitamente las 25 filas listadas.

## Optimización continua (Paso 5)
1. Funnel A/B: `leads`/`purchases` en 0 filas — sin datos para proponer variantes.
2. "Tu Planta Aliada": mapeo revisado a fondo este ciclo; ver hallazgo de arriba. Test negativo de
   las 9 peligrosas: **pasa** (ninguna de las 25 contaminadas es una de las 9 peligrosas).
3. Cita diaria: tabla `citas` sigue bloqueada, sin poder insertar.
4. Monitoreo pago: `NEXT_PUBLIC_GUMROAD_URL` existe en Vercel pero sigue apuntando a URL rota —
   incidente crítico abierto, no un hito.

## Borradores sociales (solo bitácora, sin publicar)

**Instagram** — ángulo: un hallazgo de ayer, multiplicado por 25 hoy.
> Ayer documentamos que una ficha del diccionario llevaba desde abril mostrando el chakra y el
> planeta de otra planta, sin ningún filtro. Hoy repetimos el mismo cruce sobre las 43 fichas
> "seguras" completas, no solo sobre la que ya sabíamos. El resultado: 25 de 43 tienen la misma
> contaminación. El hallazgo de ayer no era un caso aislado, era la punta visible de un patrón
> que solo aparece cuando dejas de conformarte con la primera coincidencia. #transparencia #QH

**LinkedIn** — ángulo: encontrar un caso no cierra la investigación, la abre.
> Cuando confirmamos que un dato contaminado se servía en producción sin filtro, la pregunta
> correcta no era "¿lo corregimos?" sino "¿cuántos más hay así?". Generalizar el mismo cruce a
> todo el conjunto de datos —no solo repetirlo sobre el caso ya conocido— cambió la cifra de 1
> caso a 25 sobre 43. La decisión que le llega hoy a nuestro responsable de producto ya no es
> sobre una fila: es sobre si el 58% de un dataset necesita revisión antes de construir nada
> encima. Un hallazgo aislado invita a arreglarlo; un hallazgo generalizado invita a preguntarse
> por qué el proceso que lo creó no se detectó antes. #QH #calidaddedatos

## Tareas manuales de Papu hoy

1. **Resolver el checkout Gumroad** — roto ~1 día 12h, octavo ciclo escalándolo. Revert de
   `NEXT_PUBLIC_GUMROAD_URL` a `kristiantronco.gumroad.com/l/ugsqtg` (funcional) o arreglar la
   cuenta nueva `kristian320`. Impacto directo en ventas.
2. **Decidir el alcance real del problema de `ficha_mistica` sin gate: son 25 filas, no 1.**
   Lista completa en este documento (`olivo`, `loto`, `ajo`, `castano-de-indias`, `muerdago`,
   `abedul`, `granada`, `higuera`, `rosa-de-jerico`, `frankenia`, `amla`, `neem`, `azafran`,
   `llantan`, `boldo`, `sidr`, `milenrama`, `roble`, `cinamomo`, `nigela`, `ashwagandha-fruto`,
   `azufaifo`, `incienso`, `arbol-bodhi`, `guayaba`). Decidir entre (a) extender el gate
   `ficha_verificada` a `ficha_mistica` para las 52 fichas, o (b) corregir/despublicar las 25
   filas individualmente. Bloquea también cualquier implementación responsable de "Tu Planta
   Aliada".
3. **Rotar la service role key y el token OAuth expuestos** (documentado desde 2026-07-25 y
   2026-07-29), sin confirmación de rotación todavía.

---
*Ciclo cloud automático. Sin commits de código (build pasa, sin fixes necesarios). Bitácora y
memoria commiteadas por el paso dedicado del workflow.*
