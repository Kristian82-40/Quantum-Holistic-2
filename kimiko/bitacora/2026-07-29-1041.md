# Bitácora Kimiko — Ciclo Cloud 2026-07-29 10:41 UTC

## 🔴 Checkout Gumroad: sigue caído, ya ~16h16min sin cobrar

Reverificado desde cero (Vercel API + comprobación directa de las dos URLs):

- `NEXT_PUBLIC_GUMROAD_URL` en Vercel (producción): `updatedAt` **sigue idéntico** al de los dos
  ciclos anteriores (`2026-07-28 18:25:20 UTC`, `updatedBy: null`). Papu no ha tocado la env var.
- `/producto/ritual-descanso` sigue sirviendo `https://kristian320.gumroad.com/l/ritual-descanso`
  como único CTA. Esa URL sigue en **404**. La URL anterior
  (`kristiantronco.gumroad.com/l/ugsqtg`) sigue viva, **200**.
- Ventana de checkout roto: **~16h16min** en el momento de este chequeo (10:41 UTC), desde las
  18:25 UTC del 28-jul. Tercer ciclo consecutivo confirmando el mismo estado sin cambios.
- Matiz de producto que no se había anotado hasta ahora: el fallback de captura de email
  (`RitualCheckout.tsx`) **solo se activa cuando la env var no existe**, no cuando existe pero
  apunta a un enlace roto. Es decir: quien hace clic en "Comprar" ahora mismo no ve un formulario
  de email de respaldo — ve un 404 de Gumroad y se va. La lógica del componente es correcta según
  su diseño (env var presente → CTA directo); el problema sigue siendo 100% el valor de la env
  var, no el código.

## QA — 8/8 checks OK (aparte del hallazgo de arriba)

1. **Build:** `npm ci && npm run build` ✅ pasa sin fixes (Next 14.2.5, 36 rutas, `ƒ Middleware`
   presente).
2. **Producción:** canonical y `og:url` == `https://quantum-holistic.com` ✅.
3. **Rutas 200** (siguiendo el 308 de trailing slash): `/`, `/diccionario`,
   `/regalo/primera-noche`, `/producto/ritual-descanso`, `/login`, `/registro`, `/terapeutas` →
   todas `200` ✅.
4. **`/admin` sin sesión** → `308` → `307` → `/login/?redirect=%2Fadmin%2F` ✅. `middleware.ts`
   confirmado en raíz ✅.
5. **Supabase — 52 plantas íntegras** (`content-range: 0-0/52`) ✅.
6. **9 plantas peligrosas con placeholder** ✅: `image_cientifica_url` e `image_mistica_url` en
   `null` en las 9, `ficha_cientifica` vacía en exactamente esas 9 (verificado slug por slug).
7. **`leads`/`purchases`:** 0 filas en ambas ✅ (sin datos aún para funnel A/B del Paso 5.1).
8. **`sitemap.xml`/`robots.txt`:** dominio correcto en ambos ✅.

## Hallazgo nuevo: otra ficha con identidad ajena (`olivo` ≠ ficha de olivo)

Al revisar `ficha_cientifica` fila por fila para el QA de plantas peligrosas, la entrada
`slug: olivo` (`id: 7`, `nombre_latino: Olea europaea`) tiene una ficha con `familia_botanica:
Asteraceae`, `parte_usada: Semillas` y posología en "silimarina 200-400 mg/día" — eso es **cardo
mariano** (*Silybum marianum*, Asteraceae), no olivo (*Oleaceae*). Confirmado cruzando
`app/fichas-50-valid.json`: el `id: 7` de ese archivo es literalmente "Cardo mariano". Es el mismo
mecanismo de corrupción ya documentado (upsert por `id`, commit `8b84912`) — pero esta fila cae en
las **25 sin recuperación posible por JSON de origen**: la sesión interactiva del 29-jul solo pudo
recuperar por `nombre_latino` cuando ambas especies (la correcta y la corrupta) existían en el
JSON de 50 fichas; `Olea europaea` no aparece ahí, y no queda ningún slug `cardo-mariano` vivo en
la tabla actual para revertir el cruce. No se toca: el gate `ficha_verificada` ya bloquea que esto
llegue a producción como dato clínico, y no hay una fuente en disco para restaurar el olivo real
sin generar contenido nuevo (que requeriría el mismo proceso de verificación humana que las otras
24). Queda documentado como ejemplo concreto del pendiente ya escalado, no como fix nuevo.

## Gate `ficha_verificada` — sin cambios

- 43/52 plantas con `ficha_cientifica`, **0 con `ficha_verificada = true`**. Sin movimiento desde
  la sesión interactiva del 29-jul (18 fichas recuperadas por especie).
- Tabla `citas` (Paso 5.3): sigue sin existir (`PGRST205`). Canal DDL bloqueado (sin
  `DATABASE_URL`/`SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` en el entorno cloud) — más de 30 ciclos
  consecutivos.

## Monetización

- **Checkout Gumroad: roto, ver hallazgo crítico arriba.** Pausado hasta decisión de Papu.
- Funnel `/regalo/primera-noche`: página 200, formulario presente, publica a `/api/leads`. Sin
  cambios estructurales.
- **"Tu Planta Aliada":** propuesta de esquema sigue vigente sin cambios (cruce `profiles.dosha` ×
  `ficha_mistica.afinidad_ayurvedica`, filtro hardcoded de las 9 peligrosas — confirmado por grep
  que sigue sin implementarse en el código). Gate `ficha_verificada` en 0 sigue siendo el bloqueo
  real para lanzarla con datos clínicos de por medio. Ver `KIMIKO_MEMORIA.md`.

## Contenido

- `blog_posts`: 90 `status=draft` / 19 `status=published`, sin cambio. Sin publicación este
  ciclo.
- `npm audit --json`: 0 crítico / 11 alto / 4 moderado / 1 bajo — sin cambio.
- `public/images/plants/`: 71 archivos — sin cambio.
- 2 borradores sociales nuevos, ángulos distintos a los de ciclos anteriores:
  - **Instagram** (ángulo: el fallback que no se activa): "El botón de compra no está caído en
    silencio — apunta a un enlace real que da error. Nuestro sistema solo ofrece dejar tu email
    cuando falta la configuración, no cuando la configuración apunta a un sitio roto. Así que
    ahora mismo alguien hace clic, ve un 404, y se va. Lo sabemos hace más de dieciséis horas.
    Seguimos sin tocar esa configuración sin autorización — qué cuenta de venta usar no es
    nuestra decisión."
  - **LinkedIn** (ángulo: hallazgo de la ficha ajena): "Esta semana encontramos que la ficha
    científica de una entrada de nuestro diccionario (registrada como olivo) contenía en
    realidad los datos de otra planta: familia botánica, principios activos y posología de una
    especie distinta. El error no estaba en el nombre visible, estaba en los datos que lo
    sostienen — y solo aparece si cruzas la familia botánica contra el nombre científico, no si
    lees el título. Es la razón por la que, con 52 plantas en el diccionario, cero posologías
    están publicadas como verificadas: un gate de revisión bloquea cualquier dato clínico hasta
    confirmarlo especie por especie. Preferimos ser lentos a confiar en un título."

## Tareas manuales de Papu hoy

1. **🔴 URGENTE (escalado, 3er ciclo seguido, ya ~16h16min) — El checkout de Gumroad sigue roto en
   producción.** `NEXT_PUBLIC_GUMROAD_URL` sigue apuntando a
   `https://kristian320.gumroad.com/l/ritual-descanso` (404). La URL anterior
   (`https://kristiantronco.gumroad.com/l/ugsqtg`) sigue funcionando (200). Además, quien hace
   clic ahora mismo no ve ni siquiera el formulario de email de respaldo, ve el 404 directo —el
   costo de esperar sigue subiendo con cada hora.
2. **Decidir el plan de re-verificación del diccionario**, incluyendo qué hacer con las 25 fichas
   sin fuente en disco (ejemplo concreto de hoy: `olivo` con datos de cardo mariano). Sin al menos
   una planta verificada, "Tu Planta Aliada" sigue en pausa.
3. **Rotar la service role key** expuesta en claro en `/Volumes/Papu Ext/scripts/agente-plantas.sh`
   y `scripts/runner.sh` (heredado, sigue sin confirmación), y el token OAuth expuesto el
   2026-07-25.

*(Nota: `equinacea`/`echinacea`, `ashwagandha`/`ashwagandha-fruto`, las imágenes huérfanas y el
canal DDL para `citas` siguen pendientes de decisión de Papu, sin cambio de estado.)*

---
Sin commits de código este ciclo (build pasa, sin fixes necesarios; el problema del checkout vive
en una env var de Vercel, no en el repo, y el hallazgo de `olivo` requiere fuente de contenido
nueva, no un revert). Bitácora y memoria las commitea el paso dedicado del workflow.
